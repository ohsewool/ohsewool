# 찾은 것들 — 회차 기록

이 다섯 저장소를 만들면서 **자기 코드가 자기 검사에 걸려 드러난 것들**의 기록이다.
회차 순서 그대로 두었다. 어떤 회차는 빈손이고, 그것도 남겼다 — **훑어서 안 나온 것과
안 훑은 것은 다르다.**

한 회차의 결함을 고치면 그 **형태**로 다섯 저장소를 다시 훑는 것이 여기 반복되는
방법이다. 그래서 항목들이 서로 이어진다: "계산해놓고 안 읽는 값"이 "없는 이름을 읽는
`.get`"으로, 그것이 다시 "스코프가 스레드를 못 넘는 것"으로 이어졌다.

**내 검사기가 먼저 틀린 경우도 그대로 적었다.** 여러 번 있었고, 그때마다 대조를 걸지
않았으면 "이상 없음"을 결과로 보고했을 것이다.

요약과 저장소 목록은 [README](README.md)에 있다.

## 목차

1. [통과하는 테스트가 무언가를 붙잡고 있는가](#통과하는-테스트가-무언가를-붙잡고-있는가)
2. [테스트하고 있던 코드가 그 코드가 아니었다](#테스트하고-있던-코드가-그-코드가-아니었다)
3. [저장소에 있는데 아무도 돌려본 적 없는 것](#저장소에-있는데-아무도-돌려본-적-없는-것)
4. [문서가 없는 파일을 가리키고 있었다](#문서가-없는-파일을-가리키고-있었다)
5. [한 파일에서 원칙을 말하고 일곱 파일에서 어기고 있었다](#한-파일에서-원칙을-말하고-일곱-파일에서-어기고-있었다)
6. [README의 첫 줄을 아무도 돌린 적이 없었다](#readme의-첫-줄을-아무도-돌린-적이-없었다)
7. [안전장치가 순응한 사용자를 벌하고 있었다](#안전장치가-순응한-사용자를-벌하고-있었다)
8. [규칙 넷 중 하나는 규칙이 아니었다](#규칙-넷-중-하나는-규칙이-아니었다)
9. [더 큰 모델이 더 낮았다](#더-큰-모델이-더-낮았다)
10. [계획을 문장으로 남기면 다음에도 같은 실수를 한다](#계획을-문장으로-남기면-다음에도-같은-실수를-한다)
11. [있다고 적혀 있는데 없던 검사](#있다고-적혀-있는데-없던-검사)
12. [커버리지가 가리킨 곳을 하나씩 쏴봤다](#커버리지가-가리킨-곳을-하나씩-쏴봤다)
13. [README가 앞세운 기능이 절반에서 멈춰 있었다](#readme가-앞세운-기능이-절반에서-멈춰-있었다)
14. [보안 주장의 절반만 맞았고, 하필 자신 있는 쪽이 틀렸다](#보안-주장의-절반만-맞았고-하필-자신-있는-쪽이-틀렸다)
15. [보장 문장을 전부 모아 하나씩 확인했다](#보장-문장을-전부-모아-하나씩-확인했다)
16. [`docs/`는 아무것도 안 했다고 말하고 있었다](#docs는-아무것도-안-했다고-말하고-있었다)
17. [사양이 만들어지지 않은 프로젝트를 서술하고 있었다](#사양이-만들어지지-않은-프로젝트를-서술하고-있었다)
18. [지시 파일이 이미 만든 것을 금지하고 있었다](#지시-파일이-이미-만든-것을-금지하고-있었다)
19. [첫 화면이 로그가 돼 있었다](#첫-화면이-로그가-돼-있었다)
20. [감사 도구가 자기 출력을 커밋하고 있었다](#감사-도구가-자기-출력을-커밋하고-있었다)
21. [매 실행이 다시 쓰는 것을 추적하고 있었다](#매-실행이-다시-쓰는-것을-추적하고-있었다)
22. [내 현황판이 같은 병에 걸려 있었다](#내-현황판이-같은-병에-걸려-있었다)
23. [관문이 설정을 고르는 코드에서 참조되지 않았다](#관문이-설정을-고르는-코드에서-참조되지-않았다)
24. [요금제에 적어둔 한도를 아무도 강제하지 않았다](#요금제에-적어둔-한도를-아무도-강제하지-않았다)
25. [저장소에 적힌 값으로 관리자가 될 수 있었다](#저장소에-적힌-값으로-관리자가-될-수-있었다)
26. [동시에 부르면 한도가 한도가 아니었다](#동시에-부르면-한도가-한도가-아니었다)
27. [같은 형태를 다섯 저장소로 들고 갔더니 감사 로그가 갈라졌다](#같은-형태를-다섯-저장소로-들고-갔더니-감사-로그가-갈라졌다)
28. [한도가 존재하는 이유인 그 엔드포인트에 검사가 없었다](#한도가-존재하는-이유인-그-엔드포인트에-검사가-없었다)
29. [퇴화한 숫자 하나가 통제를 통째로 끄고 있었다](#퇴화한-숫자-하나가-통제를-통째로-끄고-있었다)
30. [한 주체의 두 가지 철자](#한-주체의-두-가지-철자)
31. [통제가 작동한 순간이 어디에도 남지 않았다](#통제가-작동한-순간이-어디에도-남지-않았다)
32. [메시지는 같았고 시간이 달랐다](#메시지는-같았고-시간이-달랐다)
33. [내가 고친 것의 대가를 재봤다](#내가-고친-것의-대가를-재봤다)
34. [갓 클론한 사람이 보는 것을 처음으로 봤다](#갓-클론한-사람이-보는-것을-처음으로-봤다)
35. [파는 한도를 꽉 채운 데이터로 돌려봤다](#파는-한도를-꽉-채운-데이터로-돌려봤다)
36. [공개한 숫자를 실행과 대조하는 것이 두 저장소에 없었다](#공개한-숫자를-실행과-대조하는-것이-두-저장소에-없었다)
37. [검사 도구가 문법 오류를 "잡힘"으로 읽고 있었다](#검사-도구가-문법-오류를-잡힘으로-읽고-있었다)
38. [손으로 돌리는 검사를 돌아가게 만들었다](#손으로-돌리는-검사를-돌아가게-만들었다)
39. [지운 뒤에 남는 것, 그리고 지우지 못하게 하는 것](#지운-뒤에-남는-것-그리고-지우지-못하게-하는-것)
40. [절반만 고쳤던 것을 마저 고쳤다](#절반만-고쳤던-것을-마저-고쳤다)
41. [대조 방향이 둘이라는 것](#대조-방향이-둘이라는-것)
42. [관례를 설명한 문장이 README를 모든 검사에서 빼버렸다](#관례를-설명한-문장이-readme를-모든-검사에서-빼버렸다)
43. [하한선으로는 목록이 하나 줄어드는 것을 볼 수 없다](#하한선으로는-목록이-하나-줄어드는-것을-볼-수-없다)
44. [저장소가 자기를 세는 숫자, 그리고 아무도 발동시킨 적 없는 거부](#저장소가-자기를-세는-숫자-그리고-아무도-발동시킨-적-없는-거부)
45. [다섯 저장소의 거부 267개를 하나씩 지워봤다](#다섯-저장소의-거부-267개를-하나씩-지워봤다)
46. [스위트가 한 번도 실행하지 않는 줄이 81개 있었다](#스위트가-한-번도-실행하지-않는-줄이-81개-있었다)
47. [어제 세운 관문을 의심했다 — 구문 100%는 각 `if`가 양쪽으로 가봤다는 뜻이 아니다](#어제-세운-관문을-의심했다--구문-100는-각-if가-양쪽으로-가봤다는-뜻이-아니다)
48. [그리고 소유권 문지기 넷](#그리고-소유권-문지기-넷)
49. ["잴 수 없다"고 적어둔 것을 쟀더니, CI가 지키던 것은 47%였다](#잴-수-없다고-적어둔-것을-쟀더니-ci가-지키던-것은-47였다)
50. [문서가 두 번 적어둔 보안 항목이 두 달간 그대로 있었다](#문서가-두-번-적어둔-보안-항목이-두-달간-그대로-있었다)
51. [SHAP이 아닌 것을 SHAP이라 부르지 않는다 — 규칙을 적어둔 저장소 안에 두 번째 위반이 있었다](#shap이-아닌-것을-shap이라-부르지-않는다--규칙을-적어둔-저장소-안에-두-번째-위반이-있었다)
52. [고정할 수 없는 두 개 때문에 고정하지 못하고 있었다](#고정할-수-없는-두-개-때문에-고정하지-못하고-있었다)
53. [대조가 두 번 헛돌았다 — 내 검사가 엉뚱한 겹을 시험하고 있었다](#대조가-두-번-헛돌았다--내-검사가-엉뚱한-겹을-시험하고-있었다)
54. [신선한 것과 지금 것을 상대로 도는 것은 다르다](#신선한-것과-지금-것을-상대로-도는-것은-다르다)
55. [기록하는 행위가 기록을 뒤집었다](#기록하는-행위가-기록을-뒤집었다)
56. [전체에서만 실패하는 검사를 하나 만들었다](#전체에서만-실패하는-검사를-하나-만들었다)
57. [파일을 하나씩 따로 돌렸더니, 나흘 된 0바이트 파일이 나왔다](#파일을-하나씩-따로-돌렸더니-나흘-된-0바이트-파일이-나왔다)
58. [이 저장소가 제품을 208번째 줄에서 말하고 있었다](#이-저장소가-제품을-208번째-줄에서-말하고-있었다)
59. [포트폴리오 입구가 열여섯 회차 뒤처져 있었다](#포트폴리오-입구가-열여섯-회차-뒤처져-있었다)
60. [구글 로그인이 DB를 잠근 채 남길 수 있었다](#구글-로그인이-db를-잠근-채-남길-수-있었다)
61. [그 모양이 몇 군데나 있는지 셌다 — 22개](#그-모양이-몇-군데나-있는지-셌다--22개)
62. [삼킨 예외가 어디까지 조용한가 — 열 곳을 세고 넷을 고쳤다](#삼킨-예외가-어디까지-조용한가--열-곳을-세고-넷을-고쳤다)
63. [시계가 둘이었고, 그것을 세던 검사기는 파일 열둘을 안 보고 있었다](#시계가-둘이었고-그것을-세던-검사기는-파일-열둘을-안-보고-있었다)
64. [대조를 세는 도구가 대조받지 않고 있었다 — 그리고 자기가 검사하는 문서를 망가뜨렸다](#대조를-세는-도구가-대조받지-않고-있었다--그리고-자기가-검사하는-문서를-망가뜨렸다)
65. ["급한 셋"이 사실은 열둘이었다 — 그리고 그것을 고친 변환을 검사한 것도 틀렸다](#급한-셋이-사실은-열둘이었다--그리고-그것을-고친-변환을-검사한-것도-틀렸다)
66. ["CI가 안 도는 라우트 마흔"이 아홉이었다 — 그리고 그중 둘이 데이터를 지우는 라우트였다](#ci가-안-도는-라우트-마흔이-아홉이었다--그리고-그중-둘이-데이터를-지우는-라우트였다)
67. [거절인데 한 번도 터진 적 없는 것들 — 그리고 그것을 시험하는 내 검사가 저지른 단골 결함](#거절인데-한-번도-터진-적-없는-것들--그리고-그것을-시험하는-내-검사가-저지른-단골-결함)
68. [100% 관문이 무엇의 100%였는지 아무도 적어두지 않았다](#100-관문이-무엇의-100였는지-아무도-적어두지-않았다)
69. [다섯 저장소의 자기검사를 표로 만들었더니 규칙마다 빠진 칸이 있었다](#다섯-저장소의-자기검사를-표로-만들었더니-규칙마다-빠진-칸이-있었다)
70. [CI가 초록불인데 몇 개가 실제로 돌았는지는 아무도 안 봤다](#ci가-초록불인데-몇-개가-실제로-돌았는지는-아무도-안-봤다)
71. [설명이 실패하면 클라이언트가 파일 경로를 받았다 — 그리고 그 줄은 한 번도 돈 적이 없었다](#설명이-실패하면-클라이언트가-파일-경로를-받았다--그리고-그-줄은-한-번도-돈-적이-없었다)
72. [막혀서 남긴 게 아니라 도구가 없어서 남긴 것이었다 — 죽은 라우트 0개](#막혀서-남긴-게-아니라-도구가-없어서-남긴-것이었다--죽은-라우트-0개)
73. [남의 예측 API 토큰을 취소할 수 있는지 아무도 확인한 적이 없었다](#남의-예측-api-토큰을-취소할-수-있는지-아무도-확인한-적이-없었다)
74. [도달하는데 아무도 확인하지 않는 거부 — 그리고 그것을 재는 도구가 여섯을 못 보고 있었다](#도달하는데-아무도-확인하지-않는-거부--그리고-그것을-재는-도구가-여섯을-못-보고-있었다)
75. [`match=`는 무엇을 말하는지 고정하지 무엇인지는 고정하지 않는다](#match는-무엇을-말하는지-고정하지-무엇인지는-고정하지-않는다)
76. [예상되는 상태를 "예상하지 못한 내부 오류"로 보고하고 있었다](#예상되는-상태를-예상하지-못한-내부-오류로-보고하고-있었다)
77. [이미 사용자를 향해 쓰인 문장이 배관에서 버려지고 있었다](#이미-사용자를-향해-쓰인-문장이-배관에서-버려지고-있었다)
78. [마지막 한 겹을 시험하는 것이 하나도 없었다](#마지막-한-겹을-시험하는-것이-하나도-없었다)
79. [인증 없이 실험 기록 43건을 읽고, 지울 수 있었다 — 그 갈래에 검사가 있었는데도](#인증-없이-실험-기록-43건을-읽고-지울-수-있었다--그-갈래에-검사가-있었는데도)
80. [모르는 사람 둘이 서로의 업로드를 본다 — 적어둔 결정의 값](#모르는-사람-둘이-서로의-업로드를-본다--적어둔-결정의-값)
81. [같은 실수를 두 번 했다 — 기억을 고치는 대신 값을 낮췄다](#같은-실수를-두-번-했다--기억을-고치는-대신-값을-낮췄다)
82. [커버리지 숫자만 아무것도 안 지키고 있었다 — 두 달 치 작업이 문서에 없었다](#커버리지-숫자만-아무것도-안-지키고-있었다--두-달-치-작업이-문서에-없었다)
83. [같은 커밋에서 CI가 초록불이었다가 빨간불이 됐다 — 검사가 요청하지 않은 것을 보고 있었다](#같은-커밋에서-ci가-초록불이었다가-빨간불이-됐다--검사가-요청하지-않은-것을-보고-있었다)
84. [이 검사들은 서로에게 기대고 있는가 — 두 번은 빈손, 세 번째에서 나왔다](#이-검사들은-서로에게-기대고-있는가--두-번은-빈손-세-번째에서-나왔다)
85. [내 도구가 "확인되지 않는 것 0개"라고 했다 — 자기가 닿은 것만 세고 있었다](#내-도구가-확인되지-않는-것-0개라고-했다--자기가-닿은-것만-세고-있었다)
86. [`"@"` 한 글자로 계정이 만들어졌다 — 그리고 내 검사는 옆의 이유로 통과하고 있었다](#-한-글자로-계정이-만들어졌다--그리고-내-검사는-옆의-이유로-통과하고-있었다)
87. [서른둘이 조용히 빠져도 초록불이었다 — 관문이 수집 수를 보고 있었다](#서른둘이-조용히-빠져도-초록불이었다--관문이-수집-수를-보고-있었다)
88. ["검사가 없다"에는 두 가지가 있다 — 안 쓴 것과 못 쓰는 것](#검사가-없다에는-두-가지가-있다--안-쓴-것과-못-쓰는-것)
89. [README가 방문자에게 가장 먼저 시키는 세 줄을 CI가 한 번도 돌린 적이 없었다](#readme가-방문자에게-가장-먼저-시키는-세-줄을-ci가-한-번도-돌린-적이-없었다)
90. [실험이 통째로 죽어도 초록불이었다 — 흘러감과 확인 불가가 한 신호 뒤에 있었다](#실험이-통째로-죽어도-초록불이었다--흘러감과-확인-불가가-한-신호-뒤에-있었다)
91. [한 저장소에서 고치고 나머지를 안 셌다 — 같은 관문이 세 곳에 더 있었다](#한-저장소에서-고치고-나머지를-안-셌다--같은-관문이-세-곳에-더-있었다)
92. [샘플 여섯 중 다섯을 틀리게 판정하면서 전부 "신뢰 높음"이었다](#샘플-여섯-중-다섯을-틀리게-판정하면서-전부-신뢰-높음이었다)
93. [검사가 닿지 않는 주장 표면이 하나 있었다 — 사람들이 가장 먼저 보는 자리](#검사가-닿지-않는-주장-표면이-하나-있었다--사람들이-가장-먼저-보는-자리)
94. [내가 만든 가드가 README가 시키는 명령에서만 침묵했다](#내가-만든-가드가-readme가-시키는-명령에서만-침묵했다)
95. [한 번도 돈 적 없는 워크플로 셋을 돌렸다 — 셋 다 첫 실행에서 빨간불](#한-번도-돈-적-없는-워크플로-셋을-돌렸다--셋-다-첫-실행에서-빨간불)
96. [지는 방식도 계약이다 — 서버가 무너지는 다섯 갈래를 일부러 밟았다](#지는-방식도-계약이다--서버가-무너지는-다섯-갈래를-일부러-밟았다)
---

## 결함의 모양

96개를 시간순으로 읽을 필요는 없다. 아래는 **되풀이된 모양**과 그 실례다.

**이 분류는 사후에 붙인 것이다.** 항목이 근거이고 분류는 읽는 길이다 — 한 항목이 여러 모양에 걸치기도 한다. 세어보면 96개 중 66개가 대조(control) 이야기를 하고, 50개가 **빈손으로 끝난 확인**을 적고 있다. 후자를 함께 남기는 것이 이 목록의 규칙이다: **세 번 훑어 아무것도 안 나왔다는 것과 훑지 않았다는 것은 다르다.**

**검사가 아무것도 보지 않았다** — 통과했다는 것과 확인했다는 것은 다르다. 규정식이 깨져 빈손을 내거나, 단언이 늘 참이거나, 심을 것을 잘못 심었거나, 아예 테스트가 없거나.

- 1. [통과하는 테스트가 무언가를 붙잡고 있는가](#통과하는-테스트가-무언가를-붙잡고-있는가)
- 3. [저장소에 있는데 아무도 돌려본 적 없는 것](#저장소에-있는데-아무도-돌려본-적-없는-것)
- 11. [있다고 적혀 있는데 없던 검사](#있다고-적혀-있는데-없던-검사)
- 12. [커버리지가 가리킨 곳을 하나씩 쏴봤다](#커버리지가-가리킨-곳을-하나씩-쏴봤다)
- 37. [검사 도구가 문법 오류를 "잡힘"으로 읽고 있었다](#검사-도구가-문법-오류를-잡힘으로-읽고-있었다)
- 41. [대조 방향이 둘이라는 것](#대조-방향이-둘이라는-것)
- 53. [대조가 두 번 헛돌았다 — 내 검사가 엉뚱한 겹을 시험하고 있었다](#대조가-두-번-헛돌았다--내-검사가-엉뚱한-겹을-시험하고-있었다)
- 57. [파일을 하나씩 따로 돌렸더니, 나흘 된 0바이트 파일이 나왔다](#파일을-하나씩-따로-돌렸더니-나흘-된-0바이트-파일이-나왔다)
- 62. [삼킨 예외가 어디까지 조용한가 — 열 곳을 세고 넷을 고쳤다](#삼킨-예외가-어디까지-조용한가--열-곳을-세고-넷을-고쳤다)
- 64. [대조를 세는 도구가 대조받지 않고 있었다 — 그리고 자기가 검사하는 문서를 망가뜨렸다](#대조를-세는-도구가-대조받지-않고-있었다--그리고-자기가-검사하는-문서를-망가뜨렸다)
- 67. [거절인데 한 번도 터진 적 없는 것들 — 그리고 그것을 시험하는 내 검사가 저지른 단골 결함](#거절인데-한-번도-터진-적-없는-것들--그리고-그것을-시험하는-내-검사가-저지른-단골-결함)
- 70. [CI가 초록불인데 몇 개가 실제로 돌았는지는 아무도 안 봤다](#ci가-초록불인데-몇-개가-실제로-돌았는지는-아무도-안-봤다)
- 71. [설명이 실패하면 클라이언트가 파일 경로를 받았다 — 그리고 그 줄은 한 번도 돈 적이 없었다](#설명이-실패하면-클라이언트가-파일-경로를-받았다--그리고-그-줄은-한-번도-돈-적이-없었다)
- 74. [도달하는데 아무도 확인하지 않는 거부 — 그리고 그것을 재는 도구가 여섯을 못 보고 있었다](#도달하는데-아무도-확인하지-않는-거부--그리고-그것을-재는-도구가-여섯을-못-보고-있었다)
- 75. [`match=`는 무엇을 말하는지 고정하지 무엇인지는 고정하지 않는다](#match는-무엇을-말하는지-고정하지-무엇인지는-고정하지-않는다)
- 78. [마지막 한 겹을 시험하는 것이 하나도 없었다](#마지막-한-겹을-시험하는-것이-하나도-없었다)
- 79. [인증 없이 실험 기록 43건을 읽고, 지울 수 있었다 — 그 갈래에 검사가 있었는데도](#인증-없이-실험-기록-43건을-읽고-지울-수-있었다--그-갈래에-검사가-있었는데도)
- 83. [같은 커밋에서 CI가 초록불이었다가 빨간불이 됐다 — 검사가 요청하지 않은 것을 보고 있었다](#같은-커밋에서-ci가-초록불이었다가-빨간불이-됐다--검사가-요청하지-않은-것을-보고-있었다)
- 84. [이 검사들은 서로에게 기대고 있는가 — 두 번은 빈손, 세 번째에서 나왔다](#이-검사들은-서로에게-기대고-있는가--두-번은-빈손-세-번째에서-나왔다)
- 86. [`"@"` 한 글자로 계정이 만들어졌다 — 그리고 내 검사는 옆의 이유로 통과하고 있었다](#-한-글자로-계정이-만들어졌다--그리고-내-검사는-옆의-이유로-통과하고-있었다)
- 87. [서른둘이 조용히 빠져도 초록불이었다 — 관문이 수집 수를 보고 있었다](#서른둘이-조용히-빠져도-초록불이었다--관문이-수집-수를-보고-있었다)
- 91. [한 저장소에서 고치고 나머지를 안 셌다 — 같은 관문이 세 곳에 더 있었다](#한-저장소에서-고치고-나머지를-안-셌다--같은-관문이-세-곳에-더-있었다)
- 94. [내가 만든 가드가 README가 시키는 명령에서만 침묵했다](#내가-만든-가드가-readme가-시키는-명령에서만-침묵했다)
- 95. [한 번도 돈 적 없는 워크플로 셋을 돌렸다 — 셋 다 첫 실행에서 빨간불](#한-번도-돈-적-없는-워크플로-셋을-돌렸다--셋-다-첫-실행에서-빨간불)

**한 사실을 두 곳에 적고 한 곳만 지켰다** — 같은 값·같은 규칙이 두 자리에 있으면 한쪽만 갱신된다. 사본은 소스가 바뀌어도 그대로다.

- 5. [한 파일에서 원칙을 말하고 일곱 파일에서 어기고 있었다](#한-파일에서-원칙을-말하고-일곱-파일에서-어기고-있었다)
- 27. [같은 형태를 다섯 저장소로 들고 갔더니 감사 로그가 갈라졌다](#같은-형태를-다섯-저장소로-들고-갔더니-감사-로그가-갈라졌다)
- 30. [한 주체의 두 가지 철자](#한-주체의-두-가지-철자)
- 36. [공개한 숫자를 실행과 대조하는 것이 두 저장소에 없었다](#공개한-숫자를-실행과-대조하는-것이-두-저장소에-없었다)
- 44. [저장소가 자기를 세는 숫자, 그리고 아무도 발동시킨 적 없는 거부](#저장소가-자기를-세는-숫자-그리고-아무도-발동시킨-적-없는-거부)
- 50. [문서가 두 번 적어둔 보안 항목이 두 달간 그대로 있었다](#문서가-두-번-적어둔-보안-항목이-두-달간-그대로-있었다)
- 51. [SHAP이 아닌 것을 SHAP이라 부르지 않는다 — 규칙을 적어둔 저장소 안에 두 번째 위반이 있었다](#shap이-아닌-것을-shap이라-부르지-않는다--규칙을-적어둔-저장소-안에-두-번째-위반이-있었다)
- 65. ["급한 셋"이 사실은 열둘이었다 — 그리고 그것을 고친 변환을 검사한 것도 틀렸다](#급한-셋이-사실은-열둘이었다--그리고-그것을-고친-변환을-검사한-것도-틀렸다)
- 67. [거절인데 한 번도 터진 적 없는 것들 — 그리고 그것을 시험하는 내 검사가 저지른 단골 결함](#거절인데-한-번도-터진-적-없는-것들--그리고-그것을-시험하는-내-검사가-저지른-단골-결함)
- 69. [다섯 저장소의 자기검사를 표로 만들었더니 규칙마다 빠진 칸이 있었다](#다섯-저장소의-자기검사를-표로-만들었더니-규칙마다-빠진-칸이-있었다)
- 70. [CI가 초록불인데 몇 개가 실제로 돌았는지는 아무도 안 봤다](#ci가-초록불인데-몇-개가-실제로-돌았는지는-아무도-안-봤다)
- 73. [남의 예측 API 토큰을 취소할 수 있는지 아무도 확인한 적이 없었다](#남의-예측-api-토큰을-취소할-수-있는지-아무도-확인한-적이-없었다)
- 81. [같은 실수를 두 번 했다 — 기억을 고치는 대신 값을 낮췄다](#같은-실수를-두-번-했다--기억을-고치는-대신-값을-낮췄다)
- 82. [커버리지 숫자만 아무것도 안 지키고 있었다 — 두 달 치 작업이 문서에 없었다](#커버리지-숫자만-아무것도-안-지키고-있었다--두-달-치-작업이-문서에-없었다)
- 86. [`"@"` 한 글자로 계정이 만들어졌다 — 그리고 내 검사는 옆의 이유로 통과하고 있었다](#-한-글자로-계정이-만들어졌다--그리고-내-검사는-옆의-이유로-통과하고-있었다)
- 91. [한 저장소에서 고치고 나머지를 안 셌다 — 같은 관문이 세 곳에 더 있었다](#한-저장소에서-고치고-나머지를-안-셌다--같은-관문이-세-곳에-더-있었다)
- 93. [검사가 닿지 않는 주장 표면이 하나 있었다 — 사람들이 가장 먼저 보는 자리](#검사가-닿지-않는-주장-표면이-하나-있었다--사람들이-가장-먼저-보는-자리)
- 95. [한 번도 돈 적 없는 워크플로 셋을 돌렸다 — 셋 다 첫 실행에서 빨간불](#한-번도-돈-적-없는-워크플로-셋을-돌렸다--셋-다-첫-실행에서-빨간불)

**정의됐지만 배선되지 않은 통제** — 헬퍼가 다 있는데 아무도 import하지 않거나, 관문이 설정을 고르는 코드에서 참조되지 않거나, 요금제에 적힌 한도를 강제하는 것이 없거나.

- 11. [있다고 적혀 있는데 없던 검사](#있다고-적혀-있는데-없던-검사)
- 23. [관문이 설정을 고르는 코드에서 참조되지 않았다](#관문이-설정을-고르는-코드에서-참조되지-않았다)
- 24. [요금제에 적어둔 한도를 아무도 강제하지 않았다](#요금제에-적어둔-한도를-아무도-강제하지-않았다)
- 28. [한도가 존재하는 이유인 그 엔드포인트에 검사가 없었다](#한도가-존재하는-이유인-그-엔드포인트에-검사가-없었다)
- 69. [다섯 저장소의 자기검사를 표로 만들었더니 규칙마다 빠진 칸이 있었다](#다섯-저장소의-자기검사를-표로-만들었더니-규칙마다-빠진-칸이-있었다)
- 79. [인증 없이 실험 기록 43건을 읽고, 지울 수 있었다 — 그 갈래에 검사가 있었는데도](#인증-없이-실험-기록-43건을-읽고-지울-수-있었다--그-갈래에-검사가-있었는데도)

**문서가 지금을 서술하지 않는다** — 착수 시점에는 맞았던 문장이 남아 지시가 된다. 없는 파일을 가리키고, 만들어지지 않은 프로젝트를 서술하고, 이미 만든 것을 금지한다.

- 4. [문서가 없는 파일을 가리키고 있었다](#문서가-없는-파일을-가리키고-있었다)
- 6. [README의 첫 줄을 아무도 돌린 적이 없었다](#readme의-첫-줄을-아무도-돌린-적이-없었다)
- 16. [`docs/`는 아무것도 안 했다고 말하고 있었다](#docs는-아무것도-안-했다고-말하고-있었다)
- 17. [사양이 만들어지지 않은 프로젝트를 서술하고 있었다](#사양이-만들어지지-않은-프로젝트를-서술하고-있었다)
- 18. [지시 파일이 이미 만든 것을 금지하고 있었다](#지시-파일이-이미-만든-것을-금지하고-있었다)
- 22. [내 현황판이 같은 병에 걸려 있었다](#내-현황판이-같은-병에-걸려-있었다)
- 50. [문서가 두 번 적어둔 보안 항목이 두 달간 그대로 있었다](#문서가-두-번-적어둔-보안-항목이-두-달간-그대로-있었다)
- 58. [이 저장소가 제품을 208번째 줄에서 말하고 있었다](#이-저장소가-제품을-208번째-줄에서-말하고-있었다)
- 66. ["CI가 안 도는 라우트 마흔"이 아홉이었다 — 그리고 그중 둘이 데이터를 지우는 라우트였다](#ci가-안-도는-라우트-마흔이-아홉이었다--그리고-그중-둘이-데이터를-지우는-라우트였다)
- 72. [막혀서 남긴 게 아니라 도구가 없어서 남긴 것이었다 — 죽은 라우트 0개](#막혀서-남긴-게-아니라-도구가-없어서-남긴-것이었다--죽은-라우트-0개)
- 82. [커버리지 숫자만 아무것도 안 지키고 있었다 — 두 달 치 작업이 문서에 없었다](#커버리지-숫자만-아무것도-안-지키고-있었다--두-달-치-작업이-문서에-없었다)
- 93. [검사가 닿지 않는 주장 표면이 하나 있었다 — 사람들이 가장 먼저 보는 자리](#검사가-닿지-않는-주장-표면이-하나-있었다--사람들이-가장-먼저-보는-자리)

**경계에서 잃어버린 구분** — 한 질문에 답하는 장치가 둘이면 둘이 갈린다 — 두 개의 시계, 한 주체의 두 철자, 구조화된 값을 산문에서 도로 읽어내기.

- 14. [보안 주장의 절반만 맞았고, 하필 자신 있는 쪽이 틀렸다](#보안-주장의-절반만-맞았고-하필-자신-있는-쪽이-틀렸다)
- 26. [동시에 부르면 한도가 한도가 아니었다](#동시에-부르면-한도가-한도가-아니었다)
- 29. [퇴화한 숫자 하나가 통제를 통째로 끄고 있었다](#퇴화한-숫자-하나가-통제를-통째로-끄고-있었다)
- 30. [한 주체의 두 가지 철자](#한-주체의-두-가지-철자)
- 32. [메시지는 같았고 시간이 달랐다](#메시지는-같았고-시간이-달랐다)
- 39. [지운 뒤에 남는 것, 그리고 지우지 못하게 하는 것](#지운-뒤에-남는-것-그리고-지우지-못하게-하는-것)
- 40. [절반만 고쳤던 것을 마저 고쳤다](#절반만-고쳤던-것을-마저-고쳤다)
- 63. [시계가 둘이었고, 그것을 세던 검사기는 파일 열둘을 안 보고 있었다](#시계가-둘이었고-그것을-세던-검사기는-파일-열둘을-안-보고-있었다)
- 76. [예상되는 상태를 "예상하지 못한 내부 오류"로 보고하고 있었다](#예상되는-상태를-예상하지-못한-내부-오류로-보고하고-있었다)
- 77. [이미 사용자를 향해 쓰인 문장이 배관에서 버려지고 있었다](#이미-사용자를-향해-쓰인-문장이-배관에서-버려지고-있었다)
- 80. [모르는 사람 둘이 서로의 업로드를 본다 — 적어둔 결정의 값](#모르는-사람-둘이-서로의-업로드를-본다--적어둔-결정의-값)
- 84. [이 검사들은 서로에게 기대고 있는가 — 두 번은 빈손, 세 번째에서 나왔다](#이-검사들은-서로에게-기대고-있는가--두-번은-빈손-세-번째에서-나왔다)
- 90. [실험이 통째로 죽어도 초록불이었다 — 흘러감과 확인 불가가 한 신호 뒤에 있었다](#실험이-통째로-죽어도-초록불이었다--흘러감과-확인-불가가-한-신호-뒤에-있었다)
- 92. [샘플 여섯 중 다섯을 틀리게 판정하면서 전부 "신뢰 높음"이었다](#샘플-여섯-중-다섯을-틀리게-판정하면서-전부-신뢰-높음이었다)

**한 번도 지나간 적 없는 경로** — 선택적 의존이 늘 설치돼 있었고, 거부 분기가 정상 입력으로는 닿지 않고, 라우트가 아무도 부르지 않은 채 떠 있다. 재보기 전에는 그것이 초록불과 구분되지 않는다.

- 13. [README가 앞세운 기능이 절반에서 멈춰 있었다](#readme가-앞세운-기능이-절반에서-멈춰-있었다)
- 45. [다섯 저장소의 거부 267개를 하나씩 지워봤다](#다섯-저장소의-거부-267개를-하나씩-지워봤다)
- 46. [스위트가 한 번도 실행하지 않는 줄이 81개 있었다](#스위트가-한-번도-실행하지-않는-줄이-81개-있었다)
- 47. [어제 세운 관문을 의심했다 — 구문 100%는 각 `if`가 양쪽으로 가봤다는 뜻이 아니다](#어제-세운-관문을-의심했다--구문-100는-각-if가-양쪽으로-가봤다는-뜻이-아니다)
- 49. ["잴 수 없다"고 적어둔 것을 쟀더니, CI가 지키던 것은 47%였다](#잴-수-없다고-적어둔-것을-쟀더니-ci가-지키던-것은-47였다)
- 54. [신선한 것과 지금 것을 상대로 도는 것은 다르다](#신선한-것과-지금-것을-상대로-도는-것은-다르다)
- 66. ["CI가 안 도는 라우트 마흔"이 아홉이었다 — 그리고 그중 둘이 데이터를 지우는 라우트였다](#ci가-안-도는-라우트-마흔이-아홉이었다--그리고-그중-둘이-데이터를-지우는-라우트였다)
- 68. [100% 관문이 무엇의 100%였는지 아무도 적어두지 않았다](#100-관문이-무엇의-100였는지-아무도-적어두지-않았다)
- 71. [설명이 실패하면 클라이언트가 파일 경로를 받았다 — 그리고 그 줄은 한 번도 돈 적이 없었다](#설명이-실패하면-클라이언트가-파일-경로를-받았다--그리고-그-줄은-한-번도-돈-적이-없었다)
- 72. [막혀서 남긴 게 아니라 도구가 없어서 남긴 것이었다 — 죽은 라우트 0개](#막혀서-남긴-게-아니라-도구가-없어서-남긴-것이었다--죽은-라우트-0개)
- 73. [남의 예측 API 토큰을 취소할 수 있는지 아무도 확인한 적이 없었다](#남의-예측-api-토큰을-취소할-수-있는지-아무도-확인한-적이-없었다)
- 76. [예상되는 상태를 "예상하지 못한 내부 오류"로 보고하고 있었다](#예상되는-상태를-예상하지-못한-내부-오류로-보고하고-있었다)
- 78. [마지막 한 겹을 시험하는 것이 하나도 없었다](#마지막-한-겹을-시험하는-것이-하나도-없었다)
- 80. [모르는 사람 둘이 서로의 업로드를 본다 — 적어둔 결정의 값](#모르는-사람-둘이-서로의-업로드를-본다--적어둔-결정의-값)
- 85. [내 도구가 "확인되지 않는 것 0개"라고 했다 — 자기가 닿은 것만 세고 있었다](#내-도구가-확인되지-않는-것-0개라고-했다--자기가-닿은-것만-세고-있었다)
- 88. ["검사가 없다"에는 두 가지가 있다 — 안 쓴 것과 못 쓰는 것](#검사가-없다에는-두-가지가-있다--안-쓴-것과-못-쓰는-것)
- 92. [샘플 여섯 중 다섯을 틀리게 판정하면서 전부 "신뢰 높음"이었다](#샘플-여섯-중-다섯을-틀리게-판정하면서-전부-신뢰-높음이었다)
- 96. [지는 방식도 계약이다 — 서버가 무너지는 다섯 갈래를 일부러 밟았다](#지는-방식도-계약이다--서버가-무너지는-다섯-갈래를-일부러-밟았다)

**인용과 사용의 혼동** — 관례를 **설명하는** 문장이 그 관례의 적용을 면제시키고, 옛 값을 설명하는 독스트링이 그 값을 **쓰는** 코드로 읽힌다. 기록하는 행위가 기록을 뒤집는다.

- 42. [관례를 설명한 문장이 README를 모든 검사에서 빼버렸다](#관례를-설명한-문장이-readme를-모든-검사에서-빼버렸다)
- 55. [기록하는 행위가 기록을 뒤집었다](#기록하는-행위가-기록을-뒤집었다)
- 89. [README가 방문자에게 가장 먼저 시키는 세 줄을 CI가 한 번도 돌린 적이 없었다](#readme가-방문자에게-가장-먼저-시키는-세-줄을-ci가-한-번도-돌린-적이-없었다)

**삼키고 아무 말도 안 한다** — 예외를 잡아 흘려보내면 실패는 성공과 구별되지 않는다. 로그아웃이 세션을 못 지웠는데 `ok: True`, 잠긴 DB가 "작업이 없습니다", 오타 난 환경변수가 기본값. **의도해서 삼킨 것과 잊고 삼킨 것을 가르는 것은 그 자리에 적힌 이유뿐이다.**

- 61. [그 모양이 몇 군데나 있는지 셌다 — 22개](#그-모양이-몇-군데나-있는지-셌다--22개)
- 62. [삼킨 예외가 어디까지 조용한가 — 열 곳을 세고 넷을 고쳤다](#삼킨-예외가-어디까지-조용한가--열-곳을-세고-넷을-고쳤다)
- 90. [실험이 통째로 죽어도 초록불이었다 — 흘러감과 확인 불가가 한 신호 뒤에 있었다](#실험이-통째로-죽어도-초록불이었다--흘러감과-확인-불가가-한-신호-뒤에-있었다)
- 96. [지는 방식도 계약이다 — 서버가 무너지는 다섯 갈래를 일부러 밟았다](#지는-방식도-계약이다--서버가-무너지는-다섯-갈래를-일부러-밟았다)

**내 도구가 틀렸다** — 감사기가 문법 오류를 '잡힘'으로 읽고, 매처가 못 찾은 것을 '없음'으로 내고, 신선도 검사가 범위를 보지 않는다. 검사를 검사하지 않으면 그것도 주장일 뿐이다.

- 37. [검사 도구가 문법 오류를 "잡힘"으로 읽고 있었다](#검사-도구가-문법-오류를-잡힘으로-읽고-있었다)
- 53. [대조가 두 번 헛돌았다 — 내 검사가 엉뚱한 겹을 시험하고 있었다](#대조가-두-번-헛돌았다--내-검사가-엉뚱한-겹을-시험하고-있었다)
- 54. [신선한 것과 지금 것을 상대로 도는 것은 다르다](#신선한-것과-지금-것을-상대로-도는-것은-다르다)
- 55. [기록하는 행위가 기록을 뒤집었다](#기록하는-행위가-기록을-뒤집었다)
- 62. [삼킨 예외가 어디까지 조용한가 — 열 곳을 세고 넷을 고쳤다](#삼킨-예외가-어디까지-조용한가--열-곳을-세고-넷을-고쳤다)
- 63. [시계가 둘이었고, 그것을 세던 검사기는 파일 열둘을 안 보고 있었다](#시계가-둘이었고-그것을-세던-검사기는-파일-열둘을-안-보고-있었다)
- 64. [대조를 세는 도구가 대조받지 않고 있었다 — 그리고 자기가 검사하는 문서를 망가뜨렸다](#대조를-세는-도구가-대조받지-않고-있었다--그리고-자기가-검사하는-문서를-망가뜨렸다)
- 65. ["급한 셋"이 사실은 열둘이었다 — 그리고 그것을 고친 변환을 검사한 것도 틀렸다](#급한-셋이-사실은-열둘이었다--그리고-그것을-고친-변환을-검사한-것도-틀렸다)
- 68. [100% 관문이 무엇의 100%였는지 아무도 적어두지 않았다](#100-관문이-무엇의-100였는지-아무도-적어두지-않았다)
- 74. [도달하는데 아무도 확인하지 않는 거부 — 그리고 그것을 재는 도구가 여섯을 못 보고 있었다](#도달하는데-아무도-확인하지-않는-거부--그리고-그것을-재는-도구가-여섯을-못-보고-있었다)
- 75. [`match=`는 무엇을 말하는지 고정하지 무엇인지는 고정하지 않는다](#match는-무엇을-말하는지-고정하지-무엇인지는-고정하지-않는다)
- 77. [이미 사용자를 향해 쓰인 문장이 배관에서 버려지고 있었다](#이미-사용자를-향해-쓰인-문장이-배관에서-버려지고-있었다)
- 81. [같은 실수를 두 번 했다 — 기억을 고치는 대신 값을 낮췄다](#같은-실수를-두-번-했다--기억을-고치는-대신-값을-낮췄다)
- 83. [같은 커밋에서 CI가 초록불이었다가 빨간불이 됐다 — 검사가 요청하지 않은 것을 보고 있었다](#같은-커밋에서-ci가-초록불이었다가-빨간불이-됐다--검사가-요청하지-않은-것을-보고-있었다)
- 85. [내 도구가 "확인되지 않는 것 0개"라고 했다 — 자기가 닿은 것만 세고 있었다](#내-도구가-확인되지-않는-것-0개라고-했다--자기가-닿은-것만-세고-있었다)
- 87. [서른둘이 조용히 빠져도 초록불이었다 — 관문이 수집 수를 보고 있었다](#서른둘이-조용히-빠져도-초록불이었다--관문이-수집-수를-보고-있었다)
- 88. ["검사가 없다"에는 두 가지가 있다 — 안 쓴 것과 못 쓰는 것](#검사가-없다에는-두-가지가-있다--안-쓴-것과-못-쓰는-것)
- 94. [내가 만든 가드가 README가 시키는 명령에서만 침묵했다](#내가-만든-가드가-readme가-시키는-명령에서만-침묵했다)

---
## 통과하는 테스트가 무언가를 붙잡고 있는가

1,211개가 통과한다는 것은 **테스트가 통과한다는 뜻**이지, 그것들이 무언가를 지키고 있다는 뜻이 아니다. 확인하는 방법은 하나뿐이다 — 코드를 일부러 망가뜨리고 빨간불이 켜지는지 본다.

다섯 저장소의 안전장치 27개를 하나씩 무력화했다. 자기 승인 차단, 경로 봉쇄, `..` 거부, test split 봉인, 코퍼스 체크섬, 요청별 상태 격리, 외래키 강제, 전사본 신뢰도 필수 — **25개는 잡혔다.**

**먼저 음성 대조를 걸었다.** 전부 "잡힘"으로 나오는 결과는 그 자체로는 아무것도 증명하지 않는다. 스위트가 무관한 이유로 실패해도 — 작업 디렉터리가 틀렸거나, 수집 에러가 났거나, 의존이 빠졌거나 — 출력은 똑같다. 그래서 같은 파일에 공백만 넣는 무해한 변경을 먼저 돌려 **안 잡히는 것을 확인했다.** 컨트롤이 하나라도 잡히면 그 회차의 결과는 전부 무효다. 이건 이 프로젝트가 이미 한 번 빠진 함정이다 — 시크릿 스캐너가 정규식 에러로 매번 빈 출력을 냈고 그게 "깨끗함"으로 읽혔다.

잡히지 않은 둘은 **코드가 이미 맞았고 테스트가 붙잡고 있지 않았을 뿐**이다. 그래서 더 볼 만하다.

**되감김 감시가 처음 울릴 때 스스로 무장 해제했다** ([agent-safety-core](https://github.com/ohsewool/agent-safety-core/blob/main/docs/REDTEAM-002-findings.md)) — high-water mark가 시계를 아래로도 따라가게 바꿔도 331개가 전부 통과했다. 겉보기와 달리 사소하지 않다. 되감긴 시계에서 **첫 번째** 리스는 여전히 거부된다 — 비교가 기록보다 먼저 일어나기 때문이다. 그래서 한 번만 확인하는 스위트에는 정상으로 보인다. 그때 거부당한 그 읽기가 이미 mark를 덮어썼고, 이후의 모든 리스는 낮아진 값과 비교되어 통과한다. **로그에는 "되감김 탐지"가 찍히고, 다음 리스는 그냥 나간다.**

**상호 판독을 양쪽에서 고치고 한쪽만 확인했다** ([mcp-gateway](https://github.com/ohsewool/mcp-gateway/blob/main/tests/test_cross_repo_log_format.py)) — 두 저장소의 로그는 형식이 같아 검증기 하나로 읽는다고 적혀 있었고, 무결성 필드 이름만 달라 서로를 변조로 판정했다. 양쪽 리더가 두 이름을 모두 받도록 고쳤는데 **테스트는 agent-safety-core에만 있었다.** `event_hash`는 mcp-gateway 테스트 전체에 한 번도 나오지 않았고, 폴백을 지워도 192개가 통과했다.

두 결함은 같은 모양이다. **한 번 고치고 한 번 확인한 것** — 두 지점에 적용된 수정을 한 지점에서만 검증했다.

재현: [`tools/mutate.py`](tools/mutate.py). 변형을 적용하고, 스위트를 돌리고, 성공·실패·크래시 어느 쪽이든 원본을 되돌린다.

**이어서 공허한 테스트를 훑었다** — 통과하는 테스트가 붙잡고 있는지 물었으니, 반대쪽도 물어야 한다. 69개 테스트 파일의 함수를 AST로 훑어 세 형태를 찾았다: 단언이 아예 없는 것, 항상 참인 것(`assert <상수>`), 그리고 **단언이 전부 반복문 안에만 있는 것** — 컬렉션이 비면 본문이 안 돌아 통과한다.

거의 빈손이었고, 그게 결과다. 항상 참 0건. 단언 없는 9건은 전부 정당했다 — `json.dumps`가 터지지 않는 것, `validate_for_page`가 거부하지 않는 것이 곧 단언인 테스트들이다. 반복문 38건 중 대부분은 하드코딩된 튜플을 돈다.

**읽는 대신 실제 반복 횟수를 쟀다.** 이게 이 감사를 믿을 수 있게 만드는 부분이다 — 코퍼스를 디스크에서 읽는 것들이 진짜 위험한데, 재보니 법령 14건·PDF 구역 724개·문서 3건으로 전부 비어 있지 않았다. 처음엔 경로를 잘못 봐서 "코퍼스 없음"으로 읽었고, 그건 감사 결과가 아니라 제 실수였다.

둘만 고쳤다. 둘 다 **다른 테스트에 기대어 의미를 얻고 있었다** — 컬렉션이 비어 있지 않다는 것을 자기가 아니라 옆 테스트가 붙잡고 있어서, 그 옆 테스트를 지우면 조용히 아무것도 안 하는 테스트가 된다. 지금은 각자 몇 개를 봤는지 말한다.

---

## 테스트하고 있던 코드가 그 코드가 아니었다

변형 감사를 마치고 환경을 점검하다 나온 것. 네 저장소가 editable로 설치돼 있었는데, **설치 경로가 `/tmp/fresh/...`와 `/tmp/asc`** — 며칠 전 새-클론 검증 때 만든 사본이었고 지운 적이 없었다.

각 저장소의 자기 테스트는 살아남았다. `sys.path.insert`로 자기 `src/`를 먼저 넣기 때문이다. **저장소를 넘는 테스트에는 그 보호가 없다** — `mcp-gateway`의 스위트는 `agent-safety-core`의 `core`를 로드하는데, `/tmp/asc`에서 찾아 **4일 묵은 원장을 상대로 검증하고 있었다.** 통과했다. 현재 원장이 망가져 있었어도 통과했을 것이다.

CI는 이 문제에 노출되지 않는다 — 형제를 새로 체크아웃해 설치한다. 그래서 이 고장은 오래 쓴 개발 기계에서만 일어나고, 하필 거기서 보이지 않는다.

[`tools/check_env.py`](tools/check_env.py)가 각 모듈이 어느 체크아웃으로 해석되는지 보고한다. 저장소를 검사하지 않는다 — **저장소가 검사받고 있는 방을 검사한다.** 어떤 저장소도 자기 자신에 대해 확인할 수 없는 것이다.

### 그러다 `core`가 namespace 패키지인 걸 발견했다

`core/__init__.py`가 없었다. namespace 조각은 경쟁하지 않고 **합쳐지고**, 작업 디렉터리의 `core/`가 `sys.path` 앞이라 이긴다. 무조건 승인하는 `ExecutionLedger`를 `core/ledger.py`에 두고 `from core.ledger import ExecutionLedger`를 했더니 그쪽이 import됐다 — **승인 강제가 존재 이유인 라이브러리에서**, 경고 없이.

| | 로컬 `core/ledger.py` | 결과 |
|---|---|---|
| `__init__.py` 없음 | 있음 | **로컬 파일이 import됨** |
| `__init__.py` 있음 | 있음 | 설치된 패키지가 이김 |

같은 이유로 `document-intelligence`의 어댑터를 `document_intelligence.adapters.pdfplumber`로 옮겼다. `adapters`는 `agent-safety-core`도 내보내는 이름이라, 둘 다 설치하면 문서에 적힌 import가 `ModuleNotFoundError`가 됐다.

**이 회차에 내 검사 도구가 세 번 틀렸다.** 최상위 패키지를 훑는 스크립트가 정규 패키지 둘을 namespace로 잘못 보고했고(그래서 잠시 내가 만든 결함으로 오해했다), 대조군 빌드가 두 번 캐시를 재사용해 무효였다. 셋 다 결함 옆에 같이 적어둔다 — 검사기가 틀리면 결론도 틀린다.

---

## 저장소에 있는데 아무도 돌려본 적 없는 것

ModelMate는 애플리케이션인데 CI가 pytest만 돌리고 있었다. README가 안내하는 `uvicorn backend.main:app`과 스모크 스크립트는 **한 대의 기계에서 손으로만** 돌아간 적이 있다. `backend/main.py`는 `main_parts/*.part`를 import 시점에 조립하므로, 조립이 깨지면 모듈을 직접 import하는 테스트는 전부 통과한 채 앱만 안 뜬다. **스위트가 통과한다는 것과 앱이 뜬다는 것은 다른 주장이다.**

띄워봤더니 6.7초에 103개 라우트, OpenAPI 91경로, 제품 스모크 16/16. 그래서 CI에 넣었다 — **스모크가 실패할 줄 아는지 먼저 확인한 뒤에** 신뢰한다. 아무것도 듣고 있지 않은 포트를 향해 한 번 돌려 실패하는 것을 보고, 그 다음에 진짜 서버를 친다.

이어서 저장소의 스크립트 28개를 전부 돌렸다. **25개가 통과했고, 세 개에서 뭔가 나왔다.**

**`can_rerun`이 거짓말하고 있었다** — API가 실패한 작업에 `can_rerun: true`를 내놓고, 실제로 재실행하면 409로 거부했다. 화면에 버튼이 보이고, 누르면 에러가 난다. 원인은 익숙한 모양이다: `can_rerun`은 상태만 보고, 엔드포인트는 데이터셋 참조까지 요구했다 — **한 질문에 규칙이 둘, 맞춰보는 곳은 없음.** 이제 함수 하나가 답하고 양쪽이 그걸 읽는다. 거부 사유도 플래그와 함께 다닌다 — "다시 실행할 수 없습니다"는 "왜"가 붙어야 행동으로 옮길 수 있고, 그 why가 클릭한 뒤에야 나오는 409 안에만 있었다.

**"빌드가 깨짐"과 "npm install을 안 함"이 같은 fail이었다** — 단서는 `MODULE_NOT_FOUND` 스택 트레이스뿐. 정반대의 대응이 필요한 둘이고, 구분 못 하는 릴리스 관문은 새 체크아웃에서 절대 초록불이 안 되면서 **진짜 빌드 실패가 준비 부족과 똑같이 보인다.** 양쪽 다 확인했다 — 깨진 빌드는 여전히 fail, 의존성 없음은 조치 명령과 함께 skipped.

**QA 스크립트가 매번 저장소를 키우고 있었다** — 모델 파일과 DB는 지우고 업로드한 CSV는 안 지웠다. 그렇게 생긴 파일 셋이 커밋돼 픽스처처럼 앉아 있었다. 아무것도 참조하지 않는다. 이제 지우고, CI가 `git status`로 확인한다.

세 번째와 네 번째는 **내 측정이 틀린 것**이었다. 파이프 뒤의 `$?`를 스크립트 종료 코드로 읽어 멀쩡한 스모크를 결함으로 볼 뻔했고, 실행기가 인자를 받지 않는 스크립트에 `--base-url`을 넘겨 그 실패를 스크립트 탓으로 돌렸다. 둘 다 다시 재서 잡았다.

---

## 문서가 없는 파일을 가리키고 있었다

"아무도 돌려본 적 없는 것"을 나머지 저장소로 들고 갔다. 진입점부터 — `benchmark/run.py`, `experiments/kr_law_retrieval.py`, `core.export verify`, `mcp_gateway.audit verify` — 전부 돌았고, 두 검증 CLI는 정상 로그와 변조 로그를 제대로 갈랐다(각각 종료 0과 1). **빈손이고, 그게 결과다.**

그래서 다음 질문으로 갔다. **문서가 파일 이름을 대는 것도 검증 가능한 주장이다.**

`backend/main_parts/*.py`는 `09a1116`(2026-06-11)에 `*.part`가 됐다. 그걸 가리키는 참조 18개가 문서 네 곳에 두 달 넘게 남아 있었다. **아무도 클릭해보지 않았기 때문에 아무도 몰랐다.**

그 문서 넷은 지난 발표와 인수인계의 기록이고, 쓰일 당시엔 경로가 맞았다. 조용히 고치면 **낡은 문서가 관리되는 것처럼 보인다.** 그래서 각자 `<!-- historical: 언제 -->`로 스스로 기록임을 선언하고, 그 뒤 무엇이 달라졌는지 적는다. 검사는 선언된 기록을 건너뛴다. **낡았다는 것이 선언이면 기록이고, 선언이 아니면 사고다.**

`SECURITY.md`는 두 읽기가 다 필요했다. 한 경로는 `backend/`가 빠진 축약이었고, 다른 하나는 **과거 커밋 시점의 이름이라 옳았다** — 키가 유출된 건 이름이 `.py`이던 시절이다. 링크처럼 보이지 않고 역사처럼 읽히게 고쳤다.

**첫 판은 740건 중 161건이 없다고 보고했고, 그 숫자는 아무 뜻도 없었다.** 산문 속 파일명(`ledger.py`)까지 경로로 셌고, `tools/call`(JSON-RPC 메서드)과 `text/csv`(MIME 타입)가 슬래시가 있다는 이유로 경로로 집혔다. 규칙을 링크와 확장자 있는 백틱 경로로 좁히고 나서야 실제 18건이 드러났다. **검사기가 틀리면 결론도 틀린다** — 이번 회차에도 그게 첫 번째 결과였다.

검사는 다섯 저장소 전부에 들어갔고, **자기 자신을 검사하는 테스트 6개**를 달고 있다: 문서를 보긴 했는지, 경로를 찾긴 했는지, 실패할 줄은 아는지, 기록 선언을 존중하는지, 산문 파일명을 경로로 오독하지 않는지, 있는 경로는 통과시키는지.

---

## 한 파일에서 원칙을 말하고 일곱 파일에서 어기고 있었다

내부 경로를 끝냈으니 **외부 링크**. 여섯 저장소의 문서에서 URL 39개를 확인했다. **7개가 깨져 있었고 전부 같은 호스트였다** — 무료 플랜 만료로 내려간 Railway 인스턴스.

ModelMate README는 그 배포가 내려갔다고 적고 주소를 지우면서 **"죽은 링크는 없는 링크보다 나쁘다"**고 썼다. 그런데 `docs/` 일곱 문서가 같은 주소를 그대로 들고 있었다.

가장 나쁜 건 `docs/prediction-api.md`였다. **예측 API 사용법을 가르치면서 존재하지 않는 호스트로 `curl` 예시를 준다** — 따라 한 사람은 아무것도 못 얻는다.

대부분은 `--base-url <주소>` 형태의 QA 명령이었고, 로컬 주소로 바꾸니 **지금 실제로 동작한다.** 과거 QA 실행 보고서와 에이전트 작업 로그 둘은 그때 그 주소를 적은 것이 정확한 서술이라 기록으로 선언했다.

**검사는 네트워크를 쓰지 않는다.** "이 주소가 지금 살아 있는가"는 오프라인에서 답할 수 없지만 "우리가 죽었다고 아는 주소를 살아 있는 것처럼 적고 있는가"는 답할 수 있다. 인터넷이 끊긴 CI에서 조용히 통과하는 검사는 검사가 아니다.

나머지 32개 — GitHub, arXiv, Hugging Face, sbert.net — 는 전부 정상이다. **훑어서 안 나온 것과 안 훑은 것은 다르다.**

### 그리고 그 다음 회차에 내가 그 숫자를 부풀렸다

문서 검사를 **문서마다** 파라미터로 걸었다. 어느 문서가 걸렸는지 pytest가 이름으로 알려주니까. 그랬더니 다섯 저장소에서 **119개가 늘었다** — 검사하는 성질은 5개인데. ModelMate는 한 파일이 88개를 만들어 427에서 515로 뛰었다.

이 저장소들은 README가 주장하는 테스트 수를 CI가 실제 수집 개수와 대조한다. 숫자가 두 번 어긋난 뒤에 만든 장치다. **그 숫자가 뜻을 가지려면 내가 먼저 부풀리지 말아야 한다.**

한 성질에 테스트 하나로 합치고, 어느 문서가 걸렸는지는 실패 메시지가 말하게 했다. 합친 뒤에도 잡는지 확인했다 — 깨진 링크를 심으니 `README.md → docs/nope.md`, 죽은 주소를 심으니 `docs/prediction-api.md → web-production-5d6fa…`라고 정확히 짚는다.

**총 1,306개.** 늘리는 것보다 줄이는 게 맞는 회차도 있다.

**그리고 그 새 테스트가 CI에서 조용히 skip되고 있었다.** 샘플 PDF는 2.2MB라 저장소에 넣지 않고 필요할 때 내려받는데, 그 내려받기가 `test_pdfplumber_adapter.py` 안에만 있었다. pytest는 파일을 알파벳 순으로 돌고 `test_cross_parser_agreement`가 앞이라, CI에서는 파일이 아직 없어 **10개가 통째로 skip됐다.** 로컬에는 이미 받아둔 파일이 있어 전부 통과했고, 그래서 로컬만 보면 알 수 없었다. 한 모듈이 남긴 부수효과에 다른 모듈이 기대고 있었다 — `conftest.py`로 옮겼고, 캐시를 지우고 그 모듈만 돌려 확인했다.

**같은 저장소의 CI가 `requirements.txt`를 쓰지 않고 있었다는 것도 그때 드러났다.** `pip install pytest pdfplumber`라고 손으로 적혀 있어서, 그 파일에 무엇이 적히든 CI는 상관하지 않았다. **README가 안내하는 설치 경로가 어디서도 돌아본 적이 없었다.** 형제 저장소 넷은 전부 `requirements.txt`를 쓴다 — 이 하나만 달랐다.

---

## README의 첫 줄을 아무도 돌린 적이 없었다

지난 회차에서 "CI가 `requirements.txt`를 안 쓰고 있었다"가 나왔으니, 그 형태를 다섯 저장소로 들고 갔다.

먼저 **CI에서 무엇이 skip되는지** 전부 훑었다. `rag-profile-selector`의 코퍼스 테스트 9개뿐이고, 그건 주간 워크플로가 실제로 받아서 돌린다 — 그 워크플로에 "받고 나서도 skip이면 실패시킨다"는 단계가 있고, 로그에 `0 skipped; the fetched documents were actually checked`가 찍혀 있다. **빈손이고 그게 결과다.**

(이때 워크플로 로그에서 포맷 안 된 f-string 자리표시자를 봤다고 생각했는데, **에코된 스크립트 원문이었다.** 보고하기 전에 소스를 열어본 게 다행이었다.)

그 다음이 진짜였다. **다섯 저장소 README가 전부 `pip install`로 시작하는데, 어떤 CI도 그걸 돌린 적이 없다.** 전부 `requirements.txt`만 설치하고 저장소 자신은 설치하지 않는다. 테스트는 `conftest.py`가 `src/`를 경로에 넣어 주니 통과하고, 그래서 **패키징이 깨져도 초록불이 유지된다.** 바로 지난 회차에 어댑터를 패키지 안으로 옮겼는데, `packages.find`가 새 하위 패키지를 빠뜨렸어도 아무도 몰랐을 것이다.

네 저장소에 `packaging` 잡을 넣었다. 핵심은 **체크아웃이 아니라 설치본을 import한다**는 것 — 저장소 안에서 물으면 체크아웃이 답을 주고, 설치가 완전히 망가져 있어도 통과한다. 그래서 불러온 파일이 작업 디렉터리 밖에 있는지 먼저 확인한다. CI 로그가 `site-packages/...`를 찍는다.

**실패할 줄 아는지 확인하는 데 네 번 걸렸다.** 하위 패키지를 빼는 변형을 세 번 시도했는데 세 번 다 "통과"가 나왔다 — 비교 기준을 잘못 잡았고(`/tmp`), editable 설치의 meta-path 파인더가 `PYTHONPATH`보다 먼저 답했고, **`src/*.egg-info`가 남아 있어 `--no-cache-dir`로 빌드해도 옛 파일 목록이 살아 있었다.** 빌드 잔여물이 제 대조를 무효로 만든 게 이번 세션에서 세 번째다. 전부 지우고 나서야 `ModuleNotFoundError`가 났고, 그제야 검사에 이빨이 있다는 것을 알았다.

---

## 안전장치가 순응한 사용자를 벌하고 있었다

문서가 주장하는 실험 수치를 다시 재봤다. 코드가 여러 회차에 걸쳐 바뀌었는데 마지막 확인은 8월 19일이었다.

`agent-safety-core`의 ablation 표(arm A~F × 5지표)는 정확히 재현된다. 다만 **표의 숫자를 실제 실행과 대조하는 것이 없었다** — 시나리오를 하나 추가하면 arm A의 중복이 6에서 7로 움직이는데 관계를 고정한 테스트들은 전부 통과한 채 README만 조용히 틀려진다. 테스트 수가 두 번 어긋난 것과 같은 모양이라, 표를 README에서 **읽어** 실행 결과와 맞추는 테스트를 넣었다. (자동 생성하지 않는다 — 자동으로 쓰면 파일은 항상 코드와 일치하고 증거이기를 그만둔다.)

`rag-profile-selector`에서는 판단을 **뒤집었다.** 기대값이 워크플로와 README 두 곳에 사는 게 걸려서 README에서 파싱하도록 바꿨다가 되돌렸다. 그 수치들은 표가 아니라 산문 안에 있어서("dense 단독(0.714)보다 나쁘다") 문장을 다듬는 것만으로 정규식이 값을 놓치고, **그 단계는 실패가 아니라 경고를 내므로 검사기가 조용히 죽는다.** 중복보다 나쁜 실패다.

그리고 ModelMate에서 진짜가 나왔다.

**권고를 따른 실행과 무시한 실행이 검증에서 갈리지 않았다.** 누출 검사가 컬럼 셋을 빼라고 하고, 그대로 빼면 AUC가 1.000에서 0.778로 내려간다 — 그게 이 데모의 요지다. 그런데 검증은 **두 실행을 똑같이 `invalid`로 차단**했다. 증거 묶음에 넘어가는 누출 정보가 학습이 실제로 쓴 컬럼이 아니라 **원본 데이터셋 전체**의 것이었다. 학습 뒤 재검사는 `low`/`high`로 정확히 갈리는데 — 예전에 내가 넣은 것이다 — **아무도 읽지 않았다.**

**안전장치가 순응한 사용자를 벌하면 사람들은 그것을 끈다.**

고치는 중에 한 번 더 걸렸다. 위험도만 재검사 값으로 바꾸고 컬럼 목록은 원본을 두었더니 **아무것도 달라지지 않았다** — `_has_high_leakage`가 컬럼 목록을 먼저 본다. 절반만 고친 것은 안 고친 것이고, 그 판을 테스트로 남겨뒀다.

같은 결함이 제품 경로에도 있었다. 에이전트 실행기가 누출 경고를 검사 시점에 저장하고 학습 뒤 갱신하지 않았다.

**보고서도 고쳤다.** 검증이 "보고서를 쓰기 전에 이걸 고치라"고 판정하는데, 차단된 실행과 정상 실행의 한계 고지가 **글자 하나까지 같았다.** 보고서만 받은 사람은 둘을 구분할 수 없었다. 이제 요약 첫 문장과 한계 고지가 차단 사유를 밝힌다. 작성을 막지는 않는다 — 막으면 사용자는 이유를 볼 수 없고, 근거를 보여주는 것이 이 제품의 요지다. (README는 "보고서 작성 자체를 막는다"고 적혀 있었고, 그런 일은 일어난 적이 없다.)

---

## 규칙 넷 중 하나는 규칙이 아니었다

지난 회차의 결함이 **"계산해놓고 아무도 읽지 않는 값"**이었으니(누출 재검사가 그랬다), 그 형태를 다섯 저장소로 훑었다.

**첫 판은 그 결함을 못 찾는 검사기였다.** 읽기를 문자열로 찾았는데 `d["k"] = v`라는 **쓰기 자체가** `["k"]`를 포함한다. 그래서 대입으로 만들어진 키는 전부 "읽힘"으로 나왔다 — 정확히 문제의 값이 만들어지는 방식이다. 소비자를 전부 지우고 다시 돌려보고서야 알았고, **대조가 없었으면 "이상 없음"을 결과로 보고했을 것이다.** AST로 다시 써서 `Store`와 `Load`를 구분하니 통과했다.

그렇게 나온 것: `rag-profile-selector`의 `rule:dense-for-long-queries`가 **`query_length`라는 없는 특징을 읽고 있었다.** 있는 것은 `query_token_count`와 `query_char_count`다. `f.get(이름, 0.0)`이 조용히 0.0을 돌려주니 조건은 언제나 거짓이었고, **이 규칙은 28건 전부에서 `bm25-char`를 골랐다 — 이름과 정반대로 한 번도 dense를 고르지 않았다.**

규칙이 아니라 상수였고, 사실상 `fixed:bm25-char`를 다른 이름으로 한 줄 더 센 것이다. 그런 채로 **"규칙 넷을 시험했으나 하나만 기준선을 이겼다"**는 문장 안에 들어가 있었다. 오타 하나가 실험 하나를 조용히 무효로 만든다.

공개 수치를 고쳤다(train+val 0.4613→0.3899, test 0.2708→0.3264). **결론은 바뀌지 않는다** — 기준선을 이긴 규칙은 여전히 하나, 순이득은 여전히 1건, test split에서는 여전히 아무도 못 이긴다. 고친 규칙은 양쪽에서 더 나빠졌다.

**다만 그 행의 test 수치는 봉인 해제 이후에 다시 계산한 것이고, 문서가 그렇게 적는다.** 규칙을 고치고 test에서 다시 재는 것은 프로토콜이 막으려던 순서다. 남겨둔 이유는 실행된 적 없는 규칙의 숫자를 그대로 두는 것이 더 나쁘기 때문이다.

이제 규칙은 자기가 읽는 특징을 **선언**하고, 그 특징이 실제로 생성되는지 확인한 뒤에야 실험이 진행된다. 갈리지 않는 규칙은 실행 중 경고로 표시된다 — 고정 프로파일을 다른 이름으로 세지 않기 위해서다.

한 가지 더 기록해둔다. `document-intelligence`는 이 검사에 **해당이 없다**(dataclass 기반이라 dict 대입이 없다). 검사기가 "0건"을 내는 것과 "훑어서 깨끗함"은 다르고, 도구가 그 둘을 구분해 말하게 했다.

---

## 더 큰 모델이 더 낮았다

먼저 지난 회차 결함의 형태 — **`.get("없는_이름", 기본값)`이 조용히 기본값을 돌려주는 것** — 을 다섯 저장소로 훑었다.

**여기서도 첫 판이 구조적으로 아무것도 못 찾는 검사기였다.** "이 이름이 저장소 어딘가에서 만들어지는가"를 문자열로 판정했는데, `.get("k", d)` **호출문 자체가 `"k"`를 담고 있어서** 어떤 이름도 유령이 될 수 없었다. 결함을 일부러 되돌려도 안 잡히는 것을 보고 알았다. 정의 증거에서 읽기 문법을 빼고 나서야 대조를 통과했다.

훑은 결과는 **빈손**이다. 8건이 나왔는데 6건은 외부 법령 API의 응답 필드(`.get`이 맞는 자리)이고 2건은 의도된 선택적 인자다. 남은 사각도 적어둔다 — 이 검사는 **다른 dict에 존재하는 이름을 잘못 읽는 오타**는 잡지 못한다.

그래서 실질 작업으로 넘어갔다. **README가 스스로 인정해둔 공백**이 있었다: `e5-small`을 고른 이유가 "끝낼 수 있어서지 성능 때문이 아니다". 더 큰 `bge-m3`(568M)는 745개 조문을 CPU에서 임베딩하다 끝나지 않아 포기했었다.

`multilingual-e5-base`(278M, 2.4배)는 **7분**이면 끝난다. 돌렸다.

| | MRR@4 | top-1 | 미검색 |
|---|---|---|---|
| dense (e5-small) | **0.714** | 64.3% | 21.4% |
| dense (e5-base) | 0.688 | 57.1% | **14.3%** |
| 최선 어휘 기반 | 0.360 | 28.6% | 53.6% |

**2.4배 큰 모델이 더 낮다.** 대신 정답 조문을 더 자주 찾아낸다 — 더 찾고 더 낮은 순위에 둔다.

평균만 보면 오해한다. 질의 단위로 세면 **우세 4 · 열세 6 · 동일 18, 순 −2.** 28건 표본에서 구분되지 않는 차이다. 이 저장소는 규칙 선택기의 순이득 1건을 같은 이유로 결과로 인정하지 않았고, 여기에 다른 기준을 쓸 이유가 없다.

세 가지가 정리됐다. **편의로 고른 선택이 정확도를 깎지 않았고**(그 확인 자체가 공백이었다), **dense가 어휘 기반을 앞선다는 결론은 모델 크기에 견고하며**(0.688 대 0.360), **용량을 키우는 것은 이 코퍼스에서 재현율과 상위 정밀도를 맞바꾼다.** 되찾은 4건은 대부분 paraphrase, 잃은 6건은 대부분 situational이다 — 관측이지 주장은 아니다. 28건으로 주장할 수 있는 크기가 아니다.

---

## 계획을 문장으로 남기면 다음에도 같은 실수를 한다

`rag-profile-selector`에 남은 인정된 공백 하나: **"다음 코퍼스는 난이도 라벨이 아니라 어휘 중복 측정값으로 층화한다."**

이유는 이미 측정돼 있었다. 라벨 분포는 고른데(test: paraphrase 5·situational 4·lexical 3) 검색 난이도를 지배한 건 **질의와 정답 조문의 어휘 중복**이었고, **중복이 0인 질의 9건 중 6건이 test에 몰렸다.** test가 우연히 하드 모드가 됐고 절대 수치가 절반 이하로 떨어졌다. 그리고 `check_query_set.py`가 그 중복을 **이미 계산하고 있었다** — 계산해둔 값을 안 쓴 것이지 없어서 못 쓴 게 아니다.

계획을 문장으로만 남기면 다음 코퍼스가 올 때 다시 라벨로 나눈다. 그래서 도구를 만들고 **지금 데이터로 검증**했다.

| | train | validation | test | split 간 격차 |
|---|---|---|---|---|
| 난이도 **라벨**로 층화 (현재) | 0.2690 | 0.3301 | **0.1813** | **0.1488** |
| 측정한 **중복**으로 층화 | 0.2602 | 0.2509 | 0.2546 | **0.0093** |

격차가 **16배** 좁아지고, 중복 0인 질의도 6/1/2에서 4/2/3으로 퍼진다.

**seed로 좋은 결과를 고른 게 아니다.** 30개 seed 전부 0.0093이다 — 질의가 짧아 중복 값이 성기고 **28건 전부가 동점군에 속해서** 어떤 동점 처리든 균형이 유지된다. 그렇다고 seed가 죽은 인자인 건 아니다(seed 0과 1 사이에 12건이 이동한다). **"균형이 seed에 무관하다"와 "seed가 아무것도 안 한다"는 다른 주장**이고, 테스트가 둘을 구분해 고정한다.

**현재 split은 다시 나누지 않는다.** `--write`는 `splits.json`이 있으면 거부하고 1을 낸다. 봉인을 실수로 깨는 경로가 없어야 한다.

여기서도 하나 걸렸다. 새 테스트 셋이 **잘못된 이유로 통과**하고 있었다 — 코퍼스 본문이 없으면 `main()`이 "질의가 없다"로 1을 내는데, 봉인 검사가 그걸 "덮어쓰기를 거부했다"로 읽었다. **같은 종료 코드가 두 가지를 뜻하면 단언은 아무것도 고정하지 않는다.** 모듈 전체를 같은 조건으로 묶고, 주간 워크플로가 두 파일을 함께 돌리게 했다 — **어디서도 안 도는 테스트는 없는 테스트다.** 수동으로 돌려 43개 전부 실행, skip 0을 확인했다.

---

## 있다고 적혀 있는데 없던 검사

skip 감사를 끝까지 밀었다. 다섯 저장소 전부 로컬 skip 0, CI에서는 `rag`의 25건뿐이고 그건 주간 워크플로가 실제로 돌린다(수동 실행해 43개 전부 실행·skip 0 확인). **skip으로 세어지지 않는 조용한 건너뜀** — 테스트 본문의 이른 `return` — 도 훑었고 0건이다(검출기가 실제로 잡는지 네 가지 경우로 확인한 뒤에).

두 감사 연속 빈손이라 각도를 바꿨다. **어떤 코드가 테스트에서 한 번도 실행되지 않는가.**

`document-intelligence`의 `reading_order.py`는 85%가 실행되고 있었고, **실행되지 않는 15%는 전부 거부 분기**였다. 잘못된 입력을 막는 `raise`가 열넷인데 하나도 발동한 적이 없었다. 전부 쏴봤고 열넷 다 동작했다 — 결함은 없었지만, **거부가 시험된 적이 없으면 그건 코드에 적힌 문장이지 성질이 아니다.** 조건 하나가 뒤집혀도 정상 입력만으로는 아무 차이가 없다.

같은 방법을 안전 코어에 적용했더니 **하나가 통과했다.**

```python
resolved = candidate.resolve()
if ".." in PurePosixPath(str(resolved)).parts:
    raise ScopeError("path escapes through traversal components")
```

`.resolve()`가 `..`를 **이미 접어 없앤 뒤에** `..`를 찾는다. `/tmp/../etc/passwd`는 `/etc/passwd`가 되고 `parts`에 `..`가 남지 않는다. **어떤 입력으로도 발동할 수 없는 검사**였고, 메시지는 traversal을 거부한다고 말하고 있었다.

실질적 영향은 좁다 — 해석된 경로가 실제 inode에 고정되므로 결속은 정직했다. 그러나 **활성 검사처럼 보이는 죽은 코드는 없는 검사보다 나쁘다.** 없으면 사람이 조심하고, 있으면 보호받고 있다고 믿는다.

그리고 이건 **같은 규칙을 두 곳에 다르게 구현한 것**이었다. `mcp-gateway`의 정책은 처음부터 원시 요청에서 `..`를 거부하고 그 이유까지 주석에 적혀 있다("해석하면 서버가 나중에 하는 것과 어긋난다"). 한쪽만 동작했다.

도달 불가한 줄은 지우지 않고 **`pragma: no cover`와 이유를 달았다**. `reading_order.py`의 시작점 개수·순환 검사는 구역 4개까지 전수 탐색으로 앞선 검사가 모두 선점함을 확인했다. 중복 페이지 키 검사는 `dict`으로는 못 만들지만 서명이 `Mapping`을 받으므로 **도달 가능**해서 테스트를 넣었다 — 셋을 뭉뚱그렸으면 하나는 영영 안 도는 검사로 남았을 것이다.

**이 결함은 코드를 읽어서가 아니라 커버리지가 "그 줄이 한 번도 실행되지 않았다"고 알려줘서 나왔다.** 미실행 거부 분기는 목록으로 만들어 하나씩 쏴볼 가치가 있다.

---

## 커버리지가 가리킨 곳을 하나씩 쏴봤다

지난 회차에 두 저장소에서 "미실행 줄이 거의 전부 거부 분기"라는 것을 발견했고, 나머지 셋으로 들고 갔다. **거부와 탐지는 이 저장소들의 제품 자체**다.

**`mcp-gateway`** — `metadata_integrity` 89%, `registry` 88%. 위조된 `integrity_id`, 비유한수 메타데이터, 객체가 아닌 스키마, 미등록 서버의 도구 등 거부 18개가 한 번도 실행된 적이 없었다. 전부 동작한다. 더 흥미로운 건 **탐지** 쪽이었다: `compare_metadata`가 **서버가 통째로 나타나거나 사라지는 것**을 잡는 경로가 안 돌고 있었다 — 도구 변경은 테스트가 있었고 서버 단위는 없었다. rug-pull 탐지에서 더 큰 사건 쪽이 비어 있었던 셈이다.

**`rag-profile-selector`** — `regret` 82%, `profiles` 92%. 전부 동작한다. 여기서 **성격이 다른 것 하나를 구분해 기록했다**: `probes.py`의 승인 목록 관문은 호출자 입력이 아니라 `extract()` **자기 출력**을 검사한다. 오늘 어떤 입력으로도 발동하지 않는 게 정상이고, 지키는 대상이 미래의 편집이기 때문이다. **발동할 수 없는 검사와 오늘 발동할 일이 없는 불변식은 다르다** — 승인 목록을 좁혀 관문이 물 줄 안다는 것을 보였다.

**`modelmate`** — `backend/agents/resume.py`가 **0%**였다. 백엔드 어디에서도 import하지 않는데 문서 두 곳이 현재 구성 요소로 설명하고 import 예제까지 실었다. 모듈 자신은 정직했다("skeleton for PR-12"). 어긋난 건 문서 쪽이라 **코드를 배선하는 대신 문서를 사실에 맞췄다** — 제품의 재개는 이미 rerun 엔드포인트가 담당하고, 쓰지 않는 두 번째 경로를 배선하면 같은 질문에 장치가 둘이 된다.

**그런데 테스트를 쓰다가 진짜가 나왔다.** `review_queue.PROCEEDING_ACTIONS`에 `resume.py`가 만드는 계획 동작 넷 중 **셋만** 있었다. 빠진 `prepare_replan_placeholder` 때문에, 검토자가 `fix_required`로 해결한 항목이 재계획 단계에서 **또 검토 항목을 만드는 고리**가 생긴다. 해결된 것을 다시 검토하게 만드는 건 검토를 무의미하게 만드는 가장 빠른 길이다. 오늘은 영향이 없다 — `resume.py`가 배선돼 있지 않아서 조용히 남아 있었다.

**그리고 이번에도 제 프로브가 먼저 틀렸다.** `RegisteredTool`을 없는 인자명으로 불렀고, 생성자가 낸 `TypeError`를 "거부가 동작한다"로 읽었다 — **일곱 개가 그렇게 초록불이었고 검사하려던 거부는 하나도 실행되지 않았다.** 헬퍼가 실제 서명으로 한 벌을 만들고 필요한 것만 바꾸도록 고쳤다.

**저장소의 가드가 나를 두 번 잡았다.** 새 파일이 형제 저장소 의존을 퍼뜨리자 `test_optional_sibling.py`가 이름으로 짚었고, 허용 목록을 넓히는 대신 이미 의존하는 파일로 옮겼다 — **가드가 잡았을 때 가드를 고치는 것은 가드를 없애는 것과 같다.**

---

## README가 앞세운 기능이 절반에서 멈춰 있었다

지난 회차에 "에이전트 층은 pytest가 아니라 실행 중인 앱이 돌린다"고 **검증 없이 주장**했다. 재봤다: 앱을 계측한 채로 스모크 13개를 전부 돌려도 `backend/agents/executor.py`는 **9%에서 움직이지 않았다.** 제 주장이 틀렸다 — Agent Mode 엔드포인트 12개를 **아무도 치지 않고 있었다.**

손으로 몰아보니 사슬이 절반에서 멈춘다. 학습은 `AutoML training completed`로 성공 관측을 남기고, **바로 다음 설명 도구가 "Run AutoML training before explanation."으로 실패**한다. 같은 요청 안에서 한쪽은 썼고 한쪽은 못 읽었다.

원인은 스레드 경계였다. `automl_training_tool`은 이미 도는 이벤트 루프 안에서 호출되면 코루틴을 `ThreadPoolExecutor`로 넘기는데 — FastAPI 요청 안이 그렇다 — **`ContextVar`는 스레드 경계를 넘지 않는다.** 새 스레드는 빈 문맥에서 시작해 기본 스코프를 읽고, `set_target`과 `run_cv`가 **요청 버킷이 아니라 공유 기본 버킷**에 썼다.

**조용한 쪽이 더 나쁘다.** 요청별 격리를 넣은 이유가 정확히 그 공유 버킷을 없애는 것이었다 — 로그인이 붙은 배포에서 A가 올린 데이터를 B의 다음 요청이 분석하던 문제. **이 경로만 그리로 되돌아가 있었고, 실행된 적이 없어 아무도 몰랐다.**

| | 완료 단계 |
|---|---|
| 고치기 전 | 7 / 10 (설명 실패, 검증·보고서·API 준비도 미실행) |
| 고친 뒤 | **10 / 10** |

**진단 중에 제 흐름이 두 번 틀렸다.** 처음엔 별도 프로젝트를 만들어 넘겨서 `dataset_state_mismatch`로 막혔는데 — 업로드가 데이터셋에 프로젝트를 자동으로 붙인다 — **게이트가 옳았고 제가 틀렸다.** "Agent Mode가 실행되지 않는다"고 보고할 뻔했다. 로그인 응답의 토큰 필드명도 추측해서 틀렸다.

스모크를 만들어 CI에 넣었다. **보는 것은 "200이 왔다"가 아니라 "사슬이 끝까지 갔다"이다** — 200은 그때도 계속 오고 있었다. 수정 전 서버에 대고 돌리면 3건이 실패하고 종료 코드 1을 낸다.

---

## 보안 주장의 절반만 맞았고, 하필 자신 있는 쪽이 틀렸다

지난 회차 결함의 형태 — **스코프가 실행 경계를 넘으며 조용히 공유 버킷으로 떨어진다** — 를 백엔드의 모든 경계로 훑었다. FastAPI 배경 작업은 동기·비동기 둘 다 스코프를 지키고, `_execute_training_job` 안의 중첩 `asyncio.run`도 지킨다. **추론이 아니라 최소 앱을 만들어 쟀다.** 빈손이고, 그게 결과다 — 깨진 경계는 지난 회차에 고친 그 하나뿐이었다.

그래서 스코프 자신의 보장으로 갔다. `scope_for_user`의 docstring은 이렇게 적혀 있었다:

> `sub`는 계정 식별자이거나 데모용 `guest:<session>`이라 **둘 다 클라이언트가 스코프로 지정하는 것이 아니다.**

**게스트 쪽은 클라이언트가 지정한다.** `x-modelmate-guest-session` 헤더에서 온다. 보안 주장의 절반이 틀렸고, 하필 자신 있게 적은 쪽이었다.

실제로 성립하는 것은 더 좁고, 이제 그대로 적혀 있다:

- 게스트는 **계정** 버킷에 닿을 수 없다 — `guest:` 접두사를 서버가 붙이고 콜론을 지우므로 `guest:<무엇이든>`이 UUID와 같아질 수 없다
- 게스트는 **공유 기본** 버킷에 닿을 수 없다 — 요청별 격리를 도입한 이유가 그 버킷을 없애는 것이었다

계정 id 그대로, `../` 붙인 것, `guest:` 접두사를 흉내 낸 것, 널 바이트, `__default__` — 전부 시도했고 전부 `guest:` 아래로 떨어진다.

**성립하지 않는 것도 적었다.** 같은 세션 id를 보낸 두 게스트는 버킷을 공유하고, 정제 때문에 `a:b:c`와 `abc`가 같은 스코프가 된다. 세션 식별자란 원래 그런 것이고 쿠키와 다르지 않다 — **감춰두는 것은 같은 과신을 작은 글씨로 반복하는 것**이라 적어뒀다.

두 게스트가 서로 다른 스코프를 받는다는 것은 이미 고정돼 있었다. **클라이언트가 남의 버킷에 닿을 수 없다**는 쪽은 없었다.

---

## 보장 문장을 전부 모아 하나씩 확인했다

지난 회차 결함은 **"docstring이 안전 속성을 단언했는데 절반이 틀렸다"**였다. 그 형태로 다섯 저장소의 **읽는 사람이 보는 문장**을 훑었다 — 강한 양태(할 수 없다·불가능·보장·거부한다)와 안전 어휘(승인·격리·누출·무결성·스코프)가 같이 나오는 문장 **30개**.

대부분은 이미 신중하게 한정돼 있었다("기록은 위변조 **탐지 가능**일 뿐 위변조 불가능이 아니다"). 둘이 남았다.

**1. 프로세스에 대해 주장하고 스레드로 시험한 것.** `agent-safety-core`의 ADR-002는 "**다중 프로세스**가 같은 tip을 읽고 분기하는 시나리오가 구조적으로 불가능해진다"고 적어두었는데, 동시성 테스트는 전부 스레드였다. 한 프로세스 안의 스레드는 연결을 공유하고 GIL이 상당 부분을 직렬화한다 — 각자 연결을 열고 OS 파일 락으로 경쟁하는 것과 다른 상황이고, **더 어려운 쪽이 시험되지 않은 쪽이었다.**

프로세스로 재봤다. **주장이 성립한다** — 6개 × 12회 append에서 sequence 144개가 전부 고유하고 빈틈이 없고, 8개가 같은 lease를 노려도 정확히 하나만 획득하며, export한 체인이 검증을 통과한다. 결함은 없었고 ADR에 실측값을 적었다.

**2. 지난 회차에 한 곳만 고친 것.** docstring의 틀린 주장을 고쳤는데 **README가 같은 문장을 그대로 반복**하고 있었다 — "클라이언트가 남의 범위를 지정할 수 없는 이유는 애초에 범위를 지명하지 않기 때문이다". 게스트 범위는 클라이언트가 보낸 헤더에서 온다.

**같은 주장이 두 곳에 살면 한 곳만 고쳐진다.** 이 저장소들이 반복해서 찾아온 모양이고, 이번엔 내가 만들었다.

나머지는 시험돼 있었다 — 게이트웨이의 중복 키 거부(3건)와 끝부분 절단 탐지(2건), ModelMate의 "LLM이 결정론적 거부를 뒤집을 수 없다"(2건, 조기 반환으로 구현). **훑어서 안 나온 것과 안 훑은 것은 다르다.**

---

## `docs/`는 아무것도 안 했다고 말하고 있었다

지난 회차 결함은 **"같은 주장이 두 곳에 살면 한 곳만 고쳐진다"**였고, 그걸 만든 게 나였다. 그 형태를 기계적으로 찾아봤다 — 여섯 저장소의 문장을 근접 중복으로 짝지어 보고(구두점 차이뿐), 이름 붙은 수치 513종이 문서마다 갈리는지 봤다(전부 오탐: `hybrid-rrf-k4`와 `k8`은 서로 다른 프로파일이지 같은 값의 충돌이 아니다).

**두 검사 모두 빈손이었는데, 그 과정에서 결과 문서가 셋이라는 게 눈에 띄었다.**

`rag-profile-selector/docs/RESULTS.md`:

> **내려받은 데이터셋도 모델도 없고, 검색·학습·평가를 실행한 적이 없으며, 경험적 결과가 없다.**

그 저장소에는 법령 745조문, 프로파일 5개 비교, 봉인 해제한 test split, dense 모델 2종 비교, 311줄짜리 결과 문서가 있다. `docs/STATUS.md`는 한술 더 떠 **"다음 승인된 작업: `docs/TASKS.md`의 A1 — 저장소를 조사하고 목록화하라"**고 적혀 있었다.

**낡은 것을 넘어 모순이다.** README는 결과를 앞세우고 `docs/`는 결과가 없다고 하는데, **`docs/`가 바로 읽는 사람이 결과를 찾으러 가는 자리다.**

착수 시점 템플릿에서 나온 문서라 **세 저장소 전부** 같은 문장을 들고 있었다.

| 문서 | 처리 | 이유 |
|---|---|---|
| `docs/STATUS.md` | 기록으로 선언 | 착수 때 **무엇을 의도적으로 미뤘는지**는 지울 이유가 없다. 조용히 고치면 그 판단이 사라진다 |
| `docs/RESULTS.md` | 사실로 정정 + 진짜 결과 가리키기 | 이름이 "결과"다. 배너만 달면, 결과를 찾아 온 사람이 여전히 오도된다 |

`tests/test_no_stale_status.py`가 재발을 막는다 — 살아 있는 문서에 그 문장이 있으면 실패하고, 선언된 기록은 넘어간다. **문구를 도로 넣어 잡히는 것을 확인했다.**

---

## 사양이 만들어지지 않은 프로젝트를 서술하고 있었다

지난 회차에 `docs/STATUS.md`와 `RESULTS.md`가 "아무것도 안 했다"고 말하는 것을 고쳤다. **뿌리는 착수 템플릿 문서 전체**였으므로 나머지를 마저 봤다.

세 저장소에 **미완 체크박스 77개**가 남아 있었다. 내용은 "코드가 없음을 확인하라", "동작을 구현하지 말라". 그리고 `rag-profile-selector`에서 더 큰 것이 나왔다.

`docs/PROJECT_SPEC.md`:

> **The MVP uses HotpotQA only.**

**HotpotQA는 한 번도 내려받은 적이 없다** — 받는 스크립트조차 없다. 만들어진 것은 한국어 법령 745조문 위의 실험이다. 여섯 문서에 22번 나왔다.

문서보다 나쁜 것은 따로 있었다. **`AGENTS.md`** — 다음 작업이 무엇을 해도 되는지 정하는 파일 — 이 여전히 "MVP는 HotpotQA만 쓴다"고 지시하고 있었다. **다음 변경을 이 프로젝트가 쓰지 않는 코퍼스로 되돌릴 문장이다.**

**형제 저장소는 이걸 할 줄 안다.** `mcp-gateway`의 SPEC은 첫 문단에서 자기 범위 변경을 정확히 기록했다 — *"원래 범위는 테스트베드를 합성 MCP 서버로 제한했다. 다음에 한해, 그리고 다음에만 그 제한을 해제한다."* **여기서만 하지 않았다.**

`DECISIONS.md`에 D-002 정정을 넣었다. **당시 이유는 적혀 있지 않아서 지어내지 않았다.** 지금 확인 가능한 것만 적었다 — 실제 코퍼스가 공공누리 제1유형이라 체크섬·재현 절차를 공개할 수 있다는 것, 합성 실험이 먼저였다는 것, HotpotQA를 받지 않았다는 것.

**결정을 바꾼 것이 문제가 아니라 바꾼 것을 적지 않은 것이 문제다.** 이 저장소들의 원칙이 "뒤집은 판단은 근거와 함께 남긴다"인데, 정작 **이 프로젝트가 무엇인지를 정하는 결정**이 빠져 있었다.

계획 문서는 고치지 않고 기록으로 선언했다. **체크박스 77개를 지금 채우면 계획을 그대로 따른 것처럼 보이고, 어디서 갈라졌는지가 사라진다.** `DECISIONS.md`만 검사에서 제외했다 — **폐기된 결정을 보관하는 것이 그 문서의 본분**이다.

---

## 지시 파일이 이미 만든 것을 금지하고 있었다

지난 회차에 `rag-profile-selector`의 `AGENTS.md`가 몇 달간 쓰지 않는 코퍼스를 지시하고 있던 것을 고쳤다. **`AGENTS.md`는 README와 다르다** — README는 읽는 사람에게 설명하고, `AGENTS.md`는 **다음 작업이 무엇을 해도 되는지 정한다.** 틀리면 오해로 끝나지 않고 다음 변경을 잘못된 방향으로 민다. 그래서 네 저장소를 전부 훑었다.

`document-intelligence/AGENTS.md`:

> Current `AUTO_READY` work is limited to inspection, planning, documentation, and environment bootstrap. **It does not authorize application implementation.**

그 저장소에는 증거 모델·좌표·계층·읽기 순서 구현과 실제 PDF 어댑터가 있고 테스트 159개가 돈다. `mcp-gateway`와 `rag-profile-selector`도 같은 단계 게이트를 들고 있었다. **셋 다** "작업 전에 `docs/TASKS.md`와 `docs/STATUS.md`를 읽으라"고 하는데, 그 둘은 지난 회차에 **기록으로 선언한 문서**다 — 사슬이 끊겨 있었다.

`modelmate/AGENTS.md`는 **"PR-01 범위 — 금지: DB 스키마 구현, 실제 LLM 에이전트 구현"**을 들고 있었다. PR-32까지 진행됐고 둘 다 구현돼 있다(`persistence.py` 996줄, 선택적 LLM 플래너).

**지우지 않고 소진 표시를 달았다.** 착수 때 무엇을 의도적으로 미뤘는지는 지울 이유가 없고, modelmate의 PR-01 범위는 그 파일 자신의 핵심 규칙("로드맵 전체를 한 번에 구현하지 말 것")이 실제로 어떻게 적용됐는지 보여주는 예다.

**안전 제약은 손대지 않았다** — 실서비스·실계정 금지, 실크리덴셜 금지, 공격적 익스플로잇 금지, 승인 없는 다운로드·장시간 작업 금지, 측정하지 않은 결과 주장 금지. **소진된 것은 단계 게이트뿐이다.** 테스트가 그 구분을 확인한다: 게이트가 표시 없이 남아 있으면 실패하고, 안전 제약이 사라져도 실패한다.

---

## 첫 화면이 로그가 돼 있었다

이 파일이 그 결과다. 프로필 README가 **465줄 22섹션**까지 자랐고 전부 회차순 덧붙임이었다 — 읽는 사람은 "이게 뭔지" 열여섯 줄을 보고 나서 사백 줄의 "감사를 또 돌렸다"를 만난다. **로그이지 첫 화면이 아니고, 한 번에 한 줄씩 내가 그렇게 만들었다.**

README는 먼저 필요한 것만 남겼다 — 저장소 다섯, 각자 **처음** 걸려 넘어진 것, 반복되는 모양, 방법, 그리고 무거운 것 셋. 나머지는 여기로 옮겼다. 잘라낸 것은 없다: 분리 전후의 섹션 집합이 같은지 확인했다.

**목차 앵커 둘이 깨져 있었다.** GitHub은 제목을 소문자로 바꾼 뒤 앵커를 만드는데 내 생성기가 그러지 않아서 `#README의-첫-줄...`이 아무 데도 가지 않았다. **조용히 깨지는 종류다** — 링크는 멀쩡해 보이고 눌러본 사람만 안다. `tools/check_anchors.py`가 CI에서 본다. 대문자 앵커와 없는 앵커 둘 다로 잡히는 것을 확인했고, **내부 링크가 하나도 없으면 통과가 아니라 실패**한다.

## 감사 도구가 자기 출력을 커밋하고 있었다

같은 회차에 `agent-safety-core`를 훑다가 **`.coverage`가 git에 추적되고 있는 것**을 봤다. 다섯 저장소 전부였다.

들어온 시점이 정확히 **미실행 거부 분기를 훑기 시작한 회차**다. 매 실행마다 내용이 바뀌는 53KB 바이너리라 읽을 수 있는 diff로 나타나지 않았고, 그래서 이후 모든 커밋에 조용히 따라다녔다.

**아이러니가 요지다.** 그 훑기가 "발동할 수 없는 검사"를 찾아냈는데, 그걸 찾는 데 쓴 도구가 그동안 자기 출력을 커밋하고 있었다.

`frontend/dist`는 백엔드가 정적으로 서빙하므로 의도된 것이고 발표 PDF도 마찬가지다 — 같은 종류의 사고가 아니다. **훑을 때 그 구분을 하지 않으면 의도된 것까지 지운다.**

---

## 매 실행이 다시 쓰는 것을 추적하고 있었다

지난 항목에서 `.coverage`를 찾았다. **바로 옆에 아홉 개가 더 있었다.**

ModelMate 저장소 루트의 QA 산출물과 실행 중인 앱이 쓰는 파일이 전부 추적되고 있었다. 마지막 로컬 실행이 만든 것이 다음 커밋에 들어갔다.

`experiment_history.json`이 그 모양을 가장 분명히 보여준다. **2.3KB → 34.4KB로 자랐고, 27건 중 8건이 문서 감사를 돌리며 생긴 것**이다 — 로컬 서버에 스모크와 Agent Mode를 돌린 부수효과지 분석 작업이 아니다. 그 8건이 진짜 19건과 같은 무게로 저장소에 남아 있었다.

**매 실행마다 바뀌는 파일은 읽을 수 있는 diff로 나타나지 않고, 그래서 아무도 반대하지 않는다.**

`modelmate.db`와 `uploaded_datasets/`는 이미 무시되고 있었다 — **하나는 무시하고 아홉은 추적하던 것이 일관성 없는 상태였다.** 지금은 같은 범주로 묶었고, 추적을 끊기 전 상태는 `docs/archive/qa/2026-08-21/`에 provenance와 함께 보존했다(어느 항목이 감사 실행분인지 날짜별로 셀 수 있게).

**저장소에 이미 그 패턴이 있었다.** `docs/archive/qa/2026-06-14/`가 정확히 같은 일을 한다 — `README.md`, `provenance.json`까지. **한 번 쓰이고 이어지지 않았다.**

**첫 시도가 틀렸다.** ignore 규칙이 파일명만 보므로 **아카이브 안의 같은 이름까지 삼켰다.** 보존하려고 만든 것이 커밋되지 않으면 아무 의미가 없다. `!docs/archive/**`로 예외를 뒀고, 확인하고 나서야 알았다.

다른 네 저장소는 빈손이다. `benchmark/run.py`와 `citation_quality.py`는 `--out`을 줄 때만 파일을 쓰고 기본은 표준출력이라, 실행해도 작업 트리가 더러워지지 않는다. **돌려서 확인했다.**

---

## 내 현황판이 같은 병에 걸려 있었다

여러 회차 동안 저장소들의 낡은 문서를 고쳤다 — `docs/`가 "아무것도 안 했다"고 하고, 지시 파일이 이미 만든 것을 금지하고, 사양이 만들어지지 않은 프로젝트를 서술하는 것들. **정작 내 현황판을 대조한 적은 없었다.**

미완으로 남아 있던 세 항목을 확인했다.

**"수치 조건 — 이 금액 이하만은 아직 표현 불가"** — `QuantityConstraint`가 `Decimal`로 구현돼 있고, 단위가 다르면 거부하고, 상한 방향을 고정한 이유까지 docstring에 적혀 있다. 더 나쁜 건, 내가 **변형 테스트에서 "수치 상한 검사를 끈다"가 잡히는 것을 이미 확인**했었다는 점이다. 코드도 보고 테스트도 돌렸는데 현황판만 안 고쳤다.

**"Phase 2~5 잔여: 설명가능성, API·보안, 평가·UX"** — 셋 중 둘은 끝나 있었다. 설명가능성은 테스트 12개 파일이 건드리고 Agent Mode 스모크가 그 단계의 완료를 단언한다. API·보안은 스모크 4종에 테스트 10개 파일이다.

**그런데 확인하다가 진짜 공백이 나왔다.** UX 쪽에는 검사가 없었다 — `package.json`에 테스트 스크립트가 없고 `vite build`만 돈다. 그리고 백엔드는 `frontend/dist`를 **정적으로 서빙**한다. `src`만 고치고 빌드를 잊으면 **앱은 옛 코드를 서빙하는데 모든 검사가 통과한다**: 제품 스모크는 HTTP API를 치고, 빌드는 성공하고, pytest는 백엔드만 본다. **사용자만 안다.**

빌드 결과를 다시 만들어 비교하지 않았다. 그건 툴체인이 바이트 단위로 재현될 때만 성립하고, node·vite 버전이 조금 달라도 거짓 실패를 낸다 — **거짓 실패를 내는 검사는 사람들이 끄는 검사다.** 대신 빌드 **입력의 지문**을 dist 옆에 남기고 비교한다. (오늘 `npx vite build`가 커밋된 dist를 바이트 재현하는 것은 확인했고 그렇게 적었다. **오늘 재현된다는 것과 다른 기계에서도 재현된다는 것은 다른 주장이다.**)

**"박스를 체크하려고 확인했는데 공백이 나왔다"가 요지다.** 셋 다 끝났다고 그냥 표시했으면 — 그게 편한 결말이었다 — 프런트엔드 공백은 그대로 남았을 것이다.

남은 미완은 이제 하나, **Windows 기계에서 사람이 해야 하는 파일 업로드**뿐이다.

---

## 관문이 설정을 고르는 코드에서 참조되지 않았다

지난 회차에 프런트엔드가 "빌드된다" 외에 검사가 없다는 걸 확인했다. 남은 절반은 **계층 간 계약**이었다.

**프런트엔드가 부르는 엔드포인트가 백엔드에 있는가** — 맞춰보는 곳이 없었다. 라우트 이름을 바꾸면 프런트엔드는 조용히 깨진다: pytest는 백엔드만 보고, 제품 스모크는 HTTP API를 직접 치며(프런트가 쓰지 않는 경로까지), `vite build`는 문자열 안의 URL을 검사하지 않는다. **호출 99개가 전부 실재하는 라우트를 가리킨다** — 빈손이지만 다음 개명은 여기서 걸린다. **메서드까지 본다**: 경로는 있는데 그 메서드가 없는 것이 실제 실패 모드이고, 경로만 비교하면 `DELETE /api/state`가 통과한다.

그 형태를 다른 저장소로 들고 갔더니 **진짜가 나왔다.**

`rag-profile-selector`의 D-003은 pilot 프로파일을 넷으로 정했다(`bm25-k4`·`dense-k4`·`hybrid-rrf-k4`·`hybrid-rrf-k8`). 실제 실험은 **다른 다섯**을 돌린다 — `bm25-word`, `bm25-char`, 둘의 RRF, `dense`, 셋 융합. 코퍼스와 함께 프로파일 집합도 뒤집혔고, 코퍼스처럼 **적히지 않았다.**

낡은 목록보다 큰 문제가 있다. `profiles.py`는 승인되지 않은 설정을 거부하려고 존재하는데 **`experiments/`가 그것을 부르지 않는다** — `resolve_profile`도 `validate_profile`도 import하지 않고, 그 모듈을 쓰는 것은 테스트뿐이다. **비교표에 검토되지 않은 설정이 들어오는 것을 막는 관문이, 설정을 고르는 코드에서 참조되지 않는다.** 이 프로젝트가 **처음** 만난 결함이다 — `access.py`에 권한 헬퍼가 전부 있었고 `ledger.py`가 하나도 import하지 않았다.

**지금 배선하지 않고 이유를 적었다.** `RetrievalProfile`은 `(method, k)`인데 `bm25-word`와 `bm25-char`는 **토큰화**가 다르고(모델에 그 축이 없다) `hybrid-all`은 셋 융합이라 둘을 융합하는 `HYBRID_RRF`가 아니다. 모델을 지나가며 뜯어고치는 것은 상태를 정확히 적는 것보다 나쁘다.

`AGENTS.md`도 고쳤다. **지난 회차에 그 줄을 고치다 절반만 맞췄다** — 낡은 넷을 두고 `hybrid-all`만 덧붙였다.

성립하는 쪽은 고정했다: 실험이 돌리는 다섯과 결과 표의 다섯이 같다.

**그리고 두 번 걸렸다.** 표 파서가 헤더 *문자열* 뒤부터 잘라 아무것도 못 읽었는데 — "표에만 있는 프로파일 없음"이 빈 집합에서 통과했다 — **공허 검사가 잡았다.** 그리고 정정문을 쓰면서 `AGENTS.md`에 "HotpotQA"를 다시 넣어 **내가 만든 검사에 걸렸고, 그걸 확인하기 전에 푸시했다.** 검사가 옳았다. 없던 것은 **정정문이 원문을 인용하는 것과 옛 주장이 그대로 서 있는 것의 구분**이었고, 그건 `DECISIONS.md`에 이미 있던 예외다.

---

## 요금제에 적어둔 한도를 아무도 강제하지 않았다

지난 회차의 형태 — **관문이 있는데 그것을 쓰는 코드가 없다** — 를 들고 다섯 저장소를 다시 훑었다. 이번엔 "프로덕션이 한 번도 부르지 않는 가드 함수"를 찾았다.

**첫 검출기가 틀렸다.** 호출(`name(`)만 세는 바람에 데코레이터로 걸리거나 `Depends()`에 넘겨지는 것들이 전부 "미사용"으로 나왔다 — 목록이 길었고, 길다는 것 자체가 신호였다. 참조를 세도록 고치자 `modelmate`의 `008_usage_limits.part`에서 둘만 남았다: `get_user_role`과 `checkQuota`.

`checkQuota`를 따라가니 **진짜였다.**

`PLAN_LIMITS`의 모든 요금제가 `max_report_exports_per_day`를 갖고 있다(free 10, pro 100, team 500, admin 무제한). 사용량 테이블에 `report_exports_count` 열이 있다. `checkQuota`가 `report_export` 액션을 그 한도에 매핑한다. `docs/pricing.md`가 보고서 내보내기를 요금제 항목으로 싣는다. **그런데 세는 곳도 막는 곳도 없었다.** `increment_daily_usage`는 `jobs_today`와 `prediction_api_calls_today`로만 불렸고, `enforce_limit`은 이 키로 한 번도 불린 적이 없다. `/api/report/html`은 **사용자 인자조차 받지 않았다** — 누가 부르는지 모르는데 그 사람의 한도를 볼 수는 없다.

한도 표·DB 열·요금제 문서까지 갖춰두고 **강제만 빠진** 상태였다. `access.py`에 권한 헬퍼가 전부 있었고 `ledger.py`가 하나도 import하지 않았던 것과 같은 모양이고, 단서도 같았다: **같은 질문("이 사용자가 이 행동을 해도 되는가")에 답하는 구현이 둘인데 하나만 배선돼 있고, 둘이 아는 한도 키가 서로 달랐다.**

배선했다. 기록은 **응답 직전**에 한다 — 400으로 끝나는 호출이 할당량을 먹으면 실패가 벌이 된다. 신원이 없으면 통과시킨다: 게스트·미인증은 계정 사용량이라는 개념이 없고, 거기서 세면 아무 계정에도 속하지 않는 숫자가 쌓인다. 포트 8761에서 확인했다 — 정확히 10번째까지 200, 11번째부터 429.

**테스트를 쓰면서 두 번 틀렸다.** `inspect.getsource`로 소스를 확인하려다 세 개가 `OSError`로 깨졌다: `backend.main`은 `.part` 조각을 이어 붙여 만들어져서 함수에 대응하는 파일이 없다. 그리고 무제한 요금제 이름을 `enterprise_mock`으로 짐작했는데 실제로는 `admin`이었다 — **있다고 믿은 것과 있는 것.** 이 회차가 찾아낸 결함과 정확히 같은 종류의 착각이다.

`checkQuota`가 여전히 아무에게도 안 불린다는 것도 테스트로 고정했다. 배선하는 날 그 테스트가 실패하고, 그때 두 구현 중 어느 쪽이 권위인지 정하게 된다.

---

## 저장소에 적힌 값으로 관리자가 될 수 있었다

앞 항목의 형태를 이어 훑었다. 이번 질문은 **문서·예제 파일이 선언한 환경변수를 코드가 실제로 읽는가, 그리고 읽지 않을 때 무엇이 되는가**였다.

**검출기가 두 번 틀렸다.** ① `os.getenv("NAME")` 형태만 읽기로 세는 바람에 `modelmate`의 여덟 개를 "선언만 있고 안 읽음"으로 보고했다 — 실제로는 매핑 딕셔너리를 거쳐 `os.getenv(env_name)`으로 읽힌다. 바로 앞 회차에 가드 함수 검출기가 호출만 세다 틀린 것과 **같은 실수를 연달아** 했다. ② 고친 검출기도 `VITE_API_URL`을 놓쳤다: 문자 클래스 안의 `\b`는 단어 경계가 아니라 백스페이스다.

그 두 번을 고치고 남은 것에 진짜가 있었다.

`JWT_SECRET`의 기본값이 `modelmate-secret-key-change-in-prod`였다. **그 문자열은 공개 저장소에 그대로 적혀 있다.** 설정하지 않은 배포에서는 누구나 `{"role": "admin"}`을 그 키로 서명해 관리자로 통과한다 — 계정도 비밀번호도 필요 없다.

**추측하지 않고 해봤다.** 로컬 인스턴스에 위조 토큰을 보내니 `/api/auth/me`가 200과 `role: admin`을, `/api/me/usage`가 `limit_label: 제한 없음`과 전 항목 `null` 한도를 돌려줬다. 고친 뒤 같은 토큰은 401이다.

`.env.example`이 더 나쁘게 만들었다. `JWT_SECRET=`(빈 값)으로 안내하는데 `os.getenv`는 빈 문자열도 "설정됨"으로 본다 — **안내를 그대로 따르면 빈 키로 서명한다.** 두 줄 아래 `DB_PATH`는 `.strip() or`로 같은 함정을 이미 피하고 있었다. 한 파일 안에서 한 줄은 맞고 한 줄은 틀렸다.

`ADMIN_PASSWORD`도 같았다. 기본값 `admin1234`로 관리자 계정이 자동 생성됐고 그 비밀번호는 **어느 문서에도 없다.** `admin@modelmate.local`은 설정과 무관하게 항상 관리자이므로(이건 문서화된 동작이다) 그 계정은 어느 배포에나 있었다.

**`docs/deployment-checklist.md`는 이미 "`JWT_SECRET`: long random value"라고 적어두었다.** 확인하는 것이 없었을 뿐이다 — 같은 날 찾은 보고서 내보내기 한도와 정확히 같은 모양이다. 선언은 다 있고 강제만 없다.

배포에서는 부팅을 거부하게 했다. 로컬은 설치마다 다른 키를 파일에 만든다 — 매 부팅마다 새로 만들면 재시작이 로그아웃이 되고, **그건 사람들이 꺼버리는 종류의 안전장치다.** `ADMIN_PASSWORD`가 없으면 관리자 계정을 비밀번호 없이 만든다(로그인은 이미 그런 계정을 거부한다).

옆에서 하나 더 나왔다. `ADMIN_EMAIL`(단수)을 따로 읽어 계정을 만드는 **두 번째 시딩 블록**이 있었다. `ADMIN_EMAILS`(복수)가 설정되면 `get_admin_emails()`는 단수를 무시하는데, 그 블록은 단수 값으로 계정을 만들고 `role='admin'`을 줬다 — 문서화된 목록에 없는 이메일이 관리자가 되는 경로였고 어느 문서에도 없다.

**세 번째로 걸렸다.** 옛 값이 소스에 남았는지 보는 검사를 파일 전체 검색으로 썼는데 **내가 방금 쓴 주석에 걸렸다** — 경위를 적으며 옛 값을 인용했기 때문이다. 지난 회차에 정정문이 "HotpotQA"를 인용해 같은 함정에 빠졌었다. 이제 주석과 독스트링을 걷어내고 본다. **걷어내기가 코드를 남기는지도 함께 검사한다** — 전부 지워버리면 무엇이 있어도 통과한다.

**나머지 넷은 빈손이다.** 네 라이브러리는 환경변수를 하나도 읽지 않는다. `mcp-gateway`의 정책은 전 경로가 거부 기본값이다(`default_deny`·`unknown_server`·`unknown_tool`·`malformed_request`·`ambiguous_rules`).

---

## 동시에 부르면 한도가 한도가 아니었다

바로 앞 회차에 내가 배선한 코드를 적대적으로 봤다. **선언된 한도를 강제하게 만들었으니, 그 강제가 동시 요청에서도 성립하는가.**

이 저장소의 사용량 한도는 전부 **읽고-판단하고-쓰기를 나눠서** 한다. `enforce_*`가 현재 값을 읽어 판단하고, 한참 뒤에 `increment_daily_usage`가 센다. 그 사이에 들어온 요청은 같은 값을 읽고 같은 판단을 한다.

**쟀다.** 한도 10인 무료 계정에 동시 20건을 넣으니 **12건이 통과**했고 기록도 12가 됐다. 보고서 내보내기는 검사와 기록 사이에 **보고서 생성이 통째로** 들어가서 창이 가장 넓다 — 초 단위다.

`claim_daily_usage`는 세는 것과 판단하는 것을 한 `UPDATE`로 한다: `WHERE ... AND COALESCE(열, 0) < 한도`. SQLite가 한 문장을 원자적으로 처리하므로 스레드에서도 프로세스에서도 초과가 없다. `rowcount`가 0이면 그 문장이 아무 행도 못 바꿨다는 뜻이고 그것이 곧 "한도가 찼다"이다. 같은 조건에서 **정확히 10건**, 기록 10.

진 쪽은 보고서를 만들어놓고 429를 받는다. 일은 버려지지만 한도는 넘지 않는다. 반대로 하면(미리 세어두고 실패하면 되돌리기) 400으로 끝난 호출이 할당량을 먹는 경로가 생기고, **그건 실패를 벌로 만든다** — 앞 회차에 세운 규칙을 그대로 지킨다.

**그리고 대조가 거짓 초록불을 냈다.** 음성 대조를 넣어뒀다 — "나눠 읽고 쓰면 정말 초과하는가". 단독 실행에서는 초과를 봤는데 **전체 스위트에서는 못 봤다.** 다른 테스트가 돌면 스케줄링이 달라져 나눠 하는 방식도 우연히 직렬로 실행된다. 대조가 어떤 날은 통과하고 어떤 날은 실패하면, **통과한 날의 다른 검사들이 무엇을 확인했는지 알 수 없다.** `Barrier`로 경합 지점을 고정했다 — 전부 읽은 뒤에 전부 쓰게 만든다. 이제 타이밍에 기대지 않는다.

**나머지 한도는 아직 이 모양이 아니고, 그대로 적었다.** 분석 작업·예측 API 호출은 여전히 나눠 한다. 창은 훨씬 좁지만(행 하나 INSERT) 0은 아니다. 작업은 시작한 뒤에 거절하면 고아 행이 남으므로 예약-후-실행으로 바꾸는 것이 맞고, 그건 작업 수명주기를 건드리는 별도 작업이다. 지금 상태를 테스트가 고정한다 — **고치는 날 그 테스트가 실패하고 문서도 함께 고치게 된다.**

---

## 같은 형태를 다섯 저장소로 들고 갔더니 감사 로그가 갈라졌다

앞 회차 형태 — **읽고-판단하고-쓰기를 나누면 동시에 깨진다** — 를 다섯 저장소로 훑었다. 안전 핵심이 정확히 그 자리에 있다.

**`mcp-gateway`의 감사 로그.** `record`는 번호를 매기고, 꼬리 해시를 읽고, 파일에 붙이고, 꼬리를 갱신한다 — 네 단계였다. 동시 16건이 **40회 중 40회 해시 체인을 분기**시켰다. 두 기록이 같은 `previous_hash`를 단다. `verify_audit_log`는 매번 잡아냈다 — 조용히 틀리지 않는다는 것은 좋은 절반이고, 나쁜 절반은 **동시에 두 건을 처리한 게이트웨이가 자기 검증을 통과 못 하는 감사 로그를 남긴다**는 것, 그리고 그러면 안 된다는 말이 어디에도 없었다는 것이다. 이 제품의 중심 주장이 "해시 체인 감사"다.

**세션 예산.** `consume`이 `check` 뒤에 값을 올렸다. 300회 × 동시 32건을 예산 10에 부딪치니 **1회에서 11건이 통과**했다. 드문 것과 불가능한 것은 다르고, **넘길 수 있는 예산은 상한이 아니다.**

**중복 요청 가드.** 이게 제일 나빴다. 기존 테스트는 `proxy._pending[7]`에 값을 **손으로 넣어** 상황을 흉내 냈다 — 가드는 시험됐지만 **그 상황은 한 번도 시험되지 않았다.** 실제로 만드니 같은 id를 단 프레임 8개 중 **2~6개가 통과**했다(20회 중 18회). 그 가드는 id 하나가 결과 하나에 대응하게 하려고 있는데, 여러 개가 떠 있으면 첫 응답이 기록을 pop하고 나머지는 아무것도 없는 것에 정산한다.

셋 다 잠금으로 묶었다. 분기 0/40, 초과 0/300, 겹침 0.

**고친 방향을 정확히 적었다.** 요청 잠금은 중복 가드가 동시에 성립하게 만들지 않는다 — **동시성 자체를 없앤다.** "이미 진행 중인 같은 id"라는 상황이 생기지 않을 뿐이다. 겹침을 허용하려면 id별 라우팅이 필요하고 그때 가드는 다시 부하를 진다. stdio는 어차피 겹칠 수 없다: 버퍼가 속성 하나이고 id가 다른 프레임은 **버린다.**

**`agent-safety-core`는 빈손이다.** 같은 실행에 동시 승인 12건 → 발급 lease 항상 1개(20회). 철회와 소비가 동시에 → 정확히 한쪽만(40회, 둘 다 성공 0건). `BEGIN IMMEDIATE`가 트랜잭션 전체를 직렬화한다. 원장을 스레드끼리 공유하면 `sqlite3`가 아예 막는다. 안 나온 것도 결과이고, 성립한다는 것을 고정해뒀다.

**나머지 둘도 빈손이고, 이번엔 이유가 분명하다.** 생성자 밖에서 `self.X`를 바꾸는 곳이 `document-intelligence`·`rag-profile-selector` 모두 **0건**이다. 경쟁할 상태가 없다. 대조로 같은 검출기를 `mcp-gateway`에 걸었더니 **방금 고친 두 곳(`audit.record`, `limits.consume`)을 정확히 집어냈고**, 덤으로 `transport._buffer`를 가리켜 세 번째 결함으로 이어졌다.

**옆에서 낡은 숫자가 셋 나왔다.** README는 "코어 없이 139개 통과·5개 skip"이라고 적어두었는데 실측은 208(지금은 215)이고 **46개는 아예 수집되지 않는다.** 워크플로 주석은 또 다른 값(35)을 말하고 있었다. 총 개수는 CI가 대조해서 유지됐고 **바로 옆의 이 숫자는 아무도 재지 않아 방치됐다.** `without-core` CI 잡을 만들어 재고 대조하게 했다 — 코어가 정말 없는지 먼저 확인하고, 통과 수가 0이면 어떤 README와도 일치하므로 그것도 거부한다.

**그 잡의 grep을 처음에 틀리게 썼다.** README의 강조가 "코어" 앞에 붙는데 뒤에 붙는다고 짚었다. 로컬에서 돌려보지 않았으면 CI가 "README가 틀렸다"고 말했을 것이고, 원인은 README가 아니라 그 줄이었다.

---

## 한도가 존재하는 이유인 그 엔드포인트에 검사가 없었다

미뤄뒀던 예약-후-실행(분석 작업·예측 API 호출을 원자적 청구로 옮기기)을 하다가, 그 옆에서 두 개가 더 나왔다.

**`/api/run-cv`는 어떤 `enforce_*`도 부르지 않았다.** 세기는 했다 — `_record_workspace_analysis_result`가 `record_sync_training_job`을 거쳐 `jobs_today`를 올린다 — 그런데 막는 것이 없었다. 무료 플랜 한도 5인 계정으로 CSV를 올리고 타깃을 정한 뒤 8번 불렀다. **8번 전부 200**, 카운터는 8까지. 사용량 화면은 한도를 넘겼다고 표시하는데 아무것도 거절하지 않는다. **모델을 실제로 학습·비교하는, 한도가 존재하는 이유인 바로 그 엔드포인트다.** 빠른 분석과 에이전트 모드는 자기 입구에서 검사한 뒤 같은 계산을 시작하는데, 그 계산을 직접 부르는 문은 열려 있었다. 고친 뒤 5회 200, 6회부터 429.

그 형태 — **통제를 지나치는 문** — 를 라우트 전체로 훑었다. 상태를 바꾸는 라우트 14개가 어떤 검사도 부르지 않았고 넷은 정당한 비인증 경로(가입·구글 로그인·게스트 세션·로그아웃)였다.

**남은 것은 배포된 모델을 부르는 공개 경로 둘이었다.** `/api/v1/{model_id}/predict`와 `/api/v2/{model_id}/predict`는 세지도 막지도 않는다. 토큰도 로그인도 필요 없고 model_id만 알면 무제한인데, `docs/pricing.md`는 **일일 예측 API 호출**을 요금제 항목으로 판다(free 100 · pro 5,000 · team 25,000). 강제는 토큰 경로 하나에만 걸려 있었다.

**공개 호출인 것과 무제한인 것은 다르다.** `docs/security-notes.md`의 "public prediction invocation"은 호출자가 로그인하지 않아도 된다는 뜻이지 아무도 세지 않는다는 뜻이 아니다. 소유자 할당량에 달았다 — 호출자는 그대로 익명이다. 103회 호출에 **100회 200, 3회 429**, 카운터는 100에서 멈춘다.

**청구 위치를 한 번 잘못 놨다.** 처음에 410·404보다 앞에 넣었다가, 없는 모델을 부른 호출이 할당량을 먹는다는 걸 알아차리고 뒤로 옮겼다. 앞 회차에 "실패가 벌이 되면 안 된다"고 정해둔 규칙인데 새 코드에서 그대로 어겼다.

**그리고 가설 두 개를 측정이 반박했다.** ① `099_quick_analysis`가 enforce만 하고 세지 않는다고 봤다 — grep에 짝이 없었다. 실제로는 `record_sync_training_job`을 거쳐 센다. ② 그 헬퍼가 `project_id` 있을 때만 불리니 프로젝트 없는 분석은 안 세어진다고 봤다 — 업로드가 워크스페이스 프로젝트를 만들어서 실제로는 센다. **둘 다 코드만 읽고 결함이라고 적을 뻔했다.** 이 회차가 실제로 찾은 둘은 그 다음에 나왔다.

**지난 회차에 "고치는 날 실패한다"고 적어둔 테스트가 설계대로 실패했다.** `TestTheOtherCountersStillHaveTheGap`이 빨간불이 됐고, 새 상태를 고정하는 클래스로 바꾸면서 문서도 함께 고쳤다. 남겨둔 것을 남겨둔 대로 처리한 첫 사례다.

---

## 퇴화한 숫자 하나가 통제를 통째로 끄고 있었다

이번 질문은 **강제된다면 정확히 그 값에서 강제되는가**였다. off-by-one은 조용하고, 통과하는 검사가 가장 잘 감추는 종류다.

`modelmate`부터 쟀다. 환경변수로 한도를 작게 줄여 경계를 값싸게 때렸다 — **행 50 통과·51 거절, 열 5 통과·6 거절, 프로젝트 2개 통과·3번째 거절, 데이터셋 2개.** 전부 정확했고, 덤으로 그 손잡이들이 **실제로 먹는다는 것**이 확인됐다(그전까지는 코드를 읽어서만 알았다). `enforce_limit`은 개수라 `>=`, 업로드 검사는 크기라 `>` — 둘 다 맞는 부호다.

**중간에 한 번 오염된 측정을 냈다.** 열 경계를 재는데 4열도 429가 나왔다. 앞선 업로드가 이미 데이터셋 한도(2)를 채운 것이었다. 계정을 새로 만들어 다시 재니 정확했다. **그대로 적었으면 없는 결함을 보고했다.**

빈손을 들고 다른 저장소로 갔더니 **같은 회차에 네 개가 나왔다. 전부 같은 형태다: 퇴화한 float이 비교 가드를 빠져나가 가장 허용적인 동작에 내려앉는다.**

**`agent-safety-core` — 만료하지 않는 lease.** `approve`가 `ttl_seconds`를 검증하지 않았다. `float("nan")`을 주면 `now + nan`은 `nan`이고 **SQLite는 그것을 NULL로 저장한다.** 이 스키마에서 NULL은 "만료 없음"이다 — `claim_lease`의 만료 검사가 `expires_at is not None`으로 시작한다. 1분짜리로 의도한 승인이 영원히 살고, 행에는 이상하다는 표시가 없다. 시뮬레이션한 30년 뒤에도 소비됐다. `inf`는 `now > inf`가 참이 되지 않아 같은 곳에 도착한다. **lease는 일회용이면서 시간 제한이 있는 것이고, 그중 절반만으로는 통제가 아니다.** 만료 경계도 그때까지 아무도 정하지 않았다 — 이제 `now > expires_at`(정확히 만료 시각까지 유효)을 세 지점으로 고정한다.

**`mcp-gateway` — 전부 통과시키는 레이트 리밋.** `capacity < 1`과 `per_second <= 0`으로 검사했는데 **`NaN`은 모든 비교에서 거짓**이라 둘 다 빠져나간다. 빠져나간 뒤에는 버킷을 오염시킨다: `tokens + elapsed * nan`은 `nan`이고 `nan < 1`도 거짓이라 전부 허용된다. **capacity 5로 선언한 버킷이 100회 중 100회를 통과시켰다.** `session_budget=nan`도 같다.

**`rag-profile-selector` — 뜻 없는 질문에 그럴듯한 숫자.** `overlap_at_k`는 `k < 1`에 `ProbeError`를 냈고 `rank_agreement`는 아무것도 검사하지 않고 **0.5**를 돌려줬다. 0.5는 그 함수가 문서화한 "비교할 것이 없다"는 값이다. 여기서는 그 차이가 크다 — **0.5는 데이터에 대한 사실이어야 한다.** `k=0`이 만든 0.5는 호출자에 대한 사실이고, 출력에서 둘은 구별되지 않는다. 공개하는 수치가 진짜인지를 지키려는 저장소에서 가장 나쁜 기본값이다.

**`document-intelligence` — `True`가 확신도.** 이 모듈은 **이미 `nan`과 `inf`를 어디서나 거절하고 있었다.** 다른 넷이 이번에 따라온 쪽이 여기다. 빠진 것은 `bool` 하나였고, 하필 그것이 이 모듈이 막으려는 실패였다: 인식기가 숫자 대신 플래그를 돌려주면 **확신함**으로 기록되는데, 바로 아래 독스트링은 인식기가 얼마나 확신했는지가 파서 경계를 넘어 살아남는다고 약속한다. `False`는 반대 방향의 같은 거짓말이다.

`bool`이 네 저장소 중 셋에서 걸렸다. `int`의 하위형이라 모든 숫자 검사를 그냥 통과한다. **`mcp-gateway`의 포트 검사와 `rag`의 프로파일 검사는 이미 배제하고 있었다** — 같은 판단을 이미 두 곳에서 하고 나머지에서 빠뜨린 상태였다.

**내 테스트가 이번에도 네 번 틀렸다.** `events()`가 `detail`을 이미 파싱해서 준다는 걸 몰랐고, TTL을 든 것이 `ToolGuard.request`가 아니라 `.approve`였고(승인은 사람의 몫이라 일부러 떨어뜨려 둔 것), `validate_hybrid_k`라는 없는 이름을 불렀고, `ProbeResult`의 필수 인자를 빠뜨렸다. 전부 첫 실행에서 드러났다.

---

## 한 주체의 두 가지 철자

이번 질문은 **같은 것을 다르게 써서 통제를 지나갈 수 있는가**였다. 세 저장소에서 나왔고, 셋 다 권한 통제의 심장이다.

**`modelmate` — 대문자 하나로 관리자 가입.** 가입의 중복 검사는 `WHERE email=?`로 SQLite 기본 비교(대소문자 구분)를 썼고, 역할 판정은 `is_admin_email`이 소문자로 낮춰 비교했다. **엄격한 쪽이 계정 생성을 막고 느슨한 쪽이 권한을 줬다** — 최악의 짝이다. `admin@modelmate.local`로 가입하면 400, **`ADMIN@modelmate.local`로 가입하면 200**에 `role: admin`, `plan: admin`, `limit_label: 제한 없음`. 비밀번호는 공격자가 정한 것이다.

바로 앞 회차에 시딩된 관리자 계정을 **비밀번호 없이** 만들도록 고쳤는데, 이 경로는 그것을 통째로 지나간다 — 계정을 새로 만들어버리기 때문이다. **한 구멍을 막았는데 옆문이 열려 있었고, 옆문은 더 넓었다.**

**`agent-safety-core` — 대문자 하나로 자기승인.** 권한 분리 검사 둘(`self-approval refused`, `self-reconciliation refused`)이 호출자가 준 문자열을 `==`로 비교했다. **에이전트는 자기 `approver_id`를 스스로 고른다.** `actor_id="agent-1"`인 실행에 대해 `"Agent-1"`·`"AGENT-1"`·`" agent-1"`·`"agent-1\n"`·`"agent‑1"`이 **전부 승인됐다.** 첫 줄 말고는 전부 같은 주체가 모자만 바꿔 쓴 것이다. UNKNOWN을 남긴 에이전트가 대문자로 그 결과를 스스로 선언하는 것도 같았다.

**`mcp-gateway` — 세션 이름을 다시 쓰면 예산 초기화.** 세션은 예산을 세는 단위인데 레지스트리가 넘겨받은 문자열을 그대로 키로 썼다. 예산 3에 여섯 철자가 각자 세션을 열어 **총 18회**를 통과시켰다. 세션 예산은 레이트 리밋 아래로 기어가는 루프를 멈추는 마지막 한도다.

셋 다 NFKC → strip → casefold + ASCII 문자 집합으로 고쳤다. **정규화만으로는 부족했다** — NFKC는 `‑`(U+2011)을 ASCII 하이픈으로 접지 않는다. 유사 문자는 비교에는 다른 주체이고 읽는 사람에게는 같은 주체다.

**접지 않는 쪽의 판단도 재서 고정했다.** `mcp-gateway`의 도구·서버 이름은 대소문자를 구분하는데, 어긋남이 **전부 닫히는 방향**으로 실패한다: 요청이 다르면 `unknown_tool`, 규칙이 다르면 `default_deny`, 공백이 섞이면 `malformed_request`. 이미 닫히는 이름을 접으면 규칙이 매치하는 범위만 넓어진다. **세션 식별자는 정반대라서 접는다.** 두 판단이 왜 다른지를 코드와 테스트 양쪽에 적었다.

**새 통제가 기존 배포를 깨뜨릴 뻔했다.** `modelmate`에 `users(lower(email))` 유니크 인덱스를 넣었는데, 시딩 루프가 옛 비교를 그대로 쓰고 있었다 — 대소문자 섞인 `Admin@Modelmate.Local` 행이 있는 DB에서는 그것을 못 찾고 INSERT로 가다 새 인덱스에 걸려 **부팅이 죽는다.** 그런 DB로 실제로 띄워서 확인하고 같은 커밋에서 고쳤다. 이미 중복이 있어 인덱스를 못 만드는 경우도 띄워봤다 — 찾는 쿼리와 함께 출력하고 부팅은 계속한다.

**나머지 둘은 빈손이고, 이번에는 이유가 각각 다르다.** `rag-profile-selector`에서 철자 차이는 **증거를 잃는 방향**이다(RRF가 한 문서를 둘로 쪼개 순위를 낮춘다). 문서 식별자는 대소문자가 의미를 가질 수 있으므로 접는 것이 오히려 틀릴 수 있고, 골드가 실재하는지는 저장소 자신의 `validate_evidence_mapping`이 이미 보고 있다 — 실제 코퍼스에서 돌려 통과를 확인했다. `document-intelligence`는 페이지 안 구역 식별자 유일성을 이미 강제한다.

**내 스캐너가 세 번 인공물을 냈다.** rag 코퍼스 식별자를 훑겠다고 만든 스크립트가 매니페스트 구조를 못 읽어 0건을 냈고, 경로를 틀렸고, 마지막에는 조문 번호를 식별자로 착각해 "정확 중복 616건"이라는 그럴듯한 숫자를 냈다. **셋 다 그대로 적었으면 없는 결함을 보고했다.** 추측을 멈추고 저장소 자신의 로더와 검사기를 쓰자 답이 나왔다 — 그리고 그 답은 "이미 검사하고 있다"였다.

---

## 통제가 작동한 순간이 어디에도 남지 않았다

최근 세 회차는 전부 **입력이 통제를 지나간다**였다. 이번엔 반대편을 봤다 — 통제가 작동했을 때, 그리고 권한이 바뀌었을 때 **그 사실이 남는가.** 지난 회차에 내가 시연한 우회들이 실제로 벌어졌다면 무엇으로 알 수 있었을지가 직접 걸린 질문이다.

**`modelmate` — 성공한 권한 상승이 안 남는다.** 미들웨어는 응답이 400 이상일 때만 `monitoring_events`에 넣는다. 실패한 요청은 전부 남는다. 그런데 **권한이 올라가는 일은 전부 200으로 끝난다.** 조사할 가치가 가장 큰 사건이 하나도 남지 않았다.

쟀다. 평범하게 가입해 역할 `user`를 받고, `ADMIN_EMAILS`에 그 주소를 넣고 앱을 다시 띄웠다. DB의 역할은 `admin`으로 바뀌어 있었고 **감사 이벤트는 0건**이었다. 환경변수 한 줄과 재시작으로 계정 하나가 관리자가 되는데 그 사실을 나중에 확인할 방법이 없다. 앞 회차의 대소문자 우회 가입도 같은 성질이다 — 벌어졌다면 200으로 끝나 아무 흔적을 남기지 않았을 것이다. **한 회차 전체가 "일어난 줄도 몰랐을 일"이었다.**

**`agent-safety-core` — 거절당한 시도가 안 남는다.** 정확히 반대쪽 절반이다. `claim_lease`는 거절할 때마다 `claim_refused`를 쓰는데 `approve`와 `reconcile`은 아무것도 쓰지 않았다. 이유는 구조에 있었다: `_transaction`은 예외에 ROLLBACK하고, `claim_lease`는 이벤트를 쓰고 **`return None`**으로 빠져나가 살아남았고, 나머지 둘은 `raise`로 빠져나가 이벤트가 거절과 함께 사라졌다. **자기승인 5회 + 범위 불일치 3회, 총 8번의 거절 뒤에도 이벤트는 `created` 하나뿐.**

**두 저장소가 같은 표의 반대편을 비워두고 있었다.** 한쪽은 실패만 남기고 성공한 상승을 놓쳤고, 다른 쪽은 성공만 남기고 거절을 놓쳤다.

**`mcp-gateway`는 이미 갖춰져 있었다.** 결정 7건(허용 3 · 레이트 차단 3 · 정책 차단 1)을 전부 남긴다. 빈손이고 이유가 분명하다. 그 김에 `_limiter.consume()`의 반환값을 버리는 줄을 보고 "세기만 하고 막지 않는다"고 의심했는데, **재보니 거절은 그 앞의 `check`에서 이미 일어나고 있었다** — 용량 3에 호출 10회, 통과 3·차단 7. 코드만 읽고 적었으면 없는 결함을 보고했다.

기록이 쓸모 있으려면 두 가지가 더 필요했다. **바뀔 때만 남긴다** — 시딩은 매 부팅마다 도는데 그때마다 쌓으면 배경 소음이 되고, 배경 소음은 아무도 읽지 않는다(재시작 두 번에 2건 확인). **보존이 감사 줄을 밀어내지 않는다** — 표에 상한이 있고 오래된 순으로 지웠으니 오류가 조금만 몰려도 "누가 관리자가 됐다"가 밀려난다. 이제 일반 이벤트를 먼저 버린다(상한 10에 보안 1 + 오류 30으로 확인).

**고치다 한 번 크게 틀렸다.** 거절을 기록하려고 `approve`의 검사와 쓰기를 두 트랜잭션으로 쪼갰고, 그 순간 **동시 승인 원자성이 깨졌다** — 두 워커가 각자 검사를 통과해 둘 다 승인한다. 두 회차 전에 쓴 `test_concurrent_approvals_issue_exactly_one`이 2를 세면서 잡아냈다. **그 테스트가 없었으면 감사 기록을 얻으려고 at-most-once를 팔아넘길 뻔했다.** 지금은 검사와 쓰기가 한 트랜잭션 안에 있고 거절 기록만 밖에서 한다 — 그쪽은 원자적일 필요가 없다. 트랜잭션이 하나인지도 테스트가 지킨다.

**성공한 요청을 전부 남기는 것은 아니다.** 그렇게 하면 그 표가 트래픽 로그가 되고, 그것도 아무도 읽지 않는다. 남기는 것은 권한이 바뀐 순간과 통제가 거절한 순간뿐이다.

---

## 메시지는 같았고 시간이 달랐다

지난 회차가 **거절이 남는가**였다면 이번엔 **거절이 필요 이상을 말하는가**다.

**`modelmate` — 스톱워치만으로 계정 목록을 얻을 수 있었다.** `/api/auth/login`은 두 갈래 모두에 같은 메시지를 준다 — "이메일 또는 비밀번호가 올바르지 않습니다". 메시지는 같았고 **시간이 달랐다.** 계정이 없으면 즉시 400, 있으면 pbkdf2 26만 회를 돌고 400. HTTP로 각 25회 중앙값을 쟀다:

| 경우 | 중앙값 |
|---|---|
| 존재하는 계정 · 틀린 비밀번호 | 280.3ms |
| 없는 계정 | 10.3ms |

**270ms는 네트워크 잡음에 묻히지 않는다.** 메시지를 같게 맞춰놓고 응답 시간이 답을 알려주는 상태였다.

계정이 없을 때도 가짜 해시로 같은 계산을 돌린다. `hash_password`로 만들지 않는다 — 그러면 부팅 때 pbkdf2 26만 회를 돌려 시작이 0.25초 느려진다. 형식만 맞으면 같은 계산을 하고 실패한다. 고친 뒤 **1.03배**(293.3ms 대 284.4ms).

**남은 8.9ms는 인정했다.** DB 행을 읽는 비용이고 이 방식으로는 0이 되지 않는다. 27배와 1.03배는 다른 이야기이고, **멈추는 지점을 적어두는 것이 0이라고 말하는 것보다 낫다.**

**가입은 고르지 않고 그대로 뒀다.** "이미 사용 중인 이메일입니다"는 계정 존재를 명시적으로 알려준다. 감추려면 가입을 항상 200으로 끝내고 메일로 알려야 하는데 이 앱에는 메일 발송이 없다. **감출 수 없는 것을 감춘 척하면 사용자만 혼란스럽다.** 새는 것이 아니라 고른 것이고 그렇게 적었다.

**`mcp-gateway` — 차단 응답이 정책 지도를 함께 준다.** 재보니 이유 코드와 `rule_id`가 같이 나간다: `delete_file`은 `explicit_deny` + 규칙 이름 `deny-delete-secret`, 없는 도구는 `unknown_tool`, 규칙 없는 도구는 `default_deny`. **셋이 구별되므로 도구 이름을 훑으면 정책을 지도로 그릴 수 있다.** 규칙 이름은 의도를 담는다. 게이트웨이는 에이전트를 제약하려고 있는데 제약받는 쪽에 지도를 건넨다.

**그런데 바꾸지 않았다.** 정찰은 `PROJECT_SPEC.md` §3의 승인된 위협 집합에 없고, `AGENTS.md`는 그 밖의 위협을 추가하려면 `NEEDS_APPROVAL`에 적고 승인을 받으라고 한다. **저장소 자신의 규칙을 내 판단으로 덮지 않았다** — 측정 결과와 함께 항목으로 올리고, 지금 모양을 테스트로 고정했다. 바뀌는 날 그 테스트가 실패하고 그때 결정을 다시 하게 된다. **그냥 흘러가서 바뀌는 것과 정해서 바꾸는 것은 다르다.**

이유 코드에는 값도 있다. 정당한 클라이언트가 다음 행동을 정하는 근거이고(없는 도구인지 금지된 도구인지) 운영자가 정책을 디버깅하는 근거다. 지우면 둘을 함께 잃는다.

**예측 API 토큰은 바꿀 것이 없었다.** 해시로 조회하므로(`WHERE token_hash=?`) 부분 일치를 만들 수 없다 — 공격자가 해시의 원상을 통제해야 한다.

**시간 측정을 CI에 넣지 않았다.** 부하에 따라 들쭉날쭉해 사람들이 꺼버리는 검사가 된다. 수치는 문서에 기록으로 남기고, 테스트는 **기전**("두 갈래가 같은 계산을 한다")을 함수 층에서 고정한다. 계산을 건너뛰는 갈래가 어떤 모습인지도 음성 대조로 남겼다.

---

## 내가 고친 것의 대가를 재봤다

여섯 회차 연속 통제 축이었다. 이번엔 방향을 돌려 **바로 앞 회차에 내가 넣은 수정이 무엇을 치르게 했는지** 봤다.

앞 회차에 로그인 응답 시간이 계정 존재를 알려주는 것을 고쳤다(280ms 대 10ms). 고치는 방법은 없는 계정에도 같은 계산을 돌리는 것이었다 — pbkdf2 26만 회. **전에는 실재하는 계정을 알아야 비쌌고, 이제는 아무 문자열이나 같은 값을 치른다.**

그리고 `modelmate`의 다른 모든 한도는 **계정에** 걸린다. 로그인·가입·구글 로그인은 계정이 정해지기 전이라 **아무 한도도 없었다.**

쟀다:

| | `/api/state` 중앙값 |
|---|---|
| 평시 | 3.1ms |
| 인증 없는 로그인 60건(동시 12) 중 | **1,844.9ms (603배)** |

**60건에 앱이 사실상 멈춘다.** 인증도, 계정도, 유효한 이메일도 필요 없다.

**시간차를 되돌리지 않았다.** 그건 진짜 누출이었다 — 되돌리면 한 결함을 다른 결함으로 바꾸는 것뿐이다. 필요한 것은 애초에 있었어야 할 조임이고, IP당 분당 시도 수를 묶었다. 같은 조건에서 17.8초 → 3.4초, **603배 → 18.9배**, 400 열 건과 429 쉰 건.

**남은 18.9배도 적었다.** 허용한 10건이 각자 pbkdf2를 도는 동안의 지연이고, 지속 부하는 IP당 분당 CPU 2.8초로 묶인다. **0이 아니라 경계가 생긴 것**이고, 그 차이를 흐리지 않는 편이 낫다.

한계 둘을 코드·문서·테스트에 함께 적었다. **한 프로세스 안에서만** 센다(워커가 여럿이면 각자 자기 통). 그리고 **`X-Forwarded-For`를 믿지 않는다** — 호출자가 채우는 헤더라 그것으로 세면 헤더만 바꿔가며 무한히 통과한다. 프록시 뒤에서는 여러 사용자가 한 칸을 나눠 쓰는데, 그 방향의 실패는 조이는 쪽이다.

**스캐너가 먼저 틀렸다.** "인증을 요구하지 않는 라우트"를 훑는데 POST/PUT/PATCH/DELETE로만 잘라서 사이에 낀 GET 라우트를 삼켰고, 그 바람에 **`/api/auth/login`이 목록에서 빠졌다** — 찾으려던 바로 그것이. 모든 데코레이터로 자르니 17건이 나왔다.

**내 테스트가 또 내 주석에 걸렸다.** "조임이 해싱보다 먼저인가"를 원문 위치로 비교했는데, 조임 바로 위에 내가 쓴 주석이 `verify_password`를 인용하고 있었다. **이 저장소들에서 세 번째** 같은 함정이다(`AGENTS.md`의 "HotpotQA", 공개된 기본 비밀값 검사). 주석을 걷어내고 보되, 걷어내니 코드가 남는지도 함께 확인한다.

**라이브러리 쪽은 표면이 다르다.** `agent-safety-core`의 `approve`는 승인자 프로세스가 부르지 에이전트가 부르지 않고, `mcp-gateway`의 감사 쓰기는 레이트 리밋 뒤에 있다. 인증 이전에 비싼 계산을 무제한으로 부를 수 있는 문은 `modelmate`에만 있었다.

스모크 셋이 전부 통과했다(16/16 · 19/19 · 13/13) — **조임이 정상 흐름을 막지 않는다는 것도 재서 확인한 것**이지 짐작이 아니다.

---

## 갓 클론한 사람이 보는 것을 처음으로 봤다

일곱 회차 연속 통제 축이었다. 물러서서 **포트폴리오의 첫인상**을 봤다 — 다섯 저장소를 깨끗한 곳에 클론하고 README 첫 명령을 그대로 돌렸다. 최근에 부팅 동작을 여럿 추가했으니(JWT 키 파일 생성, 유니크 인덱스, 보안 이벤트 flush) 그 통합 검증이기도 했다.

**`modelmate`는 깨끗한 클론에서 645개 전부 통과하고, 부팅해서 API와 첫 화면을 서빙하고, `.jwt_secret`과 DB를 스스로 만든다.** 최근 회차들이 넣은 것이 새 환경에서 동작한다.

**`rag-profile-selector`만 달랐다: 261 통과 · 25 skip.** README는 286을 판다.

코퍼스 본문은 재배포하지 않는다(공공누리 제1유형, 매니페스트만 커밋). 갓 클론한 저장소에는 문서가 없고 **skip 사유에도 그렇게 적혀 있다** — 선언된 것이지 사고가 아니다. 문제는 README가 그 사실을 말하지 않는다는 것이다.

**기존 CI 가드가 이걸 볼 수 없는 이유가 구조에 있었다.** "README가 자기 테스트 수를 잘못 세는가" 검사는 **수집 수**를 비교한다. **skip된 테스트도 수집된다.** README가 286이라고 말하고 CI가 286을 수집하는 동안 실제로 도는 것은 261이었다 — 검사는 초록불이었고, 초록불이 볼 수 없는 축이었다.

README에 두 숫자를 다 적고, `tests` 워크플로가 **실제로 돌린 결과**의 통과·skip 수를 대조하게 했다. skip이 0이면 그것도 거부한다 — 코퍼스가 있는 곳에서 돌면 비교가 아무 뜻이 없다.

**`corpus.yml` 머리말은 "nine tests skip"이었다.** 쓸 당시엔 맞았고, 그 뒤 내가 층화 테스트를 추가하며 16개가 늘어 25가 됐는데 아무도 재지 않았다. 두 회차 전 `mcp-gateway`에서 찾은 것과 같은 모양 — **총계는 유지되고 옆 숫자는 썩는다.**

**README 문구를 바꾸면 검사기 둘이 함께 깨질 뻔했다.** 기존 CI 가드와 `ohsewool`의 `check_counts.py`가 둘 다 `# NNN tests` 꼴을 읽는다. 기계가 읽는 표시는 남기고 사람이 읽는 설명을 덧붙였다. **새 통제가 기존 통제를 깨뜨리는 것은 이번이 두 번째다.**

그리고 루트를 셌다. **`modelmate`의 마크다운 16개 중 아홉이 6월 문서**였다 — `codex/split-for-team` 브랜치와 "frontend teammates", "Part 1 backend owner". 그 팀 분할은 존재하지 않고, `TEAM_SPLIT.md`가 **이미 기록으로 선언하고 있는** 그 단계다.

**규칙은 이미 정해져 있었다**: 선언된 낡음은 기록이고 선언되지 않은 낡음은 결함이다. 루트의 넷은 선언돼 있었고 **아홉은 아니었다.** 같은 저장소에서 같은 판단을 절반만 적용한 상태였다. 지우지 않고 `docs/archive/2026-06-team-split/`으로 옮기고 각각에 선언을 달았다 — 어디서 읽히든 스스로 설명하게. 16 → 7이 됐고, 다른 넷은 1~2개다.

**내가 먼저 오염된 숫자를 냈다.** 처음 셀 때 18이 나왔는데 그중 둘은 내 작업 디렉터리에만 있는 gitignore된 QA 산출물이었다. **재는 곳을 잘못 골랐다** — 갓 클론한 사람이 보는 것을 재겠다면서 내 작업 디렉터리를 셌다. 클론에서 다시 세서 16을 얻었다.

---

## 파는 한도를 꽉 채운 데이터로 돌려봤다

"파는 한도가 실제로 강제되는가"는 여러 회차에 걸쳐 봤다. 이번엔 **성능 판**이다 — 무료 플랜은 5000행 × 100열을 파는데, 그 크기를 한 번도 돌려본 적이 없었다.

`/api/run-cv`(모델 비교를 **동기로** 돈다) 실측:

| 데이터 | `/api/run-cv` |
|---|---|
| 1000행 × 20열 | 18.7초 |
| 1000행 × 100열 | 58.8초 |
| 5000행 × 20열 | 62.0초 |
| **5000행 × 100열 (무료 한도)** | **253.5초** |

시간은 대략 셀 수에 비례한다(25배 셀 → 13.6배). 업로드 5.3초, `/analyze-columns` 3.6초, 보고서 2초 미만 — **시간은 전부 모델 비교다.** 파는 한도의 데이터가 한 동기 요청을 4분 넘게 붙잡는다.

**그리고 프런트엔드에 타임아웃이 아예 없었다.** `axios.create({ baseURL })`의 기본값은 0, 즉 **무한**이다. 서버가 죽거나 연결이 끊겨도 화면은 영원히 돌고 사용자는 무엇이 잘못됐는지 알 수 없다.

짧게 잡을 수도 없다는 것이 이 발견의 핵심이다. **정당한 요청이 4분 넘게 걸리는데 60초에 끊으면 파는 한도가 동작하지 않는 것이 된다.** 보통 요청 60초, 분석 경로 15분(실측의 3배 이상)으로 나눴다. 어느 쪽이든 끝은 있다.

**처음 쓴 긴-경로 목록은 양쪽으로 틀렸다.** `/run-shap`·`/optuna`·`/predict-batch`는 이 프런트엔드가 부르지 않는 죽은 항목이었고(실제 경로는 `/run-optuna`), 반대로 재보니 `/analyze-columns`(3.6초)와 보고서(1.4초)는 넣을 필요가 없었다. **또 추측으로 목록을 만들었다.** 이 저장소들에서 반복된 실수라 검사로 고정했다 — 죽은 항목이 있어도, 측정된 느린 경로가 빠져도, 빠른 경로가 들어와도 실패한다. 무엇이 측정이고 무엇이 성질상 판단인지도 목록 옆에 구분해 적었다.

**상위 플랜 크기는 재지 않았고 그렇게 적었다.** `pro_mock` 50000×300은 셀 수로 30배, `team_mock`은 100배다. `docs/pricing.md`가 mock으로 선언하고 있고, **여기서 어떤 측정도 동기 경로가 그것을 감당한다고 말하지 않는다.** 비동기 작업 경로가 이미 있으니 옮기는 것이 답이지만 하지 않았고, 하지 않았다고 적었다.

**문서 경로 검사가 내 글을 잡았다.** 새로 쓴 문단이 `work/ROADMAP.md`를 가리켰는데 그건 저장소 밖이다. 저장소가 가리킬 수 없는 곳을 가리키면 안 된다 — 예전에 내가 만든 검사가 이번엔 나를 막았다.

**그리고 내가 두 번 오독했다.** 응답의 `best_model_name`이 `None`이라 결함인 줄 알았는데 키 이름이 `best_model`이었고, `/api/state`가 비어 보인 것은 요청별 격리 때문이었다(토큰으로 다시 부르니 `best_model: Logistic Regression`, `data_shape: [5000, 99]`). **둘 다 그대로 적었으면 없는 결함을 보고했다.**

---

## 공개한 숫자를 실행과 대조하는 것이 두 저장소에 없었다

최근 여덟 회차에 다섯 저장소의 계산 경로를 여럿 건드렸다 — 정규화, 입력 검증, 원자적 청구. 그중 **공개된 수치를 만드는 경로**가 있는데, 바꾼 뒤 재현을 한 번도 확인하지 않았다.

**먼저 손으로 확인했다.** `rag-profile-selector`의 실험 둘을 다시 돌렸다. 프로파일 다섯(0.304 / 0.360 / 0.369 / 0.714 / 0.562), top-1과 미검색, 규칙 넷(0.0893 / 0.1310 / 0.2054 / 0.3899)과 최적 선택률, headroom 0.1071 — **아홉 개 수치가 전부 그대로**다. `overlap_at_k`·`rank_agreement`에 `_cutoff` 검증을 넣었고 그 둘은 `extract()`가 부르는 특징 계산인데, `k=10`에서는 동작이 같았다.

빈손이다. 그런데 확인하면서 빈 곳이 드러났다: **아무것도 그 숫자를 실행 결과와 대조하지 않는다.**

`agent-safety-core`에는 그 검사가 있다 — `test_published_benchmark.py`가 벤치마크를 돌려 README의 표와 맞춰본다. `modelmate`에도 있다 — 0.778을 `pytest.approx(0.7782)`로 실제 실행과 대조한다. **`rag-profile-selector`와 `document-intelligence`에는 없었다.** 한쪽에는 있고 형제에는 없는, 이 저장소들의 단골 모양이다.

**빈 곳인 줄 몰랐던 이유는 옆의 검사가 그럴듯했기 때문이다.** rag의 주간 `corpus` 잡이 법령을 다시 받아 체크섬을 대조한다. 그것이 지키는 것은 "코퍼스가 매니페스트가 말하는 그것인가"이고, "그 코퍼스에서 저 숫자가 나오는가"는 아니다. **둘은 다른 주장이고 함께 있어야 표가 증거가 된다.** `document-intelligence`도 비슷했다 — "페이지 15개, 거부 0건, 구역 700개 초과"는 확인했지만 README가 싣는 **724**는 아무도 보지 않았다.

둘 다 넣었다. rag는 실험을 그대로 돌려 출력을 파싱해 대조하고(안을 흉내 내면 실험이 아니라 내 재현을 검사하게 된다), document-intelligence는 **숫자를 테스트에 박지 않고 README에서 읽어와** 비교한다 — 박아두면 문서와 코드가 따로 놀 때 어느 편을 들지 알 수 없고, 읽어오면 둘 중 하나가 움직이는 순간 걸린다.

**내 파서가 다른 표를 읽어 거짓 경보를 냈다.** rag 문서에는 같은 모양의 선택기 표가 둘 있다(train+val, 봉인 해제한 test split). `rule:` 줄을 전부 훑었더니 딕셔너리가 뒤엣것으로 덮여 **아홉 개가 전부 어긋난 것처럼** 보였다. 재현 실패가 아니라 파서 문제였다. 손으로 만든 파서가 잘못된 답을 낸 것이 이 프로젝트에서 네 번째다.

**그리고 잘못된 픽스처를 썼다.** document-intelligence의 `parsed`는 속도를 위해 3페이지만 읽는데(124구역) 그것으로 724와 비교해 실패했다. 공개된 숫자는 논문 전체의 것이다. 전체를 읽는 픽스처를 따로 두고 그 구분도 검사로 남겼다.

**CI가 rag의 이 검사를 돌리지 않는다는 것도 적었다.** 코퍼스를 재배포하지 않으니 CI 클론에는 문서가 없다. 주간 잡에 붙이려면 dense 모델을 내려받아야 하는데, **그 잡의 머리말이 이미 경고하는 것이 정확히 그것이다** — 네트워크 때문에 빨간불이 되면 "코퍼스가 바뀌었다"와 "확인하지 못했다"가 같은 표시 뒤로 숨는다. 지금 성립하는 조합을 그대로 적었다: 주간 잡이 코퍼스가 그대로임을, 이 검사가 그 코퍼스에서 숫자가 나옴을 말한다.

**그리고 이 프로필의 대조기가 옛 사본을 읽고 있었다.** 다섯 저장소의 테스트 수를 맞춰보는 `tools/check_counts.py`는 `raw.githubusercontent.com`에서 형제 README를 읽는데, **그것은 CDN이고 캐시한다.** 푸시가 끝나고 `git log origin/main`이 새 커밋을 보여주는데도 raw는 몇 분간 이전 README를 줬고, 검사기는 "두 저장소의 README가 이 표와 다르다"고 말했다. 거짓이었다 — 둘은 일치했고 읽힌 사본이 낡았을 뿐이다.

**확인하지 않았으면 이미 맞는 README를 고치러 갔을 것이다.** 이 프로젝트가 자기 검사에서 반복해서 찾아온 실패 그대로다: 믿을 수 없는 경보는 믿을 수 없는 이상무와 같은 값의 주의를 쓴다. 요청에 `Cache-Control: no-cache`를 붙였다. 보장은 아니다 — CDN이 무시할 수 있다 — 그러나 이전에는 요청조차 하지 않았다.

---

## 검사 도구가 문법 오류를 "잡힘"으로 읽고 있었다

지난 회차에 `check_counts.py`가 CDN의 옛 사본을 읽고 있던 것을 찾았다. 그러면 **나머지 도구는 지금도 무언가를 하는가**가 다음 질문이다. 다섯 중 둘만 CI에 있고 셋은 손으로 돌리는 것들인데, 여러 회차 동안 아무도 돌리지 않았다.

**`mutate.py`에 구멍이 있었다.** 안전장치를 문자열 치환으로 무력화하고 스위트를 돌린 뒤 `종료 코드 ≠ 0`을 "잡힘"으로 읽는다. 그런데 치환이 **문법을 깨뜨리면** 모든 테스트가 에러나고 종료 코드가 0이 아니다 — 안전장치를 건드리지도 못했는데 지켜졌다고 보고한다.

시연했다. `document-intelligence`의 함수 서명에서 **콜론 하나를 지운** 치환이 `caught=True`로 나왔다. 그 탐침이 시험한다고 주장하는 안전장치는 실행조차 되지 않았다.

**음성 대조는 이것을 못 잡는다.** 이 도구는 음성 대조를 갖고 있고 그것이 이 도구의 자랑이다 — 공백만 바꾼 변이가 "안 잡힘"으로 나와야 결과가 유효하다. 그러나 대조는 **환경 전체에 걸리는** 조건(작업 디렉터리, 의존성 누락, 수집 에러)을 잡는다. 한 탐침이 만든 문법 오류는 **대조 탐침의 파일이 멀쩡하므로** 보이지 않는다. 대조가 있다는 것과 이 실패를 덮는다는 것은 다른 이야기다.

이제 치환한 내용을 `compile()`로 먼저 파싱한다. 안 되면 `caught`가 아니라 `replacement does not parse — this probe tested nothing`이다. `bool | None` 반환을 네 값짜리 열거로 바꿨다 — **같은 값에 두 뜻을 담았던 것**이 원인이었고, 이 프로젝트가 반복해서 찾아온 모양이다.

**대상 27개는 전부 살아 있었다.** 스위트를 돌리기 전에 값싸게 확인했다. 지난 회차에 `core/ledger.py`의 `approve`·`reconcile`을 재구성해서 `if`가 `elif`가 됐는데, **`elif`가 `if`를 포함해 우연히 맞았다.** 운이 좋았던 것이고, 그것도 확인해서 알았다.

**`find_vacuous_tests.py`의 한 범주는 신호가 0이었다.** `no_assertion` 16건이 **전부** 함수 호출을 갖고 있었다 — "이 호출이 예외를 내지 않아야 한다"는 정당한 검사다. **도구의 독스트링이 이미 그렇게 선언하고 있었으므로 결함이 아니다** — 결함이라고 적을 뻔했고, 읽어서 멈췄다. 다만 신호가 0인 목록은 읽히지 않는다(감사 이벤트에서 이미 쓴 논리다). 부르지도 단언하지도 않는 것과 부르되 단언하지 않는 것을 갈랐다: `no_assertion: 0`, `expects_no_raise: 16`.

새 범주를 만들고 보고 목록에 넣지 않아 **찾아놓고 보고하지 않는 상태**를 한 번 만들었다. 분류했지만 보고 목록에 없는 종류가 있으면 실패하게 했다.

`check_env.py`는 다섯 저장소가 전부 자기 체크아웃으로 해결된다 — 빈손이고, 그것도 결과다.

**그리고 전체 변이 감사를 실제로 돌렸다 — 27/27 잡힘, 대조 5/5 안 잡힘.** 여덟 회차에 걸쳐 다섯 저장소의 계산·인증·기록 경로를 크게 바꾼 뒤인데, 안전망이 전부 그대로 잡는다. 예전 기록의 "25개 잡힘, 2개 안 잡힘"에서 둘은 그때 고쳤고 지금은 전부 잡힌다.

**이 결과가 값을 가지려면 위의 수정이 먼저 있어야 했다.** 고치기 전이었다면 27개 중 문법이 깨진 것이 섞여 있어도 똑같이 "27/27 잡힘"이었을 것이다. 대상 존재 확인(27/27 살아 있음)과 파싱 확인이 둘 다 지나간 뒤의 27/27이라야 무언가를 뜻한다.

돌리는 데 한 시간 반이 걸렸다 — 탐침마다 해당 저장소의 스위트를 통째로 돌린다. CI에 넣지 않는 이유이고, 그래서 손으로 돌리는 도구가 **여러 회차 동안 안 돌아간 채로 있었다.** 손으로 돌리는 검사는 돌리지 않으면 없는 검사다.

**마지막 전체 변이 감사: 2026-08-23 — 37/37 잡힘, 대조 5/5 안 잡힘.** 이 줄은 `tools/check_mutation_freshness.py`가 읽는다 — 날짜가 낡거나, 잡히지 않은 변이가 있거나, 대조가 잡히면 CI가 말한다. **한 시간 반짜리 검사를 자동으로 돌리지는 못해도 안 돌린 채 잊히는 것은 막을 수 있다.**

---

## 손으로 돌리는 검사를 돌아가게 만들었다

지난 회차의 결론이 실마리였다 — **손으로 돌리는 검사는 돌리지 않으면 없는 검사다.** 다섯 도구 중 둘만 CI에 있었고 셋은 여러 회차 동안 아무도 돌리지 않았다. 그동안 그중 하나에는 구멍이 있었고, 안 돌렸으니 아무도 몰랐다.

**`find_vacuous_tests.py`에 한 기계의 절대 경로가 박혀 있었다.** `ROOT = Path("/home/jovyan/work")`. 다른 셋은 전부 `__file__`에서 유도하는데 이것만 그랬고, **그래서 다른 어디에서도 돌지 않았다.** 이 프로젝트가 이미 한 번 찾은 결함이다 — `mcp-gateway`의 테스트 셋이 `/home/jovyan/work/agent-safety-core`가 있는지로 형제 가용성을 판단하고 있었다. 같은 모양이 검사 도구 안에 남아 있었다.

경로를 유도하게 고치고 `--root`를 줬다. 그리고 **실패 조건을 하나만** 뒀다: 부르지도 단언하지도 않는 테스트. 나머지 두 범주(`expects_no_raise` 16건, 루프 안 단언 57건)는 읽을 목록이다 — 관문으로 삼으면 사람들이 이 검사를 끄고, **끈 검사는 없는 검사다.**

**그리고 CI에 넣기 직전에 더 나쁜 것을 봤다.** `check_env.py`를 CI 배치에서 돌려보니 **exit 0**이었다. 가상환경에 설치가 실패했는데도 통과했다 — 모든 모듈이 "import 안 됨(설치 안 됐으면 정상)"으로 빠지고 종료 코드가 0이 된다. **아무것도 확인하지 않는 초록불이다.** 그대로 넣었으면 CI에 체크 표시 하나가 늘고 확인되는 것은 없었다.

`--require-importable`을 붙였다. 로컬 기본값은 그대로 두고, CI는 설치 뒤 그 옵션으로 부른다. 설치된 환경에서 통과하고 설치 안 된 환경에서 실패하는 것을 **양쪽 다** 확인했다.

**`mutate.py`는 CI에 넣지 못한다** — 한 시간 반이 걸린다. 그렇다고 "가끔 손으로"는 방금 본 상태를 만든다. 마지막으로 돌린 날짜와 결과를 `FINDINGS.md`에 적고, `check_mutation_freshness.py`가 그 줄을 읽는다: 45일이 넘거나, 잡히지 않은 변이가 있거나, 음성 대조가 잡혔으면 CI가 말한다. **한 시간 반짜리 검사를 자동으로 돌리지는 못해도, 안 돌린 채 잊히는 것은 막을 수 있다.**

날짜를 검사기에 박지 않고 문서에서 읽어온다 — `document-intelligence`의 구역 수에서 쓴 것과 같은 이유다. 그리고 **하루의 여유**를 뒀다: 기록은 KST에서 적고 검사는 CI(UTC)에서 읽으니 몇 시간 동안 "내일"로 보인다. **검사하려는 것과 무관한 이유로 빨간불이 되는 검사**는 이 프로젝트가 반복해서 경고한 실패다.

CI에 "훑을 것이 없으면 실패하는가"를 확인하는 단계도 넣었다.

**그리고 CI가 나를 잡았다.** `--require-importable`을 붙였다고 적었는데 **실제로는 아무것도 삽입되지 않았다** — 삽입 대상으로 삼은 문자열이 그 파일에 없었고(그 함수에는 argparse가 아예 없다) 치환이 조용히 실패했다. 남은 것은 정의되지 않은 이름을 읽는 줄 하나였고 `NameError`로 터졌다.

**로컬 대조가 통과했던 이유가 더 나쁘다.** CI 배치를 흉내 내려고 저장소를 `git archive`로 복사해 두고 거기서 돌렸는데, 그것은 **HEAD의 사본**이지 내가 방금 편집한 파일이 아니다. 플래그는 무시됐고 exit 0이 나왔으며 나는 그것을 "통과"로 읽었다. 반대 방향의 exit 1도 다른 이유로 나온 것이었다.

**이 프로젝트가 이미 세 번 기록한 실수다: 대조가 잘못된 사본을 봤다.** 그때는 `/tmp`의 나흘 묵은 형제였고 이번엔 내가 몇 분 전에 만든 아카이브였다. 고친 뒤에는 **편집한 그 파일**을 **진짜로 네 모듈이 없는 인터프리터**(새 venv)로 돌려 양방향을 확인했다. 없는 경로로 불러서 **실패하는 것**을 먼저 본다 — 시크릿 스캐너가 빈 출력을 "깨끗함"으로 읽었던 그 실패를 이 검사에서 되풀이하지 않으려고.

---

## 지운 뒤에 남는 것, 그리고 지우지 못하게 하는 것

**실패하거나 삭제한 뒤에 자원이 남는가** — 아직 한 번도 안 본 각도다. 두 저장소에서 서로 반대편의 결함이 나왔다.

**`modelmate` — 뒷받침 없는 기한.** 데이터셋을 API로 지우고(HTTP 200) `DATASETS_DIR`을 보니 파일이 그대로였다. 프로젝트를 지우니 CSV와 배포 모델 `.pkl`이 둘 다 그대로였다.

**소프트 삭제 자체는 정직하게 밝혀져 있었다** — 삭제 영향 응답에 `will_delete_dataset_file: False`가 있다. 문제는 바로 옆 문장이었다. 화면 두 곳에 그대로 표시되는 `retention_note`가 이렇게 말했다: *"Historical summaries may remain for up to 30 days in this MVP foundation."* **30일 뒤 사라진다는 뜻으로 읽힌다. 지우는 코드는 없다.** 그리고 이 저장소 자신의 `docs/security-notes.md`가 "not ... automatic retention enforcement"라고 이미 선언하고 있었다 — **앱이 사용자에게 하는 말과 저장소가 자기에 대해 하는 말이 어긋났고, 사람들이 읽는 쪽은 앱이었다.**

숫자를 만든 손잡이 둘도 죽어 있었다. `DATASET_RETENTION_DAYS`는 **정의된 줄 말고는 파일 어디에도 나오지 않았다.** 둘 다 배선하지 않고 없앴다 — **보존 정책은 실제로 지우는 코드와 함께 오는 것**이지 그 전에 오는 손잡이가 아니다. 동작은 그대로 두고 문장만 사실에 맞췄다. **뒷받침 없는 기한은 없는 것보다 나쁘다** — 없으면 묻고, 있으면 믿는다.

프로젝트 삭제 쪽에는 파일에 대한 언급이 **아예 없었다.** 같은 사실을 한쪽만 말하고 있었다. 두 문장 모두 `DELETE /api/deployed/{model_id}`를 가리키게 했다 — 이 앱에서 실제로 파일을 지우는 유일한 경로이고, 방법이 있는데 말하지 않으면 없다고 믿게 된다.

**`agent-safety-core` — 반대편.** 여기에는 보존 통제가 진짜로 있다. 독스트링이 "schedule이 `retention_class`를 라벨에서 통제로 바꾼다"고 말하고, **시계를 둘 다 주면 정확히 그렇게 동작한다**(기한 전 거절, 기한 후 통과).

**store에만 주면 틀린다.** `PayloadStore`도 `clock`을 받고 `RetentionSchedule`도 자기 `clock`을 받는다. `created_at`은 store의 시계에서, "지금"은 schedule의 시계에서 나온다 — **서로 다른 시계의 두 시각을 빼는 것은 아무 뜻이 없다.** 테스트 시계(1000.0)를 store에만 주니 **10년 보존 클래스의 payload가 즉시 파기됐다.**

**열리는 방향으로 실패하고**, schedule에 시계를 주지 않는 것은 부자연스러운 호출도 아니다 — 만드는 것은 store이고 시계는 store에 준다. 프로덕션에서는 둘 다 `time.time()`이라 일치하므로 이 실패는 **시간을 통제하는 곳에서만** 나타난다: 테스트·재현·시계를 고정한 배포. **하필 보존 통제를 실제로 시험하는 자리다.**

**내가 이 API에서 네 번 틀리고 나서야 거기 도달했다** — 없는 인자 이름, 필수 인자 누락, 참조 대신 문자열 전달, 그리고 시계 하나. **마지막 것만 진짜 결함이었고 앞의 셋은 내 실수였다.** 구분하지 못했으면 셋 중 하나를 결함으로 적었을 것이다. 테스트를 쓰면서 다섯 번째로 틀렸다(첫 `)`에서 잘랐는데 안쪽 괄호였다).

---

## 절반만 고쳤던 것을 마저 고쳤다

지난 회차에 `agent-safety-core`에서 **한 판단에 시계가 둘**인 것을 찾아 `destroy`를 고쳤다. 그 형태를 다섯 저장소로 훑었더니 **같은 파일 안에 남은 절반**이 먼저 나왔다.

`PayloadStore.pending_retention()`이 만드는 것이 정확히 `RetentionSchedule.sweep()`의 입력이다. 그래서 **의도된 사용법 자체가 두 시계를 섞는다**:

    schedule.sweep(store.pending_retention())

`created_at`은 store의 시계에서, `now`는 schedule의 시계에서 온다. **10년 보존 클래스의 payload가 쓰이자마자 `eligible`로 나왔다.** `overdue`는 `sweep`을 부르니 같은 구멍이었다.

둘 다 `now=`를 받게 하고, **시계를 가진 쪽이 부르는 길**을 뒀다 — `PayloadStore.retention_status()`. 손으로 `now`를 넘기는 것도 되지만 **기억해야 하고, 기억에 기대는 통제가 잊히는 통제다.** schedule 없는 store에서 부르면 조용히 빈 결과를 주지 않고 말한다 — 빈 결과는 "보존 대상 없음"으로 읽힌다.

**형제 저장소는 빈손이고 이유가 분명하다.** `mcp-gateway`는 리미터가 `time.monotonic`, 감사가 벽시계인데 **목적이 다르므로 옳다** — 레이트 리밋은 단조 시계로, 감사 시각은 벽시계로 재야 한다. 둘을 비교하는 곳도 없다: 리미터가 밖으로 내놓는 것은 시각이 아니라 **기간**이고, 기간은 시계 종류를 건너도 뜻이 보존된다. `rag-profile-selector`와 `document-intelligence`에는 주입 가능한 시계가 아예 없다.

**`modelmate`는 출처가 하나였다.** 일일 한도의 "오늘"은 `_usage_today()` 하나로 정해지고 모든 읽기가 그것을 지난다. 프런트엔드는 "오늘 분석"을 표시하지만 숫자를 서버에서 받는다 — 자기 날짜를 계산하지 않는다. 빈손이고, 확인해서 얻은 결과다.

**적혀 있지 않던 것은 그 하루가 누구의 하루인가다.** `datetime.now()`는 서버의 지역 시각이고, 한도는 **서버 자정**에 리셋된다 — UTC도 사용자의 자정도 아니다. 거기서 두 가지가 따라 나온다: 다른 시간대의 사용자는 "오늘"이 자기 하루와 어긋나고, **배포의 시간대를 옮기면 경계가 움직인다**(UTC→KST면 9시간 당겨지고, 그 전환에서 사용자가 하루치를 일찍 새로 받는다). **시간대를 바꾸는 것이 한도를 리셋하는 일이 될 수 있다.**

시간대 기능을 만들지 않았다. 사용자별 시간대는 사용자에게 시간대를 묻는 데서 시작하고 이 앱은 묻지 않는다. 동작을 바꾸지 않고 사실을 적었다 — 지난 회차에 보존 문구에서 한 것과 같은 판단이다.

**내 검사가 정의 자신의 본문을 잡아 또 틀렸다.** `_usage_today`가 `datetime.now().date()`를 쓰는 것은 당연한데 def 줄만 건너뛰고 나머지를 봤다. **이 영역에서 여섯 번째 파싱 실수**라 그 구분까지 검사에 남겼다.

---

## 대조 방향이 둘이라는 것

최근 회차에서 내가 반복한 실수가 실마리였다 — **손으로 만든 파서가 여섯 번 틀렸다.** 다섯 저장소에서 **52개 테스트 파일**이 문서·소스를 문자열로 읽는다. 그것들도 당했을 수 있다.

기계적으로 확인했다: **읽는 대상을 비우고 그 검사가 실패하는지.** 열두 건 중 둘이 "비워도 통과"로 나왔고, **둘 다 내 실수였다.**

**①** `agent-safety-core`의 벤치마크 검사는 루트 `README.md`가 아니라 `benchmark/README.md`를 읽는다. 내 지도가 문자열만 보고 루트라고 짚었다 — **일곱 번째** 파서 실수다. 실제 경로로 비우니 실패한다.

**②** `rag-profile-selector`의 낡음 검사는 **"낡은 주장이 없다"를 단언**한다. 비우면 주장도 함께 사라지니 통과하는 것이 당연하다. **부재를 단언하는 검사에 비우기 대조를 건 것이 틀렸다.** 실제 STALE 문구 셋을 심어보니 셋 다 잡는다.

**여기서 얻은 것은 결함이 아니라 방법이다.** 무엇을 단언하느냐에 따라 대조가 반대여야 한다:

| 검사가 단언하는 것 | 대조 |
|---|---|
| 값이 있고 실행 결과와 같다 (presence) | 문서를 **비우면** 실패해야 한다 |
| 낡은 주장이 없다 (absence) | 문구를 **심으면** 실패해야 한다 |

비우기 대조를 absence 검사에 걸면 **언제나 통과하고, 멀쩡한 검사가 결함으로 보인다.** 하마터면 그렇게 적을 뻔했다. 이 프로젝트는 "대조 없는 초록불을 믿지 말라"를 여러 번 적었는데, **대조 자체가 방향을 잘못 잡을 수 있다**는 것은 이번에 처음 걸렸다.

그 구분을 기억에 맡기지 않으려고 `tools/check_doc_tests_bite.py`로 만들었다. **13건 전부 물려 있다**(68초). 항목이 다섯 미만으로 줄면 실패한다 — 목록이 줄면 "전부 물려 있다"가 점점 적은 것을 뜻하게 된다.

도구 자신에도 대조를 걸었다: 문서를 읽지 않는 검사를 presence로 등록하면 안 물림으로, 없는 문서와 심을 문구 없는 absence도 안 물림으로 나온다.

**CI에는 주 1회만 넣었다.** 이 확인은 각 저장소의 스위트를 실제로 돌리므로 다섯 저장소의 의존성이 전부 필요하고, PDF 어댑터 검사는 논문을 내려받는다. push마다 도는 관문에 네트워크를 넣으면 "검사가 물려 있지 않다"와 "arxiv에 닿지 못했다"가 같은 빨간불 뒤로 숨는다 — `rag`의 코퍼스 워크플로가 이미 적어둔 교훈이다. **주 1회 실패는 조사하는 실패이고, 매 push 실패는 꺼버리는 실패다.**

---

## 관례를 설명한 문장이 README를 모든 검사에서 빼버렸다

지난 회차에 배운 것을 적용했다 — **부재를 단언하는 검사는 심어봐야 안다.** 훑어서 목록을 만들고 "비었다"를 단언하는 검사 **26건**을 찾아 각각에 위반을 심었다.

**넷이 안 잡혔고, 셋은 내 심기가 틀린 것이었다.** 주석 처리된 호출을 심어놓고 목록에 죽은 항목이 생기길 기대했고, `os`를 import하지 않은 채 `os.path.exists`를 심어 **NameError로 다른 테스트가 실패한 것을 "잡음"으로 읽었다** — **두 회차 전에 `mutate.py`에서 고친 바로 그 함정에 내 탐침이 그대로 빠졌다.** 고친 도구가 있어도 내가 새로 만든 하네스는 그 교훈을 물려받지 않는다.

**넷째가 진짜였다.** `modelmate`의 `README.md`가 living-document 검사에서 통째로 빠져 있었다. 349줄에 이렇게 적혀 있다:

    …은 각자 `<!-- historical: -->`로 선언돼 있다

옛 정규식은 그 표시를 파일 어디서든 찾으므로 **관례를 설명하는 이 문장을 선언으로 읽었다.** 두 회차 전에 아카이브를 정리하며 **내가 쓴 문장**이고, 그때부터 가장 많이 읽히는 문서가 모든 검사 밖에 있었다. **조용히 빠졌다** — 아무것도 "살아 있는 문서 수가 줄었다"고 말하지 않았다.

**다시 넣자마자 가려져 있던 죽은 링크 둘이 나왔다.** 둘 다 내가 쓴 아카이브 README에 있다: 존재하지 않는 스냅숏 폴더(다른 README의 문장을 옮겨 적으며 틀렸다)와 저장소 밖 `work/ROADMAP.md`(**지난 회차에 다른 문서에서 고친 것과 똑같은 실수**). 그 파일도 자기 산문 때문에 면제돼 있었다.

선언과 언급을 구분한다: **줄 시작에, 문서 앞쪽(15줄 이내)에** 있는 것만 선언이다. 추적되는 17개 중 15개가 2~3줄이었고, 어긋난 둘은 정확히 산문 언급이었다.

**형제 저장소에서도 하나 나왔다.** 절대 경로로 형제 가용성을 판단하는지 보는 검사가 `Path("/...` **한 가지 철자만** 봤다. `os.path.exists("/...")`, 맨 문자열, `Path` 별칭, `open("/...")` — 넷 다 통과했고 **전부 이 검사가 막으려는 실수다.** 좁게 잡은 이유는 기록돼 있었고 맞는 걱정이었지만(첫 판이 `Path(__file__)`까지 잡았다), 좁힌 결과가 한 철자만 잡는 상태였다. 주석과 독스트링을 걷어낸 뒤 기계 고유 경로 리터럴을 보게 하니 넷 다 잡고 주석 인용은 넘어간다.

**이 회차의 모양은 하나다: 인용과 사용의 구분.** 내가 테스트에서 여러 번 당했고, 이번엔 저장소의 **면제 장치**와 **탐지 패턴** 안에 있었다. 관례에 대해 쓰는 행위가 그 관례의 적용을 면제시켰다.

---

## 하한선으로는 목록이 하나 줄어드는 것을 볼 수 없다

지난 회차에 `modelmate`의 README가 산문 한 줄 때문에 living-document 검사 전부에서 빠져 있었다. 고쳤지만 남는 질문은 **왜 두 회차 동안 아무도 몰랐는가**였다.

찾아보니 그 장치를 지키는 공허 가드가 전부 **하한선**이었다. `rag`는 `assert len(living()) >= 3`, `modelmate`는 `assert len(living_documents()) >= 20`. 그런데 README가 빠졌을 때 살아 있는 문서 수는 **21에서 20으로** 줄었다. **가드는 그 상태에서도 참이다.** 하한선은 "충분히 봤는가"를 묻지 "무엇이 빠졌는가"를 묻지 않고, 면제는 언제나 하나씩 늘어난다.

**두 가지를 다섯 저장소에 함께 넣었다.**

느슨한 정규식 일곱 곳(`agent-safety-core`·`mcp-gateway`·`rag`·`document-intelligence`의 `test_doc_paths.py`와 `test_no_stale_status.py`)을 `modelmate`에 이미 넣은 선언/언급 구분으로 바꿨다. **먼저 재봤다: 네 저장소 모두 지금 언급만으로 면제된 문서는 없다**(느슨/엄격 계수가 0/0, 2/2, 4/4, 2/2로 동일). 살아 있는 결함이 아니라 같은 장치라서 미리 고친 것이고, **빈 결과도 결과이므로 그렇게 적는다.**

그리고 면제된 문서를 **이름으로** 뒀다. `DECLARED_RECORDS = frozenset({...})`에 적고 실측 집합과 정확히 같은지 단언한다 — `agent-safety-core` 0개, `mcp-gateway` 2개, `rag` 4개, `document-intelligence` 2개, `modelmate` 6개. 늘어남(문서가 조용히 검사 밖으로 나갔다)과 줄어듦(기록이 사라져 거짓 실패를 낼 참이다)을 **둘 다** 본다. `agent-safety-core`는 빈 집합이고, 비어 있음을 단언하는 것도 검사다.

**대조 13개.** 살아 있는 문서에 산문 언급을 심으면 통과하고(면제되지 않는다), 진짜 선언을 맨 앞에 심으면 실패하고, 선언된 기록에서 선언을 지워도 실패한다. 다섯 저장소 전부 세 방향이 맞았다.

**그때 잡혔을지 직접 확인했다.** `modelmate`의 정규식을 옛 느슨한 형태로 되돌리고 그때의 문장을 README에 다시 심은 뒤 새 가드를 돌렸다:

    E   새로 면제됨: ['README.md']

**두 회차를 기다릴 필요가 없었다.** 가드의 모양이 문제였지 가드가 없던 것이 아니다.

**한 가지를 더 발견했다.** 정규식은 파일마다 **사본**으로 있는데 그 구분을 지키는 대조는 `modelmate`에만 있었다. 사본 하나를 옛 형태로 되돌려도 다른 파일의 대조는 아무 말을 하지 않는다 — **되돌릴 수 있는 곳마다 지켜야 한다.** 세 가지(산문 언급은 면제 안 됨, 앞쪽 선언은 면제됨, 문서 끝 표시는 선언 아님)를 일곱 파일에 각각 붙였고, `mcp-gateway`에서 정규식을 옛 형태로 되돌려 실패하는 것을 확인했다.

**로컬이 전부 초록인데 CI가 빨간불을 냈다.** 테스트를 늘리면서 다섯 저장소의 README 개수를 갱신했고 각 저장소의 개수 검사는 전부 통과했다. 그런데 `mcp-gateway`가 CI에서 실패했다 — README에는 총 개수 말고 **"코어 없이 262개 통과"라는 산문 숫자**가 하나 더 있고, 그것을 대조하는 것은 워크플로 잡뿐이다. **로컬 초록은 그 숫자를 본 적이 없다.** `rag`에도 같은 모양이 있었다("코퍼스를 받으면 298개가 전부 돈다" — 총 개수는 CI가 대조하고 산문은 아무도 보지 않아 두 회차 전 값에 멈춰 있었다).

고치러 들어간 워크플로에서 하나 더 나왔다. `mcp-gateway`의 잡은 `collected`를 재놓고 **한 번도 읽지 않았다.** README의 세 번째 숫자("그때 46개는 아예 수집되지 않는다")가 정확히 그 값에 달려 있고, 그 문장의 요점은 **skip 수보다 미수집 수가 크다**는 것이다. 재놓고 안 보면 요점이 틀려도 초록불이다. 두 가지를 대조하게 했다 — 미수집 수가 README와 같은가, 그리고 그것이 skip 수보다 큰가. 문장이 바뀌어 grep이 빈 값을 내면 통과가 아니라 실패다.

**한 사실을 두 곳에 적으면 둘 다 검사해야 한다.** 아니면 검사되는 쪽만 갱신되고 나머지는 조용히 낡는다 — 이 저장소들이 이미 세 번 겪은 일이다.

---

## 저장소가 자기를 세는 숫자, 그리고 아무도 발동시킨 적 없는 거부

두 갈래가 한 회차에서 나왔다. 시작은 같은 질문이었다 — **문서가 말하는 것 중 실제로 재는 것은 무엇인가.**

### 저장소가 자기를 세는 숫자 여덟 개

테스트 개수는 CI가 `--collect-only`와 대조한다. 그런데 README는 **저장소 자신에 대한 다른 숫자들**도 들고 있다: 파일의 줄 수, 호출 수, 사용처 수, 코퍼스 크기, 경합 스레드 수. 손으로 적혔고 **재는 것이 하나도 없었다.** 여덟 개를 재봤다.

    modelmate    api.<메서드>( 호출        99건  → 99          맞음
    modelmate    .part 파일의 STATE 사용   232건 → 266건       틀림
    modelmate    STATE를 쓰는 .part 파일   "20여 곳" → 35곳    틀림
    modelmate    persistence.py            977줄 → 996줄       틀림
    rag          법령                      14건  → 14          맞음
    rag          검색 대상 조문            745개 → 745         맞음
    rag          질의                      40건  → 40          맞음
    asc          경합 스레드               24개  → 24          맞음

**어긋난 셋은 방향이 같다** — 저장소가 자라는 동안 숫자만 그대로였다. 232는 스코핑 커밋 당시에는 맞았고(커밋 메시지가 "232 call sites"라고 적고 있다) 그 뒤로 34건이 늘었다. **한 번 맞았던 숫자가 계속 맞다고 읽힌다.**

**745는 재보지 않았으면 오보로 적을 뻔했다.** 파일 안의 조문을 그냥 세면 863이 나온다. 745는 `is_article`이고 폐지되지 않은 것만 센 값이다 — 숫자를 만든 정의와 같은 정의로 세지 않으면 같은 것을 세는 것이 아니다. 검사도 로더를 거쳐 센다.

같은 사실이 **여섯 문서**(README·STATUS·RESULTS·DECISIONS·TASKS·KR_LAW_RESULTS)에 흩어져 있어서 전부 비교한다. 지난 회차의 결론이 "한 사실을 두 곳에 적으면 둘 다 검사해야 한다"였다.

**그리고 지시 파일 셋이 낡아 있었다.** `AGENTS.md`의 착수 게이트 소진 문단은 "지금 이 저장소에는 … 테스트 N개가 돈다"를 근거로 든다. mcp-gateway 239 대 320, rag 243 대 317, document-intelligence 153 대 189. **세 저장소 모두 이 파일을 읽는 검사가 이미 있었고** — 소진 표시, 문서 경로, 안전 제약을 봤다 — **숫자는 아무도 보지 않았다.**

### 거부 76개를 하나씩 지워봤다

`document-intelligence`의 `reading_order.py`에는 "거부 하나하나가 실제로 거부하는가"를 확인하는 파일이 있다. 그 머리말이 이 저장소의 논지를 적어둔다 — *"거부가 시험된 적이 없으면 그건 코드에 적힌 문장이지 성질이 아니다."* **그 감사는 그 한 파일에서 멈춰 있었다.**

패키지 전체에 같은 시험을 걸었다. 문자열 메시지를 가진 `raise` 76개를 하나씩 `pass`로 바꾸고 매번 스위트를 돌렸다.

    잡힘        50건
    안 잡힘     23건   ← 둘은 저장소가 도달 불가로 선언해둔 줄
    비종료       1건   ← 순환 검사를 지우면 `while`이 영영 돈다. 그것도 거부가 일한다는 증거다
    문법 오류    2건   ← 여러 줄 raise. 문 단위 변이로 다시 재서 둘 다 잡히는 것을 확인했다

**진짜로 안 잡히는 것이 21건.** `coordinates.py` 13개 중 9개, `hierarchy.py` 16개 중 9개가 지워도 초록불이었다. `reading_order.py`만 20/20이었고 **그 파일에만 감사가 있었기 때문**이다.

그중 하나는 저장소가 **스스로 경고해둔 함정**이었다. `test_rejects_duplicate_regions`가 `assertRaises(ValueError)`로만 확인해서, 유일성 검사를 통째로 지우면 중복 구역이 그 다음 "종점이 정확히 하나여야 한다"에 걸린다 — **다른 이유로 거부되고 테스트는 통과한다.** 바로 옆 파일의 머리말이 그 이유를 적어두고 있었고, 그 규칙이 나중에 쓰였을 뿐이다. 커버리지는 그때도 **100%**였다.

**첫 감사는 무효였다.** `--timeout=300`을 넘겼는데 이 저장소에 그 플러그인이 없어 pytest가 사용법 오류(rc=4)로 끝났고, 스크립트는 0 아닌 종료 코드를 전부 "잡힘"으로 읽어 **76/76 초록**을 냈다. **`mutate.py`에서 두 회차 전에 고친 함정을 새 하네스가 그대로 반복한 것이 이걸로 세 번째다.** 기준선 확인과 rc 4·5 구분을 넣고 다시 돌린 것이 위 숫자다.

21건 각각을 발동시키는 테스트 26개를 넣고, 문 단위로 지우는 변이로 다시 재서 **23/23 전부 잡히는 것**을 확인했다.

**두 갈래가 같은 것을 말한다.** 한 번 확인한 것은 확인한 그 자리에서만 성립한다 — 숫자든 거부든, 다음 회차에도 성립하게 하려면 재는 것을 남겨야 한다.

---

## 다섯 저장소의 거부 267개를 하나씩 지워봤다

지난 회차에 `document-intelligence` 하나에서 한 감사를 나머지 넷으로 가져갔다. 문자열 메시지를 가진 `raise`를 하나씩 `pass`로 바꾸고 매번 스위트를 돌린다. **잡히지 않으면 그 거부는 코드에 적힌 문장이지 성질이 아니다.**

| | 대상 | 잡힘 | **안 잡힘** |
|---|---|---|---|
| `document-intelligence` (지난 회차) | 76 | 50 | **23** (2건은 도달 불가 선언) |
| `mcp-gateway` | 34 | 25 | **9** |
| `agent-safety-core` | 30 | 23 | **7** |
| `rag-profile-selector` | 31 | 25 | **6** |
| `modelmate` | — | — | 다른 방법으로 쟀다(아래) |

**`modelmate`는 변이 감사가 불가능했다.** 거부 지점이 101곳이고 스위트가 8~18분이라 12시간이 넘는다. 대신 **스위트가 도는 동안 `HTTPException`이 실제로 발생한 자리를 기록**했다 — `.part` 파일들이 이어붙여 `exec`되므로 프레임의 파일명은 디렉터리지만 줄 번호는 되돌릴 수 있다.

    소스의 발생 지점    101곳
    실제로 발생한 지점    3곳

계측기를 먼저 의심했다. parts 밖 프레임을 놓치는 것 아닌가 싶어 그것까지 기록하도록 넓혀 다시 돌렸고, 여전히 3곳이었다. 기록된 세 줄에 실제로 `HTTPException(`이 있는 것도 확인했다.

### 무엇이 나왔나

**같은 모양이 세 저장소에서 또 나왔다: 타입만 보는 단언.** `mcp-gateway`의 `test_an_empty_body_is_refused`는 빈 응답에 `pytest.raises(TransportError)`만 걸었다. 빈 본문 검사를 지우면 그 다음 `decode_frame("")`이 **같은 타입의** 다른 오류를 내고 테스트는 통과한다. **바로 위 줄의 테스트는 메시지를 고정하고 있었다** — 한 파일 안에서 규칙이 갈렸다. 어제 `document-intelligence`에서 찾은 것과 정확히 같은 형태이고, 그쪽에서도 옆 파일이 그 이유를 이미 적어두고 있었다.

**검사기를 검사하지 않았다.** `agent-safety-core`의 `assert_conforms`는 배포가 실제로 들고 온 witness가 계약을 지키는지 보라고 있는 도구다. 스위트는 **올바른 구현만** 넘겼다. 그 도구의 거부 둘이 안 잡힌 일곱에 들어 있었다 — **틀린 witness를 통과시키는 검사기**가 될 수 있었고 아무도 몰랐을 것이다.

**문지기가 서 있는지 확인한 적이 없었다.** `rag-profile-selector`의 `validate_profile`은 "승인 목록 밖의 설정으로는 실험하지 않는다"를 지키는 함수인데, 그 함수의 거부 **둘 다** 한 번도 발동한 적이 없었다.

**관리자 전용 엔드포인트 아홉 개가 비관리자를 막는지 아무도 확인한 적이 없었다**(`modelmate`). 조건 하나가 뒤집혀도 정상 사용자는 아무 차이를 못 느낀다 — 관리자만 쓰는 화면이고, 뚫린 것은 뚫린 뒤에야 보인다.

### "안 잡힘"이 곧 "테스트가 없다"는 아니다

**셋은 일어날 수 없는 것이었다.** `mcp-gateway`의 "finite JSON data"는 같은 검사를 `registry.py`가 먼저 한다. `rag`의 "필드가 승인된 `profile_id`와 다르다"는 **절대 참이 될 수 없다** — `profile_id`가 `method`와 `k`에서 파생되므로 dense를 bm25로 바꾸면 id도 함께 바뀌어 정본과 정확히 일치한다.

그리고 그 자리에서 **문서-코드 불일치**가 하나 나왔다. `validate_profile`의 독스트링은 *"only when profile is unaltered"*라고 적혀 있었고, **그것이 코드가 할 수 있는 것보다 큰 약속이었다.** 실제 보장은 "이것은 승인된 조합을 가리킨다"이지 "이 객체가 카탈로그가 발행한 그것이다"가 아니다. 문장을 사실로 고치고 분기는 이유와 함께 남겼다.

**그 구분은 재보기 전에는 알 수 없었다.** 테스트를 쓰려고 앉아서야 "이건 못 만든다"가 나왔고, 그것이 이 감사에서 가장 값싼 발견이었다.

### 내 감사기가 또 같은 함정에 빠졌다

첫 판은 무효였다. `--timeout=300`을 넘겼는데 `document-intelligence`에 그 플러그인이 없어 pytest가 **사용법 오류(rc=4)** 로 끝났고, 스크립트가 0 아닌 종료 코드를 전부 "잡힘"으로 읽어 **76/76 초록**을 냈다. `mutate.py`에서 두 회차 전에 고친 함정이다. 이번 도구에는 셋을 넣었다 — 변이 전 **기준선이 초록인지 확인**, rc 4·5는 잡힘이 아니라 **무효**, 여러 줄 `raise`는 **문 통째로** 바꾸기(첫 줄만 바꾸면 문법이 깨지고 그것도 "잡힘"이 된다).

**끝나지 않는 것도 따로 셌다.** `hierarchy.py`의 순환 검사를 지우면 `while`이 영영 돈다 — 그것도 거부가 일한다는 증거이지 "안 잡힘"이 아니다.

45건에 테스트를 넣고 **하나씩 다시 지워 전부 잡히는 것**을 재봤다.

---

## 스위트가 한 번도 실행하지 않는 줄이 81개 있었다

거부 감사는 `raise`만 봤다. 질문을 넓혔다 — **한 번도 실행되지 않는 줄이 무엇인가.** 네 저장소를 커버리지로 재니 81줄이었다(전체 3,358줄 중 97.6%).

| | 전체 | 안 돌던 줄 |
|---|---|---|
| `mcp-gateway` | 1,016 | **44** |
| `agent-safety-core` | 1,182 | **21** |
| `rag-profile-selector` | 574 | **9** |
| `document-intelligence` | 586 | **7** |

**`modelmate`는 잴 수 없었다.** `.part` 파일들을 이어붙여 `exec`하므로 프레임의 파일명이 디렉터리이고, 커버리지가 줄을 파일에 대응시키지 못한다. 대신 지난 회차에 한 계측(HTTP 거부 101곳 중 3곳만 발동)이 같은 질문의 다른 답이다.

### 무엇이 안 돌고 있었나

**문서가 시키는 명령 전체.** `mcp-gateway`의 README 19줄은 `python3 -m mcp_gateway.audit verify <log.jsonl>`을 안내한다. 그 CLI가 한 번도 실행된 적이 없었다. 손으로 돌려봤고 **네 경우 다 올발랐다** — 온전한 로그는 OK, 내용 변조·중간 줄 삭제·없는 파일은 각각 다른 이유로 실패. **결함은 없었고 없던 것은 검사다.**

**README가 약속한 것을 지키는 코드.** `HttpProxy._post`의 실제 네트워크 경로 19줄이 통째로 안 돌았다 — 모든 테스트가 `opener` 스텁을 넘겼기 때문이다. 그 안에 HTTP 오류·타임아웃·도달 실패를 각각 다르게 옮기는 세 줄이 있고, 그것이 README가 "파이프와 같은 원칙"이라고 적어둔 그 구분이다. 127.0.0.1에 뜨는 이 프로세스의 서버로 네 경우를 돌리자 **설계 하나를 처음 확인했다**: `CallTimeout`은 예외로 새지 않고 UNKNOWN 응답이 된다. `TransportError`는 그대로 올라온다. **재시도해도 되는 실패와 재시도하면 안 되는 미상은 다르다**는 것이 그 차이인데, 스텁만 쓰는 동안에는 코드에만 있었다.

**실제로 쓰라고 만든 witness의 실패 처리 전부.** `agent-safety-core`의 `FileWitness`는 자기 독스트링이 "감사받는 기계 위에 있으므로 아무것도 증언하지 않는다"고 적어둔 물건이다. 쓰라고 만든 것은 `HttpWitness`인데, witness가 409·5xx·이상한 본문을 돌려줬을 때가 한 번도 돌지 않았다. **감사받는 기계 밖에 두려고 만든 장치의 실패 처리가 검사 밖에 있었다.**

**빈 결과로 특징을 계산해본 적이 없었다.** `rag`의 안 돌던 9줄은 거의 전부 퇴화 입력의 이른 반환이다 — 결과가 비었을 때, 상위 점수가 0일 때, 비교할 순서쌍이 없을 때. 실험이 늘 결과가 있는 질의로만 돌았다는 뜻이고, **선택기는 그 값을 보고 프로파일을 고른다.**

### 대조가 헛돌 뻔했다

CLI를 확인하다가 감사 로그의 `request_id`를 바꿔 "chain intact"가 나오는 것을 보고 **하마터면 "감사 로그가 변조를 못 잡는다"고 적을 뻔했다.** 기록은 정규 JSON(`"request_id":1`, 공백 없음)인데 `"request_id": 1`로 치환했다 — **아무것도 바꾸지 않았다.** 제대로 바꾸니 정확히 잡았다. 대조가 실제로 무언가를 바꿨는지 보는 것이 대조의 절반이고, 그 단언을 테스트에 함께 넣었다.

### 가드 둘이 내 새 파일을 잡았다

`mcp-gateway`의 "코어 없이도 도는가" 검사가 내 테스트의 무방비한 `core` import를 잡았다. 지난 회차에 `rag`의 "형제를 이름으로 두는" 가드가 같은 일을 했다. **두 번 다 가드가 맞았다** — 그것이 이름으로 두고 이유를 적어두는 방식의 값이다.

### 100%를 관문으로 넣었다

네 저장소 모두 100%가 됐고 CI가 `--fail-under=100`으로 지킨다. **관문이지 목표가 아니다** — 새 방어 분기는 테스트를 받거나 `# pragma: no cover`와 이유를 옆에 적는다. 여섯 줄이 그렇게 처리됐고, **이유를 적다가 도달 불가라는 것이 드러난 것이 넷**이다: `BEGIN IMMEDIATE`가 행을 잡고 있어 compare-and-set이 실패할 수 없고, 나머지 셋은 이미 거부하는 첫 겹 위의 두 번째 겹이었다.

`rag`는 **코퍼스 없이도 100%**임을 확인한 뒤에 push 관문에 넣었다 — CI 클론에는 코퍼스가 없다.

---

## 어제 세운 관문을 의심했다 — 구문 100%는 각 `if`가 양쪽으로 가봤다는 뜻이 아니다

지난 회차에 네 저장소를 **구문 커버리지 100%**로 만들고 CI 관문을 걸었다. 하루 만에 그 관문을 다시 봤다. 구문 커버리지는 "모든 줄이 실행됐다"만 말한다. `--branch`로 재면 **각 `if`가 양쪽으로 다 가봤는가**를 묻는다.

| | 분기 | 한 번도 그 방향으로 안 간 것 |
|---|---|---|
| `mcp-gateway` | 288 | **5** |
| `agent-safety-core` | 318 | **4** |
| `rag-profile-selector` | 182 | **1** |
| `document-intelligence` | 244 | **0** |

`document-intelligence`만 이미 100%였다. **빈손인 감사도 결과이고, 아무도 돌리지 않은 감사와는 다르다.**

### 남아 있던 열 개가 무엇이었나

**넷은 "입력이 예상 모양이 아닐 때"**(mcp-gateway). `sequence`가 정수가 아닌 기록, `arguments`가 없는 호출에서 lease를 떼려는 경우, 서버가 `isError`는 맞게 주고 `content`를 다른 모양으로 준 경우, stdin이 이미 닫힌 뒤의 정리. **전부 "실패를 보고할지 그냥 터질지"를 가르는 쪽**이다.

**하나는 저널이 비어 있는 순간**(asc). 파일은 있는데 내용이 없는 상태 — 만들어놓고 아직 아무것도 안 쓴 순간이다. 없는 파일과 같게 다루지 않으면 `lines[-1]`에서 터진다.

**하나는 사본에만 없었다**(asc). `verify_export`의 빈 줄 건너뛰기에는 테스트가 있었고 **대조 쪽의 같은 코드에는 없었다.** 한 규칙을 두 곳에 구현하면 두 곳 다 확인해야 한다 — 이 프로젝트가 반복해서 적어온 그대로다.

**그리고 하나는 정상 경로였다**(rag). `validate_evidence_mapping`에 splits를 넘긴 검사는 **전부 일부러 어긋난 배정**을 넣은 것이었다. 배정이 전부 아는 질의를 가리킬 때 — 코퍼스가 멀쩡할 때 — 이 함수가 빈손을 돌려주는지는 확인된 적이 없었다. *"거부만 시험하면 전부 거부하는 구현도 통과한다"*를 여러 번 적어뒀는데, 그 문장이 분기 하나로 드러난 자리다.

**둘은 도달 불가였다.** `binding._apply`의 마지막 갈래는 Equivalence 다섯 멤버가 전부 앞에서 처리되므로 문자열을 든 채 빠져나갈 길이 없다. `ExecutionAuthority` Protocol의 `...` 본문은 선언이지 코드가 아니다 — 한 줄짜리 형제 둘이 안 걸리는 것은 `...`가 `def`와 같은 줄에 있어서일 뿐이다.

네 저장소 관문을 전부 `--branch`로 올렸다.

## 그리고 소유권 문지기 넷

`modelmate`의 남은 백로그에서 한 조각을 가져왔다. `_resource_not_found()`는 프로젝트·데이터셋·분석 실행·배포 모델 넷이 공유하는데, **남의 자원을 막는지 한 번도 발동된 적이 없었다.**

404를 쓰는 것은 의도된 선택이다 — 403은 "그 id는 존재한다"를 알려준다. 그래서 거부만 보지 않고 **없는 자원과 남의 자원이 같은 답**(코드와 문구 둘 다)을 내는지 비교한다. 소유자와 관리자가 통과하는 것, 게스트 경로 둘이 **열려 있다는 사실**도 함께 고정했다.

### 그 파일을 쓰다가 내가 테스트를 하나 지웠다

한 클래스 안에 `test_but_not_when_it_is_not_asked`를 **두 번** 썼다. 파이썬은 뒤엣것을 남기고 앞엣것을 버린다. pytest는 아무 말도 하지 않는다 — 수집 개수가 하나 줄 뿐이고, **새 테스트가 함께 들어오면 그 감소는 상쇄돼 보이지 않는다.**

다섯 저장소를 훑었다. **다른 곳에는 없었다.** 그래도 도구에 넣었다(`find_vacuous_tests.py`의 다섯째 범주). 판단이 필요 없는 모양이라 관문으로 삼았다 — 같은 이름의 테스트 둘은 어떤 읽기로도 의도가 아니고, 앞엣것은 **존재하지 않게 된다.** 클래스 안과 모듈 수준 양쪽에 심어 잡히는 것을 확인했다.

---

## "잴 수 없다"고 적어둔 것을 쟀더니, CI가 지키던 것은 47%였다

`modelmate`만 커버리지를 못 재고 있었다. `main.py`가 `main_parts/*.part`를 이어붙여 한 번에 `exec`하는데, 컴파일에 넘기는 파일명이 **디렉터리 경로**라서 coverage.py가 그 이름을 파일로 열지 못한다. 형제 넷이 분기 100% 관문을 얻는 동안 여기만 **"잴 방법이 없다"**고 로드맵에 적혀 있었다.

**잴 방법은 있었다.** 이어붙인 소스는 유효한 파이썬이고 줄 번호는 되돌릴 수 있다 — 파일 사이에 `"\n".join`이 한 줄을 넣으므로 그 한 줄만 더하면 정확히 맞는다. 같은 매핑을 두 회차 전에 `HTTPException` 발생 지점을 기록할 때 이미 확인해뒀다. `sys.settrace`로 우리 프레임만 따라가면 된다.

    .part 소스 4,256문 기준
    pytest만                          27.7%
    CI가 돌리던 스모크 셋               44.5%
    **둘을 합쳐 CI가 지키던 것**        47.3%
    스모크 열다섯을 다 돌렸을 때         56.8%

### 세 가지가 나왔다

**pytest가 스모크에 더하는 것은 92문뿐이다.** 743개 테스트가 앱 자체에 대해서는 스모크가 이미 지나간 곳을 거의 그대로 지나간다. pytest는 헬퍼와 관문 로직을 촘촘히 보고 라우트 사슬은 스모크가 본다 — 분업 자체는 합리적이지만, **"테스트 743개"가 앱의 절반을 지킨다는 뜻은 아니다.** 숫자를 세는 것과 무엇을 덮는지는 다른 질문이고, 이 프로젝트는 앞의 것만 CI로 지키고 있었다.

**스모크 열다섯 중 CI가 돌리던 것은 셋이었다.** 나머지 열둘은 저장소에 들어 있고 손으로 돌리는 것이었다. *"손으로 돌리는 검사는 돌리지 않으면 없는 검사다"* — 이 프로젝트가 이미 두 번 적어둔 문장이고(변이 감사, 형제 도구 셋), 세 번째가 여기 있었다. 열둘이 **9.4%p**를 더 짚으므로 CI에 넣었다. 서버는 이미 떠 있으니 값은 실행 시간뿐이다.

**CI 기준으로 한 줄도 안 도는 함수가 132개**다(전체 321개 중). `run_optuna`·`run_agent`·`predict_single`·`run_shap`·`auth_google` 같은 것들이다. **죽은 코드인지 먼저 확인했다** — 전부 `@app.post`에 물려 있는 살아 있는 라우트다. 그래서 죽은 코드가 아니라 시험되지 않는 기능이고, 그 목록이 로드맵의 다음 항목이다.

### 내 계측기가 두 번 틀렸다

**보고 단위를 틀렸다.** 처음엔 `.part` 파일별로 냈고 "이 파일은 0%"가 셋 나왔다. 들여다보니 **앞 파일에서 시작한 함수의 꼬리**였다 — `.part` 경계는 기능 경계가 아니다. 파일 단위 표는 "이 기능은 안 시험된다"로 읽히는데 데이터는 그 말을 하고 있지 않았다. 함수 단위로 다시 묶은 것이 위 숫자다.

**대조가 한 번 헛돌았다.** 측정기가 실행 여부를 실제로 구분하는지 보려고 "부른 함수"와 "안 부른 함수"를 비교했는데, 안 부른 쪽이 `0/0`으로 나왔다. `async def`를 `ast.FunctionDef`로 찾고 있어서 **함수를 찾지 못한 것**이었다. `0/0`은 "안 돌았다"가 아니라 "볼 것이 없었다"이고, 그대로 뒀으면 **대조가 통과한 것처럼 보였다.** 고쳐 다시 거니 3/3 · 2/27 · 0/73으로 갈렸다.

### 스모크 하나가 실패했다 — 그리고 그것도 아니었다

측정 중에 `run_usage_limits_smoke.py`가 rc=1로 끝났다. 들여다보니 "admin can login" 하나만 실패였고, 원인은 **내 측정 환경에 관리자 비밀번호가 걸려 있지 않은 것**이었다(계정 시딩은 부팅 때 `ADMIN_PASSWORD`를 읽는다). 제품 결함이 아니라 전제 조건이다. 그 전에는 rc=2였는데 그것도 실패가 아니라 **인자 부족**이었다 — 도구가 둘을 같은 표시로 묶고 있어서 "실패한 스모크"로 읽혔다. **2와 1은 다르다**: 하나는 돌다 틀린 것이고 하나는 아예 돌지 않은 것이며, 후자는 커버리지에서 통째로 빠진다. 도구가 구분해 말하게 고쳤다.

---

## 문서가 두 번 적어둔 보안 항목이 두 달간 그대로 있었다

지난 회차에 `modelmate`의 커버리지를 처음 재면서 **CI가 한 줄도 실행하지 않는 라우트 마흔 개**가 나왔다. 이번 회차의 첫 질문은 그 목록을 어떻게 가르느냐였다 — 시험되지 않는 살아 있는 기능인가, 대체됐는데 아직 마운트된 옛 경로인가.

프런트엔드가 부르는지로 갈랐다(`api.<메서드>` 호출 99개를 훑는 스캐너는 이미 있었다). **스물넷은 프런트가 부르고, 열여섯은 아무도 부르지 않는다.** 두 번째 목록에 이것이 있었다.

    @app.get("/api/debug-env")
    async def debug_env():
        key = os.getenv("GEMINI_API_KEY", "")
        all_keys = sorted(os.environ.keys())
        gemini_keys = [k for k in all_keys if "GEMINI" in k.upper() or "API" in k.upper()]

인증이 없다. 환경 변수 이름 중 "GEMINI"나 "API"가 든 것을 모은다. 그리고 **반환하지 않는다** — `return`이 지워진 채 수집만 남았다. 오늘은 `null`을 주지만 **한 줄만 되돌리면 인증 없는 경로가 키 이름 목록을 뱉는다.**

### 이미 두 번 적혀 있었다

`docs/MODEL_MATE_HANDOFF.md` §6.2: *"registered without an authentication dependency … It should be removed, disabled, or protected."* 같은 문서 §8.1은 그것을 **첫 번째 보안 관문**으로 올려뒀다. 2026-06-22 감사 스냅숏도 같은 말을 했다.

**두 달 동안 그대로였다.** 이 프로젝트가 이미 두 번 적은 문장의 세 번째 사례다 — *"손으로 돌리는 검사는 돌리지 않으면 없는 검사다"*(변이 감사, 형제 도구), 그리고 이번에는 **손으로 적어둔 조치**. 문서에 "해야 한다"고 쓰는 것은 아무것도 하지 않는다. 확인하는 검사가 없으면 조치가 아니다.

지웠다. 프런트가 부르지 않고, 스모크 열다섯 중 어느 것도 치지 않으며, 반환값이 없으니 잃는 동작이 없다. 새 검사가 경로·핸들러 이름·`os.environ` 통째 읽기를 막고, **문서가 다시 "위험"이라고 말하면 실패한다** — 반대 방향도 막는다: 목록에서 조용히 지우면 무엇이 왜 해결됐는지가 사라지므로, 항목은 남기되 해결됐다고 적어야 통과한다.

### 같은 문서의 나머지도 실측했다

일곱 개의 "verified in source" 위험 중 셋이 이미 해결돼 있었는데 문서는 여전히 위험이라고 말하고 있었다(기본 JWT·관리자 비밀번호, 추적되던 런타임 산출물, 그리고 이번의 debug 라우트). **고쳐진 위험을 계속 위험이라고 적어두면 읽는 사람이 없는 위험을 쫓고, 남아 있는 위험은 그 사이에 묻힌다.**

남아 있는 것도 숫자로 적었다: **`requirements.txt` 16개 중 고정된 것 0개.** 갓 설치하면 다른 버전이 잡히고, 이 저장소가 공개한 숫자는 지금 여기 깔린 버전 위에서 나온 것이다. 고정은 별도 작업으로 남겼다 — 이 기계에서 풀리는 휠이 모든 러너에서 풀린다는 보장이 없어서, **주장이 아니라 갓 설치 실행으로 증명해야 한다.**

### 내가 쓴 문장을 하나 정정했다

문서에 "설정 없이 부팅하면 죽는다"고 적었다. 확인해보니 **배포로 판단될 때만** 그렇고, 로컬에서는 난수를 만들어 DB 옆에 저장한다. 그 구분은 의도된 것이다(개발자는 비밀을 설정하지 않아도 되고, 배포는 저장소에 적힌 값으로 서명하면 안 된다). 과장은 거짓말과 같은 방향으로 틀린다.

그리고 그 검사를 쓰다가 **여덟 번째로 인용/사용 함정에 걸렸다.** `os.getenv("JWT_SECRET", "modelmate-...")`가 소스에 없어야 한다고 단언했는데 실패했다 — 옛 기본값을 **설명하는 독스트링**이 있었다. `#`만 걷어내고 독스트링은 남겨둔 것이 원인이고, 형제 저장소의 절대 경로 검사는 이미 둘 다 걷어내고 있었다. **인용과 사용의 구분은 매번 다시 만들어야 한다.**

---

## SHAP이 아닌 것을 SHAP이라 부르지 않는다 — 규칙을 적어둔 저장소 안에 두 번째 위반이 있었다

로드맵의 다음 항목은 **의존성 고정**이었다. `requirements.txt` 16개 중 0개가 고정돼 있고, 고정하려면 설치된 버전을 읽어야 한다. 읽다가 다른 것이 나왔다.

    shap      설치 안 됨
    optuna    설치 안 됨

둘 다 `requirements.txt`에 있는데 **이 개발 환경에는 없다.** `SHAP_OK=False`, `OPTUNA_OK=False`인데 스위트 756개도 스모크 열다섯도 초록이었다. 앱이 `try/except`로 감싸뒀기 때문이고, 그 자체는 맞는 설계다. 따라간 것은 그 다음이다 — **그러면 "SHAP 설명 단계가 완료됐다"는 보고는 무엇을 말하는가.**

`backend/tools/shap_explainer.py`에 **`import shap`이 한 줄도 없다.** feature importance나 표준화 계수를 돌려주고, 페이로드의 `explanation_type`은 그것을 정직하게 말한다. 그리고 그 파일 자신이 주석으로 규칙을 적어뒀다 — *"Never claim SHAP for something that is not SHAP: the name is what a reader uses to decide how much weight to give the explanation."*

### 규칙이 두 곳에 닿지 않았다

**도구 카탈로그.** `registry.py`가 이 도구를 *"Returns SHAP, feature importance, coefficient, or unavailable evidence"*라고 소개했다. **나올 수 없는 것을 나올 수 있다고 적어둔 것**이다. 그 문장은 `mock_response.summary`에 있고 `ToolRegistry.describe()`가 그대로 내보내므로, 도구 목록을 읽는 쪽이 그대로 본다.

**스모크.** `run_agent_mode_smoke.py`가 "설명 단계가 돌았다"를 **단계 이름**으로 판정했다 — `tool_name == "shap_explainer_tool"`. 이름이 곧 주장인 상황에서 이름은 판정 근거가 될 수 없다. 지금은 `explanation_type`까지 보고, 출력이 실제로 무엇이었는지 말한다.

**이름 자체는 그대로 뒀다.** 저장된 실행 기록(trace)이 그 이름을 참조하고, 바꾸면 지난 기록이 가리키는 도구가 사라진다. 이 저장소가 `build_pr04_mock_registry`를 개명했던 것과 다른 판단이고, 다른 이유는 하나다 — **그때는 아무것도 그 이름에 매달려 있지 않았다.** 바꾼 것은 이름이 아니라 이름이 하는 약속이다.

### 같은 규칙의 두 번째 위반이었다

README가 이 규칙을 적어두고 한 번 적용한 자리가 있다. 계수 기반 신호를 SHAP이라 부르던 것을 `standardized_coefficient`로 개명하면서 *"SHAP이 아닌 것을 SHAP이라 부르지 않는다"*라고 썼다. **그 정정이 도구 카탈로그와 스모크에는 오지 않았다.**

이 프로젝트에서 반복되는 모양이다 — 규칙을 세우고 한 자리에 적용한 뒤, **그 규칙이 적용돼야 할 나머지 자리를 세어보지 않는다.** 정규식 사본 일곱 개, 하한선 가드 다섯 개, 손으로 돌리던 도구 셋, 손으로 적어둔 조치 하나. 매번 같은 형태로 나온다.

### 내가 부를 뻔한 이름

레지스트리 등록 튜플의 세 번째 원소를 `metadata`라고 부를 뻔했다 — `{"summary": ..., "risk": ...}` 모양이라 그렇게 보인다. 실제 이름은 `mock_response`이고 `mock_runner`가 결과에 병합한다. 검사가 `entry["metadata"]`를 찾다 빈 문자열을 보고 통과할 뻔했고, **`describe()`가 무엇을 내보내는지 실제로 확인한 뒤에야 맞는 자리를 찾았다.**

---

## 고정할 수 없는 두 개 때문에 고정하지 못하고 있었다

로드맵에 두 회차 동안 열려 있던 항목이다. `modelmate`의 `requirements.txt`가 **열여섯 개를 이름으로만** 적고 있었다. 갓 설치하면 그날 최신이 잡히고, 이 저장소가 공개하는 숫자는 어느 한 조합 위에서 나온 것인데 **어느 조합인지 아무 데도 없었다.** `docs/MODEL_MATE_HANDOFF.md` §6.3이 위험으로 적어뒀고 두 달 그대로였다.

막힌 지점은 분명했다. 고정하려면 버전을 읽어야 하는데 **개발 기계에 `shap`과 `optuna`가 설치돼 있지 않다.** 열여섯 중 둘은 읽을 수가 없다. 설치는 다운로드라 승인이 필요하고, 짐작으로 적으면 **확인된 것처럼 보이는 추측**이 된다 — 그건 고정하지 않은 것보다 나쁘다. 읽는 사람이 검증된 값이라고 믿기 때문이다.

**증명자를 바꿨다.** 개발 기계가 아니라 CI가 갓 설치를 한다. `tests` 잡에 `pip freeze` 단계를 넣고, 갓 설치가 실제로 무엇을 잡는지 찍어서 그 값을 옮겼다(Python 3.11.16).

    fastapi   0.140.7  →  0.141.1
    numpy     2.1.2    →  2.4.6
    openai    2.49.0   →  3.3.1

**개발 기계와 달랐다.** 세 개는 메이저·마이너가 움직인 차이다. 이 저장소가 공개한 숫자는 왼쪽 조합 위에서 나왔고, 갓 클론한 사람은 오른쪽을 받는다 — 그 차이가 고정하는 이유 전부다.

### 확인한 범위까지만 고정했다

전이 의존(80여 개)은 잠그지 않았다. 전체 잠금은 한 파이썬에서 푼 것이 다른 파이썬에서 풀린다는 보장이 없고, 이 저장소의 CI는 **3.11(`tests`)과 3.12(`product`) 둘 다** 쓴다. 열여섯 핀으로 두 잡이 모두 초록인 것은 확인했다. **3.12가 푼 전이 조합은 찍어두지 않았으므로 거기까지 잠그려면 그쪽 로그를 먼저 떠와야 한다** — 그 문장도 함께 적었다. 확인한 것과 확인하지 않은 것을 갈라 적지 않으면, 다음 사람은 둘 다 확인된 것으로 읽는다.

`pip freeze` 단계는 관문이 아니라 **다음에 핀을 갱신할 근거**로 남겼다. 검사가 그 단계가 사라지는 것까지 막는다 — 근거가 사라지면 다음 갱신은 다시 손으로 적는 일이 된다.

### 막힌 것을 적어두면 다음 회차가 그것을 푼다

이 항목은 지난 회차에 **막힌 이유와 함께** 로드맵에 남긴 것이다: *"없는 패키지의 버전을 추측해 고정하면 확인된 것처럼 보이는 추측이 된다. 설치는 다운로드라 승인이 필요하다 — 그 승인을 받거나, CI에서 `pip freeze`를 떠와야 한다."* 이번 회차가 한 것은 그 두 번째 선택지를 실행한 것뿐이다.

**막힌 것을 "못 함"으로 적으면 다음에도 못 하고, 막힌 이유와 우회로를 적으면 다음이 그것을 집는다.** 이 프로젝트에서 같은 일이 두 번 있었다 — `modelmate`의 커버리지도 "잴 방법이 없다"로 적혀 있다가, 무엇 때문에 못 재는지가 적혀 있었기에 풀렸다.

---

## 대조가 두 번 헛돌았다 — 내 검사가 엉뚱한 겹을 시험하고 있었다

이번 회차는 두 갈래였다. 먼저 지난 회차에 세운 규칙(핀 + CI 증명)을 **나머지 네 저장소에서 세어봤다.** 이 프로젝트가 반복해서 놓치는 자리이고, 세어보지 않으면 일곱 번째가 된다.

네 저장소는 `>=` 하한만 쓴다. 그런데 **고정이 필요한 이유가 여기엔 없었다.** 공개된 숫자들(구역 724개, 조문 745개, 벤치마크 표, 두 파서 일치율)이 전부 **검사가 다시 도출한다.** 라이브러리가 바뀌어 답이 달라지면 조용히 어긋나는 것이 아니라 **빨간불이 난다.** `modelmate`는 배포되는 앱이라 설치 재현성 자체가 문제였고, 여기는 그렇지 않다. **규칙을 옮기는 것과 규칙이 풀던 문제가 여기에도 있는지 보는 것은 다르다** — 후자를 보지 않으면 모방이다.

`rag`의 재현 안내(`pip install torch transformers`)도 실제 import와 맞는지 확인했다. 맞았다. 빈손이지만 확인한 빈손이다.

### 그리고 인증 없이 쓰는 경로 셋

`modelmate`에서 CI가 한 줄도 실행하지 않는 라우트 마흔 개 중 **스물넷은 프런트가 부르는데 아무 검사도 치지 않는다.** 그중 셋이 인증 없이 쓰는 경로다 — 파일럿 문의, 베타 피드백, 프런트엔드 오류 보고. 로그인 없이 도달하고 셋 다 DB에 행을 쓴다. 입력을 다듬는 코드가 셋 다 있고, **그 코드가 한 번도 돌아본 적이 없었다.**

스물일곱 개를 넣고 대조를 걸었다. **둘이 안 물렸다.**

**차단 목록은 도달할 수 없다.** 파일럿 문의는 브라우저가 보낸 사용량 스냅숏을 허용 목록과 차단 목록 두 겹으로 거른다. `{"password": ...}`가 버려지는 것을 보고 차단 목록이 일한다고 읽었는데 — `password`는 **허용 목록에도 없어서 첫 겹에서 이미 걸린다.** 차단 목록을 통째로 꺼도 내 검사는 통과했다. 허용 목록 열 개 중 차단 낱말을 품은 이름이 하나도 없으므로 안쪽은 영영 조용하다.

**길이 자르기도 같았다.** 핸들러의 `[:500]`을 지워도 통과한다. 실제로 자르는 것은 `persist_monitoring_event`다. **저장된 값만 보면 어느 겹이 잘랐는지 알 수 없다** — 보장은 제공하는 층에서 시험해야 한다.

### 세 번째로, 고친 검사조차 헛돌았다

차단 목록의 도달 불가를 "전제가 바뀌면 말하는" 검사로 바꿨다. 허용 목록에 `session_plan` 같은 이름이 들어오면 안쪽이 살아나므로 그때 알려주게. 대조를 걸었더니 **조용했다.**

허용 목록을 **테스트에 옮겨 적었기 때문**이다. 사본은 소스가 바뀌어도 그대로다. 짝인 두 목록 중 차단 쪽만 모듈 상수였고 허용 쪽은 함수 안 지역 변수여서 읽을 수가 없었다 — **읽을 수 없으니 옮겨 적게 되고, 옮겨 적으면 갈린다.** 같은 자리로 올리고 검사가 소스를 읽게 했다.

### 세 번 다 대조가 잡았다

이 회차에서 내가 쓴 검사 셋이 헛돌았고, 셋 다 **대조를 걸어서** 알았다. 통과만 보면 스물일곱 개가 전부 초록이었다. 이 프로젝트가 "빈손도 결과다"와 짝으로 적어온 것이 이것이다 — **초록도 결과가 아니다. 무엇을 껐을 때 빨개지는지가 결과다.**

---

## 신선한 것과 지금 것을 상대로 도는 것은 다르다

`tools/mutate.py`는 다섯 저장소의 안전장치를 하나씩 무력화하고 스위트가 빨간불을 내는지 본다. 한 시간 반이 걸려 CI에 못 넣으므로 **마지막으로 돌린 날짜를 `FINDINGS.md`에 적고 그 날짜를 검사한다.** 어제 돌렸으니 신선했다.

이번 회차의 질문은 다른 것이었다 — **그 27개가 지금의 스위트를 대표하는가.**

재봤다. 탐침은 소스 파일 **열네 개**를 건드린다. 그런데 최근 열 회차에 넣은 안전장치들은 **다른 스물한 개 파일**에 있다: 보존 시계, 감사 로그 검증, 세션 id 정규화, 승인 프로파일 문지기, 읽기 순서 유일성, 좌표 유한성, 소유권 문지기, 관리자 403. **탐침이 하나도 닿지 않았다.**

**날짜는 하루 전이고 범위는 열 회차 전이었다.** 가드가 하한선일 때와 같은 종류의 눈멂이다 — 검사가 묻는 질문("언제 돌렸나")과 답이 필요한 질문("지금 있는 것을 상대로 도는가")이 다르다.

열 개를 더해 37개(+ 음성 대조 5개)로 다시 돌렸다. **36/37 잡힘, 대조 5/5 안 잡힘.**

### 잡히지 않은 하나는 통제인 척하던 줄이었다

`rag-profile-selector`의 `rank_agreement`:

    shared = [item for item in left if item in right]
    if len(shared) < 2:
        return 0.5

이 조건을 `False`로 바꿔도 348개가 전부 통과한다. **지워도 답이 같기 때문이다** — 공유 항목이 0개나 1개면 아래 이중 루프가 순서쌍을 하나도 만들지 못해 `total == 0`으로 떨어지고 거기서도 0.5가 나온다. 단축이지 통제가 아니었다.

**잡히게 만들려고 테스트를 더하지 않았다.** 그건 구현 세부를 고정하는 일이고, 이 줄이 바뀌어도 함수의 약속은 그대로다. 탐침을 **하중이 실린 자리**로 옮겼다 — 실제 통제는 "모르면 0.5"라는 값이고, 그것이 0이나 1이 되면 모르는 것이 아는 것처럼 특징에 실린다. 옮긴 탐침은 잡힌다. 이른 반환은 남기되 **통제인 척하지 않도록** 그 사실을 옆에 적었다.

### 되돌린 판단 하나

`.part` 파일에 탐침을 붙이려다 `mutate.py`가 `read_text(encoding="utf-8")`로 읽고 같은 인코딩으로 되돌린다는 것을 봤다. `.part` 54개 중 **16개가 BOM을 갖고 있다.** 복원이 파일을 바꾼다고 판단해 바이트 단위로 고쳤고, 소스에 그 근거를 적었다.

**재봤더니 틀렸다.** `read_text`는 BOM을 `\ufeff` 문자로 읽고 `write_text`는 그것을 같은 바이트로 되돌린다 — 왕복이 바이트 단위로 동일하다. 부분 문자열 매칭도 BOM에 막히지 않는다. **이점이 측정되지 않았고, 근거가 틀린 변경은 되돌렸다.** 소스에 남았다면 다음 사람이 없는 위험을 피하려 애썼을 것이다.

### 검사기가 기록과 목록을 대조하게 했다

날짜 검사만으로는 이번 문제를 못 잡는다. 완전한 해법은 없다 — 목록이 늘어야 하는지는 사람이 판단한다. 대신 **기록과 목록이 갈리는 것**은 막았다: 탐침을 더하고 기록을 안 고치거나, 기록만 고치고 탐침을 안 더하면 CI가 말한다. 양쪽으로 심어 확인했다.

---

## 기록하는 행위가 기록을 뒤집었다

`modelmate`의 라우트 백 개에 대해 물었다 — **CI가 실행하지 않는 것**과 **아무도 부르지 않는 것**은 다르다. 앞은 검사의 빈 곳이고 뒤는 표면의 빈 곳이다. 근거 셋을 봤다: 프런트엔드의 `api.<메서드>` 호출, 스모크 열다섯, 문서.

### 매처가 두 번 틀렸고, 둘 다 "없음"처럼 보였다

첫 판은 `{model_id}`를 지워 `/api/v2//predict`를 찾았다. 매개변수가 든 경로는 **전부** 안 맞았고, 문서에 버젓이 있는 v1·v2가 "언급 없음"으로 나왔다.

고친 뒤에도 틀렸다. 프런트엔드는 axios `baseURL` 때문에 **`/api` 없이** 부른다 — `/api/quick-analysis/start`를 찾으면 안 나오고 `/quick-analysis/start`를 찾아야 나온다. 이번에도 "언급 없음"이 늘었다.

**두 번 다 16개라고 잘못 셌다.** 숫자가 같아서 더 그럴듯했다. 아는 답 다섯 개를 대조로 박고 나서야 **여섯**이 나왔다. 이 프로젝트에서 매처 실패가 결과처럼 보인 것이 세 번째다 — 빈 정규식을 쓴 시크릿 스캐너, 한 철자만 보던 절대 경로 검사, 그리고 이것.

### 남은 여섯

넷은 legacy `STATE` 흐름의 **옛 API 쌍둥이**다. 프런트는 같은 기능을 다른 경로로 부른다 — `/api/shap-local/{idx}` 대신 `/api/explain/local/{idx}`, `/api/columns` 대신 `/api/analyze-columns`. 하나는 `/api/admin/users`로, **검사는 있는데 부르는 화면이 없다**(관리자 UI는 요약·피드백·문의·모니터링 넷만 부른다). 마지막은 SPA 캐치올이고 브라우저가 주소창으로 치는 자리라 호출이 있을 수 없다.

**지우지 않았다.** `docs/legacy-analysis-flow-audit.md`가 이 흐름의 기능들을 "Preserved"로 분류하고 *"동등한 빠른 분석 경로가 남지 않는 한 대체하지 말 것"*이라고 적어뒀다. 그 결정은 기능 단위이고 이 넷은 그 기능의 옛 API 쌍둥이다 — **내부 스캔만으로 공개 표면을 지우는 것은 남이 적어둔 결정을 내 판단으로 덮는 일**이고, 베타로 배포된 적이 있어 바깥 호출자가 없다고 단정할 근거도 없다. 대신 이름으로 뒀다.

### 그리고 그 절을 쓰자마자 검사가 빨개졌다

문서는 근거 셋 중 하나다. README에 *"이 넷은 아무도 부르지 않는다"*고 적는 순간, **그 넷은 문서에 언급된 라우트가 된다.** 다섯이 곧바로 "이제 누가 부름"으로 뒤집혔다.

**기록하는 행위가 기록을 뒤집는다.** 인용과 사용을 혼동한 것이 이 저장소들에서 아홉 번째다 — 관례를 설명한 문장이 문서를 모든 검사에서 빼버렸고, 옛 기본값을 설명한 독스트링이 그 값을 쓰는 코드로 읽혔고, 이번엔 "안 쓴다"는 서술이 씀의 증거가 됐다.

`<!-- surface-record: -->` 울타리로 그 절을 감싸고 검사가 그 안을 걷어내게 했다. 이 저장소가 이미 쓰는 `<!-- historical: -->`과 같은 모양이다 — **선언이 있는 구간은 서술이고, 없는 곳은 사실 주장이다.** 울타리 밖의 언급은 여전히 근거로 세고, 울타리를 없애면 실패한다. 양쪽으로 심어 확인했다.

---

## 전체에서만 실패하는 검사를 하나 만들었다

`modelmate`의 남은 백로그에서 **목록 읽기 네 개**를 쳤다 — 프로젝트, 실험 기록, 리포트, 학습 작업. 프런트가 부르는데 CI가 한 줄도 실행하지 않던 것들이고, 넷 다 `WHERE user_id=?`로 자기 것만 고른다고 쓰여 있었다. **그 조건이 실제로 거는지는 확인된 적이 없었다.**

이 저장소가 이미 겪은 실패의 반대편이다. 요청 격리를 고치기 전에는 A가 올린 데이터셋을 B의 다음 요청이 분석했다 — `STATE` 하나를 프로세스가 공유했기 때문이다. 그건 고쳤고, **DB 쪽 범위는 각 질의의 `WHERE`에 달려 있다.** 조건 하나가 빠지면 목록 하나가 남의 작업 제목을 보여준다. 세 곳의 `WHERE`를 각각 빼서 셋 다 잡히는 것을 확인했다.

기록만 관리자에게 전체를 보여주는데, **그 예외도 함께 고정했다** — 조용히 사라지면 관리자가 못 보고, 조용히 넓어지면 모두가 다 본다.

### 파일 단독으로는 통과하고 전체에서는 실패했다

`STATE` 기반 읽기 둘(`feature_info`, `explain_summary`)은 "모델이 없을 때 거부하는가"를 본다. 파일만 돌리면 통과한다. **전체 스위트에서는 실패한다** — 앞선 테스트가 학습을 돌려 기본 버킷에 모델을 남겨두기 때문이다. 검사는 그대로인데 답이 달라진다.

**전역 상태에 기대는 검사는 그 상태를 스스로 세워야 한다.** 이 저장소가 요청 격리를 만들면서 붙인 `set_scope`가 정확히 그 도구다 — 제품 코드가 요청마다 하는 일(요청별 버킷 열기)을 검사도 한다. 이 테스트만의 빈 버킷을 열고 끝나면 되돌린다. 오염을 만드는 테스트와 함께 돌려 확인했다.

이 모양은 새롭다. 지금까지 찾은 것은 **통과하지만 아무것도 확인하지 않는** 검사였고, 이번 것은 **혼자 두면 맞고 같이 두면 틀리는** 검사다. 둘 다 초록 화면에서는 구분되지 않는다.

### 두 번 짐작이 틀렸다

`explain_summary`가 모델 없이 **빈 근거**를 돌려줄 것이라 적었다. 아니다 — 400으로 거부하고 "Run cross-validation first"라고 말한다. 어느 쪽인지가 화면의 동작을 가르므로, 짐작이 아니라 실제 동작을 고정했다.

그리고 `validation_summary` 검사의 첫 단언이 `... or summary`로 끝나 **무엇이 오든 통과**하는 상태였다. 대조를 걸기 전에 눈에 띄었다 — **참인 것을 확인하는 단언과 아무것도 확인하지 않는 단언은 통과 화면에서 똑같아 보인다.** 응답 키 다섯을 이름으로 고정하고, 벤치마크 파일이 없을 때의 기본값 갈래를 따로 열어 확인했다(이 저장소에는 두 파일이 다 있어서, 그러지 않으면 그 갈래는 영영 돌지 않는다).

---

## 파일을 하나씩 따로 돌렸더니, 나흘 된 0바이트 파일이 나왔다

지난 회차에 **혼자 두면 맞고 같이 두면 틀리는** 검사를 하나 만들었다. 이번 회차의 질문은 그 반대 방향이었다 — **혼자 두면 틀리고 같이 두면 맞는** 검사가 있는가. 그쪽이 더 나쁘다: 전체만 돌리는 CI에서는 영영 보이지 않는다.

다섯 저장소의 테스트 파일 **138개를 하나씩 따로** 돌렸다.

    agent-safety-core       31개 — 혼자서 실패 0
    document-intelligence   15개 — 0
    mcp-gateway             24개 — 0  (아래 하나 제외)
    rag-profile-selector    22개 — 0
    modelmate               46개 — 0

**순서 의존은 없었다.** 빈손이지만 확인한 빈손이고, 지난 회차의 발견이 하나짜리 사고였는지 패턴이었는지를 이제 안다.

### 대신 다른 것이 나왔다

`mcp-gateway/tests/test_declared_dependencies.py`가 **0바이트**였다. 종료 코드 5, `no tests ran`. 2026-08-19 커밋이 두 파일을 만들었는데 한쪽에만 내용이 들어갔고, **나흘 동안 아무도 몰랐다.**

몰랐던 이유가 이 발견의 요점이다. 이 저장소들은 테스트 개수를 CI가 `--collect-only`와 대조한다 — 그런데 **0을 더해도 0이라 개수는 움직이지 않는다.** 공허 테스트 검사기도 못 본다: 그 도구는 "부르지도 단언하지도 않는 테스트"를 찾는데, **테스트가 있어야 그것이 공허한지 볼 수 있다.** 두 장치가 다 있는데 둘 다 통과했다.

파일을 따로 돌리는 것만이 그것을 드러냈다. 다섯 저장소를 훑으니 이 하나뿐이었다.

### 지우지 않고 이름이 약속한 검사를 썼다

빈 파일은 지우는 것이 간단하다. 그런데 이름이 `test_declared_dependencies`이고, `mcp-gateway`의 `pyproject.toml`은 이렇게 말한다:

    dependencies = []

**의존성이 없다는 것은 이 저장소의 주장이다.** MCP 서버 앞에 끼어드는 프록시가 설치할 것을 요구하지 않는다는 것은 배포 결정을 바꾸는 사실이고, 그 주장은 소스가 표준 라이브러리 밖을 **모듈 수준에서** import하지 않을 때만 참이다. 재보니 참이었다 — 밖의 이름은 하나뿐이고(`core`), 그것도 함수 안에서만 불린다.

그 지연 import가 함수 안에 있는 이유가 정확히 이 주장이다. 모듈 수준으로 올리면 코어가 없는 곳에서 **`import mcp_gateway`만으로 게이트웨이 전체가 죽는다.** 형제 저장소가 없는 환경을 하위 프로세스로 만들어 확인하는 검사는 이미 있었고, **그 import가 함수 안에 남아 있는지**를 지키는 것은 없었다. 넷을 각각 어겨 넷 다 잡히는 것을 확인했다.

### 도구에 다섯째 범주를 넣었다

`find_vacuous_tests.py`가 이제 **테스트가 하나도 없는 테스트 파일**을 잡는다. 판단이 필요 없는 모양이라 관문으로 삼았다 — 테스트 파일에 테스트가 없다는 것은 어떤 읽기로도 의도가 아니다. 0바이트 파일과 헬퍼만 있는 파일 둘 다 심어 확인했다.

**이 검사가 그 파일 안에 있을 수는 없다.** 파일이 비면 그 안의 검사도 함께 사라진다 — 자기를 지키는 검사는 자기 밖에 있어야 한다.

---

## 이 저장소가 제품을 208번째 줄에서 말하고 있었다

회차가 쌓이는 동안 한 번도 물어보지 않은 것이 있었다 — **처음 온 사람이 무엇을 먼저 보는가.**

재봤다. 다섯 저장소의 README 크기는 이렇다.

    agent-safety-core       154줄  12절
    mcp-gateway             133줄  12절
    rag-profile-selector    162줄  14절
    document-intelligence   148줄  14절
    modelmate               504줄  38절   ← 3배

`modelmate`만 다르다. 그리고 절 순서를 보니 **`## What It Does`가 208번째 줄**에 있었다. 그 앞은 배포 이력 한 절과 **발견 열넷**이다. 읽는 사람은 이 프로젝트가 무엇인지 알기 전에 "테스트 수를 내가 부풀렸다", "Agent Mode가 절반에서 멈춰 있었다", "SHAP이 아닌 것을 SHAP이라 부르지 않는다"를 차례로 지나야 했다.

### 어떻게 그렇게 됐나

회차마다 새로 찾은 것을 문서 **위쪽**에 덧붙였다. 최신이 위로 오는 것은 그 자체로 맞는 선택이고, **각 회차에는 그것이 옳았다.** 스무 번쯤 반복되자 문서가 뒤집혔다.

이 프로젝트가 반복해서 찾아온 모양이다 — 하한선 가드도, 손으로 돌리는 검사도, 탐침 목록도 같았다. **개별 변경은 각각 옳고, 합쳐진 결과를 보는 것이 없다.** 형제 넷이 멀쩡한 이유도 규칙이 있어서가 아니라 **아직 그만큼 자라지 않아서**다.

### 순서만 바꿨다

제품 열넷을 앞으로, 발견을 `## 무엇이 잘못됐고 어떻게 알았나` 하나 아래로, 라이선스·기록·함께 보기를 꼬리로. **한 글자도 지우지 않았다** — 옮기기 전후를 낱말 단위로 세어 잃은 것도 새로 생긴 것도 없음을 확인했고(다리 문단 제외), 들어오는 앵커 링크가 없다는 것도 먼저 확인했다.

지우는 쪽이 짧아지기는 한다. 그러나 이 저장소들의 값어치는 **무엇이 잘못됐고 어떻게 알았는지**에 있고, 그것을 줄이면 남는 것은 평범한 SaaS 데모다. 문제는 분량이 아니라 **순서**였다.

검사가 그 순서를 지킨다. 맨 위에 절을 붙이거나, 발견 뒤에 제품 절을 붙이거나, 우산 제목을 바꾸면 실패한다 — 셋 다 심어 확인했다. **다음 회차의 나에게 거는 검사**다.

---

## 포트폴리오 입구가 열여섯 회차 뒤처져 있었다

지난 회차에 `modelmate`의 README를 **처음 온 사람의 자리에서** 봤다. 이번엔 한 겹 위 — 다섯 저장소를 소개하는 **프로필 문서**를 같은 자리에서 봤다.

거기 이렇게 적혀 있었다:

> 기록은 **FINDINGS.md**에 있다 — 42개 항목.

**58개다.** 열여섯 회차 동안 항목은 늘었고 그 문장은 그대로였다. 이 포트폴리오가 반복해서 잡아온 종류의 결함 — 손으로 적은 숫자가 썩는 것 — 이 **자기 입구에** 있었다.

### 왜 아무도 몰랐나

기존 검사 둘이 있다. `check_counts.py`는 다섯 저장소의 **테스트 수**를 README와 대조하고, `check_anchors.py`는 목차의 **링크가 실재하는 제목을 가리키는지** 본다. 둘 다 매 push마다 돈다. 둘 다 초록이었다.

**항목 수를 세는 것은 아무것도 없었다.** 링크는 전부 살아 있었고(58개가 정확히 58개 제목을 가리킨다), 테스트 수도 맞았다. 두 검사가 각각 자기 질문에 옳게 답했고, **아무도 묻지 않은 질문이 그 사이에 있었다.**

### 그리고 1,522줄에 색인이 없었다

58개를 시간순으로 읽어야 했다. 읽는 사람이 "어떤 종류의 결함이 나왔나"를 물으면 답할 길이 없다.

**분류를 지어내지 않으려고 세어서 만들었다.** 각 항목이 실제로 쓰는 표현을 세니 이렇게 나온다 — 58개 중 **26개가 대조(control)** 이야기를 하고, **20개가 빈손으로 끝난 확인**을 적고 있고, 9개가 "초록불"이라는 낱말을 쓴다. 그 위에 여덟 모양으로 색인을 달았다: 검사가 아무것도 보지 않았다 · 한 사실을 두 곳에 적고 한 곳만 지켰다 · 정의됐지만 배선되지 않은 통제 · 문서가 지금을 서술하지 않는다 · 경계에서 잃어버린 구분 · 한 번도 지나간 적 없는 경로 · 인용과 사용의 혼동 · 내 도구가 틀렸다.

**분류는 사후에 붙인 것이라고 적어뒀다.** 항목이 근거이고 분류는 읽는 길이다 — 한 항목이 여러 모양에 걸치기도 한다(39개 항목을 46번 가리킨다). 지어낸 범주로 증거를 덮지 않으려면 그 구분을 말해둬야 한다.

### 세는 검사를 만들었다

`tools/check_findings_index.py`가 셋을 본다: README가 말하는 개수 == 목차 항목 수, 목차 항목 수 == 실제 절 수, 색인이 가리키는 번호가 전부 실재하는 항목인가. 네 방향으로 심어 넷 다 잡히는 것을 확인했고 CI에 넣었다.

**"여섯 번 같은 종류의 문제를 만났다"**도 고쳤다. 그 문장 아래 여섯 개가 나열돼 있으니 그 자체로 틀리진 않았지만, 읽는 사람은 그것을 총계로 읽는다. 지금은 *"처음 여섯 번은 이랬다"*로 적고 현재 수를 함께 말한다 — 셈이 멈춘 문장은 멈춘 줄 모르는 채 읽힌다.

---

## 구글 로그인이 DB를 잠근 채 남길 수 있었다

로드맵의 남은 백로그 — 프런트가 부르는데 CI가 한 줄도 실행하지 않는 라우트 열일곱 개 — 에서 `POST /api/auth/google`을 골랐다. 로그인 **이전**에 도달하고, 여기서 역할이 정해진다.

검사를 쓰다 진짜 결함이 나왔다.

**비밀번호로 가입한 이메일과 같은 주소로 구글 로그인을 하면** `users_email_ci` 유니크 인덱스가 INSERT를 거절한다. 예전 코드는 그 예외를 그대로 올렸고 — 500 — **`conn.close()`가 함수 마지막 줄에 있어 연결이 열린 채 남았다.** sqlite는 그 연결이 쥔 쓰기 잠금을 놓지 않는다. **그 뒤 이 프로세스의 모든 쓰기가 `database is locked`로 죽는다.**

재현했다. 한 번 부딪히면 그 뒤 아무것도 쓰지 못했다. 도달 경로도 평범하다 — 이메일로 가입한 사람이 나중에 구글 버튼을 누르면 된다.

`try/finally`는 이 저장소의 관행이었다. `052_workspace_projects.part`가 아홉 곳, `097_pilot_inquiries.part`가 세 곳에서 쓴다. **이 파일에만 없었다** — 규칙이 없어서가 아니라 한 자리가 빠져 있었고, 그 자리가 하필 예외가 나는 자리였다.

충돌을 먼저 잡아 **409로 말한다.** 두 계정을 잇는 것은 여기서 결정하지 않았다 — 이메일 소유 증명 없이 이으면 계정 탈취 경로가 되고, **로그인 핸들러가 조용히 내릴 결정이 아니다.** 무엇을 할지는 제품 결정으로 로드맵에 남겼다.

### 세 번째 승격 경로

이 저장소는 관리자 승격에서 한 번 데였다. 부팅 시 계정 시딩에 경로가 둘 있었고, 하나는 `ADMIN_EMAILS`(복수)를, 다른 하나는 `ADMIN_EMAIL`(단수)을 봤다. `get_admin_emails()`가 단수를 무시하므로 **문서화된 목록에 없는 주소가 관리자가 되는 경로**였다. 그건 지웠다.

구글 로그인이 **세 번째 경로**이고 확인된 적이 없었다. 목록 밖 계정은 관리자가 되지 않고, 목록에 넣으면 되고, 이미 관리자인 계정은 강등되지 않고, 승격은 보안 기록에 남는다 — 넷을 각각 어겨 넷 다 잡히는 것을 확인했다.

### 검사가 세 번 헛돌았다

**첫째, 429.** 같은 IP로 열 번 넘게 불러 인증 전 조임에 걸렸다. 그 통제는 이 저장소가 일부러 넣은 것이고 **제대로 돌고 있었다** — 끄지 않고 호출자를 각각 다른 IP로 바꿨다. 끄면 다음 사람이 그것이 없는 줄 안다.

**둘째, `finally`를 지워도 통과했다.** 충돌을 먼저 잡은 뒤로는 409 경로가 **쓰기를 시작하지 않아** 연결이 남아도 잠기지 않는다. 즉 재현했던 피해는 충돌 검사가 막고, `finally`는 검사가 닿지 못하는 경로를 위한 것이다. 억지로 도달하는 검사를 만드는 대신 **왜 약한지를 적었다** — 구현 세부를 고정하는 선택은 `rank_agreement`에서 한 번 거절했다.

**셋째, 구조 검사가 옆 함수를 봤다.** 함수 시작부터 1,600자를 잘라 `try:`·`finally:`·`conn.close()`가 다 있는지 봤는데, **창이 다음 함수까지 덮어** `finally`를 지워도 통과했다. 텍스트 창은 경계를 모른다 — 이 프로젝트가 손으로 만든 파서에서 여섯 번 틀린 그 자리다. AST로 `auth_google`의 `Try.finalbody`만 보게 하고서야 물었다.

---

## 그 모양이 몇 군데나 있는지 셌다 — 22개

앞 회차에 `auth_google`에서 찾은 결함: 예외가 나면 DB 연결이 열린 채 남고, sqlite가 그 연결이 쥔 쓰기 잠금을 놓지 않아 **이후 모든 쓰기가 죽는다.**

고친 뒤 물어야 할 것은 하나다 — **몇 군데나 더 있나.** 이 프로젝트가 반복해서 놓치는 자리다: 규칙을 세우고 한 곳에 적용한 뒤 나머지를 세어보지 않는 것. 정규식 사본 일곱 개, 하한선 가드 다섯 개, 손으로 돌리던 도구 셋, 손으로 적어둔 조치 하나가 전부 같은 모양이었다.

    `get_db()`를 여는 함수            87개
    예외에 연결이 남을 수 있는 것       28개
    그중 **쓰기를 하는 것 22개**        ← 잠금을 쥔다

**읽기만 하는 것과 구분해서 셌다.** 읽기 전용 누수도 연결을 새지만 쓰기 잠금은 쥐지 않는다. 같은 수로 묶으면 "28개가 위험하다"가 되고, 그 숫자는 고칠 순서를 정하는 데 쓸 수 없다.

### 스물둘을 한 번에 고치지 않았다

배포된 앱의 함수 스물둘을 기계적으로 감싸는 것은 한 회차에 감당할 위험이 아니다. 각 함수의 조기 반환·예외 경로가 제각각이고, 잘못 감싸면 지금 도는 것이 안 돌게 된다.

**인증 없이 도달하는 쓰기 넷을 고쳤다** — `auth_google`(앞 회차)·`auth_signup`·`auth_login`·`auth_logout`. 바깥에서 입력을 넣어 예외를 만들 수 있는 자리이고, 나머지는 로그인한 사용자만 닿거나 부팅 시 DDL이다(거기서 예외가 나면 부팅이 실패해 프로세스가 어차피 못 뜬다).

남은 열여덟은 **이름으로 뒀다.** 목록이 정확히 일치해야 하므로 새로 생기는 것도, **고쳐서 빠지는 것도** 걸린다 — 후자가 없으면 목록이 낡아 다음 사람이 없는 위험을 쫓는다.

### 로그아웃이 실패해도 성공이라고 답하고 있었다

`auth_logout`을 감싸다 덤으로 나왔다. 바깥이 `except Exception: pass`다. 세션 취소 UPDATE가 실패해도 **응답은 `ok: True`이고 어디에도 남지 않는다.**

응답은 그대로 뒀다. 로그아웃을 실패로 돌려주면 사용자는 토큰을 쥔 채 화면에 남는다 — 더 나쁘다. 대신 **보안 기록에 적게 했다.** 서버 쪽 취소가 안 된 것은 클라이언트가 토큰을 버리는 것과 다른 사실이고, 그 구분이 보안 기록이 있는 이유다. 이 저장소가 *"통제가 작동한 순간이 어디에도 남지 않았다"*를 고친 것과 같은 자리다.

### 대조가 또 한 번 헛돌았다

목록의 하나를 고쳐 심고 "목록이 낡으면 잡는가"를 봤는데 **잡히지 않았다.** 검사가 약한 줄 알았는데 아니었다 — 심기가 `\n    conn = get_db()`(4칸 들여쓰기)를 찾았고 대상은 12칸 안에 있었다. **심은 것이 아예 안 들어갔다.**

이 프로젝트에서 대조가 헛돈 것이 이걸로 여러 번이다. 매번 다른 이유지만 겉모습은 같다 — **"통제를 껐는데 초록불"과 "통제를 못 껐는데 초록불"이 화면에서 구분되지 않는다.** 심은 것이 실제로 들어갔는지 보는 것이 대조의 절반이다.

## 삼킨 예외가 어디까지 조용한가 — 열 곳을 세고 넷을 고쳤다

앞 회차에 `auth_logout`에서 하나 나왔다 — 바깥이 `except Exception: pass`라 **세션 취소가 실패해도 `ok: True`였고 어디에도 남지 않았다.** 하나를 고쳤으면 나머지를 센다. 이 저장소가 반복해서 놓치는 자리다.

    예외 객체를 아예 버리는 처리기 (다섯 저장소 `src`/`backend`)   44개
    그중 `pass`로 흘려보내는 것                                    8개  ← 전부 modelmate
    타입 없는 `except:`                                            2개  ← 전부 modelmate

**형제 저장소 넷은 0개다.** mcp-gateway에 다섯이 걸렸지만 읽어보니 전부 검사기가 `False`/`None`을 돌려주는 자리였고 그 자리에 이유가 적혀 있었다. 세어보고 아무것도 없었다는 것도 결과다.

**처음 센 것은 107개였다.** 검사기가 `return {'ok': False, 'text': str(e)}`를 삼킨 것으로 셌다 — 그건 삼킨 게 아니라 **호출자에게 말하는 것**이다. 좁혀서 다시 셌다: 예외 객체를 본문에서 한 번도 안 쓰고, 로그도 재발생도 없는 것. 107이 44가 됐다. 넓게 잡은 셈은 커 보이지만 고칠 것을 못 고른다.

### 타입 없는 `except:` 둘 — 둘 다 오래 걸리는 자리에 있었다

```python
except: df = None                 # CSV 파싱
except: continue                  # 모델을 차례로 학습해 보는 되돌림 고리
```

타입이 없으면 `KeyboardInterrupt`와 `SystemExit`, `MemoryError`도 잡는다. 그리고 이 둘은 **사람이 실제로 Ctrl-C를 누르는 자리다** — 큰 CSV를 읽는 중, 모델 여러 개를 차례로 학습하는 중. 거기서 Ctrl-C는 중단이 아니라 *"읽을 수 없는 파일"*과 *"다음 모델"*로 읽혔다. 학습 고리 쪽은 끊을 수가 없다.

### `sqlite3.OperationalError`를 통째로 삼킨 세 곳 — 맞는 방법은 같은 파일 200줄 위에 있었다

`052_workspace_projects.part`의 세 함수가 "표가 아직 없을 수 있다"를 견디려고 이렇게 썼다.

```python
except sqlite3.OperationalError:
    return None
```

같은 예외 클래스가 무엇을 더 나르는지 sqlite로 직접 일으켜 봤다.

```
no such table: training_jobs     ← 넘기려던 것
no such column: project_id       ← 마이그레이션이 덜 됐다 (이 파일이 컬럼을 추가한다)
database is locked               ← 앞 회차에 고친 바로 그 실패
```

뒤의 둘을 삼키면 화면은 **"이 프로젝트에 작업이 없습니다"**라고 말한다. 작업이 있는데도. **틀린 데이터를 조용히 보여주는 것이 오류를 보여주는 것보다 나쁘다.**

맞는 방법은 새로 생각할 것도 아니었다. 같은 파일 200줄 위 `add_column_once`가 이미 예상한 메시지(`duplicate column name`)만 넘기고 나머지를 올린다. 세 곳은 그 옆에서 그러지 않았다.

**재현하다 초록불을 한 번 봤다.** `BEGIN IMMEDIATE`로 잠금을 쥐고 `SELECT`를 했더니 통과했다. 잠금이 없어서가 아니라 sqlite가 그 단계에서 읽기를 아직 허용하기 때문이다 — `BEGIN EXCLUSIVE`여야 읽기까지 막힌다. 그 초록불을 "잠금은 읽기에 영향 없음"으로 읽었으면 이 항목은 없었다.

### 오타 난 환경변수가 조용히 무시됐다

`FREE_MAX_DATASETS=three`을 넣으면 `int()`가 `ValueError`를 내고 `pass`였다. **운영자는 자기가 넣은 값이 걸려 있다고 믿고, 실제로는 기본값이 걸린다.** 사용량 제한은 남용을 막는 장치라 보통 더 헐거운 쪽으로 틀린다. 응답은 그대로 뒀다(기본값으로 계속 가는 것이 부팅을 거부하는 것보다 낫다). 바꾼 것은 **아무 흔적이 없던 것**뿐이다.

### 남긴 다섯은 이름으로 뒀다 — 그리고 그중 넷은 이유가 없었다

`automl_training.py`의 누출 재검사 실패는 "누출 없음"으로 읽히지 않도록 일부러 필드를 비운다. `.jwt_secret` 쓰기 실패는 읽기 전용 파일시스템을 견디는 것이고 배포 경로는 위에서 이미 막혔다. `float(score)`는 실패가 아니라 거르개다. SHAP 실패는 `explanation_type`으로 바깥에 보인다.

그래서 "일부러 남겼다"고 쓰고, 그것을 검사로 만들었다 — *일부러 삼킨 자리에는 그 자리에 이유가 있는가.* **넷이 걸렸다.** 이유는 내 머릿속과 이 문서에만 있었고 코드에는 없었다. 그 코드를 읽는 사람은 **의도해서 삼킨 것과 잊고 삼킨 것을 구별할 수 없다.** 넷 다 그 자리에 적었다.

### 그 다음 회차에 자기 검사기가 걸렸다

앞 회차에 만든 `check_findings_index.py`는 프로필 README가 *"42개 항목"*이라고 하는데 실제로 58개였던 것을 잡으려고 쓴 것이다. 이번에 이 문서를 열었더니 색인 머리말이 **"58개를 시간순으로 읽을 필요는 없다"**라고 말하고 있었다. 항목은 61개다.

검사기는 README만 봤다. **같은 종류의 낡음을 잡으려고 만든 도구가, 자기가 검사하는 파일 안쪽은 안 봤다.** 색인 절 안의 "N개" 주장도 보게 고쳤다.

처음에는 파일 전체에 정규식을 걸었고, 그러자 항목 본문의 *"22개 중 4개가"* 같은 정당한 셈을 전부 틀렸다고 했다. 넓게 잡은 검사는 잡는 게 아니라 **사람이 끄게 만든다.** 색인 절 안으로 좁혔다.

대조 다섯을 돌렸다 — 고친 것을 하나씩 되돌려 심고 그 검사가 빨간불이 되는지. 다섯 다 빨간불. 앞 회차에 심기가 들여쓰기 때문에 아예 안 들어간 적이 있어서, **이번에는 심을 문자열이 실제로 파일에 있는지부터 확인하고 심었다.**

## 시계가 둘이었고, 그것을 세던 검사기는 파일 열둘을 안 보고 있었다

이번 회차는 시계로 시작해서 **검사기의 맹점**으로 끝났다.

### 시계가 둘이다

    datetime.now()      32곳   표시자 없음   2026-08-23T19:00:00.123456
    datetime.utcnow()    3곳   "Z" 붙음      2026-08-23T10:00:00Z

`utcnow()`를 쓰는 셋은 나중에 붙은 에이전트 하위 모듈이다. **새 모듈이 더 나은 규칙을 골랐고, 옛 것과 만나는 자리를 아무도 안 봤다** — 이 저장소가 반복해 온 모양의 반대 방향이다.

만나는 자리는 프로젝트 실행 목록이었다. `_project_runs`가 `experiments`(로컬 시계)와 `analysis_runs`(UTC 시계)를 합친 뒤 **문자열로** 정렬한다. 목록에는 "최신순"이라고 적혀 있다.

```
"2026-08-23T11:00:00"  >  "2026-08-23T10:00:00Z"
  실험(여덟 시간 전)         에이전트 실행(방금)
```

**오프셋이 0이어도 뒤집힌다.** 두 시계는 자릿수도 다르다 — `experiments`는 마이크로초까지 쓰고 `analysis_runs`는 `timespec="seconds"`다. 같은 초 안에서 `'.'`(0x2E)가 `'Z'`(0x5A)보다 앞이라 나중 것이 아래로 간다. 시간대를 고쳐도 이건 안 고쳐진다.

**클라이언트에서는 못 고친다.** 프런트도 같은 문자열 정렬을 세 곳에서 한다. 거기서 `Date.parse`로 바꿔도 낫지 않는다 — 표시자 없는 값을 브라우저는 **보는 사람의 시간대**로 읽고, 그 값을 쓴 것은 서버다. **오류를 옮기는 것이지 없애는 것이 아니다.** 그래서 프런트는 건드리지 않고 "늘어나지 않는 것"만 지킨다.

한 곳을 고치고 **다른 한 곳을 잊었다.** `_project_reports`가 `_project_runs`에서 `created_at`을 그대로 복사해 와서 다시 문자열 정렬한다. 검사가 잡았다.

### 이 컨테이너의 시계는 환경변수가 말하는 것과 다르다

재현하다 알았다.

```
TZ 환경변수      : Asia/Seoul
datetime.now()   : +00:00        ← libc에 /usr/share/zoneinfo가 없다
ZoneInfo(...)    : +09:00        ← pip tzdata 패키지는 있다
```

**환경이 선언한 시간대를 프로세스가 갖고 있지 않다.** 한 프로세스 안에 해석이 둘 있고 아홉 시간 차이다. modelmate는 `ZoneInfo`를 안 써서 지금은 안 부딪히지만, OS 쪽 `tzdata`가 설치되는 순간 **코드 한 줄 안 바뀌고 모든 표시자 없는 값의 뜻이 아홉 시간 옮겨간다.**

이것 때문에 처음 만든 재현이 초록불이었다. KST를 가정한 예시가 여기서는 재현되지 않았다 — 잠금 재현 때 `BEGIN IMMEDIATE`로 초록불을 본 것과 같은 자리다. **초록불의 이유를 묻지 않았으면 "시간대는 상관없다"로 읽었을 것이다.**

### 그리고 검사기가 파일 열둘을 안 보고 있었다

시계 수를 세다 두 방법이 안 맞았다 — 줄 훑기 32, AST 28. 이유를 봤다.

`backend/main.py`는 조각들을 이어 붙여 한 번에 컴파일한다. 그래서 **조각 하나하나는 파이썬 파일이 아니다** — 앞 조각에서 시작한 함수 본문이 다음 조각에서 이어지는 것들이 있다. 그것만 떼어 `ast.parse`에 넣으면 죽는다. **열둘이 그렇다.**

이 저장소의 구조 검사들은 이렇게 쓰여 있었다.

```python
try:
    tree = ast.parse(path.read_text(...))
except SyntaxError:
    continue          # ← 열두 파일이 여기서 조용히 빠졌다
```

**앞 회차에 제품에서 고친 바로 그 모양이, 그것을 찾는 데 쓴 검사기 안에 있었다.** 예외를 삼키고 아무 말도 안 하니 *열두 파일을 못 본 것*과 *그 파일에 아무것도 없는 것*이 화면에서 똑같았다. 숨어 있던 것:

```
무보호 쓰기 함수 3개   deploy_model · delete_deployed · deploy_model_stable
                       부팅 시 DDL이 아니라 사용자가 닿는 배포 엔드포인트다
타입 없는 except  7개   선택적 import 넷 + 예측 입력 값 변환 셋
```

**앞 두 회차가 발표한 수가 둘 다 틀렸다.** "쓰기 22개"는 25개였고, "타입 없는 `except:` 2개"는 9개였다. 두 문서를 고쳤다.

맞는 처리는 **이 저장소에 이미 있었다.** `test_declared_dependencies.py`가 같은 자리에서 건너뛰지 않고 줄 훑기로 되돌아가며, 주석에 *"건너뛰는 것이야말로 이 모듈이 막으려는 실패"*라고 적어 뒀다. 그 파일에만 있었다 — `add_column_once`가 같은 파일 200줄 위에 있었던 것과 같다.

이제 조립해서 한 번에 파싱하고 줄 번호를 조각으로 되돌린다(`tests/part_source.py`). 파싱이 실패하면 **건너뛰지 않고 죽는다** — 조립된 소스가 안 열리면 확인할 것이 없다는 뜻이 아니라 확인이 불가능하다는 뜻이다.

그리고 **이번에 쓴 검사에도 같은 `except SyntaxError`를 넣었었다.** 그 맹점을 찾아낸 파일에서. 여섯 저장소를 훑어 나머지를 확인했고, `mutate.py`의 것은 의도된 분류(컴파일 안 되는 변이는 BROKEN)라 그대로 뒀다.

대조 여덟. 결정적인 것 하나 — **단독 파싱이 안 되는 조각에 시계를 심었더니 이제 잡힌다.** 예전 검사기로는 안 잡혔을 자리다.

## 대조를 세는 도구가 대조받지 않고 있었다 — 그리고 자기가 검사하는 문서를 망가뜨렸다

앞 회차에 검사기가 파일 열둘을 안 보고 있는 것을 찾았다. 그래서 물었다 — **다른 검사기들은 저장소의 몇 퍼센트를 실제로 여는가.**

`open`·`Path.read_text`·`Path.open`을 가로채 검사기를 하나씩 돌려 **연 파일을 셌다.** 대부분 멀쩡했다. `check_anchors.py`는 `.md` 두 개를 여는데 이 저장소에 `.md`가 정확히 둘이다. `find_vacuous_tests.py`는 143개를 연다.

**처음 결과는 내 계측이 틀린 것이었다.** `find_vacuous_tests.py`가 "0개를 열고 종료 0"으로 나와 결함처럼 보였는데, 그 도구는 형제 저장소를 훑고 내 필터는 자기 저장소 안만 셌다. **도구가 아니라 자를 고쳤다.**

### 열셋 전부 물려 있었고, 그 열셋이 덮는 것은 스물다섯 중 여섯이었다

`check_doc_tests_bite.py`는 문서를 읽는 검사가 정말 그 문서에 물려 있는지 본다 — 문서를 비우거나 문구를 심어 빨간불이 되는지. **13/13 물려 있다.** 도구는 건강했다.

세어보니 다섯 저장소에서 **스물다섯 개** 테스트 파일이 실재하는 문서를 이름으로 읽는다. 열셋이 덮는 것은 **여섯**. 나머지 열아홉에는 아무 대조가 없었고, **새 검사가 생겨도 아무것도 그것을 묻지 않았다.**

기존 안전장치는 `len(CASES) < 5`였다. **하한선은 목록이 줄어드는 것만 본다** — 모집단이 목록에서 멀어지는 것은 못 본다. 이 저장소가 `DECLARED_RECORDS`에서 이미 겪은 모양이다. 이제 모집단을 매번 세고, 대조가 없는 것은 이유와 함께 이름으로 둔다. 여섯을 새로 걸었다(전부 **문서가 자기 수를 말하는** 검사 — "42개 항목"으로 이미 한 번 당한 자리다). **19/19.**

**모집단을 세는 정규식도 두 번 고쳤다.** `README|AGENTS|\.md`로 재니 43개가 나왔는데 임시 픽스처(`f.txt`, `/tmp/...`)와 **독스트링에서 문서 경로를 언급만 하는 산문**이 섞여 있었다 — 열한 번째 인용-사용 혼동이다. 저장소 안에 **실재하는** 문서를 가리키는 리터럴만 세니 25개. 그리고 새 목록에 넣은 첫 항목이 **첫 실행에서 "낡았다"로 걸렸다** — `agent-safety-core`는 글롭으로 문서를 찾아 경로 리터럴이 없다.

### 그리고 이 도구가 나를 물었다

작업 중 이 도구를 90초 타임아웃으로 죽였다. **`SIGTERM`은 `finally`를 돌리지 않는다.** 몇 단계 뒤 `git status`를 보니 modelmate의 `docs/security-notes.md`와 `docs/usage-limits.md`가 **0바이트**였다. 하마터면 빈 문서 둘을 커밋할 뻔했다.

**검사하려고 만지는 것을 망가뜨린 채 끝날 수 있는 검사기였다.** 셋을 넣었다: 비우기 전에 사본을 남기고, `SIGTERM`/`SIGINT`를 받으면 되돌리고 죽고, **시작할 때 남은 사본이 있으면 되돌리고 그 사실을 말한다.** 마지막 것이 요점이다 — 앞의 둘이 또 새도 다음 실행이 고치고, 무엇보다 조용히 넘어가지 않는다. `SIGKILL`로 죽여 확인했다.

### 그것을 고치다 더 나쁜 것이 나왔다

`document-intelligence/README.md`도 0바이트였다. 더 이른 어느 죽음에서 남은 것이다. **그런데 그 뒤 두 번의 전체 실행이 그 항목을 ✓로 보고했다.**

presence 대조는 "비우면 실패하는가"를 묻는다. **문서가 이미 비어 있으면 비우는 것은 아무 일도 아니고**, 검사는 어차피 실패하니 `✓ 비우면 실패한다`가 나온다 — 확인한 것 하나 없이. **열아홉 개 초록불 중 하나는 "문서가 망가졌다"는 뜻이었다.**

이제 이미 빈 문서는 거부한다. 미리 비워 놓고 돌려 확인했다.

### CI 대조가 엉뚱한 이유로 돌고 있었다

범위 검사가 살아 있는지 보려고 CI에 "모집단을 비우면 실패해야 한다"를 넣었다. 종료 코드만 봤다. **지키려던 안전장치를 지워도 여전히 빨간불이었다** — `REPOS`를 비우면 모집단이 비는 동시에 `UNCONTROLLED` 열셋이 전부 "낡음"으로 걸리기 때문이다. **엉뚱한 이유로 도는 대조는 대조가 아니다.** 그 문장(`하나도 못 찾았다`)이 나오는지를 보게 고쳤고, 안전장치를 지워 빨간불이 되는 것까지 확인했다.

이 회차에 고친 것 중 셋이 **내가 이 회차에 방금 만든 것**이었다. 대조 여덟.

## "급한 셋"이 사실은 열둘이었다 — 그리고 그것을 고친 변환을 검사한 것도 틀렸다

두 회차 동안 검사기를 고쳤으니 이번엔 그 검사기가 가리킨 곳을 고쳤다. 로드맵에 이렇게 적혀 있었다.

> 새로 보인 셋(`deploy_model`·`delete_deployed`·`deploy_model_stable`)은 부팅 시 DDL이 아니라 **사용자가 배포 화면에서 직접 닿는 자리**라 나머지보다 급하다.

**그 셋만 급한 게 아니었다.** 남은 스물하나를 도달 경로로 분류했다 — 라우트 데코레이터가 붙었는가, 라우트에서 불리는가, 그것들에서 불리는가.

```
부팅 시 DDL                    9개   실패하면 부팅이 실패한다. 프로세스가 못 뜬다
사용자가 닿는 것               12개   라우트 직접 5 · 라우트에서 불림 5 · 그 아래 2
```

**셋이 아니라 열둘이었다.** 앞 회차에 새로 찾은 셋만 "급하다"고 적고 **나머지 열여덟을 분류하지 않았다.** 세어본 것과 훑어본 것을 같은 문장에 적으면 이렇게 된다.

### 손으로 열두 번 고치지 않았다

배포된 앱의 함수 열둘을 손으로 감싸는 것은 조용히 틀리기 좋은 일이다. 대신 **검증할 수 있는 변환**을 썼다: `conn = get_db()` 뒤 같은 블록을 `try/finally`로 감싼 뒤, **새로 넣은 `Try`를 도로 벗겨내 AST가 글자 하나까지 같은지** 확인한다. 다르면 되돌린다.

그 확인이 두 번 일했다.

**하나.** `make_token`에는 세겹따옴표 SQL이 있다. 들여쓰기를 네 칸 미는 변환이 **문자열 안쪽 줄까지 밀어서 SQL 내용을 바꿨다.** 되돌아왔고, 여러 줄 문자열의 둘째 줄 이후는 건드리지 않게 고쳤다.

**둘.** `deploy_model`은 "0줄"로 나왔다. `conn = get_db()`가 없는 게 아니라 **함수가 조각 두 개에 걸쳐 있었다** — 시작은 `071`, 연결은 `072`. 앞 회차에 찾은 "조각은 파이썬 파일이 아니다"가 같은 자리에서 또 나왔다.

### 그런데 그 확인이 못 잡은 것이 있었다

변환을 두 번 돌렸다. 두 번째 실행이 **이미 감싼 일곱 개를 또 감쌌고, 확인은 통과했다.**

벗겨내는 쪽이 조건에 맞는 `Try`를 **전부** 벗기기 때문이다. 하나를 벗기든 둘을 벗기든 같은 본문이 나온다. *변환이 옳은가*는 물었지만 ***멱등한가*는 안 물었다.**

`git checkout`으로 되돌리고, "이미 감싸져 있으면 건너뛴다"를 넣고, 두 번 돌려 확인했다. 검사 쪽에도 넣었다 — 연결 하나에 보호막 하나.

### 남은 아홉은 이유가 목록이 아니라 성질이다

**21 → 9.** 남은 아홉은 전부 `init_db`와 `ensure_*`다. 거기서 예외가 나면 부팅이 실패하고 프로세스는 어차피 못 뜬다 — **잠금을 쥔 채 서비스가 계속되는 상황이 아니다.**

"아홉 개를 남겼다"는 다음 사람에게 아무 말도 안 한다. 그래서 목록이 아니라 **성질**을 지킨다: 남은 목록에 부팅 시 DDL이 아닌 것이 들어오면 걸린다.

README의 "쓰기를 하는 것 25개"도 그대로 뒀다. 고칠 때마다 숫자를 줄이면 **"몇 군데였나"라는 사실이 사라진다.** 수는 두고 9 + 4 + 12 = 25로 **어디로 갔는지**를 적는다. 세 묶음이 겹치지 않는 것도 검사한다 — 합이 맞아도 한 함수가 두 곳에 있으면 뜻이 없다.

대조 다섯. 그중 하나는 **"한 함수를 두 번 감싸면 걸리는가"** — 이번에 실제로 당한 것이라 넣었다.

## "CI가 안 도는 라우트 마흔"이 아홉이었다 — 그리고 그중 둘이 데이터를 지우는 라우트였다

로드맵에 **마흔 개**라고 적혀 있었고, 그 뒤 "프런트가 부르는 스물넷 중 열일곱이 남았다"로 바뀌어 있었다. pytest와 **스모크 열다섯을 합쳐** 다시 쟀다 — CI가 실제로 돌리는 것 전부로.

```
라우트 핸들러 100개 중 본문이 한 줄도 안 도는 것   9개
```

스모크 열둘을 CI에 넣은 것이 대부분을 닫았고 **로드맵의 숫자만 그대로 있었다.** 세어본 것을 적어두면 그 문장은 그날부터 낡기 시작한다.

**처음 잰 결과는 0개였다.** 너무 좋아서 다시 봤다. `ast.walk(node)`가 데코레이터 노드까지 판다 — `@app.post(...)` 줄은 import 때 반드시 실행되므로 **모든 라우트가 "돌았다"로** 나왔다. 본문만 세도록 고쳐 아홉이 됐다.

**아홉 중 둘이 사용자 데이터를 지우는 `DELETE`였다.** `clear_history`(관리자면 전원의 기록을 지운다)와 `delete_deployed`. 앞 회차에 연결 누수를 고치느라 손댄 바로 그 둘이고, **그때 나는 아무것도 실행하지 않는 코드를 고치고 있었다.** 일곱에 검사를 붙였고, 남은 둘은 상태를 갖춰야 해서 이름으로 뒀다.

### 지워진 라우트가 다음 파일에 `return` 한 줄을 남겼다

`GET /api/debug-env`는 이 목록에 이미 있던 항목이다. 지웠는데, **그 `return` 줄이 다음 조각 맨 앞에 남았다.** 조각들은 이어 붙여 컴파일되므로 그 고아 한 줄은 아무 상관 없는 `reset_session`의 본문 끝에 붙었다.

```python
async def reset_session(user=...):
    STATE.clear()
    return {"ok": True}
    return {"key_length": ..., "all_env_keys": all_keys}   # ← 도달할 수 없다
```

죽은 줄이라 실행되지 않고, 그래서 그때 쓴 `test_no_environment_disclosure.py`도 못 봤다. 하지만 읽는 사람에게는 **환경을 뱉는 라우트로 보이고**, 위 한 줄이 지워지면 실제로 그렇게 된다.

**지운 곳과 남은 곳이 다른 파일이었다는 것**이 이것이 살아남은 이유다. 조립된 프로그램 전체에서 `return`/`raise` 뒤 문장을 훑는 검사를 넣었다 — **하나뿐이었고, 그 하나가 이것이다.**

### 죽은 코드라고 부르려다 대조가 뒤집었다

`delete_deployed` 안에 소유권 검사가 **하나 더** 있다. 403을 낸다. 재보니 남이 접근하면 앞의 `assert_deployed_model_owner`가 먼저 **404**로 끊어서 그 403은 한 번도 안 터진다. 죽은 코드라 지우려 했다.

**관문의 거절을 지우고 돌렸더니 인라인 검사가 403으로 막았다.** 죽은 게 아니라 관문이 맞는 동안에만 안 도는 것이고, 그게 이중 방어가 뜻하는 바다. **지우지 않았다** — 도달하지 않는 것과 쓸모없는 것은 다르다.

### 대조 셋이 헛돌았고, 세 번 다 이유가 달랐다

- 심을 문자열이 파일에 **네 번** 나와서 `replace(..., 1)`이 **다른 함수에** 심었다. 개수만 보면 "심었다"였다.
- 관문이 아니라 그 뒤의 **중복 검사**를 껐다. 그래서 초록불이었고, 그 초록불이 위의 이중 방어를 알려줬다.
- 앵커가 **BOM 때문에** 안 맞아서 아예 안 심겼다.

함수 안에서 줄 번호를 찾아 그 줄만 바꾸도록 고쳐 다시 걸었고, 여섯 다 빨간불이 됐다.

**그리고 그 대조 도구가 파일 셋에 BOM을 붙였다.** `utf-8-sig`로 읽고 `utf-8-sig`로 쓰면 없던 파일에는 **생긴다.** 커밋 직전 `git status`에서 봤다 — 앞 회차에 문서 둘이 0바이트로 남은 것을 발견한 것과 같은 자리다. 되돌렸고, 앞 회차 커밋의 조각 여덟은 BOM 상태가 안 바뀐 것을 확인했다.

### 느슨한 단언 둘을 좁혔다

`in (403, 404)`와 "거절하거나 빈 걸 주거나"로 써둔 것 둘. **둘 중 아무거나면 통과**다. 재보니 답은 각각 하나였다 — 404, 그리고 400 `Run cross-validation first`.

## 거절인데 한 번도 터진 적 없는 것들 — 그리고 그것을 시험하는 내 검사가 저지른 단골 결함

`HTTPException` 발생 지점 102개 중 CI(pytest + 스모크 열다섯)에서 **한 번도 안 터지는 것이 66개**였다. 한 회차에 다 할 수 없어 **종류로 잘랐다.**

```
400 × 34   입력 모양이 틀렸다        — 사용자가 다시 보내면 그만이다
404 × 17   없거나 남의 것이다        — 소유권 쪽은 이미 검사가 있다
409 ×  5   지금 하면 안 된다         ← 여기
410 ×  2   꺼진 API다               ← 여기
413 ×  1   너무 크다                ← 여기
500 ×  6   서버가 졌다
```

입력 검증이 아니라 **상태를 지키는 거절**을 골랐다. 여기가 틀리면 사용자는 잘못된 요청을 고치는 게 아니라 **돌고 있는 작업이 딸린 데이터셋을 지우거나, 꺼놨다고 믿은 예측 API가 계속 응답한다.**

`/api/v1/{id}/predict`와 `/api/v2/{id}/predict`는 **쌍둥이**다. 둘 다 각자 `disabled_at`을 본다 — 한쪽만 검사하면 다른 쪽이 조용히 갈라지고, 그때 껐다고 믿은 API가 옛 경로로 계속 답한다. 둘이 **같은 답을 하는지**까지 봤다.

**사슬도 함께 본다.** 데이터셋을 지우면 그것을 쓰던 배포 모델이 `disabled_at`을 받고, 그다음 예측 호출이 410이 된다. 조각을 따로 확인하면 **사슬이 끊겨도 둘 다 초록불**이다 — 사용자가 겪는 것은 사슬이다.

### 내가 쓴 검사가 이 저장소의 단골 결함을 저질렀다

413 검사를 처음엔 이렇게 썼다: **라우트 본문의 크기 계산을 테스트 파일에 옮겨 적고 그 사본을 시험했다.** `UploadFile`을 만들기 귀찮다는 이유였다.

그건 이 목록이 계속 찾아온 바로 그 모양이다 — **한 사실을 두 곳에 적고 한 곳만 지키는 것.** 제품의 한도 계산이 바뀌어도 내 사본은 그대로 통과했을 것이고, 그 검사는 자기 자신을 시험하고 있었다. 지우고 진짜 라우트를 불렀다. `UploadFile`을 만드는 데 세 줄 들었다.

### `in (404, 409)`로 써둔 검사는 409에 닿은 적이 없었다

재실행 거절을 `in (404, 409)`로 두고 통과시켰다. **실제로 나온 것은 늘 404였다** — 픽스처가 만든 행을 목록 함수가 아예 못 봤다. 기록의 프로젝트는 `data.dataset_ref.project_id`에서 오고 `analysis_run_id`는 JSON이 아니라 **행 번호**로 `experiment-<id>`가 된다.

**둘 중 아무거나면 통과인 단언은 닿지 않은 것을 닿은 것처럼 보이게 한다.** 픽스처를 고치고, "기록이 목록에 실제로 보이는가"를 묻는 대조를 앞에 뒀다. 그리고 404와 409를 **가르는** 검사를 따로 넣었다 — 사용자가 할 일이 다르다. 하나는 목록에서 다시 고르고, 하나는 CSV를 다시 올린다.

같은 회차에 같은 종류를 두 번 만났다(`in (403, 404)`도 앞 회차에 좁혔다). **느슨한 단언은 실패를 숨기는 게 아니라 도달을 숨긴다.**

### 소프트 삭제를 하드 삭제로 착각했다

`delete_dataset`이 행을 지운다고 가정하고 검사 둘을 썼다가 빨간불이었다. 코드가 아니라 **내 가정이 틀렸다** — `deleted_at`을 찍고 `retention_status='deleted_retained'`로 남기는 보존 정책이다. **검사가 빨간불일 때 검사를 고치는 것과 가정을 고치는 것은 다르다.**

### 스위트가 느려진 범인은 내 검사가 아니었다

새 검사를 넣은 뒤 전체 스위트가 13분에서 **45분**으로 늘었다. 파일 하나만 돌리면 13초다. 내 검사가 공유 상태를 더럽혀 뒤 검사들이 무거운 일을 한다고 의심했다.

`pgrep`으로 보니 **앞선 대조 실행에서 떨어져 나온 `pytest` 프로세스가 계속 돌고 있었다.** 죽이고 다시 재니 **7분 43초** — 지금까지 중 가장 빠르다. 의심은 틀렸다.

다만 그 의심으로 찾은 것 하나는 진짜였다: `upload`은 **거절하든 성공하든 공유 `STATE`에 `analysis_status`를 남긴다.** 이 프로세스의 다른 검사가 그 값을 읽는다. 되돌리는 픽스처를 붙였다 — 느려서가 아니라 새는 게 맞아서.

대조 아홉. 이번에는 **바이트로 읽고 바이트로 써서** 심었다 — 앞 회차에 `utf-8-sig` 왕복이 없던 BOM을 세 파일에 만들었기 때문이다.

## 100% 관문이 무엇의 100%였는지 아무도 적어두지 않았다

형제 저장소 넷은 2026-08-22에 **분기 커버리지 100% 관문**을 얻었다. 이번에 그 관문이 무엇을 재는지 물었다.

```
coverage run --branch --source=core,adapters -m pytest -q
coverage report -m --fail-under=100
```

**`--source`가 저장소 전체가 아니다.** 저장소별로 관문 밖에 무엇이 있는지 셌다.

```
agent-safety-core       14개 파일 / 약 890줄   profiles/ · benchmark/
rag-profile-selector     8개 파일 / 약 1,109줄  experiments/ · scripts/
document-intelligence    2개 파일 / 약 29줄     adapters/
mcp-gateway              0개                   ← 밖이 없다
```

"100%"는 **그 소스의 100%**였고, 그 문장은 어디에도 그렇게 적혀 있지 않았다.

### 관문 밖에 거부 하나가 숨어 있었다

`agent-safety-core`의 관문 밖은 연구 코드만이 아니었다. `profiles/ap2/payment_guard.py`(97%)와 `profiles/kr_ai_act/evidence.py`(99%)는 **제품**이다.

payment_guard의 안 돌던 한 줄은 이것이었다.

```python
if record is None or record.state != UNKNOWN:
    raise PaymentRefused("reconciliation applies only to an UNKNOWN execution")
```

**정산이 끝난 실행을 다시 조정하지 못하게 막는 거부다.** 이 저장소의 주장은 "최종 상태는 최종이다" 위에 서 있는데, 그 조건이 뒤집혀 있어도 555개가 전부 통과했을 것이다. 기존 검사들이 이 줄에 닿지 못한 이유는 단순하다 — **언제나 UNKNOWN을 만든 뒤 `reconcile`을 부른다.** 그게 정상 흐름이다.

**같은 맹점이 두 번 일했다.** [다섯 저장소의 거부 267개를 하나씩 지워봤다](#다섯-저장소의-거부-267개를-하나씩-지워봤다)는 `raise` 서른 개를 하나씩 `pass`로 바꿔 돌린 감사였다. 그 서른도 `core`/`adapters`에서 모은 것이다. **관문과 감사가 같은 곳을 못 봤고, 그래서 이 거부는 어느 쪽에도 세어지지 않았다.**

### 0%가 "검사가 없다"는 뜻이 아니었다

`document-intelligence`의 `adapters/pdfplumber_adapter.py`는 **0%**였다. 검사가 없어서가 아니다 — 그 파일을 시험하는 검사는 넷 있고, **전부 하위 프로세스에서 돈다.** `sys.path`에 미끼를 놓고 이름 충돌을 만들어야 해서 그럴 수밖에 없다. in-process 커버리지는 그 실행을 못 본다.

**"검사가 없다"와 "재는 쪽이 못 본다"는 보고서에서 똑같이 0%로 보인다.** 이 목록이 계속 만나온 모양이다 — 훑지 않은 것과 훑어서 빈손인 것.

in-process 검사 하나를 더했다. 충돌 이야기가 아니라 **이 인터프리터에서 재수출이 실제로 같은 객체를 넘겨주는가**를 묻는다. 그다음 `adapters`를 관문에 넣었다.

### 안 넣는 것은 결정이고, 모르는 것은 결정이 아니다

`benchmark/`(80%)와 `experiments/`·`scripts/`(69%)는 관문에 **넣지 않았다.** `benchmark/run.py`는 CLI 진입점이라 100%를 채우려면 CI가 매번 벤치마크 전체를 돌려야 하고, `fetch_kr_law_corpus.py`는 코퍼스를 내려받는다. 재사용이 아니라 **기록된 출력이 값인** 일회성 코드다.

대신 **수를 찍는 단계**를 CI에 넣었다. 빌드를 깨지 않는다. 다음 사람이 침묵 대신 계측을 읽는다.

### 대조 둘이 헛돌았고, 이번엔 커버리지 관문의 성질을 내가 잘못 알았다

처음 건 대조는 `raise`를 `pass`로 바꿔놓고 관문이 빨간불이 되는지 본 것이었다. **초록불이었다.** 당연하다 — `pass`도 실행되는 줄이고, **커버리지 관문은 줄이 안 돈 것을 잡지 무엇을 하는 줄인지는 안 본다.** 게다가 `;`로 이어 붙여 `pytest`의 종료 코드를 삼키고 있었다.

관문에 맞는 대조는 다르다: **새로 쓴 검사를 지우고 관문이 빨간불이 되는지** 본다. 그러면 그 코드가 관문에 실제로 세어지는지를 묻게 된다. 둘 다 99%로 떨어지며 빨간불이 됐다.

### 그리고 CI가 나를 잡았다

세 저장소의 스위트를 돌리고, **그다음 README의 테스트 개수를 고치고**, 다시 안 돌린 채 밀었다. `document-intelligence`의 CI가 빨간불이었다 — `AGENTS.md`도 개수를 말하는데 README만 고쳤고, `test_instructions_current.py`가 둘을 맞춰본다.

로컬에서 통과한 이유가 요점이다. **문서를 읽는 검사가 있는 저장소에서는 문서 수정도 코드 수정이다.** 순서를 거꾸로 했다.

### 검사를 쓰다 사본을 만들 뻔했다

새 검사에 결제 처리기 가짜를 만들려다 멈췄다. 호출 규약(키워드 인자, `__call__`, `lookup`)은 이미 `test_payment_guard.py`가 들고 있다. **두 번 적으면 규약이 바뀔 때 사본만 남는다** — 앞 회차에 내가 직접 저지른 결함이라 이번엔 가져다 썼다.

## 다섯 저장소의 자기검사를 표로 만들었더니 규칙마다 빠진 칸이 있었다

앞 회차에 CI가 나를 잡았다 — `README`의 테스트 개수를 고치고 `AGENTS.md`는 안 고쳤는데, **그 둘을 맞춰보는 검사가 `document-intelligence`에만 있었다.** 그래서 물었다: **이런 자기검사가 저장소마다 몇 개씩 있는가.**

### 파일 이름으로 센 첫 표는 틀렸다

`tests/` 아래 파일 이름으로 표를 만들었더니 `modelmate`에 "문서 경로 검사 없음"이 나왔다. **있다** — `scripts/check_doc_paths.py`로, 테스트가 아니라 스크립트이고 CI가 부른다. 이름으로 세면 **다른 이름으로 존재하는 것을 없다고 한다.**

칸마다 다시 확인했다. 그러자 표가 달라졌다.

- `agent-safety-core`에는 **`AGENTS.md` 자체가 없다.** 없는 문서에 대한 일치 검사가 없는 것은 빠진 칸이 아니다.
- `modelmate`에 커버리지 관문이 없는 것은 이미 적어둔 사실이다(조각을 이어 붙여 `exec`하므로 coverage.py가 못 연다).

**남은 진짜 빈칸은 하나였다: 선언한 의존성이 참인지 보는 검사.**

### `dependencies = []`는 주장이다

```
mcp-gateway             dependencies = []      검사 있음
rag-profile-selector    dependencies = []      검사 있음 (형제 없이 스위트를 돌린다)
document-intelligence   dependencies = []      ← 없었다
agent-safety-core       ["cryptography>=42"]   ← 없었다
modelmate               requirements 16개      검사 있음
```

셋이 같은 주장을 하는데 **둘만 지키고 있었다.**

먼저 그 주장이 지금 참인지부터 쟀다. `document-intelligence`는 `pdfplumber`를 `parse_pdf` **안에서** 들여오고, `rag-profile-selector`는 형제를 함수 안에서 들여온다. **둘 다 참이었다** — 지금은. 그것을 지키는 것이 아무것도 없다는 게 문제였다. 누가 `import pdfplumber`를 모듈 맨 위로 올리면 `pip install document-intelligence` 뒤에 **import만으로 패키지가 죽고**, 개발 기계에는 이미 깔려 있어 아무 검사도 빨간불이 되지 않는다.

### 소스를 읽는 것으로 끝내지 않았다

소스 훑기는 "모듈 수준에 없다"까지만 말한다. `__init__`이 어댑터를 끌어오고 그 안에서 터지는 경우는 못 본다. 그래서 `meta_path`에 차단기를 넣어 **pdfplumber가 정말로 없는 하위 프로세스에서** import해 본다.

`ModuleNotFoundError`를 쓴다 — 맨 `ImportError`가 아니라. 형제 저장소가 첫 시도에서 그 차이 때문에 **하네스가 만든 오류를 결함으로 읽을 뻔했고**, 그 기록이 남아 있어서 같은 자리를 안 밟았다.

대조에서 그 값이 드러났다. `import`를 모듈 최상위로 올리자 **다섯 단언이 걸렸는데, 그중 하나는 차단한 하위 프로세스 쪽**이다 — 소스 훑기만 있었으면 못 잡았을 자리다.

### 양쪽 방향을 다 본다

`agent-safety-core` 쪽은 `dependencies`가 비어 있지 않아서 질문이 하나 더 생긴다. **안 선언한 것을 쓰는가**뿐 아니라 **아무도 안 쓰는 것을 선언했는가.** 뒤쪽이 없으면 설치하는 쪽이 이유 없이 무거워지고, 그 항목이 왜 있는지 다음 사람이 알 수 없다. `jsonschema`가 검사용으로 남아 있는지도 함께 지킨다 — 런타임으로 새면 선언이 거짓이 된다.

**그리고 CI가 또 나를 잡았다.** 차단 하위 프로세스가 로컬에서는 통과하고 CI에서 `ModuleNotFoundError: No module named 'document_intelligence'`로 죽었다. 로컬은 패키지가 설치돼 있어 하위 프로세스가 그것을 집었고, **CI는 `requirements.txt`만 깔고 패키지는 안 깐다**(`conftest`는 pytest 프로세스에만 경로를 넣는다).

**설치 여부에 기대는 검사는 기계마다 다른 것을 시험한다.** `sys.path`에 `src`를 명시적으로 넣고, 덤으로 **하위 프로세스가 집은 것이 이 저장소의 소스인지** `__file__`로 확인하는 대조를 더했다 — 설치된 옛 사본을 집으면 초록불인데 여기 코드는 안 본 것이다. `python3 -I`로 격리해 CI와 같은 조건을 만들어 확인했다.

대조 아홉, 전부 빨간불.

**이번에는 문서를 먼저 고치고 스위트를 돌렸다.** 앞 회차에 순서를 거꾸로 해서 CI에 잡혔다.

## CI가 초록불인데 몇 개가 실제로 돌았는지는 아무도 안 봤다

두 회차 연속으로 **CI가 나를 잡았다** — 로컬에서 통과한 것이 CI에서 빨간불이었다. 그래서 반대 방향을 물었다: **CI가 초록불일 때, 거기서 몇 개가 실제로 돌았나.**

```
agent-safety-core       565 passed
mcp-gateway             379 passed
rag-profile-selector    299 passed, 49 skipped   ← 14%가 안 돈다
document-intelligence   234 passed
modelmate               951 passed, 4 skipped
```

**건너뛴 검사는 아무것도 확인하지 않은 초록불이다.**

### 소유권 관문 검사 넷이 CI에서 한 번도 돈 적이 없다

`modelmate`의 넷은 전부 한 파일이었다 — **남의 분석 실행에 접근하면 거절하는지**를 보는 검사들. 이유는 `analysis_runs`가 없다는 것이었고, 그 표는 에이전트 경로가 처음 쓸 때 만들어지므로 **갓 만든 DB에는 없다.** 즉 CI에는 늘 없다.

개발 기계에는 예전 실행이 남긴 표가 있었다. **로컬에서는 넷이 돌았고, 정작 지켜야 할 곳에서는 안 돌았다.**

건너뛸 이유가 없었다 — 스키마를 만드는 함수가 이미 있다. 없으면 만들고, 그래도 못 만들면 그때 건너뛴다. DB를 지우고 확인했다: **16개 전부 통과, skip 0.** 관문의 거절을 꺼보니 빨간불이 된다.

### 마흔아홉은 새 하나가 숨기에 충분한 수다

`rag-profile-selector`의 49는 **이미 정직하게 적혀 있었다** — README가 "299 통과 · 49 skip(코퍼스 없이)"와 "348 tests(코퍼스 받은 뒤)"를 나란히 싣고 이유까지 쓴다. 숨겨져 있을 거라는 내 예상은 틀렸고, 그 문장은 맞았다.

문제는 다른 데 있었다. **어느 검사가 왜 건너뛰는지를 아무것도 지키지 않았다.** 이유가 다른 skip이 하나 끼어들어도 총계는 50이 되고, 아무도 그 하나를 안 본다.

`conftest.py`가 세션 끝에 집합을 맞추게 했다 — 어느 파일이 몇 개를, 같은 이유로. 하한선이 아니라 정확한 일치다. 전체 스위트를 돌릴 때만 본다: **파일 하나만 돌릴 때 걸리면 그건 검사가 아니라 방해이고, 다음 사람이 이 훅을 지운다.**

### 그 훅을 쓰다 조건 하나가 어긋난 것을 찾았다

훅이 "이유가 다른 skip이 있다"고 걸었다. 실제로 다른 것은 **사실이 아니라 문장**이었다 — 셋은 한국어로 똑같이, 하나는 영어로 다르게 적혀 있었다. 그런데 조건까지 보니 진짜 차이가 있었다.

```python
test_stratification.py         not (CORPUS / "documents").exists()
나머지 셋                       not (DOCUMENTS.exists() and any(DOCUMENTS.glob("*.json")))
```

**`fetch`가 중간에 죽어 빈 디렉터리가 남으면 그 파일만 빈 코퍼스를 상대로 돈다.** 빈 디렉터리를 만들어 재현했다. 그 파일 자신의 주석이 *"봉인 검사 셋이 그 픽스처를 쓰지 않아 잘못된 이유로 통과했다"*고 적어둔 것과 같은 종류다.

`test_published_corpus_size.py`는 바로 그 아래에 **"같은 사실을 말하는 문서들. 한 곳만 고치고 나머지가 남는 것이 이 저장소가 이미 겪은 일이다"**라고 적어두고 있었다. 문서에 대해서는 그 규칙을 세웠고, **건너뛰는 조건과 이유 자체가 네 번 적혀 있었다.** 한 곳으로 모았다.

### 안 고친 것도 적어둔다

`mcp-gateway`의 "327 passed, 6 skipped"는 결함이 아니었다. 그건 **코어 없이 도는 별도 잡**이고, README가 그 수를 싣고 CI가 맞춰본다. 이번에 rag에 만든 것을 그 저장소는 이미 갖고 있었다 — 세어보고 아무것도 없었다는 것도 결과다.

`rag`의 49를 없애려면 코퍼스를 받아야 하고, 그건 라이선스가 붙은 본문을 저장소에 넣는 일이다. **내가 결정할 것이 아니다.** 대신 49가 정확히 무엇인지를 지킨다.

### 그리고 그 훅이 CI를 죽였다

밀고 나니 rag가 빨간불이었다. 이 저장소에는 `pytest --collect-only`로 **"스위트가 조용히 줄지 않았는가"**를 보는 단계가 있는데, 거기서는 아무것도 안 돈다 — 건너뛴 것도 0이다. 훅은 그것을 "기대 집합과 다르다"로 읽고 세션을 죽였고, 그래서 `N tests collected` 줄이 아예 안 찍혀 그 단계가 실패했다.

**세션 훅은 내가 생각하지 않은 모드에서도 돈다** — collect-only, `-k` 선택, 파일 하나. 판정할 근거가 없는 실행에서 걸리면 그건 검사가 아니라 방해이고, 다음 사람이 훅을 지운다. 네 모드를 다 확인하고, **가드가 검사를 죽이지 않았는지**도 확인했다(기대 수를 하나 줄이면 여전히 빨간불).

세 회차 연속으로 CI가 나를 잡았다. 세 번 다 **로컬에는 없고 CI에는 있는 조건**이었다 — 설치되지 않은 패키지, 갓 만든 데이터베이스, 그리고 내가 안 돌려본 실행 모드.

대조 3/3(rag) + 2/2(modelmate) 빨간불.

## 설명이 실패하면 클라이언트가 파일 경로를 받았다 — 그리고 그 줄은 한 번도 돈 적이 없었다

세 회차 연속으로 CI가 나를 잡았고 세 번 다 **로컬에는 없고 CI에는 있는 조건**이었다. 그래서 그 방향을 끝까지 밀었다 — 개발 기계에만 있는 것이 또 무엇을 가리고 있는가.

### 먼저 빈손이 셋

**DB 상태.** modelmate의 스위트는 `modelmate.db`에 기대는데, 그 파일에는 여러 회차가 쌓여 있었다(사용자 541, 프로젝트 222). CI는 매번 갓 만든다. 앞 회차에 소유권 검사 넷이 그 차이 때문에 CI에서 안 돌았으니, 나머지도 그럴 수 있었다. **갓 만든 DB로 955, 그 DB가 채워진 뒤 다시 955.** 차이 없음.

**실행 순서.** 이 저장소는 순서 의존 검사를 이미 두 번 겪었다. 파일 순서를 뒤집어 돌렸다 — **962 전부 통과.**

**형제들의 잔여물.** 스위트가 작업 트리에 무엇을 남기는지 넷을 훑었다. 코퍼스와 내려받은 PDF 픽스처뿐이고 **둘 다 gitignore돼 있고 문서에 적혀 있다.**

`document-intelligence`의 픽스처 conftest는 *"`-rs`로 도는 CI는 무엇이 왜 skip됐는지 이름으로 말한다"*고 주장한다. **주장이니 확인했다** — 다섯 저장소 CI가 전부 `pytest -q -rs`로 돈다. 참이었다.

**세어보고 아무것도 없었다는 것도 결과다.** 다만 그 넷째 훑기에서 하나가 나왔다.

### `raise HTTPException(500, str(e))`

CI가 한 번도 발동시키지 않는 거부를 종류별로 훑다가 `500` 여섯을 봤다. 다섯은 관례대로 구조화된 detail을 내보낸다. 하나가 이랬다.

```python
except Exception as e:
    raise HTTPException(500, str(e))       # 030_shap.part, run_shap
```

재현했다. 클라이언트가 받는 것:

```
cannot load artifact: /home/jovyan/work/modelmate/models/u-42/best.pkl (uid=1000)
```

**절대 경로와 uid가 그대로 나간다.** 이 저장소는 [문서가 두 번 적어둔 보안 항목](#문서가-두-번-적어둔-보안-항목이-두-달간-그대로-있었다)를 한 번 겪었고 그 자리를 지키는 검사가 있는데, **예외 문자열로 나가는 길은 아무도 안 보고 있었다.**

이 줄이 왜 살아남았는지가 요점이다. **안 도는 줄이었다** — 안쪽 되돌림(계수 기반 중요도)까지 실패해야 여기 닿는다. 커버리지에도, 거부 감사에도, 라우트 감사에도 안 걸렸다. 재현하는 데도 세 번 걸렸다: 첫 시도는 없는 `feature_importances_`에서 먼저 죽었고, 둘째는 안쪽 되돌림이 성공해 버렸다.

**숨기지 않고 옮겼다.** 예외 종류(`error_type=RuntimeError`)는 화면에 띄워도 되는 만큼이라 남기고, 진짜 메시지는 보안 기록으로 보낸다. *가리는 것과 입을 다무는 것은 다르다* — 종류마저 없으면 사용자도 지원하는 쪽도 할 일이 없다.

### 세다가 내 수를 틀렸다

검사 독스트링에 **"스물넷이 `failure_detail`"**이라고 적었다. **여덟이다.** 인라인 dict와 헬퍼가 만든 것까지 한 덩어리로 셌다. 형태별로 다시 셌다.

```
문자열 리터럴          70   지어 쓴 문장이다. 런타임 값이 안 섞인다
인라인 dict            17
failure_detail(...)     8
변수에 담긴 구조체       7
예외 문자열 그대로       1   ← 여기
```

**세어보지 않고 적으면 이렇게 된다.** 이 목록이 잡아온 결함의 절반이 그 모양이고, 이번엔 내 검사의 설명문이 그랬다.

### 대조 둘이 헛돌았고, 하나는 내 단언이 공허하다는 뜻이었다

`raise`를 되심는 대조가 초록불이었다. **심은 것이 내가 뜻한 모양이 아니었다** — 조건식으로 감싸는 바람에 AST가 `Raise(Call)`이 아니라 `Raise(IfExp)`가 됐다. 평범한 `raise`로 다시 심으니 걸렸다.

둘째는 달랐다. `recommended_next_action=None`으로 바꿔도 초록불이었는데, **`failure_detail`이 기본 문구를 채워 넣기 때문**이다. 즉 `assert detail["recommended_next_action"]`은 **어떤 경우에도 참인 단언**이었다. 이 라우트의 안내는 그 기본값과 달라야 한다 — 설명이 실패했는데 *"입력값을 확인하라"*는 것은 할 수 없는 일을 시키는 것이다. 문구를 그대로 맞추게 좁혔다.

**대조가 코드가 아니라 단언을 겨눌 때도 있다.**

### 그리고 안 돌고 있던 측정 하나

역순 실행을 걸어두고 "실행 중"을 계속 봤는데, **스크립트 파일이 아예 없었다.** `pgrep`이 엉뚱한 프로세스를 잡고 있었다. 파일이 있는지부터 확인하고 다시 걸었다 — 962 통과.

*무언가가 돌고 있다고 믿는 것과 도는 것을 본 것은 다르다.* 이 목록에 같은 문장이 여러 번 나온다.

## 막혀서 남긴 게 아니라 도구가 없어서 남긴 것이었다 — 죽은 라우트 0개

앞 회차의 발견(예외 문자열이 클라이언트로 나간다)을 형제 저장소로 옮겨 보는 것으로 시작했다. **옮겨가지 않았다** — 그리고 그게 이번 회차의 절반이다.

### 모양이 옮겨가지 않는 것도 결과다

형제 넷에서 `raise` 메시지에 예외 문자열이나 경로가 실리는 자리를 훑었다.

```
agent-safety-core       9개   BindingError(str(error)) · CheckpointError(str(error)) …
rag-profile-selector    1개   SelectorError(f'{selector.name}: {error}')
mcp-gateway             0개
document-intelligence   0개
```

열이 나왔는데 **하나도 결함이 아니다.** 이것들은 **라이브러리**이고, 자기 호출자에게 예외 문자열을 주는 것은 같은 프로세스 안의 신뢰된 코드에 주는 것이다. modelmate의 경우가 달랐던 이유는 그것이 **HTTP 응답**이었다는 것 — 신뢰 경계를 넘는다.

경계를 넘는 형제는 `mcp-gateway` 하나다(게이트웨이는 클라이언트에게 거부 이유를 말한다). 거기는 `test_denial_disclosure.py`가 **지금 모양을 고정**하고 있고, 정찰 위험은 승인된 위협 집합 밖이라 바꾸지 않는다는 **결정이 문서로 적혀 있다.**

**"같은 결함이 다른 데도 있는가"의 답이 "아니오"일 때, 그 아니오에 이유가 붙어야 한다.** 여기서는 신뢰 경계다.

### 그리고 남겨둔 둘을 닫았다

라우트 감사가 아홉을 남겼고 일곱은 앞 회차에 닫았다. 둘은 *"상태를 갖춰야 도달한다"*고 적고 넘어갔었다.

```
predict_single         본문 31줄 · 0 → 28
retry_agent_step_api   본문  7줄 · 0 → 7
라우트 100개 중 죽은 것  9 → 0
```

**막혀서 남긴 것이 아니라 도구가 없어서 남긴 것이었다.** `STATE`를 되돌리는 픽스처는 앞 회차 설명-실패 검사에서 만들었고, `ensure_agent_trace_schema`는 그 앞 회차에 소유권 검사를 CI에서 돌게 만들면서 알았다. 로드맵에 "상태가 필요하다"고 적었을 때 그것은 **못 한다는 뜻이 아니라 아직 안 해봤다는 뜻**이었고, 두 회차 뒤에 도구가 손에 있었다.

`0`은 한 번 속은 적이 있는 숫자다 — 데코레이터 줄을 세는 바람에 "죽은 라우트 0개"가 나왔었다. 그래서 **세는 쪽을 대조했다**: 살아 있는 라우트 하나의 본문을 전부 '안 돎'으로 심으니 그것을 집어낸다. 개별 전환(0→28, 0→7)도 함께 봤다.

### 결정하지 않은 것을 검사로 적어두기

`predict_single`은 **사용자가 보내지 않은 값을 조용히 채운다** — 범주형은 `0`, 수치형은 학습 데이터의 평균. 읽을 수 없는 값도 마찬가지다. 그러면 응답은 *사용자가 보내지 않은 값으로 계산된 예측*인데 어디에도 그렇게 적혀 있지 않다.

거절할지, 채우되 알릴지는 제품 결정이고 사람 몫이다. **그래서 지금 무엇을 하는지만 고정했다** — 채운 사실이 응답에 없다는 것까지 단언으로. 알리기로 하는 날 그 검사가 빨간불이 되고, 그때 이 문장을 다시 읽게 된다.

*결정을 미루는 것과 잊는 것은 다르다. 검사가 그 차이를 만든다.*

### 흉내가 느슨하면 흉내를 시험한다

가짜 인코더의 `classes_`를 파이썬 리스트로 뒀더니 라우트의 `classes_.tolist()`에서 죽었다. sklearn은 배열을 준다. 배열로 고쳤다 — **흉내가 제품과 다르면 그 검사는 제품이 아니라 흉내를 시험한다.**

대조 6/6 빨간불.

## 남의 예측 API 토큰을 취소할 수 있는지 아무도 확인한 적이 없었다

CI가 한 번도 발동시키지 않는 거부 **53개** 중 `404`가 열다섯이다. 여섯을 골랐다 — 관리자 전용 조회 넷과 **예측 API 토큰의 취소·재발급 둘.**

뒤의 둘이 이 항목의 이유다. 예측 API 토큰은 **배포된 모델을 부르는 열쇠**이고, 취소 질의는 이렇게 좁혀져 있다.

```sql
SELECT * FROM prediction_api_tokens
WHERE token_id=? AND project_id=? AND owner_user_id=?
```

**그 조건이 지워져도 지금까지는 아무 검사도 빨간불이 되지 않았다.**

### 첫 검사는 그 줄을 지나지도 않았다

"남이 취소하려 하면 404"를 쓰고 통과시켰다. 대조에서 토큰 줄의 `404`를 `403`으로 바꿔봤는데 **초록불이었다** — `assert_project_owner`가 **먼저** 끊기 때문이다. 남은 그 프로젝트의 주인이 아니니 프로젝트 관문에서 404가 나오고, 토큰 질의는 실행조차 되지 않는다.

**단언은 맞았고 이름이 말하는 자리를 지나지 않았다.** 이 회차에 두 번째로 만난 모양이다 — 앞 회차의 `in (404, 409)`도 늘 404만 받고 있었다.

고쳐서 남이 **자기 프로젝트를 가지고** 남의 토큰 id를 부르게 했다. 그러면 프로젝트 관문은 통과하고(자기 프로젝트니까), **막는 것은 토큰 질의뿐이다.** 이제 그 줄을 `403`으로 바꾸면 빨간불이 되고, `owner_user_id` 조건만 무력화해도 빨간불이 된다. 재발급 쪽도 같은 문제였고 같게 고쳤다.

*방어가 여러 겹일 때, 바깥 겹이 막아주면 안쪽 겹은 시험되지 않는다. 그리고 검사 이름은 안쪽 겹을 가리키고 있다.*

### 상태 값을 베껴 적었다가 400을 받았다

관리자 상태 변경 검사에 `"triaged"`라고 썼다. **이 제품의 피드백 상태가 아니다** (`new` · `reviewed` · `planned` · `resolved` · `dismissed`). 400이 왔고, 그제야 코드를 봤다.

코드가 쓰는 집합에서 꺼내 쓰게 고쳤다. **검사가 상수를 손으로 베끼면 제품이 바뀔 때 검사만 낡고, 그 상태로 초록불이 된다** — 이 목록이 열두 번쯤 적어온 문장이고, 이번엔 내가 검사를 쓰면서 저질렀다.

### 400과 404를 가른다

없는 문의와 지원하지 않는 상태는 **관리자가 할 일이 다르다** — 하나는 id를 다시 찾고, 하나는 값을 고친다. 가르는 검사를 넣었더니 순서도 드러났다: **상태 검증이 조회보다 앞**이라, 없는 id에 잘못된 상태를 보내면 404가 아니라 400이 온다.

대조 7/7 빨간불.

## 도달하는데 아무도 확인하지 않는 거부 — 그리고 그것을 재는 도구가 여섯을 못 보고 있었다

두 회차 연속으로 같은 것에 걸렸다 — **검사가 지나가는 줄과 검사가 확인하는 줄이 다르다.** 두 번 손으로 찾았으니 세 번째는 재기로 했다.

### 세 가지 상태, 하나의 초록불

```
한 번도 도달하지 않는다      커버리지가 말해준다
도달하고 확인된다            바꾸면 검사가 빨간불이 된다
도달하는데 확인되지 않는다     ← 커버리지는 "돌았다"고 하고, 검사는 통과한다
```

가운데와 마지막이 화면에서 같아 보인다. **실행된 것과 그 결과를 누가 단언하는 것은 다르다.**

도구를 만들었다. 두 단계다.

1. 스위트를 한 번 돌리며 `HTTPException.__init__`을 감싸 **(검사 nodeid, 조각:줄)**을 적는다
2. 지점마다 상태를 `599`로 바꾸고 **그 지점을 지나는 검사만** 돌린다

2번이 이 도구를 쓸 만하게 만든다. 지점이 백 개여도 **지점당 검사는 보통 한둘**이라, 전체 스위트를 백 번 돌리는 대신 필요한 것만 돌린다. 전체가 십몇 분이다.

### 여섯이 나왔다

**넷은 부정 단언이 지나가는 자리였다.**

```python
assert raised.status_code != 410      # 무엇이 왔는지 묻지 않는다
```

그중 **셋은 내가 앞 회차에 "되돌림 방향" 대조로 쓴 것이다.** 되돌림 방향을 쓰겠다는 판단은 맞았고, 그것을 `!=`로 쓴 것이 틀렸다. 켜져 있는 모델은 410을 지나 **디스크의 `.pkl`을 찾으러 가고** 거기서 404가 난다 — 그것이 그 경로의 정답인데, `!= 410`은 404도 599도 통과시킨다.

**둘은 `pytest.raises(Exception)`이었다.** 예외판 `!=`다. 하나는 이름이 `test_enforce_limit_raises_when_over`인데 **무엇을 raise하는지는 확인한 적이 없었다** — `TypeError`도, 오타로 생긴 `NameError`도 통과한다.

여섯 중 셋은 뒤에서 `caught.value.status_code`를 확인하고 있었다. **넓은 타입 자체가 결함은 아니다** — 묻지 않는 것이 결함이다. 확인하지 않는 셋만 좁혔다.

### 그리고 그 도구가 여섯을 못 보고 있었다

첫 판은 `raise HTTPException(...)`만 셌다. **102개.** 그런데 `usage_limit_error`는 HTTPException을 **돌려주는** 헬퍼라 호출자들은 이렇게 쓴다.

```python
raise usage_limit_error(limit_key, current, limit, plan, message)
```

**그 여섯이 통째로 안 보였다.** 사용량 한도 `429`는 요금제를 지키는 자리다 — 분석 횟수, 데이터셋 업로드, 예측 API 호출, 리포트 내보내기.

추적 쪽에도 같은 문제가 있었다. 스택에서 **가장 안쪽 `main_parts` 프레임 하나만** 적었더니 여섯 호출자가 전부 헬퍼의 한 줄로 뭉쳤다. 안쪽부터 몇 개를 적고 훑는 쪽이 아는 `raise` 자리를 고르게 고쳤다.

공장을 세게 고치니 **108개**가 됐고, 다시 재니 **그 여섯 중 둘이 확인되지 않고 있었다.**

### 못 잰 것을 0으로 세지 않는다

도구가 상태를 심지 못하는 자리(상태가 리터럴이 아닌 곳)는 **"재지 못한 지점"으로 따로 찍는다.** 조용히 빼면 이 도구도 초록불이 된다 — 그리고 그 한 줄이 위의 공장을 찾아준 단서였다.

`--from`으로 저장한 추적을 재사용하는 길도 열어뒀는데, **낡을 수 있다.** 넷을 고친 직후 옛 추적으로 돌려보니 하나가 여전히 "확인 안 됨"으로 나왔다 — 고친 검사가 그 지도에 없어서 옛 검사만 돌린 것이다. 문서에 적었다: *지도가 낡으면 이 도구는 옛 상태를 보고한다.*

### 관문이 아니다

`measure_part_coverage.py`와 같은 자리에 둔다 — **어디를 봐야 하는지 알려주는 물건이지 매 푸시마다 도는 물건이 아니다.** 그 구분을 적어두지 않으면 다음 사람이 CI에 넣는다.

고친 여섯 자리를 다시 599로 바꿔 전부 빨간불이 되는 것을 확인했다.

## `match=`는 무엇을 말하는지 고정하지 무엇인지는 고정하지 않는다

앞 회차에 `modelmate`에서 **"도달하는데 아무도 확인하지 않는 거부"**를 재는 도구를 만들어 여섯을 찾았다. 규칙을 세웠으니 나머지를 센다 — 형제 저장소 넷.

### 라이브러리에는 다른 도구가 필요했다

`modelmate` 쪽 도구는 `HTTPException.__init__`을 가로챈다. 형제들은 **라이브러리**라 도메인 예외(`PayloadError`·`LedgerError`·`CorpusError`…)를 던지므로 가로챌 한 지점이 없다.

대신 **coverage의 동적 컨텍스트**를 썼다. `dynamic_context = test_function`이면 줄마다 *"어느 검사가 이 줄을 실행했나"*가 남는다. 그러면 지점마다 **그 줄을 지나는 검사만** 돌리면 된다.

변이도 달라야 했다. `pass`로 바꾸면 함수가 계속 흘러 엉뚱한 데서 죽고, **그 실패를 "확인됨"으로 읽게 된다.** 그래서 기본 클래스로 바꾼다.

```
raise PayloadError(...)  →  raise Exception(...)

pytest.raises(PayloadError)  →  안 잡힌다 → 빨간불 → 확인됨
pytest.raises(Exception)     →  잡힌다   → 초록불 → 확인 안 됨
```

### 하나가 나왔다

```
저장소                    raise  도달  확인 안 됨
agent-safety-core          86    85      1   ← core/payload.py:242
document-intelligence      79    77      0
mcp-gateway                56    51      0
rag-profile-selector       54    51      0
```

그 하나는 `retention_status`의 **"보존 일정이 없다"** 거부다. 지나가는 검사는 이렇게 쓰여 있었다.

```python
with pytest.raises(Exception, match="no retention schedule"):
    store.retention_status()
```

**`match=`가 정확해 보이게 만든다.** 문구는 고정되지만 **타입은 안 고정된다.** 이 패키지는 `PayloadError`를 내보내고 부르는 쪽은 그것으로 잡는다 — 타입이 맨 `Exception`이 되면 `except PayloadError`가 놓치고, 그때 조용히 지나가는 것은 *"보존 대상 없음"*이다. 그 검사의 독스트링이 걱정하던 바로 그것이다.

같은 파일의 셋을 전부 `PayloadError`로 좁혔다.

### 관문이 무엇을 사는지가 여기서 보인다

형제 넷은 **분기 커버리지 100% 관문**을 갖고 있다. 그래서 거부가 거의 전부 도달하고(85/86, 77/79, 51/56, 51/54) 거의 전부 확인된다. 같은 도구를 `modelmate`(관문 없음)에 돌렸을 때는 **108개 중 49개만 도달했고 그중 여섯이 확인되지 않았다.**

*관문은 "돌았는가"만 답한다. 그런데 돌게 만들어 두면 "무엇이 왔는지"를 묻는 검사도 같이 자란다.* 두 수를 나란히 두고 보니 그게 보인다.

### 도구를 만들며 두 번 걸렸다

**하나.** `branch = True`면 줄이 `line_bits`가 아니라 `arc`에 남는다. `line_bits`만 읽었더니 **"줄 0개에 검사가 붙었다"**가 나왔다 — 빈손인데 조용히 0을 보고한 것이다. 두 표를 다 읽게 하고, **그래도 0이면 죽게** 했다.

**둘.** 원래가 이미 `Exception`인 자리는 구별할 수 없다. `raise NotImplementedError`(괄호 없음)도 심을 수 없다 — 그건 거부가 아니라 **추상 메서드 표시**다. 둘 다 `0`으로 세지 않고 **"재지 못한 지점"으로 따로 찍는다.** 조용히 빼면 이 도구도 초록불이 된다.

대조: `payload.py:242`를 `Exception`으로 바꾸면 이제 빨간불이 된다.

## 예상되는 상태를 "예상하지 못한 내부 오류"로 보고하고 있었다

앞 회차에 형제 넷을 재고 나니 `modelmate`의 수가 남았다 — **거부 108개 중 도달하는 것 49개.** 형제들은 95%가 도달하는데 여기는 45%다. 도달하지 않는 쉰아홉 중 **에이전트 라우트 여섯의 `404`**를 골랐다. 같은 모양이 여섯 번 반복되니 한 번에 닫힐 줄 알았다.

**둘 다 예상과 달랐고, 그게 이 항목이다.**

### 넷은 돌릴 수 없다

```python
assert_analysis_run_owner(user, analysis_run_id)   ← 없거나 남의 것이면 여기서 404
... 조회 ...
if not result:
    raise HTTPException(404, "...")                ← 돌리려던 줄
```

행은 있고 계획도 단계도 없는 실행을 만들어 넷을 불러봤다. **전부 dict를 돌려준다** — `agent_run: None`, `steps: []`을 담은 채로. 조회 함수가 **소유자의 실재하는 행에 대해서는 빈 값을 주지 않기** 때문이다.

그럼 그 줄은 언제 도는가. **관문을 지난 뒤 조회 사이에 행이 사라졌을 때** — 경쟁 구간 방어선이다. 정상 입력으로는 못 만든다.

억지로 돌리는 검사를 지어내지 않았다. [`delete_deployed`의 403](#ci가-안-도는-라우트-마흔이-아홉이었다--그리고-그중-둘이-데이터를-지우는-라우트였다)에서 배운 것이다 — 죽은 코드라 부르려다 대조가 뒤집었다. **도달하지 않는 것과 쓸모없는 것은 다르다.** 그 사실을 적고 이름으로 뒀다: 조회가 빈 값을 주도록 바뀌면 그때 이 목록이 걸린다.

### `execute`는 500을 내고 있었다

다섯째는 달랐다.

```
execute_agent_run → ValueError("Agent Run or Plan was not found.")
                  → 전역 처리기
                  → 500 "예상하지 못한 내부 오류 — 잠시 후 다시 시도하거나 관리자에게"
```

**완전히 예상되는 상태다.** 실행을 만들어 놓고 계획을 만들기 전에 실행 버튼을 누른 것 — 제품에서 매일 일어난다.

새어 나가는 정보는 없었다. 전역 처리기가 내부 문구를 가리고 `error_id`만 준다. 그래서 [앞의 `str(e)` 결함](#설명이-실패하면-클라이언트가-파일-경로를-받았다--그리고-그-줄은-한-번도-돈-적이-없었다)과는 다르다. **틀린 것은 안내다.**

*"잠시 후 다시 시도하거나 관리자에게 문의하세요."* — 기다린다고 계획이 생기지 않고, 관리자도 할 일이 없다. 사용자가 할 일은 **목표를 해석해 계획을 만드는 것**이고, 그건 화면에 있는 버튼이다.

`409`로 바꿨다. `404`가 아닌 이유: **실행은 있다. 없는 것은 그 안의 계획이다.** 이 저장소는 "지금 하면 안 된다"를 `409`로 쓴다. 남에게는 여전히 `404`가 간다 — `409`는 *"그 실행은 있다"*를 알려준다.

### 그리고 앞서 박아둔 못이 내 변경을 잡았다

전체 스위트에서 하나가 빨간불이었다. 두 회차 전에 박아둔 것이다.

```
`failure_detail` 호출 수가 바뀌었다. 늘어난 것은 좋고, 줄었다면 무엇이 그 자리를
대신하는지 보고 이 수를 고쳐라.
```

`409` 하나를 더하면서 호출이 12에서 13이 됐다. **하한선이었다면 조용했을 것이다.** 정확한 수라서 걸렸고, 메시지가 시킨 대로 고쳤다.

*못을 박을 때는 그것이 나를 부를 날을 생각해서 문구를 쓴다.*

대조 5/5 빨간불.

## 이미 사용자를 향해 쓰인 문장이 배관에서 버려지고 있었다

앞 회차에 맨 `ValueError`가 500이 되는 것을 **한 자리** 고쳤다. 그때 알아챘어야 할 것이 있었다 — **내가 여러 회차 세어온 "거부 108개" 목록에 그런 것은 아예 없다.** `raise HTTPException`만 셌기 때문이다.

*내가 재는 것의 경계가 곧 내가 보는 것의 경계다.* 다시 셌다.

```
backend 전체의 비-HTTPException raise    7개
  resolve_jwt_secret          부팅 거부. 요청 경로가 아니다
  create_goal_first_agent_run 진짜 내부 실패. 500이 맞다
  _fallback_from_state ×2     같은 모듈 안에서 잡힌다
  execute_agent_run           앞 회차에 409로 고쳤다
  make_cv_for_target ×2       ← 여기
```

### 문장은 이미 쓰여 있었다

```python
raise ValueError("학습할 데이터가 너무 적습니다.")
raise ValueError("일부 정답 값의 데이터가 1개뿐이라 교차검증을 할 수 없습니다.")
```

**누군가 사용자를 위해 쓴 문장이다.** 한국어로, 무엇이 잘못됐는지 정확히.

그런데 세 호출 자리(`run_cv` 둘, `run_agent` 하나) 어디도 안 잡는다. 전역 처리기까지 올라가고 사용자가 받는 것은 이것이다.

```
500  예상하지 못한 내부 오류가 발생했습니다.
     잠시 후 다시 시도하거나 관리자에게 문의하세요.
```

**정답 클래스 하나에 행이 하나뿐인 CSV는 흔하다.** 재현해서 확인했다. 기다린다고 행이 늘지 않고, 관리자도 할 일이 없다. 고칠 수 있는 사람은 사용자뿐이고 **무엇을 고쳐야 하는지는 이미 코드에 적혀 있었다.**

*문장은 있었고 배관이 그것을 버리고 있었다.*

### 한 곳에서 바꾼다, 그리고 그대로 싣지 않는다

세 자리에 각각 `try/except`를 쓰면 같은 사실을 세 번 적는 꼴이다 — 이 목록이 계속 찾아온 결함. 도우미 하나가 변환하고, 세 자리가 그것을 부른다. **부르지 않는 자리가 생기면 검사가 건다.**

`str(error)`를 그대로 싣지도 않는다. 지금은 저 둘뿐이지만 그 함수 안에서 sklearn이 던지는 것이 늘면 그 문구가 그대로 나가고, 그건 [`run_shap`에서 겪은 결함](#설명이-실패하면-클라이언트가-파일-경로를-받았다--그리고-그-줄은-한-번도-돈-적이-없었다)이다. **아는 문구만 통과시키고 나머지는 일반 문장으로 바꾼다** — 모르는 `ValueError`를 심어 그것도 확인했다.

### 나머지 넷은 고치지 않았다

`create_goal_first_agent_run`의 `RuntimeError`는 INSERT가 성공한 뒤 읽기가 실패한 경우다. **진짜 내부 실패**이고 500이 맞다 — 사용자가 할 일이 없다. `resolve_jwt_secret`은 부팅을 거부하는 자리라 요청 경로가 아니다.

**"고치지 않는다"에도 이유가 붙어야 한다.** 검사로 적어 뒀다.

### 그리고 정확한 수로 박아둔 못을 하한선으로 되돌렸다

`failure_detail` 호출 수를 `== 12`로 박아뒀었다. **두 회차 연속으로 좋은 변경에 빨간불이 켜졌다** — 409 하나를 더할 때, 400 하나를 더할 때. 고칠 것은 매번 숫자 하나였다.

이 목록은 하한선을 여러 번 비판했다: *하한선은 하나가 빠지는 것을 못 본다.* 그 근거를 여기 대보면 성립하지 않는다 — **그 실패는 바로 위 검사가 잡는다.** `failure_detail`이 원시 문자열로 바뀌면 "예외 문자열을 그대로 싣는 곳" 검사가 걸린다. 정확한 수가 추가로 지키는 것은 없었고, 막는 것은 관례를 넓히는 변경뿐이었다.

**다른 검사가 그 실패를 이미 덮고 있으면 정확한 수는 값이 아니라 마찰이다.**

바꾼 뒤 관례를 **전부 걷어내는** 대조를 돌려 하한선이 여전히 무는 것을 확인했다. 규칙을 느슨하게 할 때야말로 대조가 필요하다.

대조 6/6 빨간불.

## 마지막 한 겹을 시험하는 것이 하나도 없었다

여러 회차에 걸쳐 거부를 세고 또 셌다. `raise HTTPException` **108개**, 비-HTTP raise **7개**. 앞 회차에 *"내가 재는 것의 경계가 곧 내가 보는 것의 경계다"*라고 적었는데, 그 문장이 한 번 더 맞았다.

**그 밑에 한 겹이 더 있었다.**

```
사용자   500 "예상하지 못한 내부 오류가 발생했습니다."
         error_id + "관리자에게 error ID를 전달하세요"
운영자   monitoring_events에 route · method · status · 진짜 메시지
```

미들웨어가 **아무도 안 잡은 예외를 전부 받아** 응답을 만들고 기록을 남긴다. 사용자가 "무언가 잘못됐다"를 보는 마지막 자리이고, 운영자가 그것을 되짚는 유일한 자리다.

### 그 겹을 시험하는 것이 없었다

검사 1,021개는 **전부 핸들러를 직접 부른다.** `TestClient`를 쓰는 파일이 **0개**였다. 미들웨어는 요청이 앱을 통과할 때만 도는데, 통과하는 검사가 하나도 없었다.

기록도 그 사실을 말하고 있었다. `monitoring_events`에서 `error_code='internal_error'`인 행이 **0건**이다. 앱이 자기 500을 적는 자리인데 한 번도 안 적혔다 — 500이 없어서가 아니라 **500을 내는 검사가 없어서**다.

### 응답이 시키는 대로 해봤다

*"관리자에게 error ID를 전달하세요."* 그러면 관리자는 그 id로 무엇을 볼 수 있는가.

일부러 터뜨리고, 사용자가 받은 `error_id`를 그대로 관리자 조회에 넣어봤다. **이어져 있었다** — route, method, status, 그리고 사용자에게는 안 준 진짜 메시지까지.

**설계는 멀쩡했고 확인만 없었다.** *빈손도 결과다. 다만 그 빈손을 확인한 뒤에야 그렇게 말할 수 있다.*

이제 열 개가 그 겹을 지킨다. 사용자는 내부 문장을 못 받고([`run_shap`에서 겪은 것](#설명이-실패하면-클라이언트가-파일-경로를-받았다--그리고-그-줄은-한-번도-돈-적이-없었다)), **운영자는 받고**(가린 것과 잃은 것은 다르다), `error_id`는 헤더에도 있고, **기록이 실제로 한 줄 늘어나는지**까지 센다 — 응답에 `error_id`가 박혀 있어도 저장은 실패했을 수 있고, 그 둘은 화면에서 같아 보인다.

### 보안 검사를 쓰다 하마터면 아무것도 시험하지 않을 뻔했다

`../../etc/passwd`를 샘플 다운로드 경로에 보내고 **200을 받았다.** 잠깐 취약점인 줄 알았다.

아니었다. **클라이언트가 URL을 정규화해서** `/etc/passwd/download`로 보냈고, SPA 캐치올이 index.html을 200으로 준 것이다. **요청이 그 라우트에 닿지도 않았다.**

그대로 뒀으면 "경로 탈출을 막는다"는 검사가 생기는데, 그 검사는 **통과하면서 아무것도 시험하지 않는다.** 이 목록에 여러 번 나온 문장이 또 맞았다 — *심은 것이 실제로 들어갔는지부터 봐야 한다.* 여기서는 서버에 도착하는지부터다.

인코딩한 `%2e%2e%2f`는 정규화를 지나 그 라우트까지 가서 404가 되는 것도 확인했다. 막고 있다.

대조 5/5 빨간불.

## 인증 없이 실험 기록 43건을 읽고, 지울 수 있었다 — 그 갈래에 검사가 있었는데도

앞 회차에 이 저장소의 검사가 **전부 핸들러를 직접 부른다**는 것을 알았다(`TestClient` 쓰는 파일 0개). 그러면 `user=Depends(get_current_user)`가 **한 번도 안 돈다** — 검사가 `user=OWNER`를 직접 넣기 때문이다. **그게 무엇을 가리는지 재봤다.**

### 의존을 선언하는 것은 막는 것이 아니다

```python
def get_current_user(credentials=Depends(security), x_modelmate_guest_session=Header(None)):
    if not credentials:
        ...
        return None          # ← 막지 않는다
```

자격이 없으면 `None`을 돌려준다. **선언과 확인은 다른 일**이고, 핸들러가 스스로 `require_current_user`나 소유권 관문을 불러야 한다. 검사는 둘 다 안 본다 — 사용자를 직접 넣으니까.

### 앱을 통과시켜 쟀다

매개변수 없는 `GET` 28개에 인증 없이 요청했다.

```
401/403 거절     11개
400(상태 없음)    6개
200 응답         11개
```

열하나 중 열은 괜찮았다. `/api/health`, 준비 상태, 그리고 **빈 목록** — `/api/projects`와 `/api/deployed`는 응답이 **2바이트**, 즉 `[]`다.

**`/api/history`가 45,506바이트를 줬다.**

43건. 타깃 컬럼명(`"Machine failure"`), 데이터 크기(`[10000, 11]`), 모델 이름과 점수. `experiment_history.json` — **인증 이전 시절의 공용 파일**이고 익명 분석이 전부 한곳에 쌓인다.

`DELETE`도 같은 갈래를 지난다. 인증 없이 부르니 **그 파일을 지웠다.** `ok: True`와 함께.

### 그 갈래에 검사가 있었고, 통과하고 있었다

이게 이 항목의 요점이다. `test_an_anonymous_caller_sees_nothing_of_either`가 있었다. DB에 무작위 표식을 심고 익명 응답에 그것이 없는지를 봤다.

**그런데 익명 갈래는 DB를 아예 안 읽는다.** 파일을 돌려주니 DB 표식이 들어갈 리가 없고, 단언은 **어떤 경우에도 참**이었다. 파일에 무엇이 들어 있든 통과한다.

**"아무도 안 봤다"가 아니라 "본 것이 다른 것이었다."** 처음 쓴 이 항목의 초고에는 "익명 갈래만 아무도 안 봤다"고 적었다가, 그 검사가 빨간불이 되면서 알았다.

*지나가는 것과 확인하는 것은 다르다* — 이 목록이 여러 번 적은 문장인데, 이번엔 **검사의 이름이 맞고 대상이 틀린** 경우였다.

### 고친 것과 안 고친 것

프런트는 언제나 토큰이나 `X-ModelMate-Guest-Session`을 보낸다(`frontend/src/api.js`). 이 갈래에 오는 것은 **이 제품의 클라이언트가 아니다.** `401`로 바꿨다 — 조용히 빈 목록을 주면 *"기록이 없다"*로 읽힌다. 게스트 세션으로는 여전히 자기 기록을 본다(되돌림 방향도 확인했다).

**공용 `STATE` 버킷은 안 고쳤다.** `_scope_state_to_the_caller`가 이렇게 적어두고 있다.

> Anything unauthenticated falls into the shared default, which is what the process did before scoping existed.

**결함이 아니라 적어둔 결정이다.** 그 결정이 무엇을 뜻하는지만 고정했다 — `STATE` 의존 여섯은 상태가 없으면 400, 있으면 200. 바꾸기로 하면 그 검사가 빨간불이 되고 그때 결정이 된다.

그리고 **공개 표면을 이름으로 뒀다.** 인증 없이 200을 주는 열 개가 각각 이유와 함께 적혀 있다. 새 라우트가 의존을 빼먹어도 검사는 전부 통과하니, 이 목록이 그 자리를 막는다.

### 내 검사도 한 번 걸렸다

공개 목록 검사가 **파일 단독으로는 통과하고 전체 스위트에서는 실패**했다. 앞선 검사가 `STATE`에 데이터셋을 남기면 그 여섯이 400에서 200으로 바뀌기 때문이다. 픽스처가 `STATE`를 비우고 시작하게 고쳤다 — 이 저장소가 이미 두 번 겪은 모양이다.

대조 4/4 빨간불.

## 모르는 사람 둘이 서로의 업로드를 본다 — 적어둔 결정의 값

앞 회차에 **읽기**만 쟀다 — 매개변수 없는 `GET` 28개. 쓰기는 안 봤고, `/api/history`의 `DELETE`가 걸린 것은 **우연이었다.** 그 우연이 마음에 걸려 이번엔 쓰기를 셌다.

```
쓰기 라우트 47개 · 매개변수 없는 것 30개

인증 없이 200/201   4개   로그아웃 · 프런트 오류 보고 · 세션 초기화 · 게스트 세션 시작
401/403 거절        4개
400/422            22개
```

넷은 전부 의도된 공개다. 넷은 막힌다. **문제는 스물둘이다.**

### 빈 몸통은 인증 답을 가린다

`400`과 `422`는 "막혔다"가 아니라 **"검증이 인증보다 먼저 걸렸다"**는 뜻이다. 빈 `{}`를 보냈으니 당연하다. 그 상태로 표를 읽으면 *"인증 없이 통과하는 것은 넷뿐"*이 되는데, 그건 스물둘에 대해 아무것도 모른다는 뜻이다.

유효한 입력으로 다시 두드렸다.

```
POST /api/upload   진짜 CSV 60행 → 200
```

**계정 없이 데이터셋이 올라간다.**

### 그런데 결함이 아니었다

재봤다. 익명 호출자도 요금제 `free`를 받고 **한도가 실제로 걸린다** — 10억 행을 넣으면 `429 max_rows_per_dataset`. `datasets` 표에는 행이 생기지 않는다(97 → 97). **자원 소모는 막혀 있다.**

*"익명이 업로드할 수 있다"만 적으면 무제한으로 읽힌다.* 무엇이 막혀 있고 무엇이 안 막혀 있는지 함께 적어야 다음 사람이 판단할 수 있다.

### 갈리지 않은 것은 상태다

```
A(익명)   POST /api/upload   환자기록.csv        → 200
B(익명)   GET  /api/columns  → 200  ['환자ID', '진단코드', '결과']
C(게스트)  GET  /api/columns  → 400  (자기 버킷은 비어 있다)
```

**모르는 사람 둘이 서로의 업로드를 본다.** B는 A의 컬럼 이름을 그대로 받는다.

이건 [앞 회차에 고정한 결정](#인증-없이-실험-기록-43건을-읽고-지울-수-있었다--그-갈래에-검사가-있었는데도)이다 — `_scope_state_to_the_caller`가 *"Anything unauthenticated falls into the shared default"*라고 적어뒀다. **결정은 적혀 있었고, 그것이 무슨 뜻인지는 안 적혀 있었다.**

고치지 않았다. "완전 익명 분석을 계속 허용할 것인가"는 제품 결정이고 로드맵에 사람 몫으로 있다. 여기서 한 일은 **그 결정을 눈에 보이게 만드는 것**이다.

대조로 확인했다: 익명도 갈리게 바꾸면 빨간불, 모두를 한 버킷에 넣어도 빨간불, 근거 문장을 지워도 빨간불. **바꾸기로 하면 그 검사가 말해준다.**

**막을 장치는 이미 있다.** 게스트 세션 하나면 갈리고, 프런트는 언제나 그것을 보낸다. 한 통에 들어가는 것은 세션 없는 맨 HTTP 호출자뿐이다.

### 가짜로 넣은 신원은 그것을 읽는 겹에만 보인다

로그인한 사용자도 갈리는지 보려고 `app.dependency_overrides`로 사용자를 넣었더니 **200**이 나왔다. 격리가 깨진 줄 알았다.

아니었다. **범위를 정하는 것은 미들웨어**이고, 그것은 `get_current_user`를 의존성 주입이 아니라 **직접 부른다.** 오버라이드는 라우트의 인자만 바꾸고 범위에는 아무 영향이 없었다. 헤더에 진짜 토큰을 실으니 **400** — 갈려 있다.

앞 회차에 *"검사가 사용자를 직접 넣으면 의존성이 안 돈다"*고 적었는데, 같은 함정의 다른 얼굴이다. **한 겹을 속이면 그 겹만 속는다.**

### 그리고 이 항목을 쓰자마자 검사가 빨개졌다

위 세 줄을 README에도 적었더니 `test_every_route_has_a_caller`가 `('GET', '/api/columns')`를 목록에서 빼라고 했다. **문서가 근거 중 하나**이므로, *"B가 `/api/columns`를 부른다"*고 **적는 행위**가 그 라우트를 "누가 부르는 라우트"로 만든다.

**열 번째 인용-사용 혼동이다.** 그래서 `<!-- surface-record -->` 울타리가 이미 있었고 — 새로 쓴 절이 그 밖에 있었다. 감쌌다.

**그리고 순서를 또 거꾸로 했다.** 스위트를 돌린 뒤에 README를 고치고 다시 안 돌렸다. *"문서를 읽는 검사가 있는 저장소에서는 문서 수정도 코드 수정이다"*라고 앞 회차에 적어놓고 같은 것을 반복했다. **적어둔 교훈과 지키는 것은 다른 일이다.**

*우연히 찾은 것이 하나 있으면, 우연이 아니게 다시 세어야 한다.*

## 같은 실수를 두 번 했다 — 기억을 고치는 대신 값을 낮췄다

이 목록은 대부분 코드의 결함을 적는다. 이번 것은 **내 작업 방식의 결함**이고, 두 번 반복해서야 고쳤다.

```
2026-08-23  document-intelligence  README를 224로 고치고 AGENTS.md는 안 고쳤다
2026-08-24  modelmate              README에 `/api/columns`를 적었더니 그 라우트가
                                   "누가 부르는 라우트"가 됐다 (울타리 밖)
```

둘 다 같은 순서였다. **스위트를 돌리고 → 문서를 고치고 → 다시 안 돌린 채 밀었다.** 둘 다 CI가 잡았다.

첫 번째 뒤에 이렇게 적었다.

> 문서를 읽는 검사가 있는 저장소에서는 **문서 수정도 코드 수정이다.**

그리고 두 번째를 했다. **적어둔 교훈과 지키는 것은 다른 일이다.**

### 왜 반복했는지

이유는 기억력이 아니라 **값**이다. modelmate 스위트는 9분이다. 문서 한 줄을 고치고 9분을 다시 기다리는 것은 매번 이기지 못하는 싸움이고, 그래서 매번 "이번엔 괜찮겠지"가 이긴다.

그래서 잰 것은 다른 수였다. **문서를 읽는 검사가 몇 개인가.**

```
modelmate               8개
document-intelligence   6개
agent-safety-core       5개
mcp-gateway             4개
rag-profile-selector    4개
                       27개 · 다 합쳐 87초
```

9분이 아니라 **87초**다. 그 27개만 돌리는 도구를 만들었다(`tools/check_docs_still_agree.py`).

### 목록을 두 번 적지 않는다

"문서를 읽는 검사"의 목록은 **이미 있다.** `check_doc_tests_bite.py`가 저장소의 실재하는 문서를 이름으로 읽는 검사를 세고 있고, 그 함수를 그대로 부른다.

새로 세면 두 목록이 생기고 **한쪽만 낡는다** — 이 포트폴리오가 가장 자주 잡은 결함이 그것이다. 도구를 만들면서 그 결함을 저지르면 앞뒤가 안 맞는다.

### 대조: 내가 실제로 저지른 둘을 되심었다

가상의 실패를 잡는 도구는 만들기 쉽고 값이 없다. **두 실수를 그대로 재현했다.**

```
AGENTS.md만 옛 수로 되돌림   → 빨간불 ✓ (25.1초)
울타리를 걷어냄               → 빨간불 ✓ (27.6초)
목록을 비움                   → 빨간불 ✓ (빈손을 통과로 세지 않는다)
```

**둘 다 25초 안에 잡힌다.** CI를 기다려 9분 뒤에 아는 것과 다르다.

### 그리고 CI에 넣었다 — 도구를 위해서

이 도구는 사람이 손으로 부르는 물건이라, **아무도 안 부르면 조용히 썩는다.** CI가 매번 한 번 돌려 그것이 아직 도는지 확인한다. 전체 스위트를 대신하지 않는다 — 그 구분을 적어두지 않으면 다음 사람이 이것으로 스위트를 대신한다.

색 코드를 걷어내는 줄도 하나 넣었다. 안 걷으면 요약이 잘려 **초록불인지 빨간불인지가 화면에서 안 보인다.** *읽을 수 없는 보고는 보고가 아니다.*

## 커버리지 숫자만 아무것도 안 지키고 있었다 — 두 달 치 작업이 문서에 없었다

이 포트폴리오는 셀 수 있는 것을 거의 다 검사로 묶어뒀다 — 테스트 개수, 항목 개수, 라우트 개수, 의존성 목록. 이번 회차에 물은 것은 **묶이지 않은 수가 남아 있는가**였다.

하나 있었다. **커버리지다.**

```
README가 말하던 것(2026-08-22)   다시 재본 것(2026-08-24)
pytest만            27.7%   →    48.4%
스모크              44.5%   →    53.1%
**합집합**          47.3%   →    **63.8%**
안 도는 함수        132개   →    **12개**
전체 문            4,256    →    4,323
```

**두 달 치 작업이 문서에 없었다.** 그 사이 테스트를 400개 넘게 더했는데 숫자는 그대로였다. 그리고 그 숫자는 README와 로드맵을 합쳐 **열여섯 군데**에 적혀 있었다.

### 세는 것과 지키는 것은 다르다

숫자가 틀린 게 결함이 아니다. 결함은 **아무도 안 물어봤다는 것**이다. 다른 모든 수는 틀리는 순간 빨간불이 켜지는데, 이 수만 아니었다. 그래서 조용히 두 달을 갔다 — *신선함과 참인 것은 다르다*를 여러 번 적어놓고, 정작 신선함을 확인하는 장치가 없는 자리를 못 봤다.

### 왜 검사 하나로는 안 되는가

측정은 `sys.settrace`로 스위트를 두 번 돌리는 일이라 몇십 분 걸린다. **매 푸시마다 할 수 없다.** 그래서 둘로 갈랐다.

```
빠른 검사(매 푸시)   문서가 기록에서 떠나는 것을 잡는다      0.3초
coverage.yml(주간)   기록이 현실에서 떠나는 것을 잡는다      수십 분
```

**둘 다 있어야 한다.** 빠른 검사만 두면 `docs/coverage-record.json`과 README가 **함께 틀린 채로 서로 맞을 수 있다** — 그건 이 목록이 이미 여러 번 본 모양이다(사본은 소스가 바뀌어도 그대로다). 그래서 기록에 **언제·무슨 명령으로** 쟀는지도 함께 요구하고, 그 명령이 실재하는지까지 본다. *이름만 남은 재현 절차는 절차가 아니다.*

### 늘 빨간불인 것은 아무도 안 본다

주간 재측정에 **허용치 1.5%p**를 뒀다. 러너가 다르면 선택적 의존이 있고 없고에 따라 한두 줄이 갈리고, 매주 빨간불인 워크플로는 결국 꺼진다. 다만 **문 수가 다르거나 허용치를 넘으면 반드시 실패한다** — 허용치는 눈감아 주는 자리가 아니라 잡음과 신호를 가르는 자리다.

### 대조: 실제로 일어난 표류를 되심었다

```
README를 옛 27.7%로 되돌림   → 빨간불 ✓     ← 실제로 일어났던 그것
기록만 옛 47.3%로            → 빨간불 ✓
표현을 바꿔 정규식이 못 찾게  → 빨간불 ✓     ← 빈손 통과 방지
주간 워크플로 삭제           → 빨간불 ✓
기록에서 명령 이름 제거       → 빨간불 ✓
```

셋째가 중요하다. 이 검사는 README를 **정규식으로** 읽으므로, 문장을 다듬는 것만으로 아무것도 확인하지 않는 상태가 될 수 있다. 그래서 비교보다 **먼저** 넷을 다 찾았는지 본다 — *빈 딕셔너리끼리의 비교는 언제나 통과한다.*

### 합집합을 앞세운다

기록은 세 수를 담지만 **CI가 실제로 지키는 것은 합집합**이다. 큰 수를 앞세우면 다음 사람이 pytest 단독 수치를 CI 값으로 읽는다. 그래서 합집합이 나머지 둘보다 작아지지 않는지도 검사가 본다.

## 같은 커밋에서 CI가 초록불이었다가 빨간불이 됐다 — 검사가 요청하지 않은 것을 보고 있었다

문서만 고친 커밋을 밀었더니 제품 스모크가 셋을 실패했다. **런타임 코드는 한 줄도 안 바뀐 커밋이었다.**

```
a job with no dataset reports can_rerun false   → can_rerun: true
and says why before anyone presses the button   → rerun_blocked_reason: None
and the endpoint refuses it for the same reason → 200 (409이어야 한다)
```

세 줄이 입을 모아 [옛 결함](#저장소에-있는데-아무도-돌려본-적-없는-것)이 돌아왔다고 말한다. 바로 앞 커밋에서는 같은 검사가 통과했다.

### 검사는 자기가 만들지 않은 작업을 보고 있었다

`POST /api/training/jobs`는 **언제나 새 작업을 만들지 않는다.** 그 프로젝트에 `created`/`queued`/`running` 작업이 하나라도 있으면 **그것을 돌려준다** — `duplicate_guard: True`와 함께.

스모크는 바로 앞에서 재실행을 두 번 걸어놨다. 그 작업이 끝나기 전에 "데이터셋 없는 작업"을 요청했고, 돌려받은 것은 **데이터셋이 붙은, 진행 중이던 남의 작업**이었다. 스모크는 그 작업이 `failed`가 될 때까지 기다린 다음 물었다 — 다시 실행할 수 있나? **있다.** 데이터셋이 있으니까.

*검사는 통과하거나 실패한 것이 아니라, 다른 것에 대해 답하고 있었다.*

### 로컬에서 잡히지 않았다 — 창이 0.12초였다

```
생성 응답 0.03s · queued
0.05s  running
0.12s  failed          ← 이 사이에 요청이 끼어야 한다
```

동시 요청으로 15번 두드려도 못 잡았다. 그래서 **시간 대신 상태로** 같은 조건을 만들었다 — 그 프로젝트에 `running` 작업을 DB에 하나 꽂고 스모크가 하는 그대로 요청했다.

```
돌려받은 job_id  : 꽂아둔 그 작업
duplicate_guard : True
can_rerun       : True   (기대값 False)
rerun_blocked_reason: None   (기대값 job_dataset_reference_missing)
rerun 응답       : 200    (기대값 409)
```

**세 증상이 하나도 안 틀리고 재현됐다.** 경합을 못 잡으면 경합이 없는 것이 아니다 — 재현할 다른 축을 찾으면 된다.

### 색이 바뀌는 답은 틀린 답보다 나쁘다

같은 커밋에서 초록불이었다가 빨간불이 되는 관문은, 사람에게 **읽지 말고 다시 돌리라고** 가르친다. 그리고 이번에 그 관문이 가리킨 곳은 멀쩡한 코드였다. 하마터면 안 고장 난 것을 고칠 뻔했다.

### 전제가 한 번도 확인되지 않았다

세 검사 전부 *"내가 요청한 그 작업을 돌려받았다"* 위에 서 있었고, 그 문장은 어디에도 안 적혀 있었다. **응답에는 `duplicate_guard: True`가 처음부터 들어 있었다.** 아무도 안 봤을 뿐이다.

둘을 고쳤다. 데이터셋 없는 시나리오는 **자기 프로젝트를 따로 만든다**(새 프로젝트에는 진행 중인 작업이 있을 수 없으니 경합 자체가 없어진다). 그리고 **전제를 검사로 만들었다** — 돌려받은 것이 요청한 그것인지, 정말 데이터셋이 없는지.

이제 이 상황이 오면 첫 줄이 원인을 말한다.

```
✗ the dataset-less job is the one this script asked for | duplicate_guard=True
```

예전 출력은 `can_rerun`을 가리켰다. **같은 사실, 다른 범인.**

### 같은 모양을 나머지에서도 셌다

훈련 작업을 만드는 스모크 셋을 봤다. 하나는 작업을 안 만들고, 하나(`run_dataset_delete_smoke.py`)는 **순서 덕분에 면역**이다 — 삭제된 데이터셋 검사가 가드보다 먼저 걸려 언제나 400이 난다. 결함은 하나뿐이었다. *하나를 찾으면 몇 개인지 세는 것이 규칙이고, 답이 하나여도 세어본 것과 안 세어본 것은 다르다.*

대조 4/4: 스모크를 원래 프로젝트로 되돌리면 빨간불, 가드 확인을 지우면 빨간불, 데이터셋 전제 확인을 지우면 빨간불, 그리고 **가드가 `running`을 안 보도록 변이시키면** 동작 검사가 빨간불 — 스크립트를 읽는 절반뿐 아니라 동작을 보는 절반도 문다.

## 이 검사들은 서로에게 기대고 있는가 — 두 번은 빈손, 세 번째에서 나왔다

앞 항목은 **앞선 요청이 남긴 상태** 때문에 엉뚱한 것을 검사하던 관문이었다. 그러면 다음 질문이 남는다: **이 포트폴리오의 검사 2,600개는 서로에게 얼마나 기대고 있는가.**

전부 함께 돌리면 초록불이다. 그건 각 검사가 참이라는 뜻이 아니라 **어떤 순서로 돌렸을 때** 참이라는 뜻이다.

### 세 가지로 흔들어 봤다

```
1. 파일 순서 뒤집기        158/158 통과   (약한 탐침이다 — 아래 참고)
2. 파일 하나씩 혼자 돌리기   158/158 통과   28.3분
3. 파일 안의 순서 뒤집기     158/158 통과   40.5분
```

**둘은 빈손이다.** 그것도 결과이고, 그렇게 적는다 — 다만 *빈손을 결과로 적을 자격*은 도구가 진짜를 잡을 수 있을 때만 생긴다. 그래서 각 탐침마다 진짜 의존을 심어 확인했다.

```
파일 A가 전역에 표식을 남기고 파일 B가 그것을 읽는다
  → 함께 돌리면 2 passed   → 혼자 돌리면 B가 실패 ✓
한 파일 안에서 두 번째 테스트가 첫 번째에 기댄다
  → 정의 순서로는 2 passed → 뒤집으면 실패 ✓
```

**1번이 약한 이유**를 적어둔다. 파일 순서를 뒤집어도 *상대 순서*만 바뀌지 앞선 파일이 남긴 상태는 그대로다. 다섯 저장소가 역순에서 전부 초록불이었지만, 그건 기대는 검사가 없다는 뜻이 아니라 **그 방법이 못 보는 것**이라는 뜻이다. 2번과 3번이 진짜 탐침이다.

### 세 번째 탐침 — 시계를 옮겼다

앞 셋은 **순서**만 흔들었다. 시간은 안 건드렸다. 이 저장소는 시계 결함 이력이 있으니 거기를 흔들어 봤다: 스위트를 **다른 시간대에서** 돌린다.

```
              UTC(기본)   UTC+14    UTC-11    KST(UTC+9)
agent-safety-core  ✓        ✓         ✓
mcp-gateway        ✓        ✓         ✓
rag-profile-…      ✓        ✓         ✓
document-intel…    ✓        ✓         ✓
modelmate          ✓      **1 실패**   ✓      **1 실패**
```

`test_two_clocks_do_not_meet.py`가 이렇게 못 박고 있었다.

```python
earlier = {"created_at": "2026-08-23T10:00:00Z"}
later   = {"created_at": "2026-08-23T10:00:00.500000"}
```

표시자 없는 쪽은 **서버 로컬 시각**으로 읽힌다. 오프셋이 0이면 `later`가 0.5초 뒤다. **KST에서는 그 값이 UTC 01:00이라 아홉 시간 이르다.** `later`라는 이름이 그곳에서는 거짓이 된다.

**무너진 것은 제품이 아니다.** `_newest_first`는 KST에서도 진짜 순간으로 옳게 정렬한다. 무너진 것은 **검사가 고른 리터럴**이었다.

### 검사의 이름이 이미 말하고 있었다

옛 이름은 `test_a_string_sort_inverts_them_at_this_offset_too`였다. **"이 오프셋"이 무엇인지는 기계가 정하고 있었다.** 컨테이너도 GitHub 러너도 UTC라 여기서는 영원히 초록불이고, **이 프로젝트를 만드는 사람의 기계에서만 빨간불**이다 — 그리고 그 빨간불은 자기가 방금 고친 것 탓으로 읽힌다. 앞 항목과 같은 모양이다: *색이 바뀌는 관문은 틀린 답보다 나쁘다.*

고친 것 둘. 오프셋 0을 주장하는 검사는 **오프셋 0을 스스로 만든다**(`at_offset` 픽스처가 `TZ`를 세우고 실제로 그 오프셋이 됐는지 확인한 뒤 진행한다 — 못 세웠으면 거기서 멈춘다). 그리고 **오프셋에 기대지 않는 주장**을 따로 하나 뒀다: 여섯 오프셋(-11 … +14)에서 나중 순간이 나중으로 정렬되는가. 하나를 0에 못 박았으면 다른 하나는 어디서나 참이어야 한다 — 아니면 남는 것은 *"UTC 기계에서는 맞다"*뿐이다.

대조: `_newest_first`를 옛 문자열 정렬로 되돌리면 **오프셋 0 검사도, 오프셋 무관 검사도, KST에서도** 전부 빨간불이다. 고친 검사가 옛 결함을 여전히 잡는다.

### 그리고 주 1회 다른 시간대에서 돈다

고친 그 검사는 이제 자기 오프셋을 스스로 만드니 면역이다. `timezone.yml`이 보는 것은 **아직 모르는 다음 하나**다 — KST·UTC+14·UTC-11에서 스위트 전체를 돌린다. 푸시 검사는 배포처와 같은 UTC로 두고, 값이 비싼 것은 주 1회로 미룬다.

그 작업의 첫 단계는 **`TZ`가 실제로 먹었는지 확인하고, 안 먹었으면 실패한다.** 안 그러면 UTC로 한 번 더 도는 것이고, 그 초록불은 "다른 시간대에서도 된다"로 읽힌다.

*두 번 훑어 아무것도 안 나온 뒤에 멈췄다면, 세 번째 축은 안 흔들어 봤을 것이다.*

## 내 도구가 "확인되지 않는 것 0개"라고 했다 — 자기가 닿은 것만 세고 있었다

로드맵에 **"HTTP 거부에 검사 없음 — 102개 중 60개"**가 열린 채로 남아 있었다. 낡은 숫자라 다시 쟀다.

```
도달하는 지점 58개 · **확인되지 않는 것 0개**
```

두 회차 만에 예순이 영이 됐다. **믿기지 않는 초록불은 대개 초록불이 아니다.**

### 분모가 없었다

직접 세어봤다.

```
소스의 거부 지점        112개
검사가 도달하는 지점     58개   ← 도구가 본 것은 여기까지
**한 번도 도달 안 함**   54개   ← 보고에 아예 없다
```

"확인되지 않는 것 0개"는 **닿은 58개에 대해서만** 참이었다. 나머지 54개는 한 줄도 돈 적이 없고, 그건 *확인되지 않은 것보다 나쁜* 상태다. 그런데 화면에서는 그것이 **0**으로 보였다.

이 도구가 존재하는 이유가 바로 그 구분이다.

```
한 번도 도달하지 않는다        ← 도구가 자기 보고에서 지워버린 칸
도달하고 확인된다
도달하는데 확인되지 않는다
```

**세 칸을 가르려고 만든 물건이 자기 보고에서 첫 칸을 지웠다.** 앞뒤가 안 맞는다. 이제 셋을 다 적고, 한 번도 안 닿는 것이 있으면 *"이쪽이 더 나쁘다"*라고 함께 적는다.

### 닿지 않던 54개 중 일곱을 닫았다

가장 위험한 쪽부터 골랐다 — 입력 모양을 따지는 400이 아니라 **사람이 실수했을 때**와 **요금제가 실제로 거절해야 할 때**다.

```
auth_signup    이메일 형식이 틀렸다 / 이미 쓰는 이메일이다
auth_login     계정이 없다 / 비밀번호가 틀렸다
claim_analysis_job / claim_prediction_api_call / enforce_prediction_token_limit
```

성공 경로에는 검사가 있었다. **로그인 실패는 이 제품에서 가장 자주 밟히는 갈래인데** 아무도 확인한 적이 없었다.

한도 셋은 더 나쁘다. 입구(`enforce_*`)에는 검사가 있었고 **권위 있는 자리에는 없었다.** 그 함수들은 자기 독스트링에 *"이제 세는 자리가 곧 판단하는 자리다"*, *"입구 검사는 빠른 거절로 남는다. 권위는 여기다"*라고 적어두고 있었다. **적어둔 자리가 곧 확인된 자리는 아니다.** 안 터지는 한도는 있는지 모르는 한도다.

### 시간으로 새는 것을 막는 계산에 검사가 없었다

`auth_login`의 두 거절은 같은 메시지를 낸다. 그 함수에는 이렇게 적혀 있다.

> 중앙값 10ms 대 280ms, **27배**. 메시지를 같게 해두고 응답 시간이 답을 알려주는 상태였고, 270ms는 네트워크 잡음에 묻히지 않는다.

그래서 계정이 없을 때도 가짜 해시로 같은 계산을 돌린다. **그 방어를 확인하는 것이 없었다** — 지우면 메시지는 그대로고 스위트는 초록불이다. 이제 그 계산이 실제로 일어나는지 본다.

**밀리초는 재지 않는다.** 공유 러너에서 재면 재는 것이 잡음이고, 무작위로 빨간불이 되는 검사는 결국 꺼진다. 방어가 **호출되는지**만 본다.

### 내 단언이 너무 좁아서 한 번 헛짚었다

두 실패의 응답 몸통을 통째로 비교했더니 빨간불이었다. 달랐던 것은 `request_id`와 `error_id`뿐 — 요청마다 새로 생기는 식별자이고 **어느 이메일이 가입돼 있는지 말해주지 않는다.** 누출이 아니라 내 단언이 좁았다.

그 둘을 빼고 비교하도록 고치면서 **대조를 같이 넣었다**: 진짜로 다른 몸통을 넣어도 여전히 갈라내는가. 무엇을 빼는 순간 비교가 아무것도 안 붙잡을 수 있다.

### 검사가 자기 자신을 막았다

거절 갈래를 확인하려면 일부러 여러 번 실패해야 하는데, `enforce_auth_attempt_limit`이 그것을 무차별 대입으로 보고 429를 냈다. **맞는 판단이다.**

조이개를 끄지 않고 **통만 비운다.** 끄면 그 파일이 도는 동안 방어가 없는 것이고, 그걸 눈치채지 못한 채 다른 것을 확인하게 된다.

### 다시 재기

```
          이전        이후
소스        112개      112개
도달함       58개      **65개**   확인되지 않는 것 0개
안 닿음      54개      **47개**
```

일곱 전부 변이로 확인했다 — 상태 코드를 599로 바꾸면 일곱 다 빨간불, 되돌리면 초록불. **없던 자리에 검사를 넣었다는 말은 그 검사가 무너뜨릴 수 있을 때만 참이다.**

## `"@"` 한 글자로 계정이 만들어졌다 — 그리고 내 검사는 옆의 이유로 통과하고 있었다

앞 항목에서 닿지 않던 거부 일곱을 닫았다. 그중 하나가 *"이메일 형식이 올바르지 않습니다"*였고, 검사는 로컬에서 초록불이었다. **CI에서 빨간불이었다.**

```
E  AssertionError: {"token":"***","user":{"id":"1c0ff654-…","email":"@","name":"", …}}
E  assert 200 in (400, 422)
```

**`"@"` 한 글자로 계정이 만들어진다.** 이메일은 `"@"`, 이름은 `email.split("@")[0]`이라 빈 문자열.

### 규칙이 둘이었다

```
파일럿 문의   re.match(r"^[^@\s]+@[^@\s]+\.[^@\s]+$", email)
가입          if not email or "@" not in email
```

같은 질문에 두 개의 답이 있었고, **느슨한 쪽이 계정을 만드는 자리였다.** 이 목록에서 가장 자주 나온 모양이고([한 사실을 두 곳에 적고 한 곳만 지켰다](#결함의-모양)), 이번에는 사본이 아니라 **처음부터 서로 다른 두 규칙**이었다는 점만 다르다.

엄격한 쪽이 이미 옳았다. 그래서 옮겨오는 대신 `is_email_shaped`를 한 곳에 두고 **양쪽이 그것을 읽게** 했다. 파일럿 쪽의 정규식도 지웠다 — 남겨두면 사본이고, 사본은 소스가 바뀌어도 그대로다.

### 로컬이 초록불이었던 이유가 더 중요하다

`"@"`는 로컬에서도 400을 냈다. **다른 이유로.** 앞선 실행이 남긴 `"@"` 계정이 DB에 앉아 있어서 *중복 이메일* 검사에 먼저 걸렸다.

```
로컬   "@" → 400   ← "이미 사용 중인 이메일입니다"   (앞선 실행이 남긴 행)
CI     "@" → 200   ← 새 DB. 형식 검사가 통과시킨다
```

**검사는 자기 이름이 말하는 것을 확인한 적이 없다.** 옆의 방어가 대신 답하고 있었고, 화면에서 그 둘은 똑같이 400이다.

바로 앞 회차에 "검사들이 서로에게 기대는가"를 세 축으로 흔들어 158/158 통과를 받았다. **그 탐침들은 이걸 못 봤다** — 파일 하나씩 돌려도, 순서를 뒤집어도, 그 행은 여전히 DB에 있다. 기댄 것이 *같은 실행 안의 다른 검사*가 아니라 **몇 회차 전의 내 실행이 남긴 행**이었다. 새 체크아웃에서만 보이고, 그래서 CI가 잡았다.

*무엇에 기대는지 세 축으로 흔들고도 못 찾았다면, 흔들 축이 하나 더 있는 것이다.*

이제 그 검사는 두드리기 전에 그 이메일의 행을 지우고, 두드린 뒤 **행이 생기지 않았는지**까지 본다. 400이 형식 때문에만 나올 수 있는 상태로 만들어 둔 것이다.

### 대조 셋, 그리고 인용과 사용

```
가입을 옛 느슨한 규칙으로 되돌림   → 빨간불 ✓
파일럿에 정규식 사본을 되살림     → 빨간불 ✓
공통 규칙을 `.*`로 변이           → 빨간불 ✓
```

*"옛 규칙이 코드에 남아 있지 않은가"*를 보는 검사는 처음에 빨간불이었다. 남아 있던 것은 **그 규칙이 왜 바뀌었는지 설명하는 주석**이었다. 규칙을 **설명하는** 문장이 규칙을 **쓰는** 코드로 읽힌 것이고, 이 저장소가 이미 여러 번 잡은 모양이다. 주석을 걷어내고 코드만 본다.

## 서른둘이 조용히 빠져도 초록불이었다 — 관문이 수집 수를 보고 있었다

앞 항목에서 **앞선 실행이 남긴 상태 때문에 옆의 이유로 통과하던 검사**를 찾았다. 그 축을 계속 밀었다 — 이 저장소들에는 *환경에 따라 있고 없는 것*이 또 뭐가 있나.

`document-intelligence`는 2.2MB짜리 논문 PDF를 받아서 쓴다. `tests/fixtures/`는 gitignore돼 있으니 **새 체크아웃에는 없다.** 못 받으면 skip한다 — 네트워크가 없다는 것과 코드가 틀렸다는 것은 다르니까 맞는 판단이다.

CI에는 그 skip을 잡으라고 둔 단계가 있었다. 이름이 `Refuse a silently shrinking suite`다.

### 이름이 말하는 일을 못 한다

밖으로 나가는 요청 없이 그 상태를 만들어 봤다(캐시를 치우고 주소를 거절당하는 로컬 포트로 돌린다).

```
샘플 PDF를 못 받는 상태
  수집 수  234   ← 관문이 보는 수. 하한 67과 비교해 **통과시킨다**
  실행     201 passed, **32 skipped**, 1 failed
```

**런타임 skip은 수집 수를 바꾸지 않는다.** 픽스처 안에서 `pytest.skip()`이 불려도 테스트는 이미 수집된 뒤다. 그 관문은 자기 이름이 말하는 바로 그것을 **구조적으로 볼 수 없다.**

하한선이 67인 것도 따로 문제다. 실제 수집이 234이니 **167개가 사라져도 초록불**이다. 그리고 수집 수의 *정확한* 비교는 바로 아래 README 단계가 이미 하고 있었다 — 이 단계는 못 보는 것을 보는 척하면서 아무것도 더하지 않았다.

*하한선은 목록이 줄어드는 것만 보고, 목록이 현실에서 멀어지는 것은 못 본다.*

### 그리고 하나는 skip이 아니라 죽었다

같은 상태에서 **1 failed**가 있었다.

```
FileNotFoundError: .../tests/fixtures/sample.pdf
```

`test_rejections_that_were_never_fired.py`가 픽스처를 안 쓰고 **경로를 직접 적고 있었다.** `unittest.TestCase`라 pytest 픽스처를 인자로 못 받아서다.

이 저장소의 `conftest.py`에는 바로 그 교훈이 이미 적혀 있었다.

> 한 모듈이 가진 부수효과에 다른 모듈이 기대는 구조였다. 여기로 옮기면 누가 먼저 돌든 상관이 없다.

**픽스처로 옮겼는데 한 곳이 안 따라왔다.** 규칙을 옮기는 것과 모두가 그 규칙을 읽는 것은 다른 일이다.

pytest 픽스처는 pytest만 쓸 수 있지만 평범한 함수는 누구나 쓴다. 그래서 가져오는 규칙을 `tests/sample_pdf.py`로 꺼내고 픽스처도 그것을 부르게 했다 — 두 벌이 되면 한쪽만 낡는다.

### 집합으로 지킨다

`rag-profile-selector`는 코퍼스 skip에 대해 이미 이 장치를 갖고 있다: **어느 파일이 몇 개를 건너뛰는가**를 정확한 집합으로 둔다. 하한이 아니라 일치라야 늘어나는 것도, 고쳐서 줄어드는 것도 걸린다.

같은 훅을 `document-intelligence`에 넣었다. 기대 집합은 **비어 있다** — PDF가 있으면 하나도 안 건너뛴다.

그리고 CI 단계는 지우는 대신 **바꿨다**: 이제 그 훅이 살아 있는지를 본다. 샘플 PDF를 못 받는 상태를 일부러 만들어 빨간불이 나오는지 확인하고, **종료 코드만 보지 않는다** — 스위트는 다른 이유로도 실패하고, *엉뚱한 이유로 도는 대조는 대조가 아니다.* 메시지에 `SKIP 집합 검사 실패`가 들어 있는지까지 본다.

### 나머지 셋도 셌다

```
              건너뛰는 것   집합을 지키는 훅
agent-safety-core     0        없었다
mcp-gateway           0        없었다
modelmate             0        없었다
rag-profile-selector  49*      있다
document-intelligence  0        **넣었다**
                            * 코퍼스가 없는 CI 기준
```

셋은 하나도 안 건너뛴다. **빈 집합을 지키는 것에 값이 있는가?** 있다 — `document-intelligence`의 서른둘도 어느 날 0에서 늘어난 것이다. `agent-safety-core`의 `requirements.txt`에는 *"`jsonschema`가 없으면 다섯 개가 skip된다"*고 적혀 있고, modelmate는 선택적 의존(SHAP·optuna)이 빠지면 그것을 쓰는 검사들이 조용히 사라진다.

셋에 같은 훅을 넣었다. 기대 집합은 `{}`이고, **바뀌는 것이 옳으면 그 값을 함께 고치면 된다** — 요점은 조용히 바뀌지 않게 하는 것이다.

### 대조

```
건너뛰기 하나를 심으면              세 저장소 다 빨간불 ✓
탈출구 ALLOW_UNEXPECTED_SKIPS=1     통과 ✓
파일 하나만 돌릴 때                 조용함 ✓   ← 방해가 되면 다음 사람이 훅을 지운다
샘플 PDF를 못 받는 상태             SKIP 집합 검사 실패로 빨간불 ✓
옛 CI 관문(수집 234 ≥ 67)           같은 상태에서 여전히 통과 — 아무 말도 안 한다
```

마지막 줄이 이 항목이다. **고친 것과 나란히 두면 옛 관문이 무엇을 못 봤는지가 한 줄로 보인다.**

### 넣자마자 하나를 찾았다 — 그리고 그건 결함이 아니었다

`mcp-gateway`에 훅을 넣고 밀었더니 CI가 빨간불이었다.

```
실제: {test_code_that_never_ran: 1, test_http_transport: 1,
       test_live_server: 1, test_runtime_controls: 1}
기대: {}
```

`without-core` 잡이다. README가 *"코어가 없으면 정책·무결성 계층만으로도 동작한다"*고 말하고, 그 문장을 실제로 재는 잡이다. **거기서 건너뛰는 넷은 그 잡이 재려고 존재하는 바로 그 상태다.**

훅을 약하게 만들지 않고 **그 잡에서만 껐다.** 끄면서 무엇이 대신 지키는지 적었다 — 같은 잡의 비교가 README의 `passed`와 `skipped`를 **둘 다 정확히** 맞춰본다. 빈 집합 훅보다 강하다. 기본 잡에서는 훅이 그대로 돌고, 건너뛰기를 하나 심으면 여전히 빨간불이다.

*탈출구를 쓰는 것과 눈을 감는 것은 다르다 — 무엇이 대신 보고 있는지 적을 수 있으면 전자다.*

### 내 훑기도 한 번 틀렸다

어느 저장소에 훅이 있는지 `grep -lq PATTERN a b`로 셌는데 `rag-profile-selector`가 "없음"으로 나왔다. 있다. **없는 파일 하나가 섞이면 `grep`은 일치해도 0이 아닌 코드로 끝난다.** 목록이 틀린 채로 "셋에 없다"고 적을 뻔했다 — 세는 도구가 틀리면 결론도 틀린다.

## "검사가 없다"에는 두 가지가 있다 — 안 쓴 것과 못 쓰는 것

로드맵에 에이전트가 할 수 있는 항목이 하나 남아 있었다: **거부 112개 중 47개가 한 번도 도달하지 않는다.** 그중 위험한 것부터 닫으려고 목록을 다시 뽑았다.

### 먼저 내 지도가 낡아 있었다

목록을 다시 세니 미도달이 **60개**로 늘어 있었다. 두 회차 동안 검사를 더했는데 미도달이 늘 리가 없다.

추적은 `파일:줄`로 적힌다. 앞 회차에 `051_auth_history_debug.part`에 세 줄을 더했고, **그 아래 모든 지점의 줄 번호가 밀렸다.** 이미 닫아둔 네 지점이 "한 번도 도달 안 함"으로 다시 세어진 것이다.

*측정이 바뀐 게 아니라 지도가 낡았다.* 도구 독스트링에는 이미 **"`--from`은 낡을 수 있다"**고 적혀 있었다 — 적어두는 것과 기계가 아는 것은 다르다. 이제 추적에 조각 내용의 지문을 함께 적고, 다르면 **읽기를 거부한다.**

```
옛 형식(지문 없음)   거부 ✓
지문 일치            통과 ✓
조각을 한 줄 고친 뒤  거부 ✓
되돌린 뒤            통과 ✓
```

새로 떠서 얻은 참값: **112개 중 도달 65 · 미도달 47.**

### 닫으려던 다섯은 닫을 수 없었다

47개에서 Agent Mode의 404 다섯을 골랐다. README가 앞세우는 기능이고, "없습니다"는 사람이 매일 보는 답이니까.

**검사를 쓰기 전에 두드려 봤다.** 내 것인데 안이 빈 분석 실행으로 다섯 라우트를 치니 404가 하나도 안 나온다.

```
045:202 -> 200     045:396 -> 200
045:237 -> 409     045:442 -> 200
045:256 -> 200
```

다섯 다 같은 모양이다.

```python
assert_analysis_run_owner(user, analysis_run_id)   # 행이 없으면 여기서 404
thing = <헬퍼>(conn, analysis_run_id)
if not thing:
    raise HTTPException(404, "...")                # 여기
```

헬퍼들은 **행이 있기만 하면 truthy한 봉투를 낸다** — 안이 비어도 `steps: []`가 담긴 딕셔너리다. 행이 없는 경우는 바로 위에서 이미 끝났다. **두 조건이 서로를 배제한다.**

*안 쓴 것과 못 쓰는 것은 다르고, 세는 도구는 그 둘을 구별하지 못한다.* 두드려 보지 않았으면 닿지 않는 가지에 대고 검사를 짜다가, 통과시키려고 억지 상태를 만들었을 것이다.

### 헛짚은 것 둘

**`or {}`가 범인인 줄 알았다.** 두 라우트가 `trace = _attach_dataset_summary(conn, trace or {})` 뒤에 `if not trace`를 본다. None을 `{}`로 바꿔놓고 비어 있음을 묻는 모양이라 그게 원인이라고 적을 뻔했다. 재보니 아니었다 — `get_analysis_timeline`이 애초에 truthy한 봉투를 낸다. **그 줄은 무해하고, 원인은 한 층 위였다.**

**404를 보고 "닿는다"고 읽었다.** 첫 훑기에서 목 라우트 둘이 404를 냈길래 도달 가능으로 셌는데, 내가 친 경로(`/api/agent/analysis-runs/...`)가 아예 없는 경로였다. **라우터가 낸 404를 핸들러가 낸 404로 읽은 것이다.** 실제 경로는 `/api/agent/runs/...`이고, 거기서는 200이 온다.

### 지우지 않는다 — 그리고 그것을 쟀다

값이 없는 것이 아니라 **위층이 무너졌을 때만 값이 생기는** 방어다. 주장으로 두지 않고 확인했다. 소유권 검사가 없는 실행을 통과시키도록 바꾼 뒤 없는 ID로 두드리면 —

```
/api/agent-runs/{없는id}            -> 404
/api/agent-runs/{없는id}/trace      -> 404
/api/agent/runs/{없는id}            -> 404
/api/agent/runs/{없는id}/timeline   -> 404
```

**답은 그대로다.** 그 순간 다섯이 살아나 대신 답한다. 이중 방어가 하는 일이 정확히 이것이다.

닿을 수 없는 가지 대신 **닿을 수 없게 만드는 성질**을 고정했다. 둘 중 하나가 바뀌면 저 다섯은 살아 있는 가지가 되고, 그때 그 파일이 말해준다.

### 닿는 다섯은 닫았다

같은 47개에서 실제로 닿는 것들을 골랐다. 전부 공개 예측·배포 표면의 *"먼저 이걸 하세요"*다.

```
071:9   POST /api/predict-batch    모델 없음
071:54  POST /api/deploy           학습된 모델 없음
083:5   POST /api/predict/single   Run cross-validation first
084:5   POST /api/predict/batch    Run cross-validation first
086:16  POST /api/deploy/stable    먼저 모델 비교를 실행한 뒤…
```

**순서를 건너뛴 사람이 무엇을 보는가**는 가장 자주 밟히는 갈래인데 성공 경로에만 검사가 있었다. 세 가지를 함께 고정한다: 400이 온다, **무엇을 먼저 하라고 말한다**, 그리고 500이 아니다 — 준비가 안 된 것은 서버의 잘못이 아니고, 500으로 답하면 사용자는 자기가 고칠 수 있는 것을 못 고친다.

다섯 다 변이로 확인했다(599로 바꾸면 빨간불). **미도달 47 → 42**, 그중 다섯은 이제 *"못 쓰는 것"*으로 이름이 붙어 있다.

### 대조가 엉뚱하게 돌았던 것도 적어둔다

소유권 검사를 무력화하는 첫 변이는 초록불이었다. 검사가 약해서가 아니라 **내 변이가 아무것도 안 바꿨기** 때문이다 — `_resource_not_found()`를 지워도 바로 아래 소유자 비교가 같은 404를 냈다. 함수 첫 줄에서 되돌아 나오게 바꾸니 빨간불이 됐다. *엉뚱한 이유로 도는 대조는 대조가 아니고, 엉뚱한 이유로 안 도는 대조도 마찬가지다.*

## README가 방문자에게 가장 먼저 시키는 세 줄을 CI가 한 번도 돌린 적이 없었다

로드맵의 에이전트 몫이 값싼 것만 남아서(선택적 의존 경로·500 경로·입력 모양 400) 다른 각도를 잡았다. **포트폴리오는 돌리기 전에 읽힌다.** 읽는 사람이 가장 먼저 하는 일은 README 맨 위의 명령을 복사해 붙이는 것이다.

modelmate의 그 자리는 이렇다.

```bash
python3 scripts/make_demo_data.py
python3 scripts/demo.py --leaky                    # 검사 적용
python3 scripts/demo.py --leaky --ignore-leakage   # 권고를 무시하면
```

**셋 다 CI 밖에 있었다.** 스위트가 깨지는 것보다 나쁜 자리다 — 여기서 막히면 나머지는 안 본다.

### 돌려보니 멀쩡했다. 그게 요점이 아니다

셋 다 동작했고 README가 적어둔 숫자도 맞았다.

```
README:  권고를 적용하면 AUC가 1.0에서 0.778로 내려가고 검증은 grounded가 되며,
         무시하면 invalid로 판정하고 보고서가 스스로 차단 상태를 밝힌다
측정:    적용 0.7782 / grounded · 무시 1.0 / invalid
```

**고장이 아니라 지켜지지 않는 주장이었다.** [커버리지 숫자가 두 달 낡아 있던 것](#커버리지-숫자만-아무것도-안-지키고-있었다--두-달-치-작업이-문서에-없었다)과 같은 모양이다 — 그때도 숫자가 틀린 게 결함이 아니라 아무도 안 물어봤다는 게 결함이었다.

### `exit 0`은 이 질문에 답하지 않는다

데모의 값은 **두 실행이 갈린다**는 데 있다. 누출 컬럼을 빼든 넣든 같은 점수가 나오면 데모는 여전히 종료 코드 0으로 끝나고 보여주는 것은 없다. 이 저장소는 그 구분을 이미 적어뒀다 — *"이 검사가 보는 것은 '200이 왔다'가 아니라 '사슬이 끝까지 갔다'이다."*

그래서 검사는 점수·판정·**어느 컬럼을 실제로 썼는지**까지 본다. 두 수가 달라도 이유가 누출이 아닐 수 있으니까.

숫자는 검사 파일에 다시 적지 않는다. **README가 기록이고** 검사는 그것을 읽어 측정과 맞춘다 — 두 곳에 적으면 한쪽만 낡는다.

관문에 박기 전에 재현성을 확인했다. 데이터를 지우고 다시 만들어도 0.7782 / 1.0이 그대로다(세 번). *재현되지 않는 값을 관문에 박으면 그 관문은 무작위로 빨간불이 되고 결국 꺼진다.*

### 대조 4/4 — 그중 둘은 다시 만들어야 했다

```
README 숫자를 바꾸면              → 빨간불 ✓
두 실행이 갈리지 않게 만들면        → 빨간불 ✓
AUC 라벨을 바꿔 정규식이 못 찾게    → 빨간불 ✓
'사용된 특징' 문구를 바꾸면        → 빨간불 ✓
```

뒤의 둘은 **처음에 초록불이었다.** 검사가 약해서가 아니라 내가 `scripts/demo.py`에서 문자열을 바꿨는데 그 라벨은 `backend/tools/automl_result.py`가 만들고 있었기 때문이다 — **출력이 하나도 안 바뀌었다.** 진짜 출처에서 심으니 둘 다 물었다. *엉뚱한 이유로 안 도는 대조도 대조가 아니다.*

### 그리고 내 훑기가 세 번 다르게 답했다

"다른 저장소들은 README 명령을 CI가 돌리는가"를 세 번 셌고 **세 번 다 다른 답**이 나왔다.

```
1차   전부 ✓          ← 주석에 적힌 언급까지 "돌린다"로 셌다
2차   전부 없음        ← 작업 디렉터리가 틀려 경로가 존재하지 않았다
3차   뒤죽박죽         ← 토큰 매칭이 산문 줄까지 명령으로 집었다
```

1차는 **인용과 사용의 혼동**이다 — `benchmark/run.py`를 *설명하는* 주석이 그것을 *실행하는* 줄로 읽혔다. 2차는 셸의 작업 디렉터리가 남아 있었던 것이고, 3차는 내가 만든 매처가 나빴다.

그래서 영리한 매처를 버리고 **손으로 확인할 수 있는 것만 확인했다.**

```
agent-safety-core     benchmark/run.py 는 CI가 부르지 않는다(문서화된 결정).
                      다만 test_published_benchmark.py가 run_all()을 직접 불러
                      README 표와 맞춘다 — **주장은 지켜진다**
rag-profile-selector  corpus.yml이 실험을 돌리고, 공개된 수는 별도 검사가 지킨다
modelmate             데모의 주장을 지키는 것이 **없었다** → 넣었다(46초)
```

*세 번 세어 세 번 다른 답이 나오면, 답이 아니라 세는 법을 고쳐야 한다.* 그리고 확인할 수 없는 것은 적지 않는다 — 이 절에서 다섯 저장소 전부에 대해 말하지 않는 이유다.

## 실험이 통째로 죽어도 초록불이었다 — 흘러감과 확인 불가가 한 신호 뒤에 있었다

최근 회차가 `modelmate`에 몰려 있어 형제 넷으로 돌렸다. 두 가지를 했다 — 거부 감사를 다시 돌리고, **README가 내세우는 수를 누가 지키는지** 셌다.

### rag-profile-selector: 지키는 장치는 있는데 실패를 구별하지 않았다

주간 `corpus` 잡에 *"README의 핵심 수치를 실제 실행과 대조"* 하는 단계가 있다. 좋은 장치다 — 이 수치들은 745개 조문을 임베딩해야 나오므로 매 push에 못 돌린다.

그런데 실험을 이렇게 돌린다.

```bash
python experiments/kr_law_retrieval.py > /tmp/run.txt 2>&1 || true
```

`|| true`가 종료 코드를 삼킨다. 그리고 확인 결과를 **전부 `::warning::`으로만** 냈다.

워크플로의 확인 스크립트를 그대로 떼어내 세 상황에 돌려봤다.

```
실험이 정상 출력        종료코드 0
실험이 죽음(트레이스백)  종료코드 0  ← 경고 셋 내고 초록불
숫자가 흘러감           종료코드 0
```

**실험이 통째로 죽어도 통과한다.** 그때 나오는 것은 *"dense MRR: not present in the run output"* 경고 셋인데, 그건 드리프트가 아니라 **이 확인이 아무것도 확인하지 못했다**는 뜻이다.

### 원칙은 이미 그 파일 안에 있었다

바로 **다음 단계**가 이렇게 되어 있다.

> `Refuse a run where the corpus tests skipped anyway` — `$skipped corpus test(s) skipped after fetching; this job verified nothing.` → `exit 1`

*"검사가 아무것도 확인 못 했으면 실패"*를 한 단계 아래에서 이미 쓰고 있었다. **같은 파일 안에서 원칙이 한 단계에만 적용돼 있었다.**

경고로 둔 이유도 적혀 있고, 그 이유는 옳다.

> an embedding model or library version can move these legitimately, and a red build every time a dependency updates is a build people stop reading.

**드리프트에 대해서는 맞는 판단이다.** 틀린 것은 그 판단을 *다른 실패*에까지 적용한 것이다.

그래서 둘로 갈랐다.

```
흘러감   값이 있는데 다르다        → 경고. 사람이 판단한다
못 찾음  값이 출력에 아예 없다      → 실패. 확인이 성립하지 않았다
```

README에서 값이 사라지는 경우도 "못 찾음"으로 옮겼다 — 고리가 끊긴 것이지 흘러간 것이 아니다. 그리고 `|| true`를 걷어 실험이 죽으면 그 자리에서 멈춘다.

대조 4/4: 정상 0 · 실험 죽음 1 · 흘러감 0(경고) · README에서 값 삭제 1.

### 거부 감사: 넷 다 빈손, 그리고 그 빈손을 확인했다

형제 넷의 거부 감사는 열다섯 회차쯤 묵어 있었다. 그동안 이 저장소들은 테스트 함수가 585개에서 1,369개로 늘었다.

```
mcp-gateway            도달 51  ·  확인 안 됨 0  ·  못 잰 것 1
document-intelligence  도달 77  ·  확인 안 됨 0
agent-safety-core      도달 85  ·  확인 안 됨 0
rag-profile-selector   도달 51  ·  확인 안 됨 0  ·  못 잰 것 1
                       ────────────────────────
                       도달 264  ·  **확인 안 됨 0**
```

**빈손을 결과로 적을 자격은 도구가 진짜를 잡을 때만 생긴다.** 그래서 진짜 결함을 심었다 — `document-intelligence`의 `pytest.raises(ValueError, match="finite")`를 `pytest.raises(Exception)`으로 낮췄다. 타입도 메시지도 안 보는 상태다.

```
심기 전   확인 안 되는 것 0
심은 뒤   확인 안 되는 것 1   ← 잡았다
되돌린 뒤 확인 안 되는 것 0
```

못 잰 두 지점(`transport.py:402`, `selector.py:73`)은 **0으로 세지 않고 따로 찍는다.** 조용히 빼면 이 도구도 초록불이 된다.

### 훑기가 또 한 번 좁았다

"README의 수 중 검사가 언급하지 않는 것"을 세는데, 처음엔 **굵게 강조된 수**만 봤다. 결과는 네 저장소 합쳐 "없음 0개" — 믿기 어려운 초록불이었다. 정규식이 `**0.714**` 같은 형태만 집고 산문 속 `dense 단독(0.714)`은 놓쳤기 때문이다.

넓혀서 다시 세니 `rag-profile-selector`에서 일곱이 나왔고, 그중 셋이 위 주간 잡이 지키던 바로 그 수였다. **좁은 훑기는 "없다"가 아니라 "내가 못 봤다"를 낸다.**

## 한 저장소에서 고치고 나머지를 안 셌다 — 같은 관문이 세 곳에 더 있었다

앞 항목에서 `rag-profile-signal`의 CI가 실험이 죽어도 초록불인 것을 고쳤다. 그 자리에 `|| true`가 있었으니 **같은 모양이 몇 개인지 세는 것**이 이 목록의 규칙이다. 여섯 저장소의 워크플로를 훑었다.

```
|| true             7건
continue-on-error   3건
2>/dev/null        12건
```

대부분은 정당했고 이유가 옆에 적혀 있었다 — 네트워크 실패를 다음 단계가 받는 자리, `grep`이 안 맞으면 1로 끝나는 자리, 진단용 로그 출력. **세어보고 "괜찮다"가 나오는 것도 결과다.**

그런데 열두 건의 `2>/dev/null` 중 열한 건이 같은 한 줄이었다.

```bash
collected=$(python -m pytest --collect-only -q 2>/dev/null | grep -oE '[0-9]+ tests? collected' | ...)
```

그 줄을 따라가다 **다른 것**이 나왔다.

### 87회차에 고친 그것이 세 곳에 더 있었다

`agent-safety-core`의 워크플로에 `Refuse a silently shrinking suite`가 그대로 있었다. **87회차에 `document-intelligence`에서 걷어낸 바로 그 단계다.**

그때 두 가지가 틀렸다고 적었다. 수집 수를 보므로 런타임 skip을 구조적으로 볼 수 없고, 하한이 실제보다 한참 낮다는 것. 고치고, **나머지를 세지 않았다.**

```
agent-safety-core     하한 288 · 실제  565  → 277개가 사라져도 통과
mcp-gateway           하한 146 · 실제  379  → 233개
rag-profile-selector  하한 177 · 실제  348  → 171개
modelmate             하한 281 · 실제 1156  → **875개**
```

**합쳐 1,556개가 사라져도 네 관문 전부 초록불이다.**

*하나를 찾으면 몇 개인지 세는 것*은 이 목록이 가장 자주 쓰는 규칙이고, 나는 그것을 적어두고도 한 회차 전에 어겼다. **규칙을 아는 것과 그때 적용하는 것은 다른 일이다** — 이 목록에 이미 두 번 적힌 문장이다.

### 지우기 전에 무엇이 대신 막는지 확인했다

관문을 지우는 것은 조심할 일이다. 그래서 그 자리가 하던 두 일을 이미 하는 것이 있는지 먼저 봤다.

```
수집 수     → 바로 아래 "README가 자기 수를 맞게 센다"가 **정확히** 비교한다
런타임 skip → conftest.py의 EXPECTED_SKIPS 훅이 집합으로 지킨다(86회차)
```

둘 다 네 저장소에 있다. 그리고 **읽고 넘어가지 않고 심어서 확인했다.**

```
README 수를 +7 하면      수집 565 vs 주장 572 → 불일치 잡힘 ✓ (세 저장소)
건너뛰기를 하나 심으면    SKIP 집합 검사 실패 → 빨간불 ✓ (세 저장소)
```

`modelmate`는 수집 쪽만 여기서 확인했다(1156 vs 1163). 건너뛰기 훅은 86회차에 같은 방식으로 심어 확인한 것이 있다.

지운 자리에는 **왜 지웠는지와 무엇이 대신 보는지**를 남겼다. 빈자리는 다음 사람에게 "여기엔 원래 아무것도 없었다"로 읽힌다.

### 곁가지: 빈 값이 조용히 0이 되는 자리

`mcp-gateway`의 without-core 비교에 이런 줄이 있다.

```bash
measured_uncollected=$((claimed_total - collected))
```

`collected`가 비면 bash는 **오류 없이** `$((327 - ))`를 327로 계산한다. 확인해봤다 — 종료 코드 0에 결과 327이다.

다만 그 값은 곧 README의 48과 비교되어 결국 실패한다. **조용히 통과하지는 않는다.** 메시지가 엉뚱한 곳(README)을 가리킬 뿐이라 이번에는 고치지 않고 여기 적어둔다 — *고칠 값이 있는 것과 적어둘 값이 있는 것은 다르다.*

## 샘플 여섯 중 다섯을 틀리게 판정하면서 전부 "신뢰 높음"이었다

이 결함은 검사가 아니라 **화면을 보다가** 나왔다. 스크린샷을 찍으려고 고객 이탈 CSV를 올렸더니 첫 화면이 이렇게 말했다.

```
데이터 종류: 보안/이상 탐지        신뢰: 높음
"로그인, 거래, 보안 기록으로 이상 여부를 예측하는 문제로 보입니다."
```

고객 이탈 데이터다.

### 우연한 부분일치 하나가 진짜 신호 넷을 이겼다

`last_login_days`의 **`login`** 이 보안 규칙 낱말에 걸린다. 그리고 그 규칙이 이탈 규칙보다 **앞**이라, 순서대로 보다가 거기서 멈췄다.

같은 데이터에 점수를 매겨보면 이렇다.

```
고객 이탈/CRM   4점   churn, contract, customer, tenure
보안/이상 탐지   1점   login
```

`last_login_days` 하나만 빼면 "고객 이탈/CRM"으로 바뀐다. **판정이 우연한 컬럼 하나에 달려 있었다.**

### 하나가 아니었다 — 저장소가 들고 다니는 샘플로 셌다

```
customer_churn_demo.csv           → 보안/이상 탐지     ✗
customer_churn.csv                → 보안/이상 탐지     ✗
customer_churn_leaky.csv          → 보안/이상 탐지     ✗
bike_signups.csv                  → 제조/설비 품질     ✗   avg_temperature
public_bike_signup_demo.csv       → 제조/설비 품질     ✗   avg_temperature
manufacturing_quality_demo.csv    → 제조/설비 품질     ✓
```

**여섯 중 다섯이 틀렸고, 여섯 다 "신뢰 높음"이었다.** 자전거 신청 데이터가 제조업 품질 데이터로 판정된다.

이 샘플들은 장식이 아니다. 첫 화면에 **"샘플로 체험하기"** 버튼이 있고, 처음 온 사람이 가장 먼저 누르는 것이 그것이다.

### "높음"은 세어본 결과가 아니었다

규칙 스물둘 중 **열아홉이 상수 `"높음"`을 반환**했다. 무엇을 근거로 높다는 것인지가 없었다. 화면에 "신뢰 높음"이 붙으면 사용자는 그대로 믿는다 — 그러니 그 말은 세어본 결과여야 한다.

```
근거 3개 이상   높음
근거 2개       중간
근거 1개       낮음
```

이제 자전거 데이터는 **"공공교통/이용자 통계 · 중간"**(근거 2: signup, station)이고, 이탈 데이터는 **"고객 이탈/CRM · 높음"**(근거 4)이다. 샘플 **6/6** 정확.

### 규칙 셋은 두 벌씩 있었다

보안·금융·공공시설은 같은 규칙이 두 번 적혀 있었다. 순서대로 보는 구조에서 **뒤엣것은 영영 닿을 수 없다.** 지웠고, *"규칙마다 자기 낱말만으로 이길 수 있는가"* 를 검사로 뒀다 — 못 이기는 규칙이 있으면 그건 목록에 있으나 닿지 않는 가지다.

### 무엇을 맞았는지 함께 돌려준다

`dataset_domain_evidence`에 실제로 맞은 낱말을 담는다. 판정이 이상해 보일 때 **왜 그렇게 봤는지** 물어볼 수 있어야 한다. 그리고 *돌려준 근거가 실제로 컬럼에 있는지*도 검사한다 — 없는 근거를 대는 판정은 판정이 아니라 주장이다.

### 대조 4/4

```
첫 일치 승리로 되돌리면      빨간불 ✓
신뢰도를 상수 "높음"으로     빨간불 ✓
근거를 빈 목록으로          빨간불 ✓
중복 규칙을 되살리면         빨간불 ✓
```

되돌림 방향도 뒀다. 이탈 신호를 다 빼면 보안이 **이겨야** 한다 — 그래야 이 판정이 점수 때문이지 보안 규칙을 죽여서가 아니다. 그때 신뢰도는 "낮음"이어야 하고.

### 화면에서 찾았으니 화면으로 확인했다

서버를 새 코드로 올려 같은 CSV를 업로드했다.

```
데이터 종류: 고객 이탈/CRM       예측 목적: 이탈 여부
"고객 이탈이나 해지 여부를 나타내는 표현이 보입니다."
```

*검사 2,600개가 못 본 것을 스크린샷 한 장이 보여줬다.* 이 저장소의 검사는 전부 백엔드와 소스 수준이고, **렌더된 화면을 본 적은 한 번도 없었다.**

## 검사가 닿지 않는 주장 표면이 하나 있었다 — 사람들이 가장 먼저 보는 자리

외부 리뷰를 받았고, 첫 지적을 재봤더니 그대로였다.

```
저장소                  README   GitHub 설명
modelmate               1186     239
agent-safety-core        565     273
mcp-gateway              379     124
rag-profile-selector     348     150
document-intelligence    234      52
```

**다섯 전부 틀렸다.** 어떤 것은 다섯 배 가까이 차이 난다.

### 왜 아무것도 못 봤는가

이 저장소에는 개수를 지키는 것이 여럿 있고, 그중 몇은 내가 이 목록의 앞선 항목에서 만든 것이다.

```
check_counts.py          프로필 표 ↔ 각 저장소 README
check_findings_index.py  FINDINGS 항목 수 ↔ README 주장
각 저장소 CI             자기 README ↔ pytest --collect-only
check_docs_still_agree   문서를 읽는 검사 27개
```

**전부 파일을 본다.** 그런데 저장소 설명은 파일이 아니다 — GitHub 메타데이터라 어느 체크아웃에도 들어 있지 않다. `git grep`으로도, `Path.rglob`으로도 닿지 않는다.

*검사가 닿지 않는 주장 표면이 하나 있었고, 그것이 하필 사람이 가장 먼저 보는 자리였다.* 저장소 목록에서 이름 옆에 붙는 한 줄 — README를 열기 **전에** 보는 것.

### 왜 이 어긋남이 특히 나쁜가

이 포트폴리오가 내세우는 문장이 *"검증되지 않은 초록불을 믿지 않는다"*이다. 그 주장을 하는 첫 화면의 숫자가 다섯 배 틀려 있으면, 읽는 사람이 의심하는 것은 숫자가 아니라 **그 문장**이다.

숫자가 낡은 것 자체는 놀랍지 않다 — 손으로 적은 수는 낡는다는 것이 이 목록에 이미 여러 번 적혀 있다. 놀라운 것은 **그 사실을 아는 사람이 만든 검사 넷이 전부 같은 곳(파일)만 봤다**는 점이다. 규칙은 알았고, 적용 범위를 안 물었다.

### 고쳤고, 다시 낡지 않게 묶었다

다섯을 맞춘 뒤 `check_repo_descriptions.py`를 만들어 CI에 넣었다. `gh`로 설명을 읽어 각 저장소 README의 `# N tests`와 맞춘다.

**`gh`가 없거나 인증이 없으면 건너뛰지 않고 실패한다.** 확인하지 못한 것을 통과로 세면 이 검사도 없는 것과 같다 — 이 목록이 가장 자주 적은 문장이다.

대조는 **실제로 있었던 값**을 되심었다: `document-intelligence`의 설명을 `52 tests`로 되돌리니 빨간불, 되돌리니 초록불.

### 넣은 자리가 틀렸고, 내가 만든 가드가 그것을 잡았다

CI에 붙여 밀었더니 첫 실행이 빨간불이었다.

```
FAILED — 설명을 하나도 확인하지 못했다.
```

`counts` 잡은 `ohsewool`만 체크아웃한다. 형제 README가 없으니 읽을 수가 없었고,
검사는 **"확인 못 한 것을 통과로 세지 않는다"**는 가드에서 멈췄다.

검사가 틀린 것이 아니라 **내가 자리를 잘못 잡았다.** 그리고 그 가드가 없었으면
"설명 0개 확인 · 문제 0건"으로 조용히 초록불이었을 것이다 — *빈손을 통과로 세지
않게 만들어둔 것이 그 자리를 잘못 잡은 나를 잡았다.*

형제를 체크아웃하는 `siblings` 잡으로 옮겼다. 확인은 CI와 같은 배치를 로컬에
만들어 돌려봤다(ohsewool + 형제 다섯의 README).

### 몇 시간 뒤, 그 검사가 나를 잡았다

이 항목을 닫고 세 회차 뒤(같은 날), modelmate 테스트 수를 1186→1193으로 올리면서
README만 고치고 **설명을 또 안 고쳤다.** counts CI가 빨간불을 냈다.

```
✗ modelmate    README 1193   설명 1186
```

수동으로 맞춘 다섯은 다시 낡기 시작하는 데 몇 시간이면 충분했다. **검사를 만든
값은 첫 번째 실전 포획에서 나온다** — 이번엔 손으로 훑어서가 아니라 CI가 잡았다.

### 리뷰가 짚은 나머지

같은 리뷰가 프로필 첫 화면의 전달력(직무 정체성 3줄, 대표 프로젝트 순서), 각 README의 `Problem/Contribution/Result/Run/Limitations` 요약, 데모 GIF를 지적했다. **그건 사람이 정할 일이라 손대지 않았다** — 자기소개 문안과 대표작 순서는 지원자 본인의 판단이고, 에이전트가 대신 쓸 것이 아니다. 로드맵에 옮겨 적었다.

*내가 고칠 수 있는 것은 "숫자가 맞는가"이고, "무엇을 앞세울 것인가"는 아니다.*

## 내가 만든 가드가 README가 시키는 명령에서만 침묵했다

외부 리뷰의 월간 체크리스트에 *"새 환경에서 README만 보고 실행 가능한지 직접 검증"*이 있었다. 해본 적이 없어서 했다 — 다섯 저장소를 새로 clone해 README가 시키는 대로 돌렸다.

셋은 그대로 통과했다. `document-intelligence`에서 이게 나왔다.

```
$ pytest tests/ -q          ← README가 시키는 것
201 passed, 33 skipped      종료코드 0
```

**서른셋이 건너뛰어졌는데 종료 코드가 0이다.** 그 저장소의 `EXPECTED_SKIPS`는 비어 있다 — 훅이 울렸어야 한다.

### 원인은 내가 넣은 조기 반환이었다

```python
if getattr(session.config.option, "file_or_dir", None):
    return    # 일부만 돌렸다 — 집합을 비교할 수 없다
```

`pytest tests/`는 경로를 주지만 **전부 돌린다.** 나는 *"경로를 줬다"*와 *"일부만 돌렸다"*를 같은 것으로 봤다.

```
CI      pytest -q          → 훅 살아 있음
README  pytest tests/ -q   → 훅 침묵
```

다섯 저장소 전부 같았다. **CI에서만 사는 가드였고, README를 따르는 사람에게는 없는 것과 같았다.**

### 대조를 걸었는데도 못 잡은 이유

86·87회차에 이 훅을 다섯 저장소에 넣으면서 매번 대조를 걸었다 — 건너뛰기를 심으면 빨간불, 탈출구를 주면 통과, 파일 하나만 돌리면 조용함. 세 가지를 다 확인했다.

**그 대조를 전부 `pytest -q`로 돌렸다.**

*검사를 한 가지 부름 방식으로만 확인하면, 나머지 방식에서는 확인한 적이 없는 것이다.* 대조가 없어서 놓친 것이 아니라, 대조가 **한 축만** 흔들어서 놓쳤다. 앞 항목들에서 순서·시간·상태를 흔들면서 정작 **"어떻게 부르는가"**는 한 번도 안 흔들었다.

### 무엇을 줬는가가 아니라 전부 모였는가

인자의 모양으로 가르는 것을 그만뒀다. 이제 **디스크의 `test_*.py`가 전부 수집됐는지**로 판단한다. 그러면 `pytest`, `pytest tests/`, `pytest tests/ -q` 셋이 같은 것으로 취급되고, `pytest tests/test_one.py`는 여전히 조용하다 — 그때는 판정할 근거가 정말로 없으니까.

```
pytest -q          잡음 ✓
pytest tests/ -q   잡음 ✓
파일 하나만         조용함 ✓
```

`document-intelligence`는 처음 발견한 상황을 그대로 재현해 확인했다 — **새 clone · PDF 없음 · README 방식**에서 종료 코드 1.

### 대조가 또 한 번 틀렸다

`rag-profile-selector`만 세 방식 전부 조용해서 잠깐 그 저장소의 훅이 더 망가진 줄 알았다. 아니었다 — 내가 그 저장소에만 `KR_LAW_CORPUS_PRESENT=1`을 주고 있었고, **그건 훅을 통째로 끄는 탈출구다.** 끄고 다시 돌리니 양쪽에서 잡았다.

*탈출구를 켜둔 채로 "안 잡힌다"고 적을 뻔했다.* 이 회차에만 대조가 두 번 틀렸고, 둘 다 대조 자체가 무효였지 검사가 약한 것이 아니었다.

## 한 번도 돈 적 없는 워크플로 셋을 돌렸다 — 셋 다 첫 실행에서 빨간불

주간 워크플로 셋(coverage.yml·timezone.yml·doc-tests-bite)이 만들어진 뒤 **한 번도 실행된 적이 없었다** — cron이 아직 안 왔고, push에는 안 걸린다. *한 번도 돈 적 없는 워크플로는 주장이지 검사가 아니다.* dispatch로 셋을 지금 돌렸다.

**셋 다 첫 실행에서 빨간불이었다.** 각각 다른 이유로.

### 하나 — pytest가 없다 (내 잘못, 둘)

coverage.yml과 timezone.yml 둘 다 `No module named pytest`로 죽었다. `requirements.txt`에는 pytest가 없고 기존 tests.yml은 따로 설치하는데, **두 워크플로를 쓸 때 그 줄을 안 옮겼다.** 스케줄만 믿었으면 첫 화요일에야 알았다.

### 둘 — skip을 pass로 읽었다 (내 검사기 결함)

doc-tests-bite는 rag의 "공개된 코퍼스 크기" 항목을 **"물려 있지 않다"**로 보고했다. 아니었다 — CI에는 코퍼스가 없어 대상 검사 12개가 전부 skip됐고, skip은 종료 코드 0이라 "비워도 통과"로 읽힌 것이다. **검사가 문서를 안 읽은 게 아니라 아예 안 돌았다.** 이 포트폴리오가 남에게서 반복해서 잡은 그 모양이 내 검사기 안에 있었다.

이제 셋을 가른다: **물림 / 안 물림 / 판정 불가**(전부 skip). 판정 불가는 어느 쪽으로도 조용히 넣지 않는다. 대조: 코퍼스를 치우면 판정 불가, 두면 물림.

### 셋 — 코퍼스를 준비시키니 진짜가 나왔다

잡에 코퍼스 준비를 넣고 재촉발하니 이번엔 **검사가 실제로 돌아서 실패했다.** 받은 코퍼스가 공개 수치와 달랐다.

```
스크립트 기본값   --limit 30
corpus.yml       --limit 14
커밋된 코퍼스     14건
README 안내      인자 없이 실행  ← 30건을 받는다
```

**README를 따르는 사람은 다른 코퍼스를 받고, "코퍼스 받은 뒤 348개 전부 통과"가 그 사람에게는 거짓이 된다.** 한 질문에 규칙이 둘. 기본값을 14로 맞췄다 — 인자 없는 실행이 커밋된 코퍼스를 재현한다.

### 넷 — 재측정이 둘을 더 꺼냈다

coverage.yml을 살리려면 기록부터 갱신해야 했다(그 뒤 회차들이 `.part`를 고쳐 문 수가 4323→4310). 재다가:

**스모크 둘이 조용히 빠져 있었다.** `ADMIN_PASSWORD` 없이는 pilot_inquiry·usage_limits가 "돌지 않았다"로 빠지고 합집합이 낮게 잰다(65.4%). **내가 만든 coverage.yml도 안 넣고 있었다.** 도구가 안 돈 스크립트를 이름으로 말해줘서 잡았다 — *조용히 빼지 않고 따로 찍는 보고가 자기를 만든 사람을 구했다.*

**기록이 자기 명령으로 만들 수 없는 값을 담고 있었다.** `functions_never_run: 12`인데, 기록의 `command`가 가리키는 두 `--json` 실행은 파일별 줄만 내지 **함수 통계를 내지 않는다.** 12가 어떻게 나온 수인지 재구성할 수 없었다. 빠른 검사는 "명령이 적혀 있는가"만 봤지 *그 명령이 기록의 모든 필드를 만드는가*는 안 물었다.

합산 계산을 `scripts/combine_coverage.py`로 저장소에 넣었다(줄 매핑은 무작위 8지점 원문 대조로 확인). 정직한 현재 값: **합집합 66.2% · 함수 326 중 안 도는 것 43.** README에는 12가 재현 안 되는 값이었다는 것을 그대로 적었다 — 숫자를 조용히 바꾸면 다음 사람이 12를 믿었던 이유를 모른다.

### 끝은 셋 다 초록불 — 그리고 그 초록불을 다시 의심했다

재촉발 후 셋 다 통과. coverage가 빨라 보여서 로그를 열었다 — 러너가 **실제로 쟀다**: 문 수 4310 정확 일치, 퍼센트는 허용치 안(51.4/55.8/66.5 vs 기록 51.2/56.0/66.2). 잡음은 허용하고 신호는 잡는 그 허용치 설계가 첫 실전에서 의도대로 동작했다.

## 지는 방식도 계약이다 — 서버가 무너지는 다섯 갈래를 일부러 밟았다

미도달 거부 42개에서 마지막까지 남겨뒀던 가족을 닫았다. 400들은 *"당신이 고칠 수 있다"*이고, 이 다섯 500은 **"우리가 졌다"**다.

```
020_run_cv        모든 회귀 모델 실패 / 모든 분류 모델 실패 / 재학습 되돌림 소진
098_sample_files  샘플 CSV 자리에 HTML이 온다 / 타깃 컬럼이 없다
```

뒤의 둘은 실제 사고의 흉터다 — SPA 폴백이 CSV 요청에 `index.html`을 준 적이 있고, 그 검증이 지금은 서빙 직전을 지킨다. **그 지킴이 자체는 아무도 밟아본 적이 없었다.**

### 게이트가 아니라 본문을 죽인다

이 갈래들은 정상 입력으로는 닿을 수 없다 — 앞선 게이트들("CV 먼저" 400들)이 이상한 입력을 먼저 거른다. 닿으려면 **본문이 죽어야 한다.** 채점 함수와 모델 팩토리를 던지게 바꿔치기했다.

가장 깊은 갈래(재학습 소진)가 까다로웠다: **채점은 성공하고 재학습만 죽어야** 한다. 팩토리가 모델당 한 번은 진짜 모델을 주고 그 뒤로 던지게 만들었다 — 채점 고리는 통과하고, 최고 모델 재학습이 죽고, 되돌림 고리가 나머지를 소진한 끝의 500이다. 검사 안에 *"정말 그 갈래였는가"*(팩토리 호출 수 ≥ 2)를 함께 단언한다 — **아니면 이 검사는 채점 실패(윗갈래)를 확인하고 깊은 갈래를 확인했다고 말하는 셈이다.**

### 고정한 성질 — 500이어도 지켜야 할 것

```
내장을 쏟지 않는다    응답에 Traceback / File "..." 없음
다음 행동을 말한다    recommended_next_action — 서버가 져도 사용자는
                     데이터를 바꿔볼 수 있다
어디서 졌는지 남긴다   failed_stage
```

이 제품의 사용자는 비전문가다. "우리가 졌다"조차 *다음에 뭘 해보라*와 함께 와야 한다.

### 되돌림 방향 둘

**모델 하나라도 살면 200이다** — 첫 모델만 살리고 나머지를 죽이니 결과가 온다. 이 500들이 "전부 죽었을 때만" 나온다는 것이 계약이고, 그걸 안 박으면 언젠가 부분 실패까지 500이 된다.

**멀쩡한 샘플은 여전히 내려받아진다** — 검증이 멀쩡한 것까지 막으면 다음 사람이 검증을 뗀다.

### 대조 — 상태 코드만이 아니라 누출 단언까지

다섯 지점을 599로 변이하니 전부 빨간불. 그리고 **누출 단언이 빈손이 아닌지** 따로 심었다 — 응답 메시지에 `Traceback (most recent call last):`를 넣으니 "내장이 샜다"로 문다. *한 번도 발화한 적 없는 단언은 장식이다.*
